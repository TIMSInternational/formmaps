# FormMaps .NET Migration — Completion Roadmap

Date: 2026-07-17. Living plan for finishing the Node/TS → C#/.NET migration.
Grounds: `dotnet-implementation-plan.md` (11-domain order), `cutover-matrix.md`
(risk), `source-inventory.md` (52 routes / 90 services / 123 models / 177 tests).
Companion: `agentic-migration.manifest.json` (per-slice ledger).

## 1. Where we are (main `781a72a`, 33/33 manifest slices)

| Block | State |
|---|---|
| Foundation (rails, auth-ctx, JWT, security, RLS, staging pipeline) | ✅ done (FM-001→008) |
| Reports domain — reads (7/7 endpoints) | ✅ done (FM-006, 009→012) |
| Assessments — reads (pca-exam cluster, LIA/MIL/personality results, timeline) | ✅ done (FM-013→024) |

**✅ CUTOVER (prod traffic):** Personality domain (all 6 routes, FM-023/030) fully cut
over 2026-07-22 (Milestone-1, see `personality-prod-cutover-runbook.md`). **Wave 2
Batch 1** — LIA results (FM-015), MIL results (FM-016), pca-exam catalog/config
(FM-018) — LIVE on prod traffic as of 2026-07-27 13:16 CDT (rollback drill proven both
directions: flip-off→Node in ~4min, flip-on→.NET in ~8min; CloudWatch clean). Legacy
Node routes for these 5 endpoints are now FROZEN (rollback target only, no further
Node-side changes expected). See `formmaps-platform/docs/superpowers/plans/2026-07-27-wave2-batch1-cutover.md`
and `cutover-verification-checklist.md` for full detail.

**Wave 2 Batch 2** — pca-exam session (FM-013/022), history (FM-020), completed-exams
(FM-013) — LIVE on prod traffic as of 2026-07-27 14:21 CDT. CloudWatch clean, anon +
real-auth canaries green with real seeded values round-tripped correctly (Z-string
timestamps intact, no `+00:00` divergence — FM-DOTNET-022's fix holds live). **No
Playwright acceptance spec** — traced the actual frontend call sites and found no
student-facing page currently calls `/session` or `/completed-exams` (only `/history`,
reachable only via a counselor viewing a student, not this batch's student fixture);
relied instead on the real-auth gate's response bodies, which is a stronger check for
these specific 2 routes (exact seeded values verified, not just "page rendered").
Rollback-drill NOT repeated (Batch 1 already proved the mechanism; repeating every batch
is unnecessary — an accidental extra flip during this batch's close-out was caught and
reverted immediately, no user impact). Legacy Node routes for these 3 endpoints now
FROZEN.

**Wave 2 Batch 3** — test-scores reads (FM-037: superscore, college-fit,
students/:id/test-scores) + question360 reads (FM-038: all 5 GETs) — LIVE on prod
traffic as of 2026-07-27 14:44 CDT. CloudWatch clean, anon + real-auth canaries green
(16/16 checks incl. faithful 403s for the counselor/parent-only and
evaluations:manage-gated routes — a student caller correctly 403s, matching legacy).
Playwright confirms `/dashboard/test-scores` triggers real `.NET`-served
superscore+college-fit calls for the fixture student. Legacy Node routes for these 8
endpoints now FROZEN.

**Wave 2 Batch 4** — school-admin reads (FM-039: overview, results, pca-status,
status) — LIVE on prod traffic as of 2026-07-27 15:16 CDT. `/assessments/config` +
`/assessments/schedule` deliberately left dark (those paths also carry Node-only PUTs —
the FM-037-style routing trap the manifest itself flagged). New fixture:
`test.schooladmin@formmaps.dev` password rotated (reusing `rotateFixturePassword.ts`);
no extra data seeding needed — the fixture already shares a school with
`test.student@formmaps.dev`, so real non-trivial results were visible immediately.
CloudWatch clean, anon + real-auth canaries green (8/8), Playwright confirms
`/school-admin` dashboard triggers real `.NET`-served `assessments/status`. Legacy Node
routes for these 4 endpoints now FROZEN.

**Wave 2 Batch 5** — Reports domain (FM-006, 009→012: benchmark, user-report, pca, lia,
timeline, coaching, evaluation) — LIVE on prod traffic as of 2026-07-27. All 7 `.NET`
endpoints were already deployed (ancestor of the live image since before Wave 2 batch
numbering existed) — the only gap was the `next.config.ts` rewrite block, never ported
into `formmaps-platform/frontend` (the file `app.formmaps.com` actually deploys from).
New fixture data seeded for `test.student@formmaps.dev` covering the 4 endpoints with no
prior coverage (benchmark/timeline/coaching/evaluation): `StudentGrade`,
`CourseEnrollment`, `PCAEvaluation`, `UserCareerProfile`, `EvaluationGroup`+`Feedback`,
`Booking`+`Review` (`seedBatch5ReportsFixtures.ts`). Real-auth gate green (8/8 student-actor
checks + 1/1 school-admin-actor check for benchmark, incl. faithful 403 for a student
calling benchmark and 404 for a nonexistent user). Anon canary through `app.formmaps.com`
confirms all 7 routes now carry `x-formmaps-service: formmaps-api`. Playwright confirms
`/school-admin/reports` (Test Student dialog, MIL/LIA and 360° tabs) triggers real
`.NET`-served `lia` + `user-report` calls with real seeded values (GPA 3.67, 6 completed
MIL exams, 1 completed coaching session at $50) rendering with no console errors. Legacy
Node routes for these 7 endpoints now FROZEN.

**Wave 2 Batch 6** — school-analytics reads (FM-049: overview, trends, performance-trends,
top-performers) — LIVE on prod traffic as of 2026-07-27. Corrected a stale research-draft
claim that this domain's frontend wiring "already existed" — it pointed at
`~/formmaps/apps/web/next.config.ts`, which has no linked Vercel project at all; the real
gap (same as Batch 5) was `formmaps-platform/frontend/next.config.ts`, now fixed. Seeded
one active `CounselorStudentAssignment` (test.counselor → test.student) so
`counselorCoverage` wasn't a degenerate 0% (confirmed 0→100 pre/post). Real-auth gate
green (4/4 authed checks + verified 403 for a student caller against the actual
`.NET` permission guard). Anon canary through `app.formmaps.com` confirms all 4 routes
carry `x-formmaps-service: formmaps-api`. Playwright confirms `/school-admin/analytics`
triggers real `.NET`-served `overview` + `top-performers` calls on mount, 0 console
errors. Legacy Node routes for these 4 endpoints now FROZEN.

**Wave 2 Batches 7-12** — 6 more read-only slices cut, 19 routes LIVE on prod traffic as of
2026-07-28. **Batch 7** vocational-360 (FM-033/034: catalog `instrument`/`questionnaire` +
result reads `score`/`integrated`, canAccessUser-gated, 4 routes). **Batch 8**
school-manage-reads (FM-050: `dashboard/stats`, `counselor-assignments/all`, `notes`,
`counselor-workload`, 4 routes) — FM-051/052 (profile/settings, users) deliberately
excluded, both co-flip a GET and a write on the same literal path. **Batch 9** iSAMS
integration reads (FM-053: `status`, `jobs`, 2 routes) — FM-054 (courses GET+POST) excluded,
same trap. **Batch 10** course pathways (FM-058: 1 route, the one true pure read in the
5-slice curriculum cluster; frameworks/data-mappings/prerequisites/course-import all
excluded). **Batch 11** counselor dashboard (FM-067, 4 routes) + enriched caseload
(FM-068, 1 route) — all pure GETs; counselor writes (FM-069-072) and student/parent CRUD
(FM-073-078) excluded. **Batch 12** academic gaps (FM-080: `summary`, `students/:id`,
`recommendations/:id`, 3 routes) — `/ai-recommendations` (Bedrock) stays Node by design;
rest of domain 6 (college applications/favorites/essays, student course-plan,
change-requests) is CRUD, excluded.

Every batch corrected at least one stale claim from the prior research workflow (wrong
ancestor-check reference point, wrong repo for "already wired," local-dev-DB seed-volume
assumptions) by re-deriving against the actually-deployed image/file/prod-data before
trusting it. Real-auth gate + anon canary green for all 19 routes, no regression on any
prior batch. Legacy Node routes for these 19 endpoints now FROZEN.

**✅ FIXED + LIVE (2026-07-28) — FM-079 parent child-link-scoped reads
(`FORMMAPS_ROUTE_PARENT_CHILD_READS_TO_DOTNET`).** Root cause: the `tenant_isolation` RLS
policy on `student_parent_links` (and `users`, and every other table these reads touch)
only admits a row when `studentId = current_setting('app.current_user_id')` (caller
viewing their OWN row) or the caller shares the student's school — a parent's own id
never equals their child's studentId, and parents have no school, so any read of these
tables under the caller's own Identity RLS session can **never** pass for a real parent.
**Verified this is a live, currently-broken bug in legacy Node too**
(`formmaps-platform/api/src/routes/parent.ts` — progress's link check + every subsequent
read run under `tenantContext`, never `runAsSystem`; course-plan's link check is the
same), not a deliberate business rule — confirmed by curling `app.formmaps.com` as a real
linked parent pre-fix and getting 403/404 from legacy exactly as the .NET port did.
Fixed in `ParentChildReader.cs` (`dbd9d43`, `a0d56be`): both `GetProgressAsync` and
`GetCoursePlanAsync` now run every read — the link check, the student lookup, and all
assessment/credit/grade/plan/target/course reads — on a System (RLS-bypass) session.
Also hit and fixed a second, unrelated bug surfaced only once real seeded data was
reachable for the first time (`8ce967e`): `Npgsql`'s `GetDecimal()` threw
`OverflowException` reading `student_grades.credits` (stored with scale 30, exceeding
`System.Decimal`'s max scale of 28, even though the value itself was just `1.0`) — fixed
by casting to `::double precision` in SQL and reading via `GetDouble()`, the same pattern
`CounselorCaseloadReader.cs` already established for this exact column. Redeployed to
`formmaps-api-prod` (image `staging-8ce967e`), flag flipped in Vercel prod, live-verified
through `app.formmaps.com` as `test.parent@formmaps.dev` — both routes now return real
data (GPA 3.67, Computer Science target) for the first time either has ever worked in
prod, on either backend. Legacy Node routes for these 2 endpoints now FROZEN.
| Assessments — pure engines (LIA-core, LIA item, personality tally, vocational) | ✅ done (FM-025→028) |
| Assessments — writes (LIA complete, personality, pca-exam take/submit, vocational recompute) | ✅ done (FM-029→032) |

**Honest read:** ~20–25% of the endpoint surface is .NET-capable-**on-staging**,
heavily read-weighted. **Cutover to prod = 0%** — every .NET route is flag-gated
(default off); PROD is 100% Node. The first two domains (reports, assessments)
were the low-risk, read-heavy warm-up by design (strangler, auth-last). The
majority of the app — and every structurally hard part — remains.

## 2. Remaining scope, by the 11-domain order (with honest slice estimates)

Legend: R=read slice (fast), W=write slice (slow, full gate), 🔴=high structural risk.

### Phase A — Finish Assessments (domain 3) — **~8–12 slices**
- ✅ **W** pca-exam submit/take (FM-031, DONE `a745d06`) — closed corpus #18 answer-replay (completed/TimeExpired→409, no dup, no rescore). Prod-flip deferred (Node owns /complete timeout + es-VerbalReasoning serve + insight-polyglot).
- ✅ **W** vocational authed SCORE recompute (FM-032, DONE `538c3d0`) — wires FM-028 VocationalScoring → upserts vocational_results (numeric composite + camelCase jsonb).
- ✅ **R** vocational360 result reads (FM-033, DONE `781a72a`) — GET /score/{id} + /integrated/{id} (Decimal→number, jsonb passthrough, never_computed). /recommendations (AI) + /instrument + /questionnaire catalog deferred.
- ✅ **R** vocational360 catalog reads (FM-034, DONE `1b071be`) — GET /instrument + /questionnaire.
- ✅ **R (foundation)** assembleCompleteProfile assembler (FM-035, DONE) — CompleteProfileAssembler (LIA parity-aware + DISC/PCA + 360 + academics/preferences; byte-exact Node fingerprint + toFixed(2) parity; internal lib, no route). Unblocked FM-036 + assessment-profile insights-status.
- ✅ **W** vocational INTEGRATED recompute (FM-036, DONE) — recomputeIntegratedResult: fuses 360 composite (FM-033) + PCA competences + MIL (FM-035 profile) via FM-028 engine → upserts vocational_integrated_results. POST /integrated/{id}/recompute. **Completes the vocational360 authed WRITE surface.** (Only /recommendations = AI-polyglot remains on vocational360.)
- ◑ **R** test-scores READS (FM-037, DONE) — /superscore + /college-fit (pure Superscore/ClassifyFit) + /students/{id}/test-scores (counselor-404/parent-403 role auth). GET / list + POST/PUT/DELETE writes DEFERRED (share the bare path — cut over together in a write slice).
- ✅ **R** question360 reads (FM-038, DONE) — 5 GET catalog endpoints (/GetQuestions,/all,/category,/sub-questions,/{id}); permission-asymmetric (2 need evaluations:manage). ⚠️ NO answer-key strip after all — questions_360 has no correct-answer column (that belongs to the pca_questions router). Writes deferred (Node still owns them).
- ✅ **R** school-admin reads sub-slice 1 (FM-039, DONE) — the getSchoolUser school-scoping rail + 6 reads (overview/results/pca-status/config/status/schedule). Gate HIGH folded: pca-status query school-scoped to close a super-admin cross-tenant read. Sub-slice 2 (rich /results/:studentId report, CSV export, /assessments/pipeline) + the /config,/schedule writes DEFERRED; /assessments/insights EXCLUDED (Bedrock polyglot).
- ✅ **W** test-scores writes + list (FM-040, DONE) — GET / list + POST/PUT/DELETE on the bare /api/v1/test-scores path, self-scoped (ownership→uniform 404, soft-delete idempotent, Zod range validation). Completes the test-scores bare-path surface. Gate folded 2 Codex fidelity fixes (non-object-body→400; empty-testDate→null).
- ✅ **R** school-admin reads sub-slice 2 (FM-041, DONE) — rich /results/:studentId report (computeStudentCompletion, generatedAt) + CSV export (csvSafe injection guard) + /assessments/pipeline (per-exam precedence + LIA overlay). Completes the school-admin READ surface.
- ✅ **W** question360 writes (FM-042, DONE) — POST/PUT/activate/deactivate/DELETE/bulk-create; single-flag `FORMMAPS_ROUTE_QUESTION360_TO_DOTNET` gates the whole question360 surface (reads FM-038 + writes cut over together). **question360 domain COMPLETE.**
- ✅ **W** 🔴 token-gated external write rail (FM-043, DONE `capstone`) — 6 external routes (validate-token/submit-feedback/360evolutor + vocational GET/submit/violations) on a NEW fail-closed non-tenant RLS-bypass rail (System→Bypass, anonymous→Deny). 2 dark flags. Adversarial gate folded 3 (Codex BLOCKING rate-limiter + CONFIRMED zod-email + PLAUSIBLE ToDictionary-500). **Federico RATIFIED closing the legacy submit-feedback token-expiry gap.** dotnet 0/0; unit 460 + integration 481(+1 skip). **Completes the assessments external write surface.**
- ✅ **W** school-admin config + schedule writes (FM-044, DONE) — PUT /assessments/config + /assessments/schedule (DB-only upserts; gate folded a BLOCKING raw-vs-coalesced read-back bug). ▶ **W** school-admin **email** writes (send-reminders + setup-360) = FM-045 (needs the new .NET outbound-email/SES surface). (test-scores has no config/schedule writes — its surface is complete.)
- **W** 🔴 external **token-gated write rail** (evaluation `submit-feedback`, vocationalTake external submit) — NEW fail-closed non-tenant rail; highest risk in assessments.
- TIMS vendor proxy (`pcaapi.ts` 732 lines, 13ep) — **DEFER / keep in Node** (Federico) or 2–3 slices if ported.
- AI (assessment-profile insights, Bedrock) — **stays polyglot** (~0 .NET).

### Phase B — Schools / rosters / organizations (domain 4) — **~6–10 slices**
- ✅ **R** gradebook transcript read (FM-046, DONE) — GET /gradebook/students/:studentId (transcript-by-year + GPA; pure GPA math in double, 4-dp AwayFromZero; reuses the school:manage rail). First Phase-B slice. Grade writes (POST/PUT/DELETE) deferred.
- ✅ **R+W FIXED + LIVE (2026-07-28)** — school calendar reads + writes (FM-047/048, 12 endpoints total: 3 GETs + 9 writes) — GET /calendar/academic-years (nested terms) + /assessment-periods (yearId gate-not-filter) + /holidays; academic-years POST/PUT/:id/set-current/DELETE, assessment-periods POST/PUT/DELETE, holidays POST/DELETE. Backend was already merged (`7d73720`) but the frontend rewrite + flag were never wired until this session — this batch built and proved the program's FIRST write-verification harness in the process: a second fixture school (`test-school-2`/`test.schooladmin2@formmaps.dev`) for cross-tenant IDOR probes, a write-capable canary engine (`batch-canary-write.mjs`, sibling to the read-only `batch-canary.mjs`) that creates a throwaway academic year + assessment period + holiday, read-back-verifies every field, and unconditionally cleans up, plus a Tier-2 raw-SQL check (`verifyCalendarWriteInvariants.ts`, run via `formmaps-migrate`) proving hard-delete vs. a same-service GET's blindness to soft-delete regressions. **The canary itself needed 2 rounds of real-execution-driven fixes** the code review missed: a `!==` reference-equality bug that false-positived on array/object fields (fixed with deep `JSON.stringify` comparison), a `createdId` that stayed `undefined` when a write response has no `id` field like holidays' `{data:{count}}` shape (fixed by falling back to the located row's real id), and a config bug where the assessment-period create ran before the year was current (reordered) and the holidays body needed a `{"holidays":[...]}` array wrapper the endpoint silently treated as absent otherwise. Real-auth gate passed clean (7/7 steps) directly against `formmaps-api-prod`, then again through `app.formmaps.com` post-flip. Flag `FORMMAPS_ROUTE_SCHOOL_ADMIN_CALENDAR_TO_DOTNET` flipped; hard deletes; set-current ownership-before-clear; createdBy/updatedBy NULL — all confirmed live, not just backend-tested. Legacy Node routes for all 12 endpoints now FROZEN.
- ✅ **R** school analytics reads (FM-049, DONE) — GET /analytics/{overview,trends,performance-trends,top-performers} (perm analytics:school); pure GPA/bucketing math; no-school→200 empty defaults; real read cut-over (method-unambiguous GETs). Tie-order = ratified deterministic superset (FM-032 precedent). main `a6293de`.
- ✅ **R** school.ts school:manage reads (FM-050, DONE) — dashboard/stats + counselor-assignments/all + notes + counselor-workload (4 method-unambiguous GETs; dashboard no-school 6-field shape; JsRound percentages; notes ILIKE content+student.name; workload stable sort/no-N+1). Gate folded a Codex MEDIUM (dashboard AVG-NaN → STJ 500 → coalesce NaN→0). main `f945890`.
- ✅ **R+W** school/profile + settings co-flip (FM-051, DONE) — GET+PUT /school/profile + GET+PUT /settings (one flag, path-not-method). .NET write-owner for the schools profile/settings columns; mass-assignment allow-list guard (clean per fresh+Codex); folded nullable-notify-flag clear (JSON null→NULL/200). main `a8d3bcd`.
- ✅ **R+W** school:users cluster co-flip (FM-052, DONE) — GET /users + PUT /users/:id/grade-level + POST/DELETE /counselors/:id/assign-students + GET /counselors/:id/students (one flag `FORMMAPS_ROUTE_SCHOOL_USERS_TO_DOTNET`, path-not-method). .NET write-owner for users.gradeLevel + counselor_student_assignments; assign = single-writable-tx superset (deactivate-others + ON CONFLICT reactivate); unassign HARD delete. Gate: fresh SHIP + **Codex FIXES-NEEDED** — folded 2 HIGH `@updatedAt` parity (grade write + reassignment deactivation), MED empty-schoolId falsy→403, MED+LOW GradeLevelParser (int4 overflow→500, no silent saturation; 1e3→1000). main `ed68eec`.
- ✅ **R** iSAMS integration reads (FM-053, DONE) — GET /integrations/isams/status + /jobs under one flag `FORMMAPS_ROUTE_ISAMS_READS_TO_DOTNET`. **DELIBERATELY reads-only (vendor boundary):** configure/sync/test (external iSAMS HTTP + credential encryption + cross-domain course/user/grade upserts) STAY IN NODE — to port as one unit after school-courses.ts + school-students.ts land, once credential-encryption parity is settled. status 3-shape parity (no-school / no-config / config-row lastSyncAt-null-present); jobs full-row + FM-032 tie-break. Fresh+Codex SHIP; folded LOW harness timestamp(3). main `ceeba72`.
- 🔶 **school-courses.ts mini-phase (27 endpoints, being sub-sliced):**
  - ✅ **R+W** core courses (FM-054, DONE) — GET /courses (framework+enrollment merge) + POST /courses under one flag `FORMMAPS_ROUTE_SCHOOL_COURSES_TO_DOTNET` (exact-literal /courses, GET+POST co-flip). PUT/DELETE /courses/:courseId deferred (path collides with un-ported /courses/pathways·import·ai-import). Gate: fresh SHIP + Codex MED (department fail-closed) + cross-cutting **credits raw-Decimal→JSON string** fold (trim_scale::text). main `b1800de`.
  - ✅ **R+W** curriculum frameworks (FM-055, DONE) — 4 endpoints under `FORMMAPS_ROUTE_CURRICULUM_FRAMEWORKS_TO_DOTNET`; create-vs-update undefined-asymmetry (customize credits/localName + updateFrameworks enabled); credits decimal.js STRING. Gate: fresh+Codex both FIXES-NEEDED → folded (customize credits numeric-string, enabled undefined-skip, credits→string). main `344d02c`.
  - ✅ **R+W** data-mappings (FM-056, DONE) — GET+POST /data-mappings + POST /bulk-approve (collision-free literals) under `FORMMAPS_ROUTE_DATA_MAPPINGS_TO_DOTNET`; PUT/DELETE /:id defers (ai-suggest collision). Ratified safe divergence: bulk-approve no-ids → 0 approved (legacy Prisma drops the filter → approves the WHOLE school). No 23505 catch → dup → 500 (not 409, unlike createCourse). confidence Decimal→decimal.js STRING (trim_scale::text). Gate: prior fresh reviewer SHIP w/ 1 LOW; this session **folded the LOW** — confidence string parse `AllowLeadingSign|AllowDecimalPoint|AllowExponent` (verified vs real decimal.js + .NET): eliminates the 2 fail-OPEN divergences (`"1,000"`/`" 0.85 "`), realistic strings + legacy rejections agree; residuals (hex/Inf/NaN/underscore) all fail-CLOSED (safe). Codex inconclusive (hung); fold independently ground-truth-verified. main `3892721`.
  - ✅ **R+W** prerequisites (FM-057, DONE) — 5 endpoints under `FORMMAPS_ROUTE_PREREQUISITES_TO_DOTNET`: GET /courses/:courseId/prerequisite-chain (courses:read), PUT /courses/:courseId/prerequisites (courses:write), GET /prerequisites/{check,eligible,missing} (curriculum:manage). Chain BFS (depth-DESC STABLE, cycle-safe, credits heterogeneous catalog-string/non-catalog-number-0); resolveCourse id-then-exact-code; eligibility engine (grade-gate JS-falsy, two-set resolution-all/enumeration-active); eligible double-nested envelope + gradeLevel/dept filters; PUT courseIds→codes school-scoped + @updatedAt bump + corequisites String[] semantics. Gate: fresh SHIP + Codex FIXES-NEEDED → folded (corequisites-vs-404 order, null-rule flatMap→500, bare-string/non-string courseId); dismissed 2 unreachable (Decimal-exp, JsParseInt int32-sat). The two /courses/:courseId/* sub-paths are more specific than the deferred bare /courses/:courseId → no collision. main `5dffc55`.
  - ✅ **R** derived pathways (FM-058, DONE) — GET /courses/pathways (curriculum:manage) under `FORMMAPS_ROUTE_PATHWAYS_TO_DOTNET`. Pure `PathwaysComputer` port of computePathways: root→leaf chains in the transitively-reduced prereq DAG (redundant-edge pruning, cycle-safe, roots=no-catalog-prereqs+has-dependents, dedup by code-key, dept grouping, MAX_CHAINS=200/MAX_CHAIN_LEN=12 caps); reader loads active catalog (isActive+status='active' ORDER BY code ASC). JS `localeCompare` dept/chain sort → `InvariantCulture` ICU Compare, empirically pinned vs Node en-US gold (`'|'`<digit ordering, ordinal gets it backwards). Literal `pathways` segment is collision-free (more specific than the deferred bare /courses/:courseId, disjoint from FM-054 /courses) → no negative-lookahead needed. Gate: fresh SHIP → folded null-element robustness-superset pin; Codex inconclusive (wedged). main `9822e84`.
  - ✅ **R+W** course bulk-import CORE (FM-059, DONE) — POST /courses/import (202, importCourses) + GET /courses/import/:jobId (getImportJob) under `FORMMAPS_ROUTE_COURSE_IMPORT_TO_DOTNET` (courses:write). Per-row validate→upsert-by-(schoolId,code); native ImportJobStatus enum; ImportRowParser (JS-truthy credits + RowTypeInvalid) + JsParseFloat. **ATOMIC IMPORT + PER-ROW SAVEPOINT RECOVERY** — one writable session, but each row's DB work under a SAVEPOINT so a per-row 23505 (concurrent same-code import) is recovered and the job completes instead of the request 500ing. Gate: fresh SHIP + Codex FIXES-NEEDED (3 HIGH) → folded (savepoint recovery [Codex caught the poisoned-tx 500 fresh under-weighted], numeric credits, gradeLevels/dept/desc type-invalid, JsParseFloat whitespace); documented non-string-filename-keeps-202. main `5ce2eac`. **▶ /courses/import/:jobId/download-failures DEFERRED to FM-060** (JSON.stringify-of-arbitrary-jsonb rawRow byte-parity; rewrite stays Node meanwhile).
  - ✅ **R** import failures CSV (FM-060, DONE) — GET /courses/import/:jobId/download-failures + getImportFailuresCsv, under the existing `FORMMAPS_ROUTE_COURSE_IMPORT_TO_DOTNET` flag (3rd rewrite). **FailuresCsvBuilder** = faithful JS `JSON.stringify` walker over the jsonb rawRow (gold-pinned vs Node: compact, Postgres jsonb key order, JS escaping, <>&+/non-ASCII literal) + `csvSafe` injection guard; ORDER BY rowNumber ASC (deterministic superset). Gate: fresh FIXES-NEEDED (1 MEDIUM) → folded the **`text/csv; charset=utf-8` mojibake bug** (Express res.send appends charset; port emitted bare text/csv → accented content mangles) HERE + retroactively in the FM-041 precedent (SchoolAdminEndpoints); Codex inconclusive. main `271f353`. **Completes the school-courses.ts course-import surface.**
  - ✅ **W** deferred /:id writes (FM-061, DONE) — PUT/DELETE /courses/:courseId (courses:write, → **403** "Course not in your school", SOFT delete) + PUT/DELETE /data-mappings/:id (school:data-mapping, → **404** "Mapping not found", HARD delete), each under its existing flag, PLUS the batched **negative-lookahead** rewrites (`/courses/:courseId((?!import|pathways|ai-import)…)` + `/data-mappings/:id((?!bulk-approve|ai-suggest)…)`) — rewrites match path-not-method, so the lookahead stops a flag flip misrouting POST /courses/ai-import etc. to .NET. 12-field undefined-omit mass-assignment (raw copy, Prisma-strict typing). Gate: fresh FIXES-NEEDED (1 fail-open) → folded the FM-056 credits string-mask in BOTH update + create paths; Codex inconclusive. main `9cc9484`. **⚠️ routing must be re-verified in the live frontend next.config at cutover.** **Completes the school-courses.ts write surface — only the AI polyglot (ai-import/ai-suggest, Bedrock) stays Node.**
  - ✅ **Completes school-courses.ts** (only Bedrock ai-import/ai-suggest stay Node).
  - ✅ **R+W FIXED + LIVE (2026-07-28)** — course catalog CRUD (FM-054+FM-061, 4 endpoints: GET/POST `/courses` + PUT/DELETE `/courses/:courseId`) cut over to real prod traffic. Second write-coupled domain (after Calendar), reusing its harness (`test-school-2`/`test.schooladmin2@formmaps.dev`, `batch-canary-write.mjs`) rather than rebuilding it — see `docs/superpowers/plans/2026-07-28-school-courses-crud-cutover.md`. **Resolved FM-061's own flagged warning** ("routing must be re-verified in the live frontend next.config at cutover"): the rewrite was genuinely absent from `formmaps-platform/frontend/next.config.ts` (a prior research-workflow draft had checked the wrong, non-Vercel-linked `apps/web/next.config.ts` reference copy and wrongly called it already-wired) — written fresh this cutover, verified via `tsc`+`next build` before deploy. Real execution caught 3 more defects a clean plan/review couldn't: (1) the canary config's `expectAbsentAfterWrite` field has no engine support — a delete step's read-back correctly finding no row would have misreported success as failure; matched the proven working pattern (`expectedStatus` only, no read-back) already used by Calendar's actual committed delete steps. (2) the `formmaps-migrate` ECS task-def's `entryPoint`+`command` both being `["sh","-c",...]` silently no-ops (Docker concatenates them into `sh -c sh -c "<script>"`, so the real script never runs) — this means task-def revision 38, which held `test.schooladmin2`'s rotated prod password, could NOT have been what actually rotated it via ECS; that password's real origin is unaccounted for. (3) `verify-school-courses-write-invariants.ts`'s `import { Client } from "pg"` fails at runtime (`pg` isn't an installed dependency, though it type-checks clean) — fixed to use `basePrisma.$queryRawUnsafe`; **Calendar's identical-shaped `verify-calendar-write-invariants.ts` likely has the same latent bug and was probably never actually executed**, despite being described as having run. Tier-2 check (once fixed) independently confirmed the real soft-delete: `{"isActive":false,"status":"archived","createdBy":null}` on the canary's created-then-deleted row. Real-auth gate green against `formmaps-api-prod` directly, then again through `app.formmaps.com` post-flip (after clearing one canary-vs-canary collision: the domain's soft-delete + a `(schoolId,code)` unique constraint not scoped to `isActive` means a fixed marker code from run 1 blocks run 2's create — cleared via a one-off hard-delete of the synthetic row, real fix would need a per-run-unique marker or an explicit pre-run cleanup, noted for whoever reuses this config again). Spot-checked Calendar/Reports/school-analytics/iSAMS post-flip — no regression. Flag `FORMMAPS_ROUTE_SCHOOL_COURSES_TO_DOTNET` flipped. Legacy Node routes for all 4 endpoints now FROZEN.
- 🔶 **school-students.ts (22 endpoints, being sub-sliced — mounted /api/v1/school-admin):**
  - ✅ **R** roster identity reads (FM-062, DONE) — GET /students (list) + /students/:studentId (detail) + /students/:studentId/community-service, all `school:manage`. Shipped **DARK** (no rewrite: /students/:studentId carries an un-ported DELETE, path-not-method → FM-047 precedent; co-flips with the writes). List **envelope asymmetry** (no-school {success,data:{data:[],total:0}} vs happy-path bare {data,total,page,limit,totalPages}); detail GPA (gradeMap+JsRound) + credit fallback + PCA/MIL/Eval360 status + verbatim PCA/MIL/Eval360 keys via Dictionary; community-service `hours` = raw Decimal → **string** (trim_scale::text). Fresh reviewer SHIP (no blocking/high/med; 2 LOW accepted); Codex inconclusive. main `3cb1997`.
  - ✅ **R** parent-link reads (FM-063, DONE) — GET /parents (school-wide grouped-by-lower(email) roster + school-wide stats; **case-sensitivity asymmetry**: display grouping lowercase → collapses, but stats.totalParents = case-sensitive Prisma groupBy → counts separately; search trimmed) + GET /students/:id/parents (studentInCallerSchool: Super-Admin raw-role bypass, else student-in-caller-school → else **404 "Not found"**; TimeProvider accepted/expired/pending status). Shipped DARK. Fresh reviewer SHIP (faithful 1:1, no blocking/high/med; 1 LOW accepted); Codex inconclusive. main `262b418`.
  - ✅ **R** course-plan reads (FM-064, DONE) — GET /students/:id/course-plan (role-gated school_admin/Super Admin + studentInCallerSchool; merged plan+grades enrollments w/ grade-key asymmetry + derivedGradeLevel) + /course-plan/change-requests (full-row passthrough, credits STRING, enum-`status` filter reproduces Prisma's validation 500) + /course-request-deadline (school:manage). Gate folded 2 MED (JS-`||` empty-string coalescing; derivedGradeLevel NaN→null); Codex inconclusive. main `0051f69`. **✅✅ COMPLETES the school-students.ts READ surface.**
  - ✅ **W** non-SES writes (FM-065, DONE) — DELETE /students/:id (soft delete, ownership→404 no-write) + PUT /course-request-deadline (idempotent schoolId upsert). Establishes the school-students WRITE rail. Gate folded 2 write-safety divergences (non-string deadline coercion; malformed-JSON phantom NULL write). main `721102b`.
  - ✅ **W** verify + review (FM-066, DONE) — PUT /community-service/:id/verify (CommunityServiceStatus enum + Super-Admin scope) + PUT .../change-requests/:id/review (CourseChangeStatus enum + studentInCallerSchool + conditional student_course_plan create on approved+add). Gate (fresh+Codex converged, BLOCKING) folded non-object/malformed-body 500-crash + non-string-status phantom write (+ fixed the same latent crash in FM-065). main `b2cba19`.
  - ✅ **DECISION RESOLVED (2026-07-23) — the SES-entangled invites STAY IN NODE (option A).** student invite/resend/bulk-onboard + parent invite/unlink/resend are atomic email-coupled units (mint token + write user row + send SES email as ONE op); splitting the DB half off = a cross-service distributed-txn hazard, and porting SES sends contradicts the against-prod-email-in-.NET lean. Invites stay Node like AI/Bedrock. **→ school-students.ts is now EFFECTIVELY COMPLETE on the .NET side** (only the email-coupled invites + AI stay Node). bulkOnboardPreview (POST, no writes/email) left in Node too to keep the bulk-onboard flow cohesive. (The FM-067/068 numbers were freed and FM-067 reused for the first Phase-C slice below.)
- ⚠️ **GUARDRAIL CORRECTION (FM-054):** raw Prisma Decimal column passthrough → JSON **string** (decimal.js toString; `trim_scale::text`), NOT number. `::double precision`→number only for computed/`Number()`-ified values. Candidate prior-slice audit: `universities.acceptanceRate` (FM-037).
- ▶ **After school-courses:** **`school-students.ts`** roster reads/writes → THEN the deferred school.ts writes (iSAMS write side [vendor boundary], staff-invite [needs FM-045 SES], logo→S3). Medium risk (school-scoped).

### Phase C — Counselor / student / parent workflows (domain 5) — **~12–18 slices** 🔴
- `counselor.ts` (495), `student.ts` (410), `parent.ts` (395), counselor-notes, counselor-sessions.
- 🔴 access-control-heavy: assignment-scoped (counselor), child-link-scoped (parent), own-record (student). Reuse the shipped `canAccessUser` rail + regression corpus #1 (parent-invite IDOR). Reads first, then writes.
- ✅ **R** counselor dashboard self-contained reads (FM-067, DONE `f9ebfe2`) — the FIRST counselor slice. 4 counselor:dashboard reads under one dark flag `FORMMAPS_ROUTE_COUNSELOR_DASHBOARD_TO_DOTNET`: GET /dashboard (caseload count + KPI counts + follow-up/recent-note lists), GET /dashboard/change-requests (caseload pending course-change requests, schoolId-scoped), GET /me/students/{studentId} + GET /students/{studentId} (identical assignment-gated student detail). All GET-only (no path-not-method hazard). New `Counselor` namespace (reader/interface/endpoints), read-only RLS session. **Deferred:** enriched caseload GET /me/students (listEnrichedStudents, 530-LOC service → own slice); onboarding verify/complete stay Node (email/token invite flow). Parity preserved: pendingRequests count has NO isActive filter vs the change-requests list DOES; total = returned page length (not a COUNT); change-request rows keep nested student{name} + studentName + all raw cols + credits-as-string; note content 200-char truncation; assignment-miss 404 "Not found" vs user-miss 404 "Student not found". Gate: fresh reviewer caught a HIGH (Codex missed) — the `now` comparison bound Kind=Utc → timestamptz session-GUC cast diverging on a non-UTC session; folded (Kind=Unspecified + ms-truncate, the LiaSessionWriter tz rule) + a red-if-regressed non-UTC Testcontainers reader test. Codex CONFIRMED all parity edges; accepted (documented): id tie-break superset, shared auth-envelope, rewrite path-not-method (no non-GET verb on these paths), assignment-check DB-error→500 (pathological). Full suite green (unit 659 + integration 1225(+2 skip) + contract 1); staging + anon canary GREEN. ▶ NEXT Phase-C: (done below) → counselor notes/alerts/sessions/availability (GET+PUT co-flip surfaces) → student.ts reads → parent.ts reads.
- ✅ **R** counselor enriched caseload (FM-068, DONE `a32453e`) — the deferred companion to FM-067: GET /me/students (counselorAnalyticsService.listEnrichedStudents) under its own dark flag `FORMMAPS_ROUTE_COUNSELOR_CASELOAD_TO_DOTNET` (exact literal /me/students, distinct from FM-067's /me/students/{id}). Compute-heavy read (≈FM-035/FM-058 pattern): a **pure `EnrichedCaseloadComputer`** (GPA JsRound-2dp + credit fallback, LIA/PCA/360 badges [eval360 min(total,3)], at-risk status, credit-progress %, career-path jsonb precedence name||careerName||cluster||clusterName, search+status filter, name-ICU-localeCompare / gpa(??-1) / alertCount / gradeLevel(??0) sort, pagination) + `CounselorCaseloadReader` (~10 SQL loads: assignment students + grades/PCA sessions/360 groups/PCA evals/career profiles/alert-count groupBy/school course credits + required-credits resolution [active grad rule set for current AY, else 120]). Parity: pca_evaluations NOT isActive-filtered (matches legacy); credits Number()→::double precision; page max(1,parseInt||1) / limit min(50,max(1,parseInt||20)); assessmentStatus {LIA,PCA,"360"} exact keys via Dictionary. Gate: fresh reviewer SHIP (faithful, no BLOCKING/HIGH/MEDIUM); folded deterministic ORDER BY s.id + PCA-badge/career test coverage; Codex async wrapper didn't return this run. 22 computer unit + 6 endpoint + 5 Testcontainers reader tests; full suite green (unit 681 + integration 1238[+2 skip] + contract 1); staging + anon canary GREEN. **✅ Completes the counselor.ts READ surface** (only onboarding [Node, email/token] + AI-briefing [Bedrock] reads stay Node). ▶ NEXT = counselor write surfaces (availability done below) → student.ts reads → parent.ts reads.
- ✅ **R+W** counselor availability (FM-069, DONE `9057873`) — the FIRST counselor WRITE slice (establishes the counselor write rail). GET+PUT /me/availability under one dark flag `FORMMAPS_ROUTE_COUNSELOR_AVAILABILITY_TO_DOTNET` (co-flip, same path → path-not-method). Permission counselor:sessions. `ICounselorAvailabilityRepository`: GET on a read-only RLS session (row or minimal { timezone:"UTC", weeklySchedule:[] } default); PUT upsert on the UNIQUE userId (writable session + CommitAsync), weeklySchedule = verbatim jsonb. Body resolution: timezone = body.timezone||"UTC" (JS ||; truthy-non-string → "UTC" documented divergence), weeklySchedule = body.weeklySchedule||body.slots||[] (JS-truthiness — empty array [] is truthy and WINS); primitive/malformed body → 500 (express-strict + the app's global-500 handler, reviewer-verified). Mass-assignment guard (fixed columns); caller writes ONLY their own row (userId = Actor.UserId, no path/body userId → no IDOR). Gate: fresh reviewer SHIP (no BLOCKING/HIGH/MEDIUM); folded body-edge test coverage. 19 endpoint (faked repo) + 2 Testcontainers repository tests; full suite green (unit 681 + integration 1259[+2 skip] + contract 1); staging + anon canary GREEN (GET+PUT → 401). ▶ NEXT Phase-C: counselor sessions → counselor notes → student.ts/parent.ts reads (alerts done below).
- ✅ **R+W** counselor alerts (FM-070, DONE `b166dd7`) — GET /me/alerts + PUT /me/alerts/:id/read under one dark flag `FORMMAPS_ROUTE_COUNSELOR_ALERTS_TO_DOTNET` (two paths). Permission alerts:read. `ICounselorAlertsRepository`: GET read-only list (caseload from active assignments; where {studentId ∈ caseload, isActive} + optional ?unreadOnly; real COUNT total; createdDate DESC + id tie-break; page max(1,parseInt||1)/limit min(100,max(1,parseInt||50)); raw alert rows); PUT writable+commit mark-read (findUnique→AlertNotFound→404 "Alert not found"; ensureCounselorStudentAccess→NotAssigned→404 "Not found"; else isRead/readBy/readAt/updatedAt). **🔒 SECURITY FOLD (Federico-RATIFIED): the legacy GET had an IDOR — `if (req.query.studentId) where.studentId = qs(...)` OVERWROTE the caseload `{in: studentIds}` filter, leaking any student's alerts within the school. The port SCOPES the override to the caseload (queryIds = filter∈caseload ? [filter] : []; non-caseload → empty, NO leak). Dark flag → Node still serves old behavior until cutover, no live regression. Proven by a Testcontainers red-if-regressed test (`StudentId_filter_outside_caseload_leaks_nothing`).** Gate: fresh reviewer SHIP (IDOR fold verified fully closed, no BLOCKING/HIGH/MEDIUM); folded paging/severity/readAt coverage. Endpoint (faked repo) + 6 Testcontainers repository tests; full suite green (unit 681 + integration 1277[+2 skip] + contract 1); staging + anon canary GREEN. ▶ NEXT Phase-C: counselor notes → student.ts/parent.ts reads (sessions done below).
- ✅ **R+W** counselor sessions (FM-071, DONE `a0ef93f`) — GET /me/sessions + PUT /me/sessions/:id/complete under one dark flag `FORMMAPS_ROUTE_COUNSELOR_SESSIONS_TO_DOTNET` (two paths). Permission counselor:sessions. `ICounselorSessionsRepository`: GET read-only list (own active sessions; where {counselorId, isActive} + optional ?status applied only when non-empty AND ≠ "all"; real COUNT; startTime DESC + id tie-break; page max(1,pi||1)/limit min(50,max(1,pi||20)); LEFT JOIN users for studentName; ALL counselor_sessions columns raw + nested student{name} + studentName [RAW, no "Student" fallback — differs from dashboard]; calendarEventIds verbatim jsonb); complete writable+commit (findUnique→ownership: missing OR counselorId≠caller→NotYourSession→403 "Not your session"; notes = counselorNotes ?? notes ?? "" [JS NULLISH — "" kept, null falls through] → non-string → "" → slice(5000); malformed/primitive body → 500 BEFORE the ownership check [express parses pre-handler]). **⚠️ PUT /me/sessions/:id/cancel DELIBERATELY NOT ported — its `syncRecordSafe("counselorSession", id, "cancel")` calendar-sync side-effect stays in Node (the AI/email/calendar polyglot boundary); different path → not rewritten. Canary confirmed cancel → 404 on .NET.** Gate: fresh reviewer SHIP (no BLOCKING/HIGH/MEDIUM, no security; independently verified the express-500 handler + ?? semantics); folded page-clamp coverage. Endpoint (faked repo) + 3 Testcontainers repository tests; full suite green (unit 681 + integration 1304[+2 skip] + contract 1); staging + anon canary GREEN. ▶ NEXT = counselor notes (FM-072).
- ✅ **R+W** counselor notes CRUD (FM-072, DONE `75eec6b`) — GET/POST /students/:studentId/notes + PUT/DELETE /notes/:noteId + PUT /notes/:noteId/complete-followup under ONE dark flag `FORMMAPS_ROUTE_COUNSELOR_NOTES_TO_DOTNET` (three rewrites, path-not-method). **Auth asymmetry:** GET/POST/DELETE = INLINE raw-role check (counselor/school_admin/"Super Admin" exact-string; else 403 "Insufficient permissions"), a RAW "counselor" additionally needs `ensureCounselorStudentAccess` (404 "Not found"); DELETE author-ownership bypass applies ONLY when role==="counselor" (school_admin/Super Admin delete ANY note). PUT + complete-followup = permission `counselor:notes` + author-ownership (missing OR authorId≠caller → 403 "Not authorized"). `ICounselorNotesRepository`: GET read-only paged list ({studentId, isActive} + optional ?type-if-truthy; real COUNT; createdDate DESC + id tie-break; page max(1,pi||1)/limit min(50,max(1,pi||20)); JOIN users for authorName; dual `author:{name}` + `authorName` emission [spread-then-add]); create/update/soft-delete/complete-followup writable+commit (ownership + write in ONE session — atomic, closes a live-TS TOCTOU). **Body coercion parity:** create JS-|| defaults (type||"general", content required-string [""=valid, missing/null/non-string→500], isPrivate||false, tags||[], followUpDate `x?new Date:null`) + Prisma type-500s; PUT partial update (only present keys, no defaults) with **DEFERRED InvalidBody** — a non-owner with a bad-type body → 403 not 500 (type-500 checked inside the repo AFTER ownership). @updatedAt bumped on soft-delete + complete-followup (Prisma @updatedAt); tags text[] verbatim; Kind=Unspecified+ms-trunc writes; ISO-Z reads; mass-assignment fixed columns. Malformed/primitive body → 500 (express.json-first divergence: gated behind RequireIdentity in .NET, placed before the access check to best mirror Node). Gate: fresh reviewer **SHIP** (no BLOCKING/HIGH/MEDIUM/security; LOWs = slice-wide accepted [extra `code` field, raw-body Content-Type] / pathological [single-element date-string array] / determinism superset [id tie-break, FM-032]); folded a red-if-regressed createdDate/id tie-break test. 52 tests (24 endpoint faked-repo + 28 Testcontainers repo); full suite green (unit 681 + integration 1355[+2 skip] + contract 1); staging deploy 30047880099 + anon canary GREEN (all 5 routes → 401). **✅ counselor.ts is now COMPLETE on the .NET side** except onboarding (Node email/token), cancel (Node calendar-sync), ai-briefing (Bedrock/Node). ▶ NEXT = student.ts portfolio (FM-073).
- ✅ **R+W FIXED + LIVE (2026-07-28)** — counselor notes CRUD (FM-072, 5 endpoints) cut over to real prod traffic. Third write-coupled domain shipped, reusing Calendar's harness end-to-end. Availability/alerts/sessions (FM-069-071) deliberately NOT cut over this pass — no safe round-trippable write path via the API alone (alerts/sessions have no create endpoint, only one-directional state transitions; availability is a singleton per-counselor upsert row with no delete, and its single-object GET response would crash the canary engine's list-based row-locate). `test.counselor@formmaps.dev`'s standard fixture password (`Test1234!`) worked directly — no rotation needed. Real-auth gate green (4/4 steps) directly against `formmaps-api-prod`, then again through `app.formmaps.com` post-flip, using `test.counselor` (write), `test.student` (deny, 403 on every step — fails the raw-role gate outright), `test.schooladmin2` (cross-tenant, 403 only on the two permission-gated steps — GET/POST/DELETE use a raw-role check with no school-ownership check, a documented pre-existing legacy-parity quirk, not a regression). Tier-2 check (`verifyCounselorNotesWriteInvariants.ts`, Prisma-based per the school-courses lesson) independently confirmed via `formmaps-migrate`: `{"isActive":false,"followUpCompleted":true}` on the canary's created-then-deleted note — soft-delete and the complete-followup write both verified at the DB layer. Spot-checked counselor dashboard/caseload + school-courses post-flip — no regression. Flag `FORMMAPS_ROUTE_COUNSELOR_NOTES_TO_DOTNET` flipped. Legacy Node routes for these 5 endpoints now FROZEN.
- ✅ **R+W** student portfolio CRUD (FM-073, DONE `ce59409`) — GET /portfolio + GET /portfolio/summary + POST /portfolio + PUT/DELETE /portfolio/:id (routes/student.ts + studentService.ts, mounted /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_PORTFOLIO_TO_DOTNET` (three rewrites; /portfolio/summary MUST precede /portfolio/:id else the param swallows "summary"). **Self-scoped** (req.userId) — RequireIdentity + RLS, NO role/permission. New `StudentPortfolio` namespace: `IStudentPortfolioRepository` + repo + `StudentPortfolioValidation` (Zod-parity, first-error message in schema-declaration order — mirrors `TestScoreValidation`; createPortfolioSchema / updatePortfolioSchema.partial()). **Key parity:** hoursPerWeek/totalHours are `Decimal(65,30)` → emitted as decimal.js-normalized STRING on the row via `trim_scale(col)::text` (Prisma Decimal.toJSON) but summed as `::double precision` (== JS Number()) in the summary; create JS-|| defaults (`type` present-"" → "activity"; isCurrent||false; achievements/skills||[]; **activityCategory OMITTED-when-absent → DB default 'other'**, cast `@cat::"StudentActivityCategory"`); update = zod-partial + `findUnique` (no isActive) ownership 404 "Item not found" + per-field `bounded()` slice ({type:50,title:200,organization:200,startDate:50,endDate:50,description:2000,role:200,activityCategory:100, default 500} — strings only; **observable: `type` has no zod max so >50 is sliced**) + @updatedAt bump; attachments verbatim jsonb; summary = byType counts + totalHoursPerWeek (non-null sum) + totalVolunteerHours (type=="volunteer" non-null totalHours) + skills union + categories=byType.size. **Body handling:** express.json({strict:true}) → malformed OR top-level PRIMITIVE body → 500 (pre-route); an ARRAY → zod "Expected object, received array" → 400; empty → {} → "Required" (create). Gate: fresh reviewer **SHIP** (no BLOCKING/HIGH/MEDIUM/security — no IDOR, mass-assignment-guarded; **folded the primitive-body-500** [was passing primitives to zod→400]); LOWs = decimal.js exponential for non-physical extremes / jsonb numeric normalization / byType-skills determinism superset [legacy findMany has no orderBy] / dup-JSON-key last-vs-first — all pathological or documented. Schema hand-written from schema.prisma (the enum type + role/totalHours/weeksPerYear/achievements/activityCategory/endDate columns are **prod db-push drift**, no committed migration). 47 tests (23 validation unit + repo Testcontainers + endpoint faked-repo); full suite green (unit 704 + integration 1383[+2 skip] + contract 1); staging deploy 30052499380 + anon canary GREEN (all 5 routes → 401). ▶ NEXT = student.ts applications (FM-074).
- ✅ **R+W FIXED + LIVE (2026-07-28)** — student portfolio CRUD (FM-073, 5 endpoints) cut over to real prod traffic. Fourth write-coupled domain, first to establish a THIRD harness variant: self-scoped/identity-only (no server-side permission gate at all on this endpoint — `RequireSelf` is `RequireIdentity` only), soft-delete, cross-user-IDOR-only (ownership = `studentId==caller`, so the only meaningful adversarial probe is a different identity entirely, not a permission tier). No new fixtures needed — `test.student@formmaps.dev` as write actor (acting on its own portfolio), `test.schooladmin2@formmaps.dev` as the cross-user probe (404 on PUT/DELETE against another student's item id, per the uniform "Item not found" 404). Canary passed clean on the first try, both runs (direct-to-backend, then through `app.formmaps.com` post-flip) — no fix round needed. Tier-2 check (`verifyStudentPortfolioWriteInvariants.ts`, Prisma-based) confirmed `{"isActive":false,"organization":"Canary Org Updated"}` on the created-then-updated-then-deleted item — soft-delete and the update write both verified at the DB layer. `hoursPerWeek` asserted as the STRING `"5"`/`"7.5"` per the `trim_scale(...)::text` convention, not the number. Spot-checked counselor-notes + student course-plan post-flip — no regression. Flag `FORMMAPS_ROUTE_STUDENT_PORTFOLIO_TO_DOTNET` flipped. Legacy Node routes for these 5 endpoints now FROZEN.
- ✅ **R+W** student applications core CRUD (FM-074, DONE `9d4b5f9`) — GET /applications + GET /applications/deadlines + GET/PUT/DELETE /applications/:id + POST /applications (routes/student.ts + studentService.ts, /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_APPLICATIONS_TO_DOTNET` (3 rewrites; /applications/deadlines precedes /applications/:id). Self-scoped (RequireIdentity + RLS). New `StudentApplications` namespace: `IStudentApplicationRepository` + repo + `StudentApplicationValidation` (Zod-parity, first-error message in schema order; **createApplicationSchema with `.default()` on type→"university" + column→"researching"**). **Two validation styles:** POST = zod (400 on fail); **PUT = RAW body + `bounded()` per-key, NO zod** — Prisma enforces types → 500 **DEFERRED past ownership** (non-owner + bad body → 404 not 500, atomic one session), with per-column nullability (name/type/column reject null → 500; location/matchScore/deadline/notes accept null → NULL). **`matchScore` is `Int?` validated as `z.number()`** → a non-integer (POST) or out-of-int4-range (PUT) → 500 at the column. column/appStatus enum cast `::"ApplicationColumn"` (committed) / drift columns (fitClassification/applicationDeadline/deadlineType/universityId/appStatus + CollegeAppStatus enum) = **prod db-push drift**, harness DDL hand-written. express.json({strict:true}): malformed/primitive → 500; array → zod "Expected object, received array" 400 (POST) / no-keys empty update (PUT). The 3-seg sub-resource paths (essays/checklist/classify/ai-review) are NOT swallowed by the 2-seg `:id` → stay Node (essays+checklist = later slices; classify/ai-review/checklist-generate = Bedrock). Gate: fresh reviewer **FIXES-NEEDED** → folded **#2 BLOCKING** (PUT `matchScore` beyond int4 was `(int)`-wrapped + silently written + 200 → now rejected → 500, matching Prisma int4 overflow — a real data-integrity catch) + **#1 LOW** (POST non-string `column` → zod `invalid_type` "Expected '…', received <type>" not `invalid_enum_value`, since ZodEnum type-checks first). **#3** (anon+malformed → 401 vs 500) NOT folded = the universal express.json-before-auth divergence (RequireIdentity runs first). ⚠️ the enum `invalid_type` gap also exists in FM-073 portfolio + testscore + question360 validators — **future consistency pass**. 49 tests (17 validation unit + repo Testcontainers + endpoint faked-repo); full suite green (unit 719 + integration 1412[+2 skip] + contract 1); staging deploy 30056599381 + anon canary GREEN (all 6 → 401; /applications/x/essays → 404 = :id doesn't swallow the sub-path). ▶ NEXT = student.ts community-service (FM-075).
- ✅ **R+W** student community-service CRUD (FM-075, DONE `c28a8b5`) — GET/POST /community-service + PUT/DELETE /community-service/:id (routes/student.ts + studentService.ts, /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_COMMUNITY_SERVICE_TO_DOTNET` (2 rewrites). Self-scoped (RequireIdentity + RLS). New `CommunityService` namespace: `ICommunityServiceRepository` + repo + `CommunityServiceValidation`. **NEW parity surfaces (first in the migration):** (a) **`z.string().email()`** — reuses `ExternalEmailNormalization.IsValidZodEmail` (zod-v3 regex port); (b) **date `.refine(!isNaN(Date.parse(s)) && ≤ Date.now())`** → msg "Date must be a valid, non-future date", **using an injected TimeProvider for `Date.now()`**; (c) **update top-level `.refine("At least one field is required")`** firing ONLY when no field present (field-level errors win — zod runs fields before refines). **GET = computed envelope** `{data: entries, totalHours, totalHoursRequired}` — schoolId-scoped when the user has a school, date DESC, Σ hours (Decimal→::double), serviceHoursRequired(Int?) ?? 0 (0 for no-school). **hours Decimal → `trim_scale::text` string on row** (FM-073 pattern) + ::double sum. POST → NoSchool 400 "No school" + enum default pending; edit/delete gated on **owner+isActive+status=="pending"** (atomic gate+write one session) → 404 "Not found"; DELETE → {data:null}. `::"CommunityServiceStatus"` cast; express.json({strict}) malformed/primitive→500, array→zod 400. Gate: fresh reviewer **SHIP** (no HIGH/MEDIUM/security — no IDOR, mass-assignment-guarded; LOWs = whitespace-only-body 500-vs-400 [`IsNullOrWhiteSpace` convention across ALL slices] / empty-vs-NULL schoolId [FK-precluded] / >29-digit hours GetDecimal [shared] / id tie-break superset / tz-less datetime UTC-safe — all pathological/documented, no folds). 36 tests (15 validation unit + repo Testcontainers + endpoint faked-repo); full suite green (unit 734 + integration 1433[+2 skip] + contract 1); staging deploy 30062214232 + anon canary GREEN (all 4 → 401). ▶ NEXT = student.ts parents-link (FM-076).
- ✅ **R+W FIXED + LIVE (2026-07-28)** — student community-service CRUD (FM-075, 4 endpoints) cut over to real prod traffic. Fifth write-coupled domain, reusing the THIRD harness variant established by student portfolio (FM-073): self-scoped/identity-only (no server-side permission gate — `RequireSelf` is `RequireIdentity` only), soft-delete, cross-user-IDOR-only. **New wrinkle vs. portfolio:** edit/delete additionally gated on `status=="pending"` server-side (`IsEditableAsync`) — the canary's create→update→delete sequence stays within that window since create defaults to `pending`, so no extra handling was needed. No new fixtures — `test.student@formmaps.dev` as write actor, `test.schooladmin2@formmaps.dev` (rotated fresh this session via the `formmaps-migrate` ECS mechanism, no cached password existed) as the cross-user probe (404 on PUT/DELETE against another student's entry id). Canary passed clean on the first try, both runs (direct-to-`formmaps-api-prod`, then through `app.formmaps.com` post-flip) — third domain in a row with no fix round needed. Tier-2 check (`verifyCommunityServiceWriteInvariants.ts`, Prisma-based) confirmed `{"isActive":false,"organization":"Canary Org Updated","status":"pending"}` on the created-then-updated-then-deleted entry via a direct ECS diagnostic query. `hours` asserted as the STRING `"3"`/`"4.5"` per the `trim_scale(...)::text` convention (same as portfolio's `hoursPerWeek`). Playwright: `/dashboard/community-service` (real page, calls the endpoint on mount) renders the correct empty state (0 hrs across Verified/Pending/Remaining) with 0 console errors. Spot-checked student portfolio post-flip — no regression. Flag `FORMMAPS_ROUTE_STUDENT_COMMUNITY_SERVICE_TO_DOTNET` flipped. Legacy Node routes for these 4 endpoints now FROZEN.
- ✅ **R+W** student parent-links CRUD (FM-076, DONE `89e8feb`) — GET /parents + POST /parents/invite + DELETE /parents/:parentLinkId + POST /parents/:parentLinkId/resend (routes/student.ts + studentService.ts, /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_PARENTS_TO_DOTNET` (4 rewrites; /parents/invite precedes /parents/:parentLinkId). Self-scoped (RequireIdentity + RLS). **⚠️ NOT SES-coupled** (invite/resend only mint/refresh an `invitationToken` + return a frontend `invitationUrl` — NO email; the earlier "SES-coupled" flag in the FM-075 note was WRONG). New `StudentParents` namespace: `IStudentParentRepository` + repo. **Token via `InvitationTokenGenerator` (`crypto.randomBytes(32).toString("base64url")` port — already in repo); tokenExpiresAt = now+48h.** POST /parents/invite = **RAW body (no zod)**: parentEmail required (falsy→400 "parentEmail required"; **truthy non-string→500 reproducing `.toLowerCase()` throwing**); parentName||"" / relation||"parent" (truthy non-string→500 at Prisma String col); email lowercased. **unique (studentId, parentEmail) → a duplicate caught as Postgres 23505 → Duplicate → 500** (right {success,message} shape, NOT raw ASP.NET 500). **delete/resend gate = ownership-ONLY (NO isActive check — matches legacy `deleteParentLink`/`resendParentInvite`)** → 404 "Link not found". invite → {id, invitationUrl}; resend → {invitationUrl}; delete → {success:true}; list → full rows (raw-passthrough invitationToken = faithful, legacy emits it too). invitationUrl = `FRONTEND_BASE_URL || "https://app.formmaps.ai"`/parent/onboarding?token=X. Gate: fresh reviewer **SHIP** (no BLOCKING/HIGH/MEDIUM/security — no IDOR, mass-assignment-guarded, atomic gate+write one session; verified express global-500-on-body-error [index.ts:409 ignores err.status], base64url token, ownership-only gate; one LOW = Content-Type raw-body-read convention across ALL slices). Folded reviewer coverage note (delete already-inactive OWNED link → still true). 25 tests (repo Testcontainers incl. unique-dup + token-regen + endpoint faked-repo); full suite green (unit 734 + integration 1458[+2 skip] + contract 1); staging deploy 30063920868 + anon canary GREEN (all 4 → 401). ▶ NEXT Phase-C: **student.ts essays** (5 ep: POST/GET /applications/:id/essays + PUT /applications/:id/essays/:eid [non-AI] — the ai-review stays Node) + **checklist** (POST/GET /applications/:id/checklist + PUT .../:cid [non-AI] — generate stays Node) → **parent.ts** (child-link-scoped, IDOR corpus #1). **✅ student.ts CORE done: portfolio + applications + community-service + parents.**
- ✅ **R+W** student application ESSAYS + CHECKLIST CRUD (FM-077, DONE `2ce3de3`) — GET+POST /applications/:id/essays + PUT /applications/:id/essays/:eid + GET+POST /applications/:id/checklist + PUT /applications/:id/checklist/:cid (routes/student.ts + studentService.ts, /api/v1/student), the non-AI application sub-resources, under ONE dark flag `FORMMAPS_ROUTE_STUDENT_ESSAYS_CHECKLIST_TO_DOTNET` (4 rewrites). Gated by **verifyAppOwnership** (parent app.studentId == caller). New `StudentApplicationSubResources` namespace: one `IApplicationSubResourceRepository` + repo + one endpoints class (both sub-resources = one cohesive slice). **AI siblings stay Node (Bedrock):** POST .../essays/:eid/ai-review is a 5-seg path excluded by segment count (essays/:eid is single-segment); POST .../checklist/generate collides with /checklist/:cid (same single-trailing-segment shape) so :cid carries a **`((?!generate)[^/]+)` negative-lookahead** (FM-061 pattern — cids are UUIDs; the staging canary confirmed a direct .NET POST to /checklist/generate → 405 on the {cid} PUT route, i.e. exactly what the lookahead prevents from being rewritten in prod). Disjoint from the FM-074 /applications/:id (2-seg) block. **Parity nailed:** POST required-field (title / itemName) JS-truthy gate → 400 "<field> is required" **BEFORE** ownership; every other type mismatch = a Prisma reject → **500 DEFERRED past ownership** (create) and **past ownership AND the sub-resource's own existence 404** (update) — the FM-074 raw-PUT deferred pattern with an extra 404 layer. **Two distinct dueDate resolvers:** create `x ? new Date(x) : null` (JS-falsy→null, string parse, **number = epoch ms**, `true` = new Date(1), object/array→500) vs update = the RAW value Prisma coerces **as an ISO string ONLY** (null→NULL, string parse, **number/bool → Prisma DateTime reject → 500**). Create coalescing: prompt/notes `|| null`, category `|| "other"`, wordLimit `|| null` (**0 → null on CREATE**); update wordLimit has no `||` so **0 is a stored value**. Essay update applies bounded() slices {title:200, prompt:2000, currentDraft:50000, status:50, dueDate:50} + **bumps draftVersion on a currentDraft change** (raw compare, `|| 0` fallback); checklist update = raw (NO bounded) + **sets/clears completedAt on an isCompleted transition** (true when stored-false → now; false when stored-true → NULL; no-op otherwise). Fixed-column INSERT/SET (mass-assignment guard); ownership at the parent-app level under RLS; timestamps Kind=Unspecified + ms-trunc; response = raw Prisma passthrough (every column, camelCase, schema order, dates ISO-Z). Gate: fresh reviewer **SHIP** (no BLOCKING/HIGH/MEDIUM — no IDOR, mass-assignment-guarded, both 404 gates verified before the deferred 500). Two LOWs ACCEPTED, no folds (both precedent-consistent with the shipped FM-065/072 create-date resolver): (a) single-element-array dueDate on CREATE → the port 500s but JS `new Date(["2024-01-01"])` ToPrimitive→string→a valid date [pathological; would need a cross-cutting change to the shared resolver — a future consistency pass]; (b) tz-less datetime string → AssumeUniversal UTC vs JS server-local [documented, UTC-safe]. 61 tests (endpoint faked-repo + repo Testcontainers incl. ownership/deferred-500/bounded-slice/draftVersion-bump/completedAt-transition); full suite green (unit 736 + integration 1519[+2 skip] + contract 1); staging deploy 30066222653 + anon canary GREEN (all 6 → 401; ai-review → 404, generate → 405 on direct-.NET = lookahead validated). **✅ student.ts non-AI surface COMPLETE (portfolio + applications + community-service + parents + essays + checklist).** ▶ NEXT = **parent.ts** (child-link-scoped, IDOR corpus #1, ~395 LOC).
- ✅ **R+W FIXED + LIVE (2026-07-28)** — student applications (FM-074, 5 endpoints), parent-links (FM-076, 4 endpoints), and essays+checklist (FM-077, 4 endpoints) ALL THREE cut over to real prod traffic in one pass (13 endpoints total). Ninth, tenth, and eleventh write-coupled domains — all self-scoped identity-only, reusing the exact portfolio/community-service harness pattern (test.schooladmin2 as the cross-user-identity adversarial probe, no separate deny tier needed). Zero new fixtures across all three. FM-074 shares the `student_applications` table with FM-081 (college applications) — same table, two independent route surfaces, matching legacy Node's own design; reused FM-081's Tier-2 invariant script directly rather than duplicating it. FM-076's canary used a disposable, never-before-seen invite email (not `test.parent@formmaps.dev`, which already holds a permanent accepted link for FM-079 — reusing it would have collided with the unique `(studentId, parentEmail)` constraint and hit the domain's own documented "duplicate invite → 500" quirk). FM-077's canary bootstraps a throwaway application via FM-074's own endpoint, creates+updates an essay and a checklist item against it (no delete exists for either sub-resource — confirmed from source, none attempted), then relies on the canary engine's per-step `cleanup` field to soft-delete the bootstrap application unconditionally at the end. **All three canaries passed clean on the first try** — ninth, tenth, and eleventh domains in a row with zero fix rounds. One combined Tier-2 diagnostic query (via `formmaps-migrate`) confirmed every invariant at once: the application row `{isActive:false, notes:"Canary note updated"}`, the parent link `{isActive:false}`, the bootstrap application `{isActive:false}`, the essay `{isActive:true, status:"in_review"}` (correctly still active — no delete expected), and the checklist item `{isActive:true, isCompleted:true}`. Spot-checked college favorites + community-service post-flip — no regression. Flags `FORMMAPS_ROUTE_STUDENT_APPLICATIONS_TO_DOTNET` + `FORMMAPS_ROUTE_STUDENT_PARENTS_TO_DOTNET` + `FORMMAPS_ROUTE_STUDENT_ESSAYS_CHECKLIST_TO_DOTNET` all flipped in the same deploy. Legacy Node routes for all 13 endpoints now FROZEN. **Only FM-078 (parent portal) remains in the student/parent cluster** — still recommended for its own dedicated session per its dual-party ownership model, though `test.parent@formmaps.dev` is now already rotated and tokenable from this session, removing one of its two stated blockers.
- ✅ **R+W** parent portal SELF-SCOPED surface (FM-078, DONE `f863b92`) — GET /profile + GET /notifications + PUT /notifications/read-all + PUT /notifications/:id/read + GET /evaluations/pending + DELETE /:parentLinkId (routes/parent.ts, /api/v1/parent), the 6 authenticated caller-owned endpoints, under ONE dark flag `FORMMAPS_ROUTE_PARENT_PORTAL_TO_DOTNET` (6 rewrites). New `ParentPortal` namespace (`IParentPortalRepository` + repo + endpoints). **Stays Node:** onboarding verify+complete (anonymous flow ending in password-hash + JWT + refresh + auth-cookie write), POST /invite + POST /:parentLinkId/resend (SES email — the standing email-stays-Node decision), and the child-link-scoped child reads /children/:studentId/progress + course-plan (IDOR corpus #1, compute-heavy + runAsSystem — the FM-079 slice). **Parity:** GET /profile spreads `{...user, children}` (absent user → just `{children}`; children = accepted+active links inner-joined to the child user [orphan links dropped], shape {studentId, studentName, gradeLevel, relationship:l.relation}); GET /notifications page/limit clamp `Math.max(1,parseInt||1)`/`Math.min(50,Math.max(1,parseInt||20))` (JS parseInt + `||`-falsiness via `PcaExamPagination.JsParseInt`+FalsyOr) + `unreadOnly=="true"` literal + `{data:{data,total,page,limit}}`; PUT mark-read + DELETE link both return **403 "Access denied" uniformly** (missing == unauthorized — no existence leak); DELETE dual-party ownership (parentUserId OR studentId == caller); mark-all `updateMany {userId,isRead:false}` **NO isActive filter** → `{updatedCount}`; evaluations/pending scoped by the **DB email lowercased** (not the token) with `!email → []` and `evaluatedUser.name || "your student"` fallback (null OR ""), token returned (evaluator's own invite). **The DELETE /:parentLinkId single-seg catch-all rewrite carries a `((?!profile|notifications|invite|onboarding|children|evaluations)[^/]+)` negative-lookahead** — critically keeping POST /invite on Node (canary: a direct-.NET POST /invite → 405 on the {parentLinkId} DELETE route = exactly what the lookahead prevents from being rewritten). Fixed-column writes (mass-assignment guard); reads read-only / writes writable+commit; timestamps Kind=Unspecified+ms-trunc; ORDER BYs add an id tie-break (deterministic superset). Gate: fresh reviewer (read all files, 2nd attempt after a 0-tool no-op) **SHIP** (no BLOCKING/HIGH/MEDIUM; 4 LOWs = deterministic-order superset / extra `code` on 401 / null-field JSON [confirmed no global WhenWritingNull] / RLS-parity established — no folds). 32 tests (endpoint faked-repo + repo Testcontainers incl. profile-join + dual-party delete IDOR + pending email-scope + pagination clamp); full suite green (unit 736 + integration 1551[+2 skip] + contract 1); staging deploy 30068224591 + anon canary GREEN (6→401; invite→405 [lookahead-validated], onboarding/children/resend→404). ▶ NEXT = **FM-079** = the child-link-scoped child reads /children/:studentId/progress (link→403 "Not linked", GPA/credits/assessment badges) + /children/:studentId/course-plan (link→404, runAsSystem plan/target/coursePlan reads).
- ✅ **R** parent child-link-scoped reads (FM-079, DONE `359b50e`) — GET /children/:studentId/progress + GET /children/:studentId/course-plan (routes/parent.ts, /api/v1/parent), the two authenticated CROSS-USER reads (a parent reading a linked child), under ONE dark flag `FORMMAPS_ROUTE_PARENT_CHILD_READS_TO_DOTNET` (2 rewrites). **IDOR corpus #1** — both gated by an accepted+active StudentParentLink (parentUserId==caller, isActive, isAccepted); no-link / unaccepted / inactive / other-parent's link all rejected. New `ParentChildReads` namespace (`IParentChildReader` + reader + endpoints). **✅ parent.ts COMPLETE on the .NET side** (only onboarding [auth-cookie/JWT/password] + invite/resend [SES] stay Node). **Parity:** progress distinguishes link→**403 "Not linked to this student"** vs student-missing→**404 "Student not found"** (link check BEFORE the user lookup); course-plan collapses both to **404 "Student not found"** (existence-hiding). **Two RLS sessions (the load-bearing subtlety):** progress reads assessments/credits/grades on the caller's **Identity** session (legacy tenantContext, NO runAsSystem); course-plan checks the link on Identity then reads plan/target/course-plan on a **System (RLS-bypass)** session (legacy runAsSystem — a school-less parent's tenant RLS can't see those school-scoped rows). **Compute:** GPA = Round2 (`SchoolAnalyticsMath.JsRound(x*100)/100`) over the A..F gradeMap (unknown/empty/whitespace grade → no point); **GPA is NOT school-gated** (computed for school-less students too — a bug I caught mid-build where GPA was wrongly nested inside `if(schoolId)`, fixed + red-if-regressed test) while creditsEarned IS school-gated (Σ Number(credits) raw/unrounded, only when the student has a school); creditsRequired = the current-AY active rule set's totalCreditsRequired (two-step findFirst) else 120; **percentage = Math.round(earned/required*100) with NO 100 cap** (unlike the counselor caseload — pinned by a >100% test); mil averageScore = round(avg scorePercentage); pca completed = any pca_evaluations row (NOT isActive-filtered); eval360 active-total + completed; isOnTrack = gpa==null || gpa>=2.0. Decimal→double = JS Number(); dates ISO-Z `.fff`. Gate: fresh reviewer (read all files) **SHIP** (no BLOCKING/HIGH/MEDIUM; 3 LOWs = progress-under-Identity-RLS faithful-to-legacy / int-cast-safe / plan tie-break faithful — no folds). ⚠️ **CUTOVER-VERIFY** (corpus rule): before flipping the flag, confirm a REAL linked parent sees real progress data (if prod RLS restricts a school-less parent's assessment/grade reads, progress under-reports — faithful to legacy, but the authed-round-trip gate must confirm). 34 tests (endpoint faked-reader + reader Testcontainers incl. link IDOR gate + GPA/credit/badge compute + no-100-cap + school-less-GPA + course-plan System reads). Full suite green (unit 736 + integration 1568[+2 skip] + contract 1); staging deploy 30070091125 + anon canary GREEN (both→401; FM-078 siblings still 401 = no regression). ▶ NEXT = a new domain (parent.ts + student.ts non-AI both DONE) — Phase D (academic-gaps.ts / college.ts / course-planning) or the deferred school.ts/school-courses.ts writes.

### Phase D — Recommendations / academic gaps / course planning (domain 6) — **~8–12 slices**
- `academic-gaps.ts` (535), `college.ts` (395, college-fit Decimal), course planning, superscore.
- ✅ **R** academic-gaps non-AI reads (FM-080, DONE `8eab1b5`) — the FIRST Phase-D slice. GET /summary + /students/:studentId + /recommendations/:studentId (routes/academic-gaps.ts, mounted /api/v1/school-admin/academic-gaps) under one dark flag `FORMMAPS_ROUTE_ACADEMIC_GAPS_TO_DOTNET` (3 rewrites). The 4th route /ai-recommendations/:studentId (Bedrock) STAYS Node (distinct literal, keeps aiLimiter). New `AcademicGaps` namespace: a pure `AcademicGapsComputer` (ports lib/gradMatch `courseMatchesCategory` + lib/gradStatus `creditDeficitStatus` + each endpoint's per-student gap math) + `AcademicGapsReader` (read-only RLS loads). New permission `grades:read` (coarse) + getUserAndSchool rail (schoolId null→400 "No school linked" BEFORE the role check; roleName.ToLowerInvariant() not in {school_admin,counselor}→403 "Forbidden", raw compare NOT a normalizer; counselor scoped to active assignments). 404 collapse (missing/wrong-school/counselor-unassigned → 404 "Student not found"). **Two DELIBERATE per-endpoint credit divergences preserved:** /summary earned adds Number(g.credits) even when the grade's course is MISSING, vs /students totalCreditsEarned SKIPS a missing-course grade (`if(!course)continue`); the elective fallback is applied ONLY in /students' catCredits (/summary missing-count + /recommendations do not). **Asymmetric empty shapes:** /summary no-rules/no-students → {data:[]} (NO summary key); /students no-rules → 3-field {gaps:[],creditsEarned:0,creditsRequired:0} (NO identity fields, vs the 6-field happy path); /recommendations no-rules → {recommendations:[]}. progressPercent = min(100, JsRound(earned/req*100)); status on_track/off_track(strict >30%)/at_risk; recommendations = per short category first-3 available with a JS-number reason string. Credits ::double precision. The TS `areaStats` block is dead code (never returned) — correctly omitted. Determinism ORDER BY supersets over legacy unordered queries. Gate: fresh reviewer (17 tools, read all files) **SHIP** — all 12 checkpoints faithful, only accepted determinism supersets. 48 tests (18 computer unit + 12 endpoint faked-reader + 18 reader Testcontainers; new harness academic-gaps-schema.sql, first category_requirements use). Full suite green (unit 754 + integration 1598[+2 skip] + contract 1); staging deploy 30081231364 + anon canary GREEN (3 reads→401; ai-recommendations→404 = Node-only). **▶ NEXT Phase-D = `college.ts`** (~395, college-fit Decimal CRUD: applications/list/essays/comments) / course-planning / superscore — OR the deferred school.ts/school-courses.ts writes (iSAMS write, staff-invite [needs .NET SES], logo→S3).
- ✅ **CRUD** college applications (FM-081, DONE `b06c439`) — **sub-slice 1 of 3 of the `college.ts` mini-phase.** GET+POST /students/:studentId/applications + PUT+DELETE /applications/:id (routes/college.ts Feature 1, mounted /api/v1/college) under ONE dark flag `FORMMAPS_ROUTE_COLLEGE_APPLICATIONS_TO_DOTNET` (2 rewrites, co-flip path-not-method). Mount = authenticate + tenantContext, NO subscription → RequireIdentity only. **Establishes the shared `ICollegeAccessResolver`** (the `getStudentAccess` rail, reused by FM-082/083): a FRESH `users` read of the caller's OWN {schoolId,roleName} (NOT the JWT); schoolId null → deny; role.ToLowerInvariant() **sequential (NOT else-if) guards** — student→self-only, counselor→active counselor_student_assignments, school_admin→target shares caller school, role∉{student,counselor,school_admin,super admin}→deny, super admin unrestricted. **Every caller collapses ANY access failure to a uniform 404 "Not found"** (legacy discards getStudentAccess's internal 400/403/404). **Parity:** PUT/DELETE findUnique {id,isActive:true} → **404 "Application not found" (DISTINCT)** BEFORE the access **404 "Not found"**. List = REDUCED shape (collegeName=name, deadlineDate=applicationDeadline) with **checklistCount/essaysCount UNFILTERED by isActive** (Prisma _count) via correlated subqueries; createdDate DESC (+id tie-break). Create: required collegeName||universityId (JS-truthy) → 400 AFTER access; name = universities lookup (uni.name) else collegeName, JS-|| "Unknown"; type='college'; statusToColumn map; appStatus stored RAW → invalid-enum / truthy-non-string → 500; `x?new Date(x):null` create date resolver; deadlineType/fitClassification/universityId = x||null (empty-string falsy→null, truthy-non-string→500). Update: present-keys-only RAW, **NOT bounded** (college.ts does not slice); String? cols null→NULL / string(incl "")→value / non-string→500; appStatus raw enum-500; column-sync when appStatus is a truthy statusToColumn key; **type-500 DEFERRED past the existence(404)+access(404) gates**. Delete: existence→404 / access→404 / soft-delete / returns {success:true} ONLY (no data). college.ts Features 2 (search/list/favorites) + 3 (essays/comments) stay Node (later sub-slices). Gate: fresh reviewer (21 tools, read all files + TS) **SHIP** — no BLOCKING/HIGH/MEDIUM; 2 LOWs pre-existing/inherited (shared FM-065/077 date-resolver epoch edge; FM-074 row shape). 34 tests (7 access-resolver + 6 repository Testcontainers + 14 endpoint faked; new harness college-applications-schema.sql). Full suite green (unit 754 + integration 1632[+2 skip] + contract 1); staging deploy 30085153508 + anon canary GREEN (4 endpoints→401; /search + /students/:id/list + /essays/:id/comments → 404 = Node-only). **▶ NEXT college.ts = FM-082 Search+Favorites** (GET /search + GET/POST /students/:id/list + PUT/DELETE /list/:id — University Decimal→number, favorite upsert-reactivate + 409 "Already in list") → **FM-083 Essays+Comments** (collegeEssay + essayComment; wordCount JS `content.trim().split(/\s+/).length` quirk). Both reuse `ICollegeAccessResolver`.
- ✅ **R+W FIXED + LIVE (2026-07-28)** — college applications CRUD (FM-081, 4 endpoints) cut over to real prod traffic. Sixth write-coupled domain overall, FIRST role-based-access domain (every prior write-coupled cutover was either simple RBAC or fully self-scoped) — reused the same `batch-canary-write.mjs` harness with a real permission tier for the first time. Deny probe = `test.parent@formmaps.dev` (role `parent`, outside `ICollegeAccessResolver`'s allowed set `{student,counselor,school_admin,super admin}` — always denied regardless of school, confirmed via a direct DB role check before trusting it); cross-tenant probe = `test.schooladmin2@formmaps.dev` (school_admin of `test-school-2`, denied via the target-shares-school check). Both fixtures needed a fresh password rotation this session (no cached password existed for either on this machine) — ran as two independent `formmaps-migrate` ECS tasks, one password per fixture. Canary passed clean on the first try, both runs (direct-to-`formmaps-api-prod`, then through `app.formmaps.com` post-flip) — **fourth domain in a row with zero fix rounds** (Counselor Notes → Student Portfolio → Community Service → College Applications). Tier-2 diagnostic query (via `formmaps-migrate`, not the checked-in `verify-college-applications-write-invariants.ts` — same rationale as community-service, avoids a throwaway ops-image rebuild) confirmed `{"isActive":false,"name":"__CANARY_CA_2026-07-28__","appStatus":"applying"}` on the created-then-updated-then-deleted row. Spot-checked community-service post-flip — no regression. Flag `FORMMAPS_ROUTE_COLLEGE_APPLICATIONS_TO_DOTNET` flipped. Legacy Node routes for these 4 endpoints now FROZEN. **Sets up FM-082/083 to reuse the same access-resolver + fixture set with no new scoping work.**
- ✅ **SEARCH+CRUD** college search + favorites (FM-082, DONE `a713e5e`) — **sub-slice 2 of 3 of the `college.ts` mini-phase.** GET /search + GET/POST /students/:studentId/list + PUT/DELETE /list/:id (routes/college.ts Feature 2) under ONE dark flag `FORMMAPS_ROUTE_COLLEGE_FAVORITES_TO_DOTNET` (3 rewrites). **`/search` has NO access gate** (any authenticated caller): dynamic WHERE (isActive + `name ILIKE '%q%'` [Prisma contains-insensitive, wildcards unescaped] + `state=` + acceptanceRate `>=min`/`<=max`); **minAdmRate/maxAdmRate = JS `parseFloat`** (ported prefix-parse incl. NaN/Infinity; a present-empty query param is JS-falsy → filter dropped); 17 select fields — **acceptanceRate `Decimal`→number** (`::double precision`), sat*/act*/studentCount `Int?`→number, **tuition `Json`→jsonb passthrough**, rest strings; take 20, name ASC. **Favorites reuse `ICollegeAccessResolver`**: list = **INNER JOIN universities** (Prisma include, no university.isActive filter) + nested `university:{10-field subset}`, userId+isActive scope, createdDate DESC; POST **upsert-reactivate on the unique (userId,universityId)** — access→400 "universityId required" (after access)→non-string-uid→500→findUnique→existing&&active→**409 "Already in list" (short-circuits BEFORE the fit type-check)**→else reactivate/create→201; **create-vs-reactivate fitClassification asymmetry** (CREATE absent-fit→NULL default; REACTIVATE absent-fit→UNCHANGED [Prisma undefined]); PUT /list/:id (fit raw update, deferred-500, 404 "Not found") + DELETE (soft, `{success:true}`). Gate: fresh reviewer (19 tools, read all files + TS + query.ts + models) **SHIP** — no BLOCKING/HIGH/MEDIUM; 3 LOWs pathological/convention (bracket-notation query-param coercion; parseFloat-NaN acceptanceRate filter [Prisma `gte:NaN` = a pre-cutover live-probe note]; DELETE/GET malformed-body convention). 23 tests (5 repository Testcontainers + 14 endpoint faked; new harness college-favorites-schema.sql). Full suite green (unit 754 + integration 1655[+2 skip] + contract 1); staging deploy 30087856552 + anon canary GREEN (5 endpoints→401; /essays/:id/comments → 404 = Feature-3 Node-only). **▶ NEXT college.ts = FM-083 Essays+Comments** (GET+POST /students/:id/essays, PUT+DELETE /essays/:id, POST+GET /essays/:id/comments — collegeEssay + essayComment; **wordCount JS `content.trim().split(/\s+/).length`** [whitespace-only→1]; comment author join) — **COMPLETES the college.ts mini-phase.**
- ✅ **ESSAYS+COMMENTS** college essays + comments (FM-083, DONE `35979ae`) — **sub-slice 3 of 3 of the `college.ts` mini-phase → COMPLETES it (Features 1/2/3 all ported).** GET+POST /students/:studentId/essays + PUT+DELETE /essays/:id + POST+GET /essays/:id/comments (routes/college.ts Feature 3) under ONE dark flag `FORMMAPS_ROUTE_COLLEGE_ESSAYS_TO_DOTNET` (3 rewrites; single-segment /essays/:id does NOT swallow /essays/:id/comments → no lookahead). All six **reuse `ICollegeAccessResolver`** (any access failure → uniform 404 "Not found"); PUT/DELETE + both comment routes findUnique {id,isActive:true} → **distinct 404 "Essay not found"** BEFORE access. **`wordCount = content ? content.trim().split(/\s+/).length : 0`** ported via the EXACT ECMAScript whitespace set (`JsString` — 25 code points, incl U+FEFF, excl U+0085 — matches JS `.trim()` AND `\s`); a **whitespace-only content string → 1** (trim→""→split→[""]→length 1). **create-vs-update content asymmetry:** CREATE `content||null` (""→null) vs UPDATE RAW (""→"" stored); explicit `wordCount` override wins over the content-derived value. Deferred type-500s past the existence(404)+access(404) gates (title String NOT NULL; status EssayStatus `::"EssayStatus"` cast; wordCount Int; content String? — a truthy-non-string/`false`/invalid-enum/non-int → 500). express.json malformed/primitive body → 500 BEFORE the 404s. List = FULL essay row + **UNFILTERED `_count.comments`** (Prisma _count, no isActive) createdDate DESC; comment POST content-required→400 (after essay-404+access-404), authorId=caller; comment GET include author:{id,name,roleName} (users INNER JOIN, both non-null), createdDate ASC. Delete soft, `{success:true}` only. No AI/Bedrock sub-routes on Feature 3. Gate: fresh reviewer (11 files) **SHIP** — no BLOCKING/HIGH/MEDIUM; 1 LOW = id-ASC determinism-superset tie-break (accepted, no fold). 46 tests (8 repository Testcontainers + 38 endpoint faked; new harness college-essays-schema.sql). Full suite green (unit 754 + integration 1701[+2 skip] + contract 1); staging deploy 30090803917 + anon canary GREEN (6 endpoints→401; unmapped /college/essays → 404). **▶ NEXT Phase-D: course-planning / superscore OR the deferred school.ts / school-courses.ts writes (iSAMS write, staff-invite [needs .NET SES], logo→S3).**
- ✅ **R+W FIXED + LIVE (2026-07-28)** — college favorites (FM-082, 3 endpoints) AND college essays+comments (FM-083, 3 endpoints) both cut over to real prod traffic in the same session as FM-081. Seventh and eighth write-coupled domains, both reusing FM-081's `ICollegeAccessResolver` + fixture set (`test.parent` deny, `test.schooladmin2` cross-tenant) with zero new scoping work — the payoff of doing FM-081 first. Favorites: real universities catalog confirmed already seeded in prod (no new fixture needed), canary exercises the upsert-reactivate path (add→update fitClassification→delete). Essays: canary exercises create→add-comment→update status→delete in one plan; comments have no delete endpoint so none was expected. **Both canaries passed clean on the first try** — sixth (favorites) and seventh (essays) domains in a row with zero fix rounds. Tier-2 diagnostic queries (via `formmaps-migrate`, same rationale as FM-075/081) confirmed `{"isActive":false,"fitClassification":"Canary Reach"}` on the favorite and `{"isActive":false,"title":"__CANARY_ESSAY_2026-07-28__","status":"in_review"}` on the essay. Spot-checked college applications post-flip — no regression. Flags `FORMMAPS_ROUTE_COLLEGE_FAVORITES_TO_DOTNET` + `FORMMAPS_ROUTE_COLLEGE_ESSAYS_TO_DOTNET` both flipped in the same deploy. Legacy Node routes for all 6 endpoints now FROZEN. **Completes the entire college.ts mini-phase (FM-081/082/083) on real prod traffic.**
- ✅ **CRUD** student course-planning (FM-084, DONE `13302b4`) — **sub-slice 1 of 3 of the `course-plan.ts` mini-phase** (the student's OWN self-scoped surface, distinct from the school-admin course-plan reads [FM-064] + parent child course-plan reads [FM-079]). GET /course-plan + POST /course-plan/courses + DELETE /course-plan/courses/:courseId (routes/course-plan.ts, mounted /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_COURSE_PLAN_TO_DOTNET` (3 rewrites, co-flip path-not-method). Self-scoped (RequireIdentity, caller Identity/tenant RLS — authenticate+tenantContext, no runAsSystem). **Establishes the `requireSchoolMembership` rail** (a fresh `users` read of the caller's OWN schoolId): a missing user row OR null schoolId is school-less → GET returns the minimal `{plan:{enrollments:[]},recommendations:[]}` 200 (**NO studentId/gradeLevel/totalCreditsEarned keys**), POST/DELETE → **400 "You are not affiliated with a school"**. **Parity:** GET resolves the year via `?academicYearId` → findUnique by id **ONLY (NOT school-scoped** — RLS bounds it) else the current-year findFirst(isCurrent); a null year → no plan rows. enrollments = **RAW active studentCoursePlan passthrough** (full row, 14 cols schema order; term/status/notes/createdBy/updatedBy nullable), scoped studentId+schoolId+academicYearId+isActive, ORDER BY sortOrder ASC (+id tie-break). totalCreditsEarned = Σ over the caller's active **COMPLETED** grades of Number(credits) (`::double precision`), **computed even when no year resolves**. POST maps **semester→term** (String?: absent/null→NULL, string→value, non-string→500) + courseId (required String: missing/null/non-string→500, **empty-string "" VALID**), status hardcoded "planned"; the Prisma type-500s **DEFER past the NoSchool/NoCurrentYear 400 gates**; `{success:true}` only. DELETE = **hard** deleteMany scoped studentId+courseId+status="planned"+schoolId, always `{success:true}` once the caller has a school (idempotent). change-requests + recommendations + eligibility on the same router **stay Node** (later slices; the bare /course-plan exact source does NOT swallow them). No AI/Bedrock. New `StudentCoursePlan` namespace (`IStudentCoursePlanRepository` + repo + endpoints). Gate: fresh reviewer (15 tools, read all port files + course-plan.ts + access.ts + query.ts + schema.prisma + tests + next.config) **SHIP** — no BLOCKING/HIGH/MEDIUM; folded 2 test-coverage pins (empty-string courseId→Created; explicit-null semester→NULL term); 2 LOWs accepted no-fold (empty-string schoolId unreachable UUID-FK; **bracket-notation `?academicYearId[x]=y` coercion** → empty-enrollments vs current-year, pathological caller-own-data-only, same accepted class as FM-082). 33 tests (14 repository Testcontainers + 19 endpoint faked; new harness student-course-plan-schema.sql). Full suite green (unit 754 + integration 1734[+2 skip] + contract 1); staging deploy 30106417426 + anon canary GREEN (3 endpoints→401 missing_identity; change-requests/recommendations/eligibility → 404 = Node-only). **▶ NEXT course-plan.ts = FM-085 change-requests** (POST/GET/DELETE /course-plan/change-requests[/:requestId] — courseChangeRequest create/list/cancel, schoolAssessmentSettings deadline default) → **FM-086 recommendations + eligibility** (the two compute GETs: port checkAssessmentCompletion [wraps computeStudentCompletion] + computeEligibilityMap [reuses FM-057 prereq logic]).
- ✅ **CRUD** student course change-requests (FM-085, DONE `f5469f9`) — **sub-slice 2 of 3 of the `course-plan.ts` mini-phase.** POST+GET /course-plan/change-requests + DELETE /course-plan/change-requests/:requestId (routes/course-plan.ts L92-143, mounted /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_CHANGE_REQUESTS_TO_DOTNET` (2 rewrites, path-not-method co-flip). Self-scoped; reuses the FM-084 `requireSchoolMembership` rail (school-less → POST/DELETE 400 "You are not affiliated with a school", GET the empty envelope). **POST** replays the legacy raw-body coalescing into a courseChangeRequest create: courseId (required String), **credits = body.credits || 0** (JS-falsy→0; number→value; numeric string→decimal.js parse; truthy non-numeric→500), **gradeLevel = body.gradeLevel || user.gradeLevel || 9** (a truthy body.gradeLevel must be an **integer-valued Number in int4 range** — accepts `10.0`/`1e1` tokens [F1 fold], fractional/out-of-range/string/true→500), action (required CourseChangeAction enum, `::"CourseChangeAction"` cast → invalid label 500 via caught PostgresException), **dueDate = body.dueDate ? new Date(body.dueDate) : settings.courseRequestDeadline || null** (truthy→TryResolveJsDate [string/epoch-ms/true=1ms/invalid→500]; falsy/absent→the schoolAssessmentSettings deadline or null), nullable strings raw. Every Prisma type-500 is **DEFERRED past the membership 400**; response `{success:true, data:<full 21-col row>}` @201. **GET** paginates (page max(1,parseInt||1) / limit min(50,max(1,parseInt||20))) the caller's rows where studentId+schoolId+isActive (+ `?status` raw → enum cast → invalid label 500), createdDate DESC (+id tie-break); no-school → 200 `{data:[],total:0,page,limit,totalPages:0}` (totalPages:0 literal); happy → totalPages=ceil(total/limit). **DELETE** = findUnique by id (**NO isActive filter**) → !row/not-owner/wrong-school/status≠pending → **400 "Cannot cancel"**; else soft isActive=false → `{success:true}`. Full-row passthrough: **credits decimal.js STRING** (trim_scale::text), action/status labels, dates ISO-Z. recommendations + eligibility on the same router **stay Node** (FM-086). Gate: fresh reviewer (19 tools, read all files) **SHIP** — no BLOCKING/HIGH; folded F1 (gradeLevel `10.0`/`1e1` integral-Number-token → now 201) + endpoint clamp/integral tests; F2 (credits JSON number > C# decimal range → fail-closed residual, documented) + F3 (dueDate single-elem-array coercion) accepted no-fold (pathological, DataMappings-residual class). 50 tests (repository Testcontainers + endpoint faked; new harness course-change-requests-schema.sql). Full suite green (unit 754 + integration 1784[+2 skip] + contract 1); staging deploy 30110429562 + anon canary GREEN (3→401 missing_identity; FM-084 /course-plan still 401 = no regression; /course-plan/recommendations + /course-plan/eligibility → 404 = Node-only). **▶ NEXT course-plan.ts = FM-086 recommendations + eligibility** (the two compute GETs: port `checkAssessmentCompletion` [wraps computeStudentCompletion over 4 DB loads] + `computeEligibilityMap` [whole-catalog prereq map, reuses FM-057 checkEligibility logic]; userPreferences.preferredFields course keyword scoring, take 100 slice 10) — **COMPLETES the course-plan.ts mini-phase + Phase-D non-Bedrock surface.**
- ✅ **COMPUTE** course-plan recommendations + eligibility (FM-086, DONE `a55cbdb`) — **sub-slice 3 of 3 of the `course-plan.ts` mini-phase → COMPLETES it (FM-084/085/086) + closes Phase-D's non-Bedrock surface.** GET /course-plan/recommendations + GET /course-plan/eligibility (routes/course-plan.ts L149-200, mounted /api/v1/student) under ONE dark flag `FORMMAPS_ROUTE_STUDENT_COURSE_PLAN_COMPUTE_TO_DOTNET` (2 rewrites). Self-scoped. **LOCAL keyword scoring — NOT Bedrock** (the aiLimiter'd recs live in the separate course.ts → Node). **recommendations** gates on **checkAssessmentCompletion** (ported: 4 loads — pca_exam_sessions completed exam types, lia_assessment_sessions completed **parity** session [**snake_case** user_id/is_active], evaluation_groups, pca_evaluations **no-isActive** — into the REUSED `SchoolAdmin.StudentCompletion` = computeStudentCompletion [liaCompleted **DISTINCT**≥5, evalRequired=min(evalTotal,3), pcaCompleted=any]); **!allDone → `{data:[],locked:true,completion:<7 fields incl readyForInsights>}` (NOT school-gated)**; else score the active catalog (take 100, ORDER BY id superset) minus enrolled: base 50, +15 per lowercased preferredField substring of `title shortDescription category`, +10 if rating>4 (the always-truthy Prisma-Decimal `c.rating &&` guard collapsed), min(100), **stable** sort desc, take 10; response items = **`{...course, matchScore}` the FULL 36-col Course passthrough** (rating/recommendedScore Decimal→decimal.js **STRING**, arrays, jsonb syllabus, ISO-Z dates, null createdBy/updatedBy **emitted**). **eligibility**: no schoolId → `{data:[]}`; else **computeEligibilityMap** (FULL school catalog by UPPER(code) resolution [incl inactive/draft], enumerate active+status="active", grade-level truthiness `g!=0`, **raw code** for uncataloged prereq vs **resolved code** for uncompleted) → reduced `{courseId,courseCode,eligible,missing}`. New `CoursePlanComputers` (pure scorer + eligibility map) + `ICoursePlanComputeReader` + reader + endpoints; reuses `StudentCompletion`. Gate: fresh reviewer (27 tools, read all files) **SHIP** — no BLOCKING/HIGH/MEDIUM; verified every legacy quirk (no-isActive pca_evaluations, rating-truthy collapse, min(evalTotal,3), DISTINCT-LIA, raw-vs-resolved missing, 0-gradeLevel falsiness, 36-field passthrough null-emit); folded 2 test-hardening pins; 1 LOW (syllabus non-canonical jsonb number formatting) accepted no-fold (unreachable, more-faithful-to-DB). 24 tests (9 computer unit + 7 reader Testcontainers + 8 endpoint faked; new harness course-plan-compute-schema.sql). Full suite green (unit 763 + integration 1798[+2 skip] + contract 1); staging deploy 30114431977 + anon canary GREEN (recommendations + eligibility → 401 [previously 404 = now .NET-served]; FM-084/085 siblings still 401 = no regression; **whole course-plan.ts router now .NET-served**). **▶ NEXT = a NEW domain: deferred school.ts/school-courses.ts writes (iSAMS write, staff-invite [needs .NET SES], logo→S3 — polyglot-gated) OR open a structurally-hard phase (F docs/PDF = lowest-risk of the remaining tail; E messaging/video, G Stripe, H auth = high-risk).**

- ✅ **W** iSAMS configure write (FM-087, DONE `c0c83ec`) — the deferred school.ts iSAMS WRITE side, ported as ONE endpoint. POST /integrations/isams (schoolService.configureIsams) under the dark flag `FORMMAPS_ROUTE_ISAMS_CONFIGURE_TO_DOTNET` (1 rewrite, the exact literal `/api/v1/school-admin/integrations/isams` — distinct from the /status+/jobs reads [FM-053] and the /sync+/test writes). **The ONLY iSAMS write ported: a self-contained isamsConfig upsert + AES-256-GCM credential encryption. `sync`/`test` STAY IN NODE** (the SSRF-hardened undici vendor client — per-socket DNS re-validation + rebind/cloud-metadata blocking; `sync` creates user rows) — the same cohesive vendor+side-effect boundary that kept invites [FM-067/068] and calendar-sync [FM-071] polyglot. **NEW `AesGcmFieldCipher`** = byte-compatible port of `lib/fieldEncrypt.ts`: **.NET's native `AesGcm` rejects non-12-byte nonces, but Node uses a 16-byte IV AND `isEncrypted()` requires exactly 16 bytes, so it's built on BouncyCastle GCM** (standard GHASH-based J0 derivation, identical to OpenSSL); Node key-derivation parity (64-hex→raw else base64→first 32B); **lazy key** so a missing `FIELD_ENCRYPTION_KEY` never bricks startup/anon. `IsamsConfigWriter` = **SELECT-then-branch upsert** reproducing the create-vs-update asymmetry (**`authType:0` → "api_key" on CREATE but 500 on UPDATE**), `endpoint = body.endpoint || body.apiUrl` (JS-||), Prisma undefined-omit vs null-SET-NULL, a truthy non-string apiKey → encrypt-throw → 500 with no write, unconditional updatedBy/updatedAt on update. Endpoint: RequireIdentity(401)→school:manage(403)→body(malformed/primitive→500)→schoolId(null→400 "No school")→200 `{id,endpoint,configured:true}`. Gate: fresh reviewer (17 files, 21 tools, **INDEPENDENTLY ran Node `createDecipheriv` against the golden vector**) **SHIP** — no BLOCKING/HIGH/MEDIUM; 3 LOWs accepted no-fold (SELECT-then-branch concurrency window = the same window Prisma's own upsert has; base64 key strictness already commented; express.json-first anon/no-perm+malformed→401/403 the accepted class). 43 tests (14 cipher unit incl. a **golden Node cross-impl vector** + 15 writer Testcontainers + 14 endpoint faked). Full suite green (unit 777 + integration 1827[+2 skip] + contract 1); staging deploy 30120169646 + anon canary GREEN (health 200; POST /integrations/isams → 401 + `x-formmaps-service` [.NET-served]; POST /sync + /test → 404 [Node-only, correct]; GET /status → 401 [FM-053, no regression]). Dark. **⚠️ INFRA GATE (cutover-only):** prod .NET `FIELD_ENCRYPTION_KEY` must match prod Node so a .NET-written credential round-trips through the Node `sync` path; and (Milestone-1 lesson) the rewrite must also land in the live `formmaps-platform/frontend/next.config.ts` before any prod flip. **▶ NEXT = Phase F** (routes/upload.ts real S3 rail [presigned, bucket `formmaps-platform-uploads`] + resume.ts + report.ts PDF — the broken POST /school/logo [memoryStorage → stores `/uploads/undefined`, superseded by /upload/school-logo] is NOT ported) + **staff-invite SES** (reuse the existing `SesEmailSender` rail + a staff-onboarding template).

### Phase E — Messaging / notifications / video (domain 7) — **~10–16 slices** 🔴 architectural fork
- `messages.ts` (586), `video.ts` (533), notifications. **May not fit request/response cleanly** — real-time likely needs SignalR (or stays Node); video is an external-provider integration. **DECISION: rebuild in .NET vs keep real-time in Node (polyglot).**

### Phase F — Documents / uploads / resume / PDF (domain 8) — **~6–10 slices**
- `resume.ts` (579), PDF generation pipeline, S3 uploads. PDF/file flows are a new infra surface in .NET.
- ✅ **UPLOADS** file uploads / S3 rail (FM-088, DONE `017e2e6`) — **OPENS Phase F.** The six `/api/v1/upload/*` multipart POSTs (school-logo, profile-image, resume, course-import, grade-import, portfolio-attachment) under ONE dark flag `FORMMAPS_ROUTE_UPLOAD_TO_DOTNET` (6 exact-literal rewrites). **The FIRST multipart + S3 surface in .NET.** All `multer.single("file")` + self-scoped (authenticate + tenantContext → RequireIdentity, caller RLS). **NEW `IObjectStorage`/`S3ObjectStorage`** = port of `lib/s3.ts` uploadAndGetUrl (PutObject ContentDisposition=attachment + 24h presigned GET; key `{folder}/{unixMs}-{6 base36}{ext}`; **AWSSDK.S3 4.0.101.4**; DI factory-lambda client so cred resolution defers to first use, like SES). **NEW `FileUploadValidation`** = validateMagicBytes (per-type signatures + 7-bit ascii mask + <4-bytes→false) + sanitizeFilename (ECMAScript-`\w`) + CSV parse (lowercased keys, short-row key omission, dup-header last-wins incl. the short-row intersection). `UploadRepository` = school.logoUrl + coach.imageUrl writes. Per-endpoint order + exact messages; catch → 500 "Upload failed". **⚠️ The broken `POST /school/logo` (school.ts:173, multer.memoryStorage → stores `/uploads/undefined`, never S3) is NOT ported — superseded by this `/upload/school-logo`.** Gate: fresh reviewer (16 files, independent Node global-error-handler analysis) **SHIP** — no BLOCKING/HIGH; folded all 3 findings + red-if-regressed tests: **multer's global 5MB limit reproduced** (>5MB → 500 "Internal server error", NOT the handler's "Upload failed" — multer errors before the handler; bounds the S3 write; the CSV explicit >5MB 400 kept as dead code mirroring Node), Content-Type params stripped (busboy parity), CSV dup-header+short-row intersection. 49 tests (26 unit + 23 endpoint faked). Full suite green (unit 803 + integration 1850[+2 skip] + contract 1); staging deploy 30125439806 + anon canary GREEN (6 → 401 `x-formmaps-service`; unmapped → 404 = no over-capture). Dark. **⚠️ INFRA GATE (cutover-only):** S3 bucket `formmaps-platform-uploads` + `s3:PutObject`/`GetObject` on the App Runner role (presigned reads, not public). **▶ NEXT Phase F: `resume.ts`** (PDF + S3, reuses this rail) → **`report.ts`** (PDF + SES attachment send — extend the SES rail; PDF-render may stay polyglot if a heavy headless dep).
- ✅ **RESUME §/TEMPLATE** resume section + template writes (FM-089, DONE `69434cd`) — **Phase F sub-slice 2.** Four self-scoped, non-AI routes of `resume.ts` under ONE dark flag `FORMMAPS_ROUTE_RESUME_SECTIONS_TO_DOTNET`: PUT /:id/sections/order, POST /:id/sections, DELETE /:id/sections/:sectionId, PUT /:id/template (mounted `/api/resume`, NOT /api/v1; unique 2nd segments "sections"/"template" → no collision with the Node-only resume CRUD / cross-user GET /:id / AI routes; /sections/order precedes /sections/:sectionId). Auth = RequireIdentity → `ISubscriptionGuard` (the requireSubscription mount). Every op findUnique's by id (**NO isActive** — a soft-deleted owned resume stays editable) + **ownership in CODE** (resumes has **NO RLS**) → 404 "Resume not found" **before** body validation (FM-072 defer convention). NEW pure `ResumeSections` (reorder map+filter drop-not-found/shrink/dup-preserve; add allowedTypes→custom + title slice(0,200) + items slice(0,100) + uuid; delete filter; sectionOrder gate; template truthiness). jsonb ::jsonb write; updatedAt Kind=Unspecified. Gate: fresh reviewer (11 files, cross-checked index.ts global-500 + query.ts) **SHIP** — no BLOCKING/HIGH; **PRE-FOLDED the one MEDIUM**: legacy `(sections as [])||[]` then `.map/.push/.filter` throws → **500 on a truthy non-array `sections`** (reachable via a Node create/update with `sections:{}`) → reproduced as `IsCorruptSections`→CorruptSections→500, **with the reorder subtlety** (empty sectionOrder never touches sections → tolerated; add/delete always 500). Accepted LOWs: anon/no-sub+malformed→401/403 (express.json-first class), content-type, numeric-id (unreachable), rate-limit (infra). 56 tests (31 unit + 9 repo Testcontainers + 16 endpoint faked). Full suite green (unit 834 + integration 1874[+2 skip] + contract 1); staging 30138186949 + anon canary GREEN (4→401 `x-formmaps-service`; GET /:id + /ai-edit + /api/resume → 404 = Node-only). Dark. *(Docker crashed twice mid-suite = infra; restart + re-ran clean.)*
- ✅ **RESUME CRUD** resume list + create (FM-090, DONE `47bb408`) — **Phase F sub-slice 2b-i.** Three self-scoped, non-AI routes of `resume.ts` under ONE dark flag `FORMMAPS_ROUTE_RESUME_CRUD_TO_DOTNET`: GET /default (a **static** empty-resume scaffold, no DB), GET / (list own active), POST / (create) — mounted `/api/resume`. GET / and POST / share the exact `/api/resume` path (Next matches path-not-method → the flag co-flips them); `/default` is a distinct literal. Auth = RequireIdentity → `ISubscriptionGuard` (the requireSubscription mount); **ownership code-only** (resumes has **NO RLS**) → userId scoped in WHERE/INSERT. GET / = findMany{userId,isActive} ORDER BY updatedAt DESC (id ASC tie-break) → `{data:[full 22-col rows]}` (8 jsonb cols read `::text`→JsonElement→verbatim; ISO-Z; nullable emitted). POST / = per-field JS-`||` defaults — name/template/careerField are String cols (**truthy non-string → Prisma String 500**); the 8 jsonb cols accept **any truthy JSON verbatim** (empty `[]`/`{}` are JS-truthy → WIN over the default; only falsy falls back); id `gen_random_uuid`, createdDate/updatedAt now (Kind=Unspecified), documentEdits/originalFileKey/…/hasOriginal/isActive/createdBy/updatedBy take **DB defaults**; INSERT…RETURNING → 201 `{data:<full row>}`. NEW `IResumeRepository`+`ResumeRow`, pure `ResumeCreate` coalescing, `ResumeRepository`, `ResumeCrudEndpoints`. Gate: fresh reviewer (8 files, **column-by-column vs Prisma model Resume** + mount + a bare-path sibling) **SHIP** — no BLOCKING/HIGH/MEDIUM; full-row key order **exact** (22 in schema order). 2 accepted-convention LOWs, **no folds**: jsonb `::text` number re-serialization (the passthrough idiom in all 7 read surfaces); auth-before-body 401-vs-500 (express.json-first class). 36 tests (20 unit + 8 repo Testcontainers + 8 endpoint faked; new harness resume-crud-schema.sql = full 22-col table). Full suite green (unit 854 + integration 1890[+2 skip] + contract 1); staging 30140224341 + anon canary GREEN (/default + / GET + POST → 401 `x-formmaps-service`; GET /:id + /:id/original → 404 = Node-only, no over-capture). Dark. **▶ NEXT Phase F: FM-2b-ii `resume.ts` cross-user GET /:id (`IUserAccessGuard.CanAccessUserAsync` + resolveSecureUserId) + PUT/DELETE /:resumeId (bounded fields + sanitizeDocumentEdits) via a single-seg negative-lookahead `(?!ask|tailor|extract-job-posting)`; then FM-2b-iii GET /:id/original (extend `IObjectStorage.GetUrl` presigned 300s inline); then `report.ts`** (PDF + SES attachment — extend `IEmailSender` over SES SendRawEmail). AI routes stay Node.

### Phase G — Billing / Stripe (domain 9) — **~8–12 slices** 🔴 money
- `stripe.ts` (422), subscriptions, webhooks, `coach.ts` (409), `coach-bookings.ts` (381).
- 🔴 idempotency + webhook signature + reconciliation; regression corpus #13/#14/#25 (stripe-money, status-IDOR, idempotency). Mirrors the TIMS billing-write strangler pattern.

### Phase H — Auth / session final cutover (domain 10) — **~8–12 slices** 🔴🔴 keystone
- login/refresh/password-reset/MFA/session, `user.ts` (525). Corpus #2 (change-email ATO), #17 (role preservation), #29 (forgot-pw hardening). **Auth cuts over LAST** — the keystone; after this, Node is retired.

### Phase I — Retire Node (domain 11) — decommission + the cost cleanup (`nexa-platform-aws-cost-audit`).

**Cross-cutting (every phase):** persistent audit log (compliance CB-1 analog — currently log-only), the `tims-interop` contracts (still docs-only, no code), and per-domain **frontend flag-wiring + prod canary + legacy-route freeze**.

**Total remaining ≈ 70–110 slices.** At a sustained agentic pace (reads fast, writes/🔴 slow), this is a **multi-month effort measured in dozens of sessions**, not weeks.

## 3. The three strategic risks (and what to do about them)

1. **0% prod cutover is the dominant risk.** 30 slices are dark. The strategy's core claim — safe incremental flag-flip in prod with route-level rollback — is **unproven in production**. Every added dark slice grows the untested-cutover tail. → **Prove ONE prod cutover early** (personality is ready — full lifecycle, no dual-write). Validate flag-flip + rollback on real traffic *before* porting 70 more slices that assume it works.
2. **The hard tail concentrates the effort.** Reports+assessments were the easy 25%. Messaging/video (real-time — may not fit .NET request/response), billing (money+webhooks), and auth (the keystone) are 3–5× harder per slice. Budget accordingly; don't extrapolate pace from the read slices.
3. **Moving target.** The live TS app is revenue-generating and still getting features (assessment fix wave, etc.). Every TS feature = more parity surface. → **Decide a feature-freeze posture** on the domains being actively migrated, or accept re-derivation cost.

## 4. Recommended execution sequence (to actually complete it)

1. **Milestone 1 — "Assessments .NET-owned in prod":** finish Phase A (FM-031 pca-exam → vocational write → remaining reads → token rail), THEN **cut over the ready pieces to prod** (personality first, then LIA/pca-exam) to prove the mechanism. This is the highest-leverage near-term goal — it both advances a domain AND de-risks the whole strategy.
2. **Milestone 2 — Schools + workflows (Phases B, C):** the access-control middle; reuse `canAccessUser`. Reads → writes.
3. **Milestone 3 — Recommendations/academic (Phase D).**
4. **Milestone 4 — Structural domains (Phases E, F):** resolve the messaging/video polyglot decision first; budget extra.
5. **Milestone 5 — Billing (Phase G):** full money-rigor.
6. **Milestone 6 — Auth cutover + Node retirement (Phases H, I):** the keystone, last.

Throughout: stand up **persistent audit** (compliance) and wire **frontend flags + prod canary + legacy freeze** per domain — a domain isn't "done" until its legacy route is disabled/read-only (the cutover-matrix definition).

## 5. Decisions Federico must make (unblock the plan)

- **Prove-cutover-now vs port-everything-first?** → recommend prove-now (personality).
- **TIMS vendor proxy (`pcaapi`): port to .NET or keep in Node permanently?** (plan leans defer/keep-Node.)
- **Messaging/video: rebuild in .NET (SignalR) or keep real-time polyglot in Node?** — a real architectural fork; real-time may legitimately stay Node.
- **Feature-freeze the TS app on domains under migration, or keep shipping (moving target)?**
- **Priority/pace:** this is a months-long effort at agentic pace. Is finishing the migration the priority vs shipping product features on the live app?

## 6. Immediate next 3 slices (ready to execute)
1. ✅ **FM-031 pca-exam submit** — DONE `a745d06` (313u+1c+200i; fresh+Codex gate; staging canary green). Closed corpus #18.
2. **FM-032 vocational authed write** — reuse FM-028 engine + the write rail. (Token-gated external submit = Phase D risk, separate.)
3. **FM-033 personality PROD cutover** (Milestone 1 proof) — flip `FORMMAPS_ROUTE_PERSONALITY_*` on prod web + canary + rollback drill. Federico-gated (prod). Personality is dual-write-free → the readiest cutover to prove the mechanism.
