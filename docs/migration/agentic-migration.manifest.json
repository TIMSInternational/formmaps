{
  "schemaVersion": 1,
  "project": "formmaps",
  "activeRepository": "TIMSInternational/formmaps",
  "strategy": "strangler-migration",
  "sourceOfTruth": {
    "legacyRepositoryPath": "/Users/federicotafur/formmaps-platform",
    "legacyBackend": "api",
    "frontend": "apps/web",
    "newBackend": "services/api"
  },
  "globalGuardrails": [
    "Keep the existing production platform running during migration.",
    "Use the legacy Node backend as the behavioral specification.",
    "Preserve RLS and fail-closed tenant behavior.",
    "Do not trust development headers outside Development.",
    "Do not allow Prisma and EF Core to freely mutate the same table.",
    "Use tims-interop for product-to-product contracts."
  ],
  "tasks": [
    {
      "id": "FM-DOTNET-001-platform-shell",
      "status": "completed",
      "goal": "Create the .NET 10 API shell with health, version, roadmap endpoint, CI, and tests.",
      "ownedPaths": [
        "services/api",
        "docs/migration"
      ],
      "validation": [
        "npm run api:test",
        "curl /health",
        "curl /version",
        "curl /api/v1/migration/roadmap"
      ]
    },
    {
      "id": "FM-DOTNET-002-request-context",
      "status": "completed",
      "goal": "Implement auth-compatible request context primitives, role normalization, tenant guard, and protected smoke endpoint.",
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/middleware/authenticate.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/auth.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/requestContext.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/prismaRls.ts",
        "apps/web/src/lib/api/apiClient.ts",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Domain/Auth",
        "services/api/src/FormMaps.Application/Auth",
        "services/api/src/FormMaps.Api/Auth",
        "services/api/src/FormMaps.Api/Endpoints/RequestContextEndpoints.cs",
        "services/api/tests/FormMaps.UnitTests/Auth",
        "docs/migration/agentic-migration.manifest.json",
        "docs/migration/agentic-migration-workflow.md",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "outputs": [
        "RequestContext model",
        "role normalization parity",
        "tenant fail-closed guard",
        "development-only request context decoder",
        "GET /api/v1/context/current",
        "GET /api/v1/context/protected-smoke"
      ],
      "guardrails": [
        "Development headers must be ignored outside Development.",
        "Protected smoke endpoint must deny anonymous requests."
      ],
      "validation": [
        "npm run api:test",
        "curl /api/v1/context/current",
        "curl /api/v1/context/protected-smoke without headers returns 401",
        "curl /api/v1/context/protected-smoke with development headers returns 200"
      ]
    },
    {
      "id": "FM-DOTNET-003-jwt-validation",
      "status": "completed",
      "goal": "Validate legacy-compatible HS256 JWTs from access_token cookie first, then Authorization bearer fallback, and lock the RLS GUC decision contract.",
      "dependsOn": [
        "FM-DOTNET-002-request-context"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/lib/auth.ts",
        "/Users/federicotafur/formmaps-platform/api/src/middleware/authenticate.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/requestContext.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/prismaRls.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/authCookies.ts",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Auth",
        "services/api/src/FormMaps.Application/Auth",
        "services/api/src/FormMaps.Api/appsettings.json",
        "services/api/tests/FormMaps.IntegrationTests/Auth",
        "services/api/tests/FormMaps.UnitTests/Auth",
        "docs/migration"
      ],
      "outputs": [
        "LegacyJwtRequestContextFactory",
        "HS256 issuer/audience/lifetime/signing-key validation",
        "cookie-first token precedence with no bad-cookie bearer fallback",
        "development header fallback only when no real token exists",
        "TenantGucPlanResolver for system/super-admin/identity/deny decisions"
      ],
      "guardrails": [
        "Use issuer formmaps-api unless environment overrides it.",
        "Use audience formmaps-frontend unless environment overrides it.",
        "No protected endpoint may accept unsigned or unverified claims.",
        "No database query may run against RLS-protected tables until the GUC application layer exists."
      ],
      "validation": [
        "npm run api:test"
      ]
    },
    {
      "id": "FM-DOTNET-004-security-middleware-parity",
      "status": "completed",
      "goal": "Port production API security middleware parity before .NET owns real product traffic.",
      "dependsOn": [
        "FM-DOTNET-003-jwt-validation"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/index.ts",
        "/Users/federicotafur/formmaps-platform/api/src/middleware/rateLimiter.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/env.ts",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api",
        "services/api/tests/FormMaps.IntegrationTests",
        "docs/migration"
      ],
      "outputs": [
        "startup environment validation",
        "credentialed CORS allowlist",
        "security/no-store headers",
        "mutation content-type enforcement",
        "request size and timeout policy",
        "redacted logging policy",
        "JSON body sanitization policy",
        "API/auth/sensitive/AI rate limit policy"
      ],
      "guardrails": [
        "Production must fail fast when required secrets are missing.",
        "Do not weaken local development ergonomics to fake production config.",
        "Do not port Stripe raw-body handling until the Stripe route is being migrated."
      ],
      "validation": [
        "npm run api:test",
        "dotnet build services/api/FormMaps.slnx"
      ]
    },
    {
      "id": "FM-DOTNET-005-rls-safe-database-context",
      "status": "completed",
      "goal": "Add database connectivity and a safe strategy for setting app.current_school_id/app.current_user_id before queries.",
      "dependsOn": [
        "FM-DOTNET-004-security-middleware-parity"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/lib/prisma.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/prismaRls.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/rls",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Infrastructure",
        "services/api/src/FormMaps.Application/Auth",
        "services/api/tests/FormMaps.IntegrationTests",
        "docs/migration"
      ],
      "outputs": [
        "PostgreSQL connectivity configuration",
        "RLS session GUC applier",
        "system bypass, deny, and identity database-context tests",
        "read-only legacy-table access policy"
      ],
      "guardrails": [
        "Do not grant the application role DDL privileges.",
        "Do not run queries against RLS-protected tables without applying TenantGucPlanResolver.",
        "Do not let .NET and Prisma both own schema migrations for the same table.",
        "No write endpoint may ship before WITH CHECK-equivalent tenant tests exist."
      ],
      "validation": [
        "npm run api:test",
        "dotnet build services/api/FormMaps.slnx"
      ]
    },
    {
      "id": "FM-DOTNET-006-first-reporting-read-endpoint",
      "status": "completed",
      "goal": "Migrate one read-only reporting/dashboard endpoint with role and tenant parity tests.",
      "dependsOn": [
        "FM-DOTNET-005-rls-safe-database-context"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/report.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/student.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests/FormMaps.IntegrationTests",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/reports/benchmark",
        "tenant/role parity tests",
        "feature-flag-ready route contract"
      ],
      "guardrails": [
        "Read-only only; no write endpoint in this slice.",
        "All data access must use IFormMapsDatabaseSessionFactory.",
        "Do not bypass RLS for user-facing data unless the legacy route is explicitly public/system context."
      ],
      "validation": [
        "npm run api:test",
        "dotnet build services/api/FormMaps.slnx"
      ]
    },
    {
      "id": "FM-DOTNET-007-live-db-smoke-and-route-flag",
      "status": "completed",
      "goal": "Add staging database smoke coverage for the benchmark endpoint and define frontend/API routing feature flag before canary traffic.",
      "dependsOn": [
        "FM-DOTNET-006-first-reporting-read-endpoint"
      ],
      "inputs": [
        "services/api/src/FormMaps.Api/Endpoints/ReportEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Reports/SchoolBenchmarkReportReader.cs",
        "apps/web/src/lib/api/apiClient.ts",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/tests/FormMaps.IntegrationTests",
        "apps/web",
        "docs/migration"
      ],
      "outputs": [
        "gated staging database smoke test",
        "documented staging canary runbook",
        "frontend route feature flag for reports benchmark",
        "rollback notes"
      ],
      "guardrails": [
        "Do not point production frontend traffic at .NET until staging DB smoke passes.",
        "Do not weaken RLS or use system context for school benchmark data.",
        "Keep rollback route-level."
      ],
      "validation": [
        "npm run api:test",
        "npm run web:build",
        "FORMMAPS_RUN_BENCHMARK_DB_SMOKE=1 ... npm run api:test -- --filter FullyQualifiedName~BenchmarkReportDatabaseSmokeTests"
      ],
      "notes": [
        "The database smoke is intentionally opt-in and requires staging DATABASE_URL, school id, and user id.",
        "Production traffic must stay on Node until the runbook smoke and staging web canary pass."
      ]
    },
    {
      "id": "FM-DOTNET-008-staging-benchmark-canary",
      "status": "completed",
      "goal": "Deploy the .NET API to staging, run the benchmark DB smoke with real staging credentials, and verify the web route flag canary/rollback path.",
      "dependsOn": [
        "FM-DOTNET-007-live-db-smoke-and-route-flag"
      ],
      "inputs": [
        "docs/migration/benchmark-route-canary-runbook.md",
        "docs/migration/staging-benchmark-canary-status.md",
        "apps/web/next.config.ts",
        "services/api/tests/FormMaps.IntegrationTests/Reports/BenchmarkReportDatabaseSmokeTests.cs",
        "services/api/src/FormMaps.Api/appsettings.json",
        ".github/workflows/formmaps-api-ci.yml",
        ".github/workflows/formmaps-api-staging-deploy.yml",
        "infra/aws",
        "package.json"
      ],
      "ownedPaths": [
        ".github/workflows/formmaps-api-ci.yml",
        ".github/workflows/formmaps-api-staging-deploy.yml",
        "docs/migration",
        "infra/aws",
        "services/api",
        "apps/web",
        "package.json"
      ],
      "outputs": [
        "production API Dockerfile",
        "staging canary command runner",
        "same-repo AWS staging infrastructure as code",
        "GitHub Actions staging deploy workflow",
        "staging .NET API environment checklist",
        "staging infrastructure discovery/status report",
        "benchmark DB smoke result",
        "staging web route canary result",
        "verified rollback evidence"
      ],
      "guardrails": [
        "Do not enable the production web route flag before staging canary and rollback verification pass.",
        "Do not expose staging JWT_SECRET, DATABASE_URL, or bearer/cookie values in git or logs.",
        "Keep the canary scoped to /api/v1/reports/benchmark only."
      ],
      "validation": [
        "npm run api:test",
        "npm run api:build",
        "npm run web:build",
        "npm run api:docker:build",
        "npm run infra:staging:validate",
        "FORMMAPS_STAGING_DOTNET_API_BASE_URL=... npm run api:staging-canary -- --health-only",
        "FORMMAPS_RUN_BENCHMARK_DB_SMOKE=1 ... npm run api:test -- --filter FullyQualifiedName~BenchmarkReportDatabaseSmokeTests",
        "staging browser request to /api/v1/reports/benchmark returns the .NET envelope",
        "staging rollback flag returns the route to Node"
      ],
      "notes": [
        "The same-repo staging infra and manual deploy workflow are in place.",
        "The bootstrap stack has been deployed, creating ECR and the deploy/App Runner access roles.",
        "The service stack has been deployed, creating the App Runner staging API at https://zsmkrbkhc7.us-east-1.awsapprunner.com.",
        "GitHub Actions run 29451159278 passed on commit 81e53eed0d37dc850a30c952dc29d5c371e86e15.",
        "The health-only staging canary passed and an unauthenticated benchmark request correctly returns 401.",
        "The authenticated benchmark canary is still pending because FORMMAPS_STAGING_BENCHMARK_BEARER_TOKEN is not configured.",
        "Before broader testing, rotate the copied staging database credential because a pre-fix deployment exposed it in CloudWatch logs.",
        "Completion still requires authenticated benchmark data, gated staging DB smoke evidence, staging web route verification, and route-level rollback verification.",
        "DONE 2026-07-15b: staging DATABASE_URL now points at a dedicated read-only role formmaps_staging_ro (created via one-off Fargate on formmaps-ops using the RDS master secret; not the shared prod formmaps_app credential).",
        "DONE 2026-07-15b: root cause of first authenticated read failure was staging App Runner EgressType=DEFAULT (no VPC egress) so it could not reach the private Aurora; fixed to EgressType=VPC via the nexa-api-vpc-nat connector and codified in infra/aws/formmaps-api-staging-service.yml (new VpcConnectorArn parameter).",
        "DONE 2026-07-15b: authenticated benchmark canary passed against the deployed .NET (200 + X-FormMaps-Service header + real school data); npm run api:staging-canary passed.",
        "DONE 2026-07-15b: staging web canary passed with the route flag ON (benchmark served by .NET) and route-level rollback verified with the flag OFF (benchmark served by Node). Staging web = https://formmaps-staging-web.vercel.app.",
        "DONE 2026-07-15b: benchmark parity cross-referenced against legacy report.ts GET /api/v1/reports/benchmark (all 5 metrics faithful; the gpaDistribution-vs-totalStudents population mismatch is legacy behavior, not a .NET regression).",
        "OPEN: in-process BenchmarkReportDatabaseSmokeTests cannot run outside the private VPC; the deployed in-VPC .NET read is the DB-read proof instead. Production traffic cutover decision is still deferred."
      ]
    },
    {
      "id": "FM-DOTNET-009-reports-user-report",
      "status": "completed",
      "goal": "Migrate the read-only comprehensive student report GET /api/v1/reports/user-report/{userId} with canAccessUser ownership parity (own-record / counselor-assignment / same-school / super-admin), IDOR-safe 404 denial, and data parity against the legacy handler.",
      "dependsOn": [
        "FM-DOTNET-008-staging-benchmark-canary"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/report.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/access.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/prismaRls.ts",
        "services/api/src/FormMaps.Api/Endpoints/ReportEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Reports/SchoolBenchmarkReportReader.cs",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests/FormMaps.IntegrationTests",
        "services/api/tests/FormMaps.UnitTests",
        "apps/web",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/reports/user-report/{userId}",
        "canAccessUser ownership guard parity (privileged roles + counselor assignment + same-school; non-privileged own-record only)",
        "IDOR-safe 404 denial",
        "student/academic/assessments/courses report reader",
        "ownership + tenant + data parity tests",
        "web route feature flag + rollback"
      ],
      "guardrails": [
        "Read-only only; no write endpoint in this slice.",
        "All data access must use IFormMapsDatabaseSessionFactory under RLS.",
        "Denial must be 404 Not found; never reveal whether the target exists.",
        "Privileged cross-scope reads must match legacy prismaRls GUC behavior without leaking cross-tenant data.",
        "Do not weaken benchmark parity, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "npm run api:test",
        "dotnet build services/api/FormMaps.slnx",
        "authenticated staging canary for user-report ownership cases",
        "staging web route flag + rollback"
      ],
      "notes": [
        "DONE 2026-07-15b: built agent-driven (explore map -> TDD build -> fresh adversarial review + Codex -> fixes). dotnet build 0/0; 27 unit + 1 contract + 44 integration tests pass.",
        "Adversarial review caught and fixed two divergences: (1) own-record endpoints must use an identity-only guard (new RequireIdentity), NOT RequireTenantContext, because legacy /user-report has no school-context gate; (2) the ownership guard must branch on the RAW Actor.Role (not NormalizedRole) or alias roles like 'admin' escalate to cross-tenant Super Admin.",
        "Benchmark-style parity confirmed against legacy report.ts /user-report (all metric groups faithful; uniform 404 denial kept as an intentional IDOR hardening vs legacy's split Not found/User not found).",
        "Staging canary on real Country Day data: self-read -> 200 with real report; non-existent id -> 404 via the real school_admin same-school lookup; anonymous -> 401. Web cutover: FORMMAPS_ROUTE_USER_REPORT_TO_DOTNET=true -> web serves the route from .NET (x-formmaps-service header + data), flag off -> route returns to Node. Dynamic :userId rewrite verified both directions.",
        "OPEN: positive same-school-student -> 200 and cross-school -> 404 and counselor-assignment paths are covered by unit/integration tests but not yet canaried on real data (needs a real student/other-school userId). Codex adversarial task stalled (CLI hang); the fresh cross-agent review + code read + canary served as the gate."
      ]
    },
    {
      "id": "FM-DOTNET-010-reports-pca",
      "status": "completed",
      "goal": "Migrate the read-only PCA report GET /api/v1/reports/pca/{userId} with canAccessUser ownership parity (own-record / counselor-assignment / same-school / super-admin), IDOR-safe 404 denial, and data parity against the legacy handler (evaluations without an isActive filter, no coKey, full career profile row).",
      "dependsOn": [
        "FM-DOTNET-009-reports-user-report"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/report.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/access.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Api/Endpoints/ReportEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Reports/UserReportReader.cs",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests/FormMaps.IntegrationTests",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/reports/pca/{userId}",
        "canAccessUser ownership guard parity reused from user-report (identity-only RequireIdentity + CanAccessUserAsync)",
        "IDOR-safe 404 denial",
        "PCA report reader (user existence + evaluations + full career profile row)",
        "ownership + data parity tests"
      ],
      "guardrails": [
        "Read-only only; no write endpoint in this slice.",
        "All data access must use IFormMapsDatabaseSessionFactory read-only under the caller RLS session.",
        "No isActive filter on evaluations; legacy returns active AND inactive rows.",
        "coKey must never be returned; the evaluation select is an explicit column list that omits it.",
        "Denial must be 404 Not found; never reveal whether the target exists.",
        "Reuse the user-report ownership guard unchanged; do not invent a new guard or use resolveSecureUserId coercion.",
        "Do not weaken benchmark/user-report parity, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-15b: built agent-driven (explore map -> TDD build). Reuses FM-DOTNET-009 rails (RequireIdentity + IUserAccessGuard) unchanged; only new surface is the 3-query read-only passthrough reader.",
        "Reader parity verified: pca_evaluations WHERE userId only (active+inactive, createdDate DESC), coKey excluded; user_career_profiles via row_to_json (all numerics are Int -> no Decimal-as-string divergence; frontend does not parse profile timestamps so the row_to_json vs Prisma ISO-Z format is a documented no-op); completed = count>0.",
        "dotnet build 0/0; 27 unit + 1 contract + 49 integration (+5 pca) pass. Staging canary on real data: self -> 200 with correct shape (careerProfile null + empty evals for staff), non-existent -> 404, anonymous -> 401. Web flag FORMMAPS_ROUTE_PCA_REPORT_TO_DOTNET added (dynamic :userId rewrite, identical to user-report's proven mechanism).",
        "Lighter gate than user-report (guard reused from the already-reviewed FM-009; simple read-only passthrough) — verified by own parity read + real-data canary. Codex adversarial CLI remained hung all session.",
        "OPEN: careerProfile-populated passthrough not canaried on real data (jack.young is staff, no profile) — covered by unit/integration tests; canary when a real student-with-profile userId is available."
      ]
    },
    {
      "id": "FM-DOTNET-011-reports-lia-timeline-coaching",
      "status": "completed",
      "goal": "Migrate three read-only report endpoints as one batched slice: GET /api/v1/reports/lia/{userId}, GET /api/v1/reports/timeline/{userId}, GET /api/v1/reports/coaching/{userId}, each with canAccessUser ownership parity (own-record / counselor-assignment / same-school / super-admin), IDOR-safe 404 denial, and exact data parity against the legacy handlers.",
      "dependsOn": [
        "FM-DOTNET-010-reports-pca"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/report.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/access.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Api/Endpoints/ReportEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Reports/PcaReportReader.cs",
        "docs/migration/auth-tenant-context-contract.md"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests/FormMaps.IntegrationTests",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/reports/lia/{userId}",
        "GET /api/v1/reports/timeline/{userId}",
        "GET /api/v1/reports/coaching/{userId}",
        "canAccessUser ownership guard parity reused unchanged from user-report/pca (RequireIdentity + CanAccessUserAsync)",
        "IDOR-safe 404 denial",
        "LIA report reader (percentile remap dual-path), timeline report reader (heterogeneous merge + stable sort), coaching report reader (whitelist join + BigInt)",
        "ownership + data parity + sensitive-field-exclusion tests"
      ],
      "guardrails": [
        "Read-only only; no write endpoint in this slice.",
        "All data access must use IFormMapsDatabaseSessionFactory read-only under the caller RLS session.",
        "/lia: NO isActive filter on exam sessions + percentiles remap numerical_speed->NumericVelocity (key-name mismatch); dual path (parity LIA percentiles else per-type exam scorePercentage).",
        "/timeline: isActive=true IS applied on all three sources + score key ABSENT on evaluation/course events (only mil carries score) + exclude invitationToken/evaluatorEmail from the evaluation select.",
        "/coaching: booking->coach JOIN excludes coach hourlyRate/platformCommission/email and booking paymentIntentId/coachNotes/notes/meetingLink/cancellationReason + amount is BigInt emitted as a JSON number (0n/null -> null on session amount).",
        "Denial must be 404 Not found; never reveal whether the target exists; read-only under caller RLS.",
        "Reuse the user-report/pca ownership guard unchanged; do not invent a new guard or use resolveSecureUserId coercion.",
        "Do not weaken benchmark/user-report/pca parity, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-15b: 3 read endpoints, reuse FM-009 rails. Adversarial review SAFE TO SHIP (LIA overallScore rounding fix applied). dotnet 0/0; 27u+1c+64i green. Staging canary: lia/timeline/coaching self->200 correct shape + x-formmaps-service, non-existent->404, anon->401. Web flags added. OPEN: readers fake-tested only (no DB-level parity test); LIA jsonb path not canaried (needs a student-with-LIA-session; jack.young=staff)."
      ]
    },
    {
      "id": "FM-DOTNET-012-reports-evaluation",
      "status": "completed",
      "goal": "Migrate the last reports read-domain endpoint GET /api/v1/reports/evaluation/{sessionId} (session-scoped). Resolve the evaluation group by id (no isActive filter), then gate access on the group's evaluatedUserId via canAccessUser (RequireIdentity + CanAccessUserAsync reused unchanged), with IDOR-safe uniform 404 and exact data parity against the legacy handler (report.ts:176).",
      "dependsOn": [
        "FM-DOTNET-011-reports-lia-timeline-coaching"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/report.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/access.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/prismaRls.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Api/Endpoints/ReportEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Reports/CoachingReportReader.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests/FormMaps.IntegrationTests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/reports/evaluation/{sessionId}",
        "Two-phase reader: ResolveGroupAsync (group core incl. evaluatedUserId, no isActive filter) -> canAccessUser on evaluatedUserId -> ReadReportAsync (student name + active feedback)",
        "canAccessUser ownership guard parity reused unchanged (RequireIdentity + CanAccessUserAsync)",
        "IDOR-safe uniform 404 (closes the legacy existence leak from the distinct 'Evaluation group not found' message)",
        "averageRating (Decimal?) emitted as JSON string via trim_scale()::text; feedbackItems (jsonb) raw passthrough; sensitive columns excluded",
        "web route flag FORMMAPS_ROUTE_EVALUATION_REPORT_TO_DOTNET (default off)",
        "7 integration tests (ordering, averageRating string, jsonb passthrough, studentName omit-when-null, sensitive-field exclusion, uniform 404)"
      ],
      "guardrails": [
        "Read-only only; no write endpoint in this slice.",
        "All data access must use IFormMapsDatabaseSessionFactory read-only under the caller RLS session.",
        "Group resolved by id with NO isActive filter (legacy findUnique); feedback filtered to isActive=true with NO orderBy (legacy findMany has none).",
        "Access is gated on the group's evaluatedUserId (NOT the sessionId) via the reused canAccessUser guard.",
        "averageRating must serialize as a JSON string (Prisma Decimal? -> decimal.js toString) or null; feedbackItems must pass through as raw jsonb; evaluatorEmail/invitationToken/token flags must never appear.",
        "studentName is omitted (not null) when the evaluated user row is absent, matching legacy student?.name; every other Prisma null (completedDate, feedback averageRating/completedAt) stays explicit JSON null.",
        "Denial and not-found both return 404 Not found; never reveal whether the group exists.",
        "Do not weaken benchmark/user-report/pca/lia/timeline/coaching parity, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: last reports read endpoint. dotnet 0/0; 27u+1c+71i green (+7 evaluation). Adversarial review (fresh agent): Finding 1 (RLS-superset HIGH) DISSOLVED - verified legacy Prisma applies the SAME RLS GUC plan on every query (prismaRls.ts SET LOCAL current_user_id/current_school_id), so legacy is also two-layer RLS+canAccessUser with identical policies; no divergence. Finding 2 (studentName null-vs-omit LOW) FIXED (omit-when-null + test). Finding 3 (jsonb 5.0-vs-5 number canonicalization LOW) documented, unreachable for integer ratings. Finding 4 (500 body) verified faithful. Reports read-domain now 7/7 .NET-capable on staging. OPEN: reader fake-tested only; same-school real-data 200-shape path NOT canaried (needs a real Country Day evaluation-group id via gated in-VPC read; jack.young=staff has no own 360). Prod cutover still deferred (Federico)."
      ]
    },
    {
      "id": "FM-DOTNET-013-pcaexam-reads",
      "status": "completed",
      "goal": "First assessments-domain slice (Phase A reads). Add a reusable subscription-entitlement guard (legacy requireSubscription) and its first two consumers: GET /api/pcaexam/session/{sessionId} and GET /api/pcaexam/completed-exams/{userId}, with exact parity against the legacy examRouter handlers + assessmentService getSession/getCompletedExams.",
      "dependsOn": [
        "FM-DOTNET-012-reports-evaluation"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/middleware/requireSubscription.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/subscriptionAccess.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Api/Endpoints/ReportEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Auth/UserAccessGuard.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "ISubscriptionGuard/SubscriptionGuard (legacy requireSubscription parity) + pure SubscriptionAccess.GrantsAccess",
        "GET /api/pcaexam/session/{sessionId} (full-row passthrough)",
        "GET /api/pcaexam/completed-exams/{userId} (dedup-by-examId)",
        "guard order RequireIdentity -> RequireSubscription -> canAccessUser; uniform IDOR-safe 404",
        "web flags FORMMAPS_ROUTE_PCAEXAM_{SESSION,COMPLETED_EXAMS}_TO_DOTNET (default off)",
        "12 subscription unit tests + 2 dedup unit tests + 7 endpoint integration tests"
      ],
      "guardrails": [
        "Read-only only; no write endpoint in this slice.",
        "Subscription guard reads roleName+schoolId from the DB (NOT the JWT) under caller RLS, matching legacy; non-student roles and school-affiliated students bypass; school-less individual students need subscriptionGrantsAccess (past_due=grace; strict now>nextBillingDate+grace hard-expiry).",
        "Subscription-check DB failure returns 503 Service temporarily unavailable (fail-closed), matching legacy; never 500.",
        "getSession full-row: @map'd violation_count/flag_for_review aliased to Prisma field names; examType/status enums ::text; violations jsonb raw passthrough; no answer-key relation selected.",
        "getCompletedExams: where{userId,isCompleted,isActive} orderBy startTime DESC, exact select, dedup-by-examId newest-wins, {sessions,uniqueCompleted,count} shape.",
        "Denial + not-found both 404 Not found; do not weaken prior slices, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: first assessments slice. dotnet 0/0; 41u+1c+78i green (+14u +7i). Adversarial review (fresh agent) SAFE TO SHIP - only Low finding (503-vs-500 on subscription-check DB failure) FIXED (try/catch -> 503, ILogger); 2 nits (negative grace env, whitespace schoolId) unrealistic no-action; entitlement logic + full-row + dedup + guard order verified faithful. Staging canary green incl. REAL-DATA 200: anon session/completed->401, authed(jack school_admin)+non-existent session->404 uniform, authed(jack)+completed-exams/self->200 {sessions:[],uniqueCompleted:[],count:0} (proves RequireIdentity->sub-guard non-student BYPASS->canAccessUser self->reader shape, all .NET-served). OPEN: readers fake-tested only (no DB-level reader parity test); individual-student subscription-DENIAL path unit-tested only (jack.young=school_admin, not a school-less student). Reusable subscription guard now unblocks the whole cognitive+exam surface. Prod cutover deferred (Federico)."
      ]
    },
    {
      "id": "FM-DOTNET-014-pcaexam-catalog",
      "status": "completed",
      "goal": "Phase A reads continued: GET /api/pcaexam/exams (listExams) + GET /api/pcaexam/exams/{examId} (getExamWithQuestions), reusing the FM-013 subscription guard, with exact parity to the legacy examRouter handlers + the answer-key non-leak security invariant.",
      "dependsOn": [
        "FM-DOTNET-013-pcaexam-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Api/Endpoints/ExamEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/ExamSessionReader.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/pcaexam/exams (active exam list, no orderBy)",
        "GET /api/pcaexam/exams/{examId} (exam + answer-stripped questions ordered by questionNumber)",
        "answer-key non-leak: correctAnswer + explanation excluded at SQL AND un-representable in the ExamQuestion DTO",
        "web flag FORMMAPS_ROUTE_PCAEXAM_CATALOG_TO_DOTNET (default off)",
        "5 integration tests incl. the answer-key-strip regression pin"
      ],
      "guardrails": [
        "Read-only only.",
        "CORPUS: correctAnswer/explanation must NEVER appear in any question-fetch response (answer key stays server-side).",
        "listExams: WHERE isActive=true, no orderBy; getExamWithQuestions: exam by id (no isActive filter, findUnique parity) + questions WHERE isActive orderBy questionNumber ASC.",
        "data jsonb passthrough; enums ::text; global catalog so NO canAccessUser; guard = RequireIdentity + RequireSubscription.",
        "Missing exam -> 404 'Exam not found' (global catalog, no IDOR surface, matches legacy message).",
        "Do not weaken prior slices, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: catalog reads. dotnet 0/0; 41u+1c+83i green (+5i). First slice under the regression-corpus discipline ([[formmaps-csharp-port-regression-corpus]]) - pins the answer-key non-leak invariant (structurally guaranteed: stripped at SQL + un-representable in DTO, stronger than legacy's post-query strip). Fresh-agent review SAFE TO SHIP (all catalog fields/isActive/ordering/enum/jsonb/404/guard-scope/500 verified faithful; 2 nits no-action). Staging canary GREEN incl. answer-key non-leak proven on REAL DATA: anon /exams->401; authed(jack) /exams->200 real list (feature-detection-001, 60 q); authed /exams/feature-detection-001->200 with 60 REAL questions and answer-key-leak=FALSE (keys=id,examId,questionNumber,questionText,type,data,isActive,createdBy/Date,updatedBy/At - NO correctAnswer/explanation); /exams/nonexistent->404 'Exam not found'. Corpus invariant verified end-to-end on prod data."
      ]
    },
    {
      "id": "FM-DOTNET-015-lia-results",
      "status": "completed",
      "goal": "Phase A reads continued: GET /api/v1/lia/session/{sessionId}/results (strict self-ownership) + GET /api/v1/lia/user/{userId}/results (canAccessUser on the path :userId), reusing the FM-013 subscription guard, with exact parity to the legacy liaRouter/lia-results-service handlers and pinning the band-gap regression fix (#21).",
      "dependsOn": [
        "FM-DOTNET-014-pcaexam-catalog"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/lia.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/lia/lia-results-service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/types.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Infrastructure/Reports/LiaReportReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/ExamEndpoints.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/lia/session/{sessionId}/results (strict self-ownership: session.userId == caller, status='completed')",
        "GET /api/v1/lia/user/{userId}/results (newest completed+active session; canAccessUser on path :userId)",
        "LiaPerformanceLevels pure module (band-gap fix #21 golden-pinned: highest band min<=p, 74.5 -> 'high'; faithful inclusive-range subtests)",
        "snake_case results payload (jsonb passthrough; global_percentile as JSON number via ::double precision; echoed performance_level; NO numerical_speed->NumericVelocity remap)",
        "web flag FORMMAPS_ROUTE_LIA_RESULTS_TO_DOTNET (default off)",
        "68 unit + 9 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "CORPUS #21 (band-gap): GetPerformanceLevel selects the highest band whose min<=p (74.5 -> 'high', never 'insufficient'); golden-pinned on gap decimals + out-of-range/NaN. Ported+pinned though the read path ECHOES the stored performanceLevel (the fn is the Phase-C write-path function).",
        "Subtest levels are a faithful bug-for-bug port of the legacy inclusive-range matcher (gap decimal 11.5 -> 'insufficient' preserved).",
        "Session endpoint = STRICT self-ownership (NOT canAccessUser) - no privileged cross-user access; user endpoint = canAccessUser on the raw path id.",
        "global_percentile Decimal -> ::double precision -> JSON number (matches Number(x ?? 0)); jsonb '?? {}' / '?? []' defaulting covers SQL-null AND jsonb 'null' token; timestamps = JS toISOString shape (3 ms digits + Z) via SpecifyKind(Utc).",
        "Uniform IDOR-safe 404 on every missing/denied branch; no sensitive fields (email only as user_name fallback).",
        "Reads the SAME source as the shipped reports/lia reader (newest completed+active lia_assessment_sessions); do not weaken prior slices, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: LIA result reads. dotnet 0/0; 110u+1c+92i green (+68u +9i). Fresh-agent adversarial review = SAFE TO SHIP (all fields/parity/security/timestamps/band-gap-off-read-path verified faithful; its 1 Med RLS-parity concern DISSOLVED - legacy Prisma resolveGucPlan applies Identity-mode RLS on every query for counselor/school_admin, so Node getUserResults is RLS-scoped identically to the .NET reader, precedented by the live+canary-green reports/lia reader on the same table). Codex cross-model = 1 Medium FIXED+PINNED (jsonb 'null' token must default to {}/[] like legacy '?? {}'; single-source coalesce in the assembler + golden test) + 1 latent Low DOCUMENTED (JS coerces stringified jsonb numerics; the Node writer never produces them - a Phase-C write-path note, not coded). Staging canary GREEN (redeploy run 29504628601, health 200): anon session/user -> 401 missing_identity; authed(jack school_admin) session/nonexistent -> 404 'Not found' + x-formmaps-service; authed(jack) user/<own id> -> 404 (full chain: identity->sub-bypass->canAccessUser self-allow->reader null, jack=staff no LIA session); authed(jack) user/nonexistent -> 404. OPEN follow-up (gated, same as FM-011/013): real-data 200 shape + the counselor active-assignment RLS 200 path need a real Country Day student LIA-session id via a gated in-VPC read (Aurora private). LATENT Phase-C notes: (a) jsonb numeric trailing-zero/scale echo and (b) stringified-numeric coercion - both unreachable on app-written data, guard when the C# completion write-path lands."
      ]
    },
    {
      "id": "FM-DOTNET-016-mil-results",
      "status": "completed",
      "goal": "Phase A cognitive-cluster reads: GET /api/v1/mil/results/{userId} — the synthesized MIL DTO (no MIL table). Dual-path (primary from the newest completed LIA session's percentiles; fallback to pca_exam_sessions) with the legacy 300-point weightedComposite; canAccessUser on the path id; never-404 on missing data.",
      "dependsOn": [
        "FM-DOTNET-015-lia-results"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia/scoring.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Infrastructure/Assessments/LiaResultReader.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/ExamSessionReader.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/mil/results/{userId} (synthesized; canAccessUser on path id; never-404 on missing data)",
        "MilComposite pure module (port of the 300-pt weightedComposite: DOMAIN_WEIGHTS 20/100/60/80/40 order, quintile bands boundary-to-lower, classified on the UNROUNDED percent) - golden-pinned incl. the exactPercent=20.4 -> percent 20 / band Bajo boundary case",
        "MilResultsSynthesizer pure (primary LIA-percentiles path + pca_exam_sessions fallback + zeros) unit-pinned",
        "camelCase payload; cognitiveProfile numericVelocity<-numerical_speed; Decimal->::double precision JSON number; JS-toISOString timestamps",
        "web flag FORMMAPS_ROUTE_MIL_RESULTS_TO_DOTNET (default off)",
        "24 unit + 4 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "weightedComposite parity: perDomain in DOMAIN_WEIGHTS order (PatternRecognition,VisualRotation,NumericVelocity,WorkingMemory,VerbalReasoning) NOT MIL_EXAM_MAP order; quintile cutoffs 20/40/60/80/100 boundary-to-lower + >100 defensive top; band classified on the UNROUNDED exactPercent (display-rounding must not misclassify).",
        "primary-vs-fallback = legacy `if (parity?.percentiles)` JS-truthiness (IsTruthy: {} truthy, SQL-null/jsonb-'null' falsy -> fallback).",
        "examResults in MIL_EXAM_MAP order + canonical examIds; TYPE_TO_SUBTEST (NumericVelocity->numerical_speed); primary score===percentile===round(percentiles[subtest]??0); fallback percentile=null, examName `|| type`, score `|| 0`, overallScore=avg-of-completed (not global), lastCompletedAt=rows[0].endTime.",
        "canAccessUser on the RAW path id (not force-own); access-deny -> uniform IDOR-safe 404; synthesis NEVER 404s on missing data (200 zeros) - matches legacy, no new enumeration surface.",
        "Do not weaken prior slices, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: MIL synthesized results. dotnet 0/0; 134u+1c+96i green (+24u +4i). Fresh-agent adversarial review = SAFE TO SHIP (all 10 axes MATCH: weightedComposite order/unrounded-classification, IsTruthy primary/fallback, remaps, TruthyOrZero/`|| type`, ::double precision, camelCase, ISO fff'Z', SQL casing, two-reader RLS disposal, canAccessUser-raw-path + uniform-404 + never-404; Low items all unreachable). Codex cross-model = SAFE TO SHIP, no divergence, no fix needed. Staging canary GREEN (deploy 29507548525, health 200) incl. a REAL LIVE 200: anon -> 401 missing_identity; authed(jack school_admin) SELF -> 200 synthesized zeros DTO + x-formmaps-service (examResults[5] in order, perDomain[1].type=VisualRotation proving DOMAIN_WEIGHTS order live, camelCase cognitiveProfile w/ numericVelocity, all statuses not_started, overallScore 0 - the never-404 empty-fallback path proven live); authed(jack) nonexistent user -> 404 (canAccessUser deny). OPEN follow-up (gated, same as FM-011/013/015): the PRIMARY-path real-data 200 (weightedComposite/percentiles from a real completed LIA session) needs a real Country Day student LIA-session id via a gated in-VPC read (Aurora private); jack=staff exercised only the empty fallback."
      ]
    },
    {
      "id": "FM-DOTNET-017-personality-results",
      "status": "completed",
      "goal": "Phase A cognitive-cluster reads: GET /api/v1/personality/session/{sessionId}/results (strict self-ownership) + GET /api/v1/personality/user/{userId}/results (canAccessUser). Echoes stored resolvedType + dimension_scores; reconstructs the localized narrative at read time from the verbatim 16-profile bilingual bank.",
      "dependsOn": [
        "FM-DOTNET-016-mil-results"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/personality.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-session-service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-bank.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-scoring.ts",
        "/Users/federicotafur/formmaps-platform/api/src/data/personality-profiles.data.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Infrastructure/Assessments/LiaResultReader.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/v1/personality/session/{sessionId}/results (STRICT self-ownership)",
        "GET /api/v1/personality/user/{userId}/results (canAccessUser on path :userId)",
        "PersonalityProfileBank: the 16-profile bilingual narrative bank embedded VERBATIM as personality-profiles.json (extracted from the legacy 98KB personality-profiles.data.ts; byte-identical, deep-diff-verified) + localizeProfile port",
        "PersonalityResultsAssembler (buildResults port): snake-top-level / camelCase-nested hybrid; dimension_scores reordered [EI,SN,TF,JP] filter(Boolean); score.dimensions verbatim jsonb passthrough; echoed resolvedType",
        "web flag FORMMAPS_ROUTE_PERSONALITY_RESULTS_TO_DOTNET (default off)",
        "22 unit + 9 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "Profile bank embedded VERBATIM (deep-diff-verified 16/16 profiles, 0 field diffs); localize picks es (bare alias/tagline, else *_es) vs en (*_en / alias_en) exactly (en = language=='en').",
        "buildResults: user_name coalesce; variant/language normalize; type=resolvedType??''; dimension_scores=[EI,SN,TF,JP].map(storedDims[d]).filter(Boolean) (drop missing AND falsy null/false/0); score.dimensions = storedDims ?? {} VERBATIM (arrays pass through, only null coalesces); toISOString||null; violation_count; flag_for_review.",
        "getResults strict-self (userId==owner AND status=='completed' AND resolvedType truthy; findUnique NO isActive filter); getUserResults newest completed+active, null if !resolvedType. Unknown-non-empty stored type -> getProfileByType throws -> uniform 500 (parity).",
        "Response = snake_case top level + camelCase nested (score.dimensions / dimension_scores stored jsonb keys + profile.* localized) - reproduce exactly.",
        "Session endpoint STRICT self (NOT canAccessUser); user endpoint canAccessUser raw path; uniform IDOR-safe 404 on every deny/missing. Do not weaken prior slices, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: personality result reads. dotnet 0/0; 158u+1c+105i green (+22u +9i incl. the Codex fix). PROFILE BANK STRATEGY: extracted the 98KB legacy personality-profiles.data.ts to personality-profiles.json, embedded as a .NET EmbeddedResource, loaded once + localized at read time (avoids error-prone hand-transcription; deep-diff proved byte-identical). Fresh-agent review = SAFE TO SHIP (bank byte-perfect, localize asymmetry correct, buildResults/casing-hybrid/timestamps/gating/500-landmine/security all MATCH; 2 Low unreachable). Codex = FIXES NEEDED (2 Medium on adversarial jsonb) FIXED+PINNED: non-object dimensionScores must pass through to score.dimensions verbatim ('?? {}' coalesces ONLY null) + filter(Boolean) drops falsy dimension values (null/false/0) - both restore byte-parity though unreachable with the well-formed writer; 1 Low (non-array violations JS-length quirk) DOCUMENTED unreachable (writer stores null|array only), not replicated. Staging canary GREEN (redeploy 29510889228, health 200): anon session/user -> 401 missing_identity; authed(jack school_admin) session/nonexistent -> 404 + x-formmaps-service; authed(jack) user/<own id> -> 404 (full chain, jack=staff no personality session); user/nonexistent -> 404. OPEN follow-up (gated, same as prior slices): real-data 200 (the localized narrative for a real completed session) needs a Country Day student personality-session id via a gated in-VPC read; bank shape is otherwise backstopped by byte-identical embed + unit/integration tests."
      ]
    },
    {
      "id": "FM-DOTNET-018-pcaexam-config-reads",
      "status": "completed",
      "goal": "Phase A pca-exam catalog reads: GET /api/pcaexam/exams/{examId}/instructions + GET /api/pcaexam/exam-config/{examId} — subscription-only global catalog projections of pca_exams (legacy getExamInstructions / getExamConfig).",
      "dependsOn": [
        "FM-DOTNET-017-personality-results"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "services/api/src/FormMaps.Infrastructure/Assessments/ExamCatalogReader.cs"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/pcaexam/exams/{examId}/instructions ({id,name,type,timeLimitMinutes,description,totalQuestions,instructions}, instructions==description)",
        "GET /api/pcaexam/exam-config/{examId} ({id,name,type,timeLimitMinutes,totalQuestions,instructions} - NO separate description key)",
        "web flag FORMMAPS_ROUTE_PCAEXAM_CONFIG_TO_DOTNET (default off)",
        "6 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "Subscription-only (RequireIdentity + RequireSubscription, NO canAccessUser, NO admin); missing -> 404 'Exam not found' (global catalog, not the ownership-uniform 404).",
        "instructions carries BOTH description and instructions (==description); exam-config OMITS the separate description key (only instructions==description) - the exact legacy field-set difference.",
        "type enum ::text; pca_exams columns non-null (description String @default('') so GetString safe); param bound; no answer-key/question rows touched. Do not weaken prior slices, RLS, tenant isolation, or the security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: pca-exam config/instructions catalog reads (added to the existing /api/pcaexam ExamEndpoints group). dotnet 0/0; 158u+1c+111i green (+6i). Lighter-but-full gate justified (trivial catalog projections mirroring FM-014). Fresh-agent review = SAFE TO SHIP (both field-sets exact incl. the description-key difference, 404 'Exam not found', subscription-only guard + short-circuits, description non-null so GetString safe, no route shadowing /exams/{examId} vs /exams/{examId}/instructions, no leak). Codex first run timed out mid-read (foreground 2-min); re-dispatched background (per project rule fresh-review+canary are the gate, don't block on Codex). Staging canary GREEN incl. REAL-DATA 200s (deploy 29516639354, health 200): anon->401; authed(jack school_admin) instructions/feature-detection-001->200 + x-formmaps-service, keys=[id,name,type,timeLimitMinutes,description,totalQuestions,instructions], instructions==description (real: PatternRecognition, 60 q, 3 min); instructions/nonexistent->404 'Exam not found'; exam-config/feature-detection-001->200 keys=[id,name,type,timeLimitMinutes,totalQuestions,instructions] (NO description key); exam-config/nonexistent->404. Field-set difference proven LIVE. LATENT (corpus note): if pca_exams.description ever migrates to nullable, GetString would 500 vs legacy null-passthrough."
      ]
    },
    {
      "id": "FM-DOTNET-019-pcaexam-statistics",
      "status": "completed",
      "goal": "Phase A pca-exam admin read: GET /api/pcaexam/statistics/{examId} — ADMIN_ROLES-gated aggregate over completed+active sessions (legacy getExamStatistics).",
      "dependsOn": [
        "FM-DOTNET-018-pcaexam-config-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/pcaexam/statistics/{examId} ({examId,totalAttempts,averageScore,highestScore,lowestScore,uniqueUsers})",
        "reusable PcaAdminGate (raw exact-match [Super Admin, school_admin], NOT normalized) -> 403 'Admin access required'",
        "pure ExamStatistics.Compute golden-pinned (avg=round(avg*10)/10 half-up; highest/lowest raw; empty->zeros)",
        "web flag FORMMAPS_ROUTE_PCAEXAM_STATISTICS_TO_DOTNET (default off)",
        "3 unit + 4 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "ADMIN_ROLES gate = RAW exact match [Super Admin, school_admin] (reject admin/schooladmin/superadmin/counselor), 403 'Admin access required' AFTER subscription BEFORE DB read; never 404 (empty->zeros 200); no school scoping (global exams, matches legacy).",
        "averageScore round(avg*10)/10 half-up (AwayFromZero); highest/lowest raw un-rounded max/min or 0; uniqueUsers distinct userId. Float (no Decimal). Do not weaken prior slices/RLS/security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: pca-exam statistics admin aggregate (new reusable PcaAdminGate rail for future all-results). dotnet 0/0; 161u+1c+115i (+3u +4i). Fresh review SAFE (raw-role gate rejects aliases+counselor, aggregate math/rounding/SQL/security faithful; scorePercentage non-null so GetDouble safe; order-dependent sub-ULP sum unreachable). Codex SKIPPED (trivial pure aggregate + context limit; project rule = fresh+canary are the gate). Canary GREEN real-data: anon->401; jack(school_admin)/feature-detection-001->200 {totalAttempts:4, avg:45.8, high:85, low:16.66666666666666 raw, uniqueUsers:4} + x-formmaps-service (ADMIN gate + aggregate proven live). Branch named ...-history but contains statistics (reused). 🔴 IMPORTANT FINDING (blocks the full-row slices): STJ serializes DateTimeOffset(zero offset) as '...+00:00' (NOT '...Z', no ms) - byte-DIVERGENT from Node toISOString '...Z'. => FM-013 ExamSession (/session, /completed-exams) has a LATENT timestamp divergence never canaried on real timestamps; and the remaining full-row slices (pca-exam /history/:userId + /all-results) MUST use ISO-Z STRING timestamps (like LIA/MIL/personality ToIsoZ), NOT DateTimeOffset. STJ List<object> serializes each element by RUNTIME type (heterogeneous rows OK). ▶ NEXT = FM-020 pca-exam /history/:userId (canAccessUser + LIA synth, heterogeneous full+partial rows, Z-string timestamps via a new full-row DTO) + /all-results (ADMIN + pagination, same Z-string full-row DTO)."
      ]
    },
    {
      "id": "FM-DOTNET-020-pcaexam-history",
      "status": "completed",
      "goal": "Phase A pca-exam full-row read: GET /api/pcaexam/history/{userId} — canAccessUser-gated exam history with 5 synthesized per-subtest LIA rows prepended to the full real pca_exam_sessions rows, dedup-by-examId (synth wins) -> {sessions, latest} (legacy getExamHistory).",
      "dependsOn": [
        "FM-DOTNET-019-pcaexam-statistics"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/pcaexam/history/{userId} ({sessions, latest})",
        "PcaHistorySession full-row DTO with ISO-Z STRING timestamps (NOT DateTimeOffset -> STJ +00:00) + violations jsonb passthrough",
        "SynthesizedHistoryRow partial DTO (~18 fields; omits violations/violationCount/flagForReview/isActive/createdBy/createdDate/updatedBy/updatedAt) -> heterogeneous List<object> serialized by runtime type",
        "pure ExamHistorySynthesizer golden-pinned (MIL_EXAM_MAP order; id=lia-{parityId}-{subtest}; SUBTEST_BY_TYPE incl NumericVelocity->numerical_speed; scorePercentage=round(pct); accuracy=round(correct/answered*1000)/10; totalTimeSpent=round(durationMs/1000); dedup synth-wins)",
        "ExamHistoryReader (full pca_exam_sessions rows + newest completed+active LIA session; `if(percentiles truthy)` JS-truthiness gate; Z-string timestamps)",
        "web flag FORMMAPS_ROUTE_PCAEXAM_HISTORY_TO_DOTNET (default off)",
        "4 unit + 3 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "Guard chain RequireIdentity -> RequireSubscription -> canAccessUser(raw path userId) -> uniform IDOR-safe 404; anon -> 401; NEVER 404 on empty data (reader never null -> {sessions:[],latest:[]} 200).",
        "Timestamps MUST be JS-toISOString Z-strings (yyyy-MM-ddTHH:mm:ss.fff'Z'), NOT DateTimeOffset. Synth rows PARTIAL (omit the 8 real-only fields). Dedup by examId synth-first-wins. Do not weaken prior slices/RLS/security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: pca-exam /history/:userId (first full-row + LIA-synthesis read). dotnet 0/0; 165u+1c+118i (+4u +3i). GATE: fresh-agent adversarial review + Codex both run (prior fresh review had been killed mid-run, redone). Fresh review: all 10 parity axes MATCH (MIL order, SUBTEST_BY_TYPE, id, rounding, dedup synth-wins, partial-vs-full heterogeneity, Z-strings, `if(percentiles)` truthiness gate, SQL @map aliases, guard chain). Its one blocking [HIGH] RLS-policy-dependency concern DISSOLVED (same as FM-012 Finding 1 / FM-015 Med): premise that legacy is not row-scoped is WRONG — legacy Prisma applies the IDENTICAL RLS GUC plan on every query (prisma.ts:41 resolveGucPlan + prismaRls.ts:36 SET LOCAL app.current_user_id/current_school_id), so legacy+.NET are RLS-scoped identically; the live legacy app serves counselor/school_admin student reads, so policies grant that visibility and .NET inherits it; furthermore this endpoint reads only pca_exam_sessions (FM-013) + lia_assessment_sessions (FM-015) — both already ship reads on this exact rail, canaried green. Codex: FIXES-NEEDED but every finding marked reachable=no EXCEPT the anon-401 body string (which is the port-wide ProtectedRequestGuard contract shipped+canaried across FM-009..FM-019 = status-parity not body-parity, decided). Latent unreachable items (GetInt32 throw on fractional jsonb count; AwayFromZero vs Math.round on negative percentiles; both-null LIA timestamp -> new Date() fallback) DOCUMENTED, NOT replicated (JS-coercion accidents on corrupt data, not intended behavior) — consistent with FM-015/016/017 decisions. NO code fix; ship. Canary GREEN (direct .NET, deploy 29523034268, health 200): anon->401 missing_identity + x-formmaps-service; jack(school_admin)/SELF(sub)->200 {sessions:[],latest:[]} (staff, empty as expected; full chain identity->sub-bypass->canAccessUser self-allow->empty reader); jack/nonexistent->404 'Not found' uniform. OPEN follow-up (gated, same as FM-013/015/016): real-data synth 200 (a student with an LIA session showing 5 synth + real rows, dedup) needs a Country Day student id via a gated in-VPC read. LATENT (flag): FM-013 ExamSession (/session, /completed-exams) DateTimeOffset->+00:00 timestamp divergence never canaried on real timestamps — decide fix->Z-strings vs verify frontend tolerates. NEXT = FM-021 /all-results (ADMIN raw-gate [Super Admin, school_admin] via PcaAdminGate -> 403 'Admin access required' before lookup; page=max(1,parseInt||1), limit=min(100,max(1,||20)), skip=(page-1)*limit; getAllResults {data:sessions, total, page, limit, totalPages=ceil(total/limit)} -> double-nested data.data; REUSE PcaHistorySession Z-string DTO, NO synthesis, NO canAccessUser — leaks userId+violations cross-school per legacy; JS parseInt leading-int semantics, not int.TryParse)."
      ]
    },
    {
      "id": "FM-DOTNET-021-pcaexam-all-results",
      "status": "completed",
      "goal": "Phase A pca-exam admin read: GET /api/pcaexam/all-results — ADMIN_ROLES-gated, paginated list of ALL completed+active pca_exam_sessions across every user/school (legacy getAllResults).",
      "dependsOn": [
        "FM-DOTNET-020-pcaexam-history"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/query.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application",
        "services/api/src/FormMaps.Infrastructure",
        "services/api/tests",
        "apps/web/next.config.ts",
        "docs/migration"
      ],
      "outputs": [
        "GET /api/pcaexam/all-results (double-nested {success, data:{data, total, page, limit, totalPages}})",
        "pure PcaExamPagination golden-pinned (JS bare parseInt incl 0x hex + `||` falsiness NaN/0->default; page=max(1,..||1); limit=min(100,max(1,..||20)); skip=(long)(page-1)*limit)",
        "AllResultsReader (page + COUNT under read-only RLS; reuses PcaHistorySession Z-string DTO)",
        "shared PcaExamSessionRowMapper (single source for the full pca_exam_sessions row projection; ExamHistoryReader refactored onto it — no shape change)",
        "reuses PcaAdminGate (raw exact ['Super Admin','school_admin']) -> 403 'Admin access required' before DB",
        "web flag FORMMAPS_ROUTE_PCAEXAM_ALL_RESULTS_TO_DOTNET (default off)",
        "9 unit + 5 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "Guard chain RequireIdentity -> RequireSubscription -> PcaAdminGate (403 'Admin access required' BEFORE any DB read, never 404); anon -> 401. NO canAccessUser, NO school scoping in code (cross-school leak of userId+violations is intentional per legacy; RLS scopes school_admin to same-school identically in both stacks).",
        "Pagination = faithful JS bare parseInt (0x hex) + `||` (NaN AND 0 -> default) + clamps; skip 64-bit to avoid int overflow. Rows = full PcaHistorySession ISO-Z strings (NOT DateTimeOffset). totalPages=ceil(total/limit), total 0->0. Do not weaken prior slices/RLS/security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: pca-exam /all-results (admin + pagination). dotnet 0/0; 192u+1c+123i (+27u +5i over FM-020). GATE: fresh-agent review = SAFE TO SHIP (all 9 axes match: raw-admin gate, parseInt/falsiness, double-nested envelope+casing, totalPages, full-row no-scoping, sequential-vs-Promise.all equivalent, RLS parity, shared-mapper extraction byte-identical, no reachable throw) + folded its 1 LOW (skip int32 overflow -> (long) multiply, golden-pinned) + dead-using nit. Codex = FIXES NEEDED, folded its reachable LOW (bare parseInt 0x hex prefix — legacy assessment.ts:168 has no radix; JsParseInt now honors 0x, 8 golden cases) + WAIVED its unreachable LOW (Convert.ToInt32(COUNT) needs 2.1B rows). Codex CONFIRMED school_admin is same-school RLS-scoped identically in both stacks (dissolves cross-school concern). Extracted shared PcaExamSessionRowMapper (unifies the full-row projection with the FM-020 history reader; refactor byte-identical, FM-020 tests still green). Web flag + next.config rewrite added. Canary GREEN with REAL DATA (direct .NET, deploy 29526061457 of 788c4e2, health 200): anon->401 missing_identity+x-formmaps-service; jack(school_admin) /all-results->200 double-nested {data:[20 rows], total:22, page:1, limit:20, totalPages:2} — row0 startTime '2026-06-17T21:40:03.141Z' (Z-STRING proven live, no +00:00), violationCount present (full row), userId present; /all-results?page=2&limit=5->200 {page:2, limit:5, totalPages:5, 5 rows} (pagination live). Non-admin->403 path integration-pinned only (canary token=jack=admin). NOTE: canary ran on 788c4e2; the only post-canary change is the pure-function 0x-hex JsParseInt fix (golden-tested, no endpoint/SQL/guard change, does not affect the decimal canary paths). ▶ NEXT = FM-022 (fix FM-013 ExamSession /session + /completed-exams DateTimeOffset->Z-strings; frontend tolerates +00:00 via new Date() so no functional break, but fix for byte-parity + reuse PcaExamSessionRowMapper on the /session full-row). Then personality take-flow, assessment-profile/timeline, test-scores, question360, vocational360, school-admin -> Phase B."
      ]
    },
    {
      "id": "FM-DOTNET-022-examsession-zstrings",
      "status": "completed",
      "goal": "Byte-parity fix: pca-exam /session + /completed-exams emit ISO-Z string timestamps (not DateTimeOffset -> STJ +00:00). Closes the FM-013 latent timestamp divergence.",
      "dependsOn": [
        "FM-DOTNET-021-pcaexam-all-results"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/src/FormMaps.Infrastructure/Assessments",
        "services/api/tests"
      ],
      "outputs": [
        "GET /api/pcaexam/session/{sessionId} now returns PcaHistorySession (unified onto the shared full-row DTO+PcaExamSessionRowMapper) with ISO-Z string timestamps — deletes the duplicate ExamSession DTO",
        "GET /api/pcaexam/completed-exams/{userId}: CompletedExamRow startTime/endTime -> ISO-Z strings via PcaExamSessionRowMapper.IsoZ",
        "Z-string assertions pinning both endpoints (red-if-regressed)"
      ],
      "guardrails": [
        "Format-only change (DateTimeOffset -> ISO-Z string) + violations non-null passthrough (provably byte-equal to the old JsonElement? path: null column -> \"violations\":null, key present).",
        "No guard/SQL/logic change; guard chains + uniform 404 + anon 401 unchanged. /session output byte-identical to old ExamSession except timestamps.",
        "Do not weaken prior slices/RLS/security middleware."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: closes the FM-013 ExamSession DateTimeOffset->+00:00 latent divergence flagged at FM-019/020. Root: STJ renders DateTimeOffset(zero offset) as +00:00 != Node toISOString Z. Frontend TOLERATED it (consumes timestamps as strings via new Date(), which parses +00:00 identically to Z — no functional break; verified apps/web has no exact-string-equality/cache-key use), so this is a byte-parity fix not a bug fix. Approach (architecturally-correct): /session unified onto PcaHistorySession via the shared PcaExamSessionRowMapper (the deleted ExamSession DTO had a byte-identical field set), so /session/history/all-results now share ONE full-row projection (no future drift); /completed-exams subset uses IsoZ. dotnet 0/0; 192u+1c+123i (net-zero test count; modified DTOs + added Z-string assertions). GATE: fresh review = SAFE TO SHIP (all 7 checks MATCH/UNCHANGED incl. field-by-field parity, violations-null parity [no global ignore-null config], byte-identical column list, IsoZ ms-truncation, guards unchanged, no dangling refs, no new throws); Codex = SAFE TO SHIP (no findings). Pre-existing out-of-scope note (NOT changed): /session missing -> 'Not found' vs legacy 'Session not found' = the port's intentional uniform-404 IDOR hardening (corpus #28). Canary GREEN real-data (deploy 29527813263, health 200): anon /session->401; admin /session/{real id from /all-results}->200 startTime '2026-06-17T21:40:03.141Z' (Z-STRING live, no +00:00), createdDate+updatedAt Z, violationCount+violations key present (full-row intact); /session/nonexistent->404; /completed-exams/self->200 count 0 (staff empty; same IsoZ proven on /session). ALL pca-exam full-row timestamp emitters now Z-string. ▶ NEXT = personality take-flow session reads (/access + /session/:id), then assessment-profile derive/insights-status + assessments-timeline reads, test-scores reads, question360 GETs, vocational360 catalog/score reads, school-admin views -> Phase B engines."
      ]
    },
    {
      "id": "FM-DOTNET-023-personality-takeflow",
      "status": "completed",
      "goal": "Phase A personality take-flow reads: GET /api/v1/personality/access (checkAccess) + GET /session/{sessionId} (getOwnedSession). Completes the personality read cluster.",
      "dependsOn": [
        "FM-DOTNET-022-examsession-zstrings"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/personality.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-session-service.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/src/FormMaps.Infrastructure/Assessments",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "GET /api/v1/personality/access ({has_access, has_completed, existing_session_id?, reason?}) — snake_case, optional keys OMITTED when absent",
        "GET /api/v1/personality/session/{sessionId} (7-key snake_case projection {id,status,variant,language,resolved_type,started_at,completed_at}) — ISO-Z timestamps, uniform 404",
        "pure PersonalityAccess.Evaluate (completed-wins-over-in-progress golden-pinned)",
        "PersonalitySessionReader (self-scoped: /access = user's active sessions newest-first; /session = id+user_id+is_active owned row)",
        "2 web flags FORMMAPS_ROUTE_PERSONALITY_{ACCESS,SESSION}_TO_DOTNET + rewrites",
        "5 unit + 5 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "Both self-scoped on context.Tenant.UserId (NO canAccessUser, NO path userId). Guard RequireIdentity -> RequireSubscription -> reader. /session ownership WHERE id+user_id+is_active -> null -> uniform IDOR-safe 404; anon -> 401.",
        "/access: existing_session_id + reason OMITTED (JsonIgnore WhenWritingNull) when absent; reason only on completed branch. /session: all 7 keys present even when null; no leaked columns (dimension_scores/violations/flag_for_review/user_id). ISO-Z string timestamps (not DateTimeOffset). Do not weaken prior slices/RLS/security."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: personality take-flow reads (/access + /session/:id). Completes the personality read cluster (results = FM-017). dotnet 0/0; 197u+1c+128i (+5u +5i). GATE: fresh review = SAFE TO SHIP (all 9 axes MATCH: completed-wins ordering, omitted optional keys via JsonIgnore WhenWritingNull + snake_case, 7-key projection all-present-when-null no leaked cols, uniform-404 ownership, ISO-Z via PcaExamSessionRowMapper.IsoZ [personality timestamps are timestamp(3) not timestamptz -> correct Z], self-scoped no canAccessUser, no route shadowing /session vs /session/results, snake_case JsonPropertyName wins over camelCase policy; 2 unreachable-under-NOT-NULL-schema Low/Info notes). Codex = SAFE TO SHIP (parity confirmed incl. adversarial access orderings [in_progress-newer/completed-older etc], no RLS/sub divergence, no .NET-only throw). Canary GREEN (deploy 29528932080, health 200): anon /access->401+x-formmaps-service; jack(school_admin) /access->200 {has_access:true, has_completed:false} — existing_session_id+reason OMITTED (JsonIgnore proven LIVE); anon /session->401; jack /session/bogus->404 uniform. OPEN follow-up (gated, same as FM-017): real /session 200 7-key shape needs a real personality-session id (jack=staff has none). ▶ NEXT = FM-024 assessments-timeline (/me/timeline 3-source merge + /me/timeline/stats; RequireIdentity only, NO subscription; reuse PcaExamPagination default 50), then Phase B LIA-core engine port (golden-fixture-pinned)."
      ]
    },
    {
      "id": "FM-DOTNET-024-assessments-timeline",
      "status": "completed",
      "goal": "Phase A assessments-timeline reads: GET /api/v1/assessments/me/timeline (getTimeline 3-source merge) + /me/timeline/stats (getTimelineStats). Self-scoped, RequireIdentity only (NO subscription).",
      "dependsOn": [
        "FM-DOTNET-023-personality-takeflow"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Api/Endpoints",
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/src/FormMaps.Infrastructure/Assessments",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "GET /api/v1/assessments/me/timeline ({events,total,page,limit,totalPages,summary}) — heterogeneous events, camelCase",
        "GET /api/v1/assessments/me/timeline/stats ({overallCompletion, assessmentBreakdown})",
        "pure AssessmentTimeline.BuildTimeline/BuildStats golden-pinned (3-source merge, stable date-DESC sort, in-memory pagination, ISO-Z dates, stats total=5+evals+courses)",
        "AssessmentTimelineReader (pca_exam_sessions + evaluation_groups + course_enrollments, self-scoped)",
        "PcaExamPagination.Resolve gains defaultLimit param (backward-compat 20; timeline passes 50)",
        "web flag FORMMAPS_ROUTE_ASSESSMENT_TIMELINE_TO_DOTNET + rewrites (both endpoints)",
        "7 unit + 3 integration tests"
      ],
      "guardrails": [
        "Read-only only.",
        "RequireIdentity ONLY (mount lacks requireSubscription); self-scoped on context.Tenant.UserId (NO canAccessUser, NO path userId); anon -> 401.",
        "Events: score present on pca (even 0) OMITTED on eval/course; STABLE date-DESC sort (OrderByDescending, ties pca<eval<course); course date=enrolledAt??createdDate; in-memory paginate (64-bit skip, past-end->empty); ISO-Z dates; stats total=5+evals+courses (hardcoded pca 5). Do not weaken prior slices/RLS/security."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: assessments-timeline reads. dotnet 0/0; 204u+1c+131i (+7u +3i). GATE: fresh review + Codex BOTH = FIXES NEEDED, CONVERGED on ONE MED (int32 overflow in BuildTimeline skip: (page-1)*limit int32 wraps negative for huge page -> Skip(negative) returns first page vs legacy empty slice) — FIXED (compute skip in long, past-end->empty; golden-pinned Large_page_returns_empty test), same class as the FM-021 skip fix. Everything else parity-clean both reviews (event mapping/score-omission/titles/status, stable sort tie-break, enrolledAt??createdDate, totalPages, stats hardcoded-5+rounding+completedCourses, Z-dates, RequireIdentity-only-no-subscription self-scoped, SQL @@map+camelCase cols+isActive, camelCase JSON, backward-compat all-results default 20). 2 INFO (STJ escapes ° -> \\u00B0 decodes identically; export/report out of scope). Canary GREEN (redeploy 29530887712 of d638dbe, health 200): anon /me/timeline->401; jack /me/timeline->200 {total:0,totalPages:0,summary:{0,0,0},events:[]} (self-scoped empty — jack=staff has no OWN sessions; the /all-results rows were cross-school admin view); ?page=2&limit=1->200 {page:2,limit:1,totalPages:0}; /me/timeline/stats->200 {overallCompletion:0, breakdown pca{0,5}} (hardcoded pca total=5 proven live). OPEN follow-up (gated): non-empty real-data timeline (heterogeneous events + score-on-pca) needs a real student id/token. ▶ NEXT = FM-025 Phase B: port LIA-core scoring engine + percentile mapper (pure, golden.json-pinned: scoringCases/percentileCases/globalPercentileCases; no DB/canary) — unblocks Phase C LIA write path."
      ]
    },
    {
      "id": "FM-DOTNET-025-lia-core-engine",
      "status": "completed",
      "goal": "Phase B slice 1: pure port of the LIA-core scoring engine + percentile mapper (lib/lia-core scoring-engine.ts + percentile-mapper.ts), verified byte-for-byte against the shared cross-repo golden.json. No DB/endpoints/canary. Unblocks the Phase C LIA write/completion path.",
      "dependsOn": [
        "FM-DOTNET-024-assessments-timeline"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/scoring-engine.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/percentile-mapper.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/types.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/__fixtures__/golden.json"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/tests"
      ],
      "outputs": [
        "LiaScoring (calculateRawScore/FinalScore/SubtestScores/getMax/Min/clampScore; configs PR4/VB2/NS2/WM1/VR3; roundHalfUp==Math.Round AwayFromZero; WM unrounded)",
        "LiaPercentileMapper (getPercentile range lookup w/ restored -Inf boundary rows -> gap/NaN THROW; calculateGlobalPercentile mean Math.Floor(x*100+0.5)/100)",
        "embedded lia-percentile-tables.json (228 ranges extracted from PERCENTILE_TABLES, +/-Inf->+/-1e9, double min/max preserving WM -0.01)",
        "LiaCoreGoldenTests: embedded golden.json (byte-identical, sha256 7d9e7b0e...) pins 90 scoringCases + 449 percentileCases + 6 globalPercentileCases + 5 configs + 606 levelCases (101 global + 505 subtest via LiaPerformanceLevels) + throw cases"
      ],
      "guardrails": [
        "Pure functions only (no DB/endpoints/HTTP). Byte-parity via the SHARED golden.json (cross-repo sha256 PARITY-MANIFEST.json w/ tims-suite).",
        "getPercentile: first-match range, restored -Inf boundary rows so <first is dead + gaps/NaN THROW (never silent 100/0). global = JS Math.round == Math.Floor(x+0.5). Do not weaken; item-level answer scoring (isAnswerCorrect/VR theta-90/correctAnswer/question-bank) deferred to the Phase C submit slice."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: FIRST Phase B slice — LIA-core scoring engine ported PURE + golden-pinned, byte-parity proven (no DB/canary). dotnet 0/0; 214u+1c+131i (+11u). Approach: extract PERCENTILE_TABLES -> embedded JSON (like the FM-017 bank embed), hardcode the 5 configs (cross-checked vs golden.configs), embed golden.json in tests, harness runs EVERY case. GATE: fresh review SAFE + Codex FIXES-NEEDED, both folded — fresh caught levelCases unpinned (MED vacuous-gap) -> wired golden.levelCases (606) into harness so LiaPerformanceLevels[FM-015] is cross-repo-pinned; Codex caught the extraction dropped the 5 leading -Infinity percentile-0 rows (MED: fractional-negative gap returned 0 vs TS throw, unreachable for integer finalScores but a public-API divergence) -> re-extracted INCLUDING them (double min/max, +/-1e9), throw-on-fractional-gap pinned; + global rounding AwayFromZero->Math.Floor(x+0.5)=JS Math.round (LOW, unreachable neg) + count pins. Fresh independently re-derived all tables + 606 levelCases (0 mismatches); golden.json confirmed byte-identical (sha256 match). Corpus #11 (penalty divisors, roundHalfUp, norm-table percentiles, global mean-of-5, WM=60, gap/NaN THROW) now COVERED + golden-pinned. ▶ NEXT (Phase B/C): item-level answer scoring (isAnswerCorrect + VR theta->theta-90 chirality [corpus #10] + correctAnswer + question-bank, golden questionBank[305]-pinned) at the LIA take/submit slice; then personality tally + RIASEC vocational engines; OR continue Phase A reads (test-scores/question360/vocational360/school-admin)."
      ]
    },
    {
      "id": "FM-DOTNET-026-lia-item-level-scoring",
      "status": "completed",
      "goal": "Phase B slice 2: pure port of LIA item-level answer scoring (lib/lia-core scoring-engine.ts item-level fns + question-bank.ts static bank + verbal-en.ts answer resolver). Visual-rotation scored CHIRALITY-only; the theta->theta-90 remap of the 378 VR glyphs (corpus #10) lives in the static bank data, proven by pinning BuildQuestionBank() against the shared golden.json questionBank[305]. No DB/endpoints/canary. Feeds the Phase C LIA take/submit path + the DB seed.",
      "dependsOn": [
        "FM-DOTNET-025-lia-core-engine"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/scoring-engine.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/question-bank.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/verbal-en.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/types.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia-core/__fixtures__/golden.json"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/tests"
      ],
      "outputs": [
        "LiaAnswerScoring (isAnswerCorrect trim/upper/ordinal with the exact ECMAScript JsWhitespace trim set; calculateCorrectAnswer dispatcher verbal+unknown THROW; validatePatternRecognition/NumericalSpeed/WorkingMemory/VisualRotation; getVerbalAnswerForLanguage en-override practice#2->C; BuildQuestionBank)",
        "NormalizeVisualFigure/VisualFiguresMatch chirality-only (base glyph R vs mirrored U+1589; rotation suffix _90/_180/_270 ignored)",
        "embedded lia-question-bank.json (305 raw items extracted from the shared golden.questionBank; computed answers omitted so the engine must produce them, verbal static answers carried)",
        "LiaAnswerScoringTests: BuildQuestionBank pinned vs golden.questionBank[305] (all 305 computed/static keys incl. VR chirality) + normalization / JS-trim-set / tie-break / throws / verbal-resolver units"
      ],
      "guardrails": [
        "Pure functions only (no DB/endpoints/HTTP). Visual-rotation MUST stay chirality-only (U+1589 base glyph, rotation ignored) -- corpus #10, mutation-verified red-if-regressed. isAnswerCorrect both-empty->true is the pure-fn contract; the caller guards skip semantics. Verbal keys are pre-defined (throw, never compute). Normalization trims the EXACT JS strip set (incl U+FEFF, excl U+0085) -- do not fall back to plain .Trim()."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: SECOND Phase B slice -- LIA item-level answer scoring ported PURE + golden-pinned (no DB/canary). dotnet 0/0; 246u+1c+131i (+32u). KEY finding: the VR theta->theta-90 remap (corpus #10) is NOT runtime math -- it was a one-time data-transcription remap baked into the static bank; scoring ignores rotation and compares CHIRALITY ONLY (base glyph upright R vs mirrored U+1589). Embed lia-question-bank.json extracted from the shared golden.questionBank[305] (byte-identical chain: legacy==tims-suite==monorepo-test-embed==PARITY-MANIFEST sha 7d9e7b0e; NO cross-repo golden mutation needed). BuildQuestionBank() reproduces all 305 correct_answers (4 subtests computed, verbal static) == golden, mutation-verified red on a VR-chirality break. GATE: fresh review SAFE + Codex FIXES-NEEDED (1 Low: JS .trim() strips U+FEFF/keeps U+0085; .NET Trim() the opposite) -> folded the exact ECMAScript JsWhitespace trim set, mutation-pinned both directions. Corpus #10 (lia-vr-rotation) now COVERED + golden-pinned. NEXT (Phase B/C): personality scoring tally (corpus #12) + RIASEC vocational+integration engines; then Phase C authed WRITES (LIA/personality/exam take+submit+complete using these engines); OR continue Phase A reads (test-scores/question360/vocational360/school-admin)."
      ]
    },
    {
      "id": "FM-DOTNET-027-personality-scoring",
      "status": "completed",
      "goal": "Phase B slice 3: pure port of the personality scoring tally (services/personality/personality-scoring.ts). Binary forced-choice over EI/SN/TF/JP; A->first pole, B->second; higher count wins (tie->first, balanced); 4 winners -> 4-letter type; normalizedIntensity = round(intensity / maxPerDimension * 100) on the 10/20 variant scale (corpus #12, NOT div answered). No DB/endpoints/canary. Output shape (keyed PersonalityScore.Dimensions + bare resolvedType) matches the FM-017 read side; feeds the Phase-C personality complete/write path.",
      "dependsOn": [
        "FM-DOTNET-026-lia-item-level-scoring"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-scoring.ts",
        "/Users/federicotafur/formmaps-platform/api/src/__tests__/personality-scoring.unit.test.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-session-service.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/tests"
      ],
      "outputs": [
        "PersonalityScoring (ScorePersonality tally + DIMENSIONS / DIMENSION_POLES / VARIANT_ITEMS_PER_DIMENSION(10/20) / TIE_BREAK_TO_FIRST_POLE + GetMaxPerDimension + ClampPercent + RoundHalfUp = Math.Floor(x+0.5) JS-Math.round parity)",
        "records PersonalityAnswer / PersonalityDimensionScore / PersonalityScore (Dimensions = KEYED map {EI:...} == legacy Record<Dimension,DimensionScore>, matching the FM-017 reader)",
        "PersonalityScoringTests: the legacy unit cases mirrored + corpus-#12 divisor discriminator + tie-break / type-order / defensive-ignore / keyed-shape / null-guard / clamp"
      ],
      "guardrails": [
        "Pure functions only. Engine is DEFENSIVE (invalid choice / unknown|null dimension ignored, never throws) -- strict A/B->400 + retake->409 stay in the SERVICE (Phase C), PIN-WHEN-BUILT. normalizedIntensity divisor MUST be maxPerDimension (10/20), NOT answered (corpus #12). Dimensions MUST serialize as a KEYED object {EI:...} (not an array) or the FM-017 reader returns empty dimension_scores; the Phase-C write must emit camelCase inner fields + preserve the UPPERCASE dimension keys."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: THIRD Phase B slice -- personality scoring tally ported PURE + unit-pinned (no shared golden.json exists for personality; mirrored the TS unit test as spec). dotnet 0/0; 263u+1c+131i (+17u). Corpus #12 TALLY COVERED (div maxPerDim(10/20)x100 NOT div answered; tie->first pole+balanced), mutation-verified red on the divisor swap AND the tie-break flip. GATE: fresh SAFE + Codex FIXES-NEEDED, CONVERGED on 2 folded -- (High/Med) output was a LIST -> changed to keyed IReadOnlyDictionary (== legacy Record<Dim,DimensionScore>) so it serializes to the {EI:...} jsonb the FM-017 reader (gates on JsonValueKind.Object) consumes [an array reads back empty]; (Low) a null dimension threw on ContainsKey(null) -> guarded to ignore like TS tallies[null]. Unknown-variant fail-closed confirmed intentional by both. Item bank (120 bilingual items, UNUSED by scoring) deferred to a personality catalog/take-flow slice. strict-A/B-400 + retake-409 remain SERVICE/Phase-C (corpus #12 residual, PIN-WHEN-BUILT). NEXT (Phase B/C): RIASEC vocational + integration engines (vocationalScoringService), OR Phase C authed WRITES (LIA/personality/exam take+submit+complete using the FM-025/026/027 engines), OR Phase A reads (test-scores/question360/vocational360/school-admin)."
      ]
    },
    {
      "id": "FM-DOTNET-028-vocational-scoring",
      "status": "completed",
      "goal": "Phase B slice 4: pure port of the vocational-360 scoring engine + the 3-channel integration engine (vocationalScoringService.ts + vocationalIntegrationService.ts). NOTE: NO RIASEC/Holland logic exists — it is a config-driven, dimension-weighted vocational-360 engine + a (360 x PCA x MIL) -> integratedComposite integration. No DB/endpoints/canary. Feeds the Phase-C vocational recompute/write path; the service emits the Decimal columns as JSON numbers (Number(Decimal)).",
      "dependsOn": [
        "FM-DOTNET-027-personality-scoring"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/vocationalScoringService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocationalIntegrationService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/__tests__/vocationalScoringService.test.ts",
        "/Users/federicotafur/formmaps-platform/api/src/__tests__/vocationalIntegrationService.test.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocational360Service.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/tests"
      ],
      "outputs": [
        "VocationalScoring (round2 = exact JsRound; normalize; band; dimensionScoreForGroup; renormalizeGroupWeights; aggregateDimension; composite; computeRankings [OrderedTally insertion-order + stable sort; LastWins Map semantics]; computeVocationalResult) + records + discriminated ready/not_ready",
        "VocationalIntegration (competencesToScore / milToScore / normalizeIntegrationWeights / integrate / computeIntegratedResult; ordered missing [360,pca,mil])",
        "shared JsString.JsTrim (ECMAScript WhiteSpace+LineTerminator strip set; LiaAnswerScoring[FM-026] migrated onto it)",
        "VocationalScoringTests + VocationalIntegrationTests mirror both TS suites + round2-ULP + duplicate-question last-wins + BOM open-text pins"
      ],
      "guardrails": [
        "Pure functions only. round2 = exact JS Math.round (JsRound: x-floor(x)>=0.5 ? floor+1 : floor, NOT Math.Floor(x+0.5) which has a ULP artifact). Map-keyed lookups MUST be last-wins (ToDictionary throws on a duplicate question-number / dimension-key; TS new Map keeps last). Rankings tie-break = JS-Map insertion order + STABLE sort (OrderedTally + OrderByDescending). Dimension band on the UNROUNDED aggregate while score is rounded. PHASE-C SERIALIZATION CONTRACT: emit the Decimal columns as JSON NUMBERS (Number(Decimal)), NOT strings; serialize the CONCRETE outcome type (or configure polymorphism) so ready-payload fields are not lost — the abstract ScoringOutcome/IntegrationOutcome base would drop them. Readiness gates (needs_self_plus_one; ordered missing [360,pca,mil]) are contract."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx"
      ],
      "notes": [
        "DONE 2026-07-16: FOURTH Phase B slice. KEY: the 'RIASEC' working title was WRONG — there is NO Holland/RIASEC logic; this is a config-driven dimension-weighted vocational-360 engine + a 3-channel integration. Both source files 100% pure. Mirrors both TS unit suites verbatim (no golden.json for vocational). dotnet 0/0; 290u+1c+131i (+27u). GATE: fresh SAFE + Codex FIXES-NEEDED, CONVERGED, folded (a) round2 -> byte-exact JsRound [fresh F1: Math.Floor(x+0.5) diverges from JS Math.round at n*100==0.49999999999999994; verified in node], (b) Map LAST-WINS for duplicate question-numbers/dimension-keys [Codex rated 'Critical' citing a dup-question-50 in the live instrument — that specific evidence did NOT verify vs the actual source (hallucinated line refs 2198/2251/...); but the Map-vs-ToDictionary divergence is real, folded as faithfulness], (c) JS-trim for open-text U+FEFF -> extracted a shared JsString.JsTrim + migrated FM-026 LiaAnswerScoring onto it (consolidated the duplicated ECMAScript trim primitive), (d) ScoringRule numeric fields int?->double? to match TS number. Output-contract abstract-base-serialization risk documented as a Phase-C guardrail (records carry all fields per concrete type). All 4 folds mutation-verified red-if-regressed. LESSON reinforced: verify Codex severity/evidence against real code before acting. NEXT: Phase C authed WRITES (wire the FM-025/026/027/028 engines into LIA/personality/exam/vocational take+submit+complete, idempotent, new write-owner rails), OR Phase A reads (test-scores/question360/vocational360/school-admin), OR the vocationalInterestMap recommendation micro-engine."
      ]
    },
    {
      "id": "FM-DOTNET-029-lia-complete-write",
      "status": "completed",
      "goal": "Phase C, the FIRST authored WRITE in the .NET backend: port legacy completeSession (LIA session completion). TOCTOU-safe idempotent write (SELECT FOR UPDATE + conditional UPDATE WHERE status<>completed), two coverage gates, the shipped Phase-B engines score, PII-free audit event; plus the FIRST real-DB Testcontainers harness. Flag-gated staging-only; PROD stays Node. The prod route-flip is deferred until the take/submit slice owns the whole session lifecycle (porting only /complete while Node owns /start,/answer,/timeout = a dual-write on lia_assessment_sessions).",
      "dependsOn": [
        "FM-DOTNET-025-lia-core-engine",
        "FM-DOTNET-024-assessments-timeline"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/lia/lia-results-service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/lia.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/migrations/20260703000000_lia_tims_parity/migration.sql"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/src/FormMaps.Infrastructure/Assessments",
        "services/api/src/FormMaps.Infrastructure/Data",
        "services/api/src/FormMaps.Api/Endpoints/LiaEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "LiaSessionWriter (write-owner: OpenWritableAsync + SELECT FOR UPDATE + idempotency + 2 coverage gates + tally + conditional UPDATE WHERE status<>completed + PII-free audit; completed_at truncate-to-ms + tz-independent timestamp binding; affected==0 fails closed)",
        "LiaCompletion (LiaCompletionResult snake_case DTO + LiaCompletionScorer pure + ILiaSessionWriter)",
        "write rail: FormMapsDatabaseSession.CommitAsync + IFormMapsDatabaseSessionFactory.OpenWritableAsync + NpgsqlFormMapsDatabaseSessionFactory.OpenAsync(readOnly)",
        "POST /api/v1/lia/session/{sessionId}/complete (RequireIdentity->RequireSubscription->writer; Completed->200, IncompleteCoverage->409 'Assessment not complete', NotInProgress->400 'not_in_progress', else uniform 404)",
        "FIRST real-DB Testcontainers harness: LiaWriteDatabaseFixture (schema-only, non-UTC server as the tz regression pin) + LiaSessionWriterTests (8) + LiaCompleteEndpointTests (6)",
        "web flag FORMMAPS_ROUTE_LIA_COMPLETE_TO_DOTNET (default off) + next.config rewrite"
      ],
      "guardrails": [
        "TOCTOU: SELECT FOR UPDATE + conditional UPDATE WHERE status<>'completed' under ReadCommitted (the loser re-reads status='completed' and returns the stored bundle, NO write). An already-completed session returns its stored scores with NO write (no double-score / double-bill). completed_at = Kind=Unspecified UTC wall-clock truncated to ms + bound DbType.DateTime2 (tz-independent, matches Prisma @db.Timestamp(3); a Kind=Utc/timestamptz binding would shift on a non-UTC server). affected==0 FAILS CLOSED (no phantom commit/audit/success). Audit event PII-free (IDs + scores only), emitted only after commit. DUAL-WRITE CAVEAT: the prod route-flip stays deferred until the take/submit slice owns start/answer/timeout. M2: the insight-funnel (Bedrock) trigger stays polyglot / out of the write path -> prod-cutover checklist item."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST /complete -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: the FIRST authored WRITE + the first real-DB harness. dotnet 0/0; 292u+1c+146i. The harness caught a real write-path bug: completed_at drifted 1ms (ToIsoZ truncates to ms but Postgres timestamp(3) rounds) -> truncate up front. GATE: fresh review FIXES-NEEDED + Codex FIXES-NEEDED, both CONVERGED. Folded: M1 (tz-independent timestamp binding; the write harness now runs the container under America/New_York so the store-vs-return assertions are the regression pin), L1 (400 body 'not_in_progress' exact), the affected==0 fail-closed (Codex NOTE: never emit a completion audit for a write that did not land), + new LiaCompleteEndpointTests pinning the HTTP status/body mapping both reviewers flagged as untested, + strengthened idempotency (all fields) + audit-absence-on-reject assertions. Documented-not-replicated (unreachable on app-written data; prior-slice JS-coercion policy): NULL/string-valued stored scores in the idempotent branch (this writer only persists well-formed jsonb); endedAt truthiness vs string-kind gate. Corpus write-guards #18/#19/#25 now COVERED (idempotent no-double-score, incomplete-coverage->409, no double-write). NEXT: Phase C authed WRITES (personality/exam/vocational submit+complete wiring FM-025/026/027/028), OR Phase A reads (test-scores/question360/vocational360/school-admin)."
      ]
    },
    {
      "id": "FM-DOTNET-030-personality-write",
      "status": "completed",
      "goal": "Phase C, the FULL personality session lifecycle write (legacy personality-session-service.ts start + answer + complete). Because .NET owns the WHOLE lifecycle there is NO dual-write on personality_assessment_sessions -> personality is the FIRST prod-cut-over-able domain (unlike LIA/FM-029). Reuses the FM-029 write rail, FM-027 PersonalityScoring, and the FM-017 results reader.",
      "dependsOn": [
        "FM-DOTNET-029-lia-complete-write",
        "FM-DOTNET-027-personality-scoring",
        "FM-DOTNET-017-personality-results"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-session-service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/personality.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/personality/personality-bank.ts",
        "/Users/federicotafur/formmaps-platform/api/src/data/personality-items.data.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/src/FormMaps.Infrastructure/Assessments",
        "services/api/src/FormMaps.Api/Endpoints/PersonalityEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "PersonalityItemBank + embedded personality-items.json (laboral 40 + estudiantil 80, byte-faithful extract; ServeItems drops poles = answer-key non-leak; FindItem/ItemCount)",
        "PersonalitySessionWriter (StartAsync resume/create/retake-block; SaveAnswerAsync upsert + server-derived dimension + strict A/B + status/ownership guards; CompleteAsync TOCTOU FOR UPDATE + conditional UPDATE, idempotent, coverage gate, FM-027 score, returns via FM-017 reader; affected==0 fail-closed; PII-free audit on create + complete; tz-independent timestamps)",
        "PersonalityWrite.cs (PersonalityWriteStatus enum, SessionStartPayload/AnswerResult DTOs, 3 outcome records, IPersonalitySessionWriter)",
        "POST /api/v1/personality/{start, session/{id}/answer, session/{id}/complete} with lenient body parsing + exact legacy handleError status/body map",
        "PersonalityWriteDatabaseFixture (personality_assessment_sessions + personality_responses, non-UTC server pin) + PersonalitySessionWriterTests(16) + PersonalityWriteEndpointTests(13) + PersonalityItemBankTests(8)",
        "web flags FORMMAPS_ROUTE_PERSONALITY_{START,ANSWER,COMPLETE}_TO_DOTNET (default off) + next.config rewrites",
        "[JsonPropertyName] camelCase added to PersonalityDimensionScore (the dimension_scores jsonb contract)"
      ],
      "guardrails": [
        "dimension_scores jsonb inner keys MUST be camelCase (winningPole/normalizedIntensity/balanced) — the FM-017 reader echoes them verbatim to the frontend; a PascalCase jsonb silently blanks the results radar/bars (fresh+Codex C1/HIGH). Strict A/B raw (never truncated: 'BLAH' != 'B'). Server derives the dimension from the item bank; poles never served. complete idempotent (completed -> no rescore) + coverage-gated + TOCTOU (SELECT FOR UPDATE + conditional UPDATE WHERE status<>'completed') + affected==0 fail-closed. Resume revalidates owner + is_active (getOwnedSession parity). itemNumber mirrors Number() (numeric string coerces). Audit PII-free, only after commit. NO dual-write -> prod-cut-over-able. Timestamps tz-independent (Kind=Unspecified + DbType.DateTime2)."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST start/answer/complete -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: the FULL personality lifecycle write (start+answer+complete) -> personality is the FIRST prod-cut-over-able domain (no dual-write). dotnet 0/0; 300u+1c+175i. GATE: fresh review CRITICAL C1 + Codex HIGH (SAME bug: dimension_scores PascalCase vs legacy camelCase, latent until the first .NET write exercised the casing the FM-017 reader had only seen from legacy Node; masked by a green suite) -> FIXED (camelCase on the record) + round-trip pinned. Codex-only catches folded: itemNumber Number()-string coercion (MED), ResumeAsync owner+is_active revalidation (MED, fresh missed it). L1/L2 documented (unreachable/faithful). Reuses FM-029 rail + FM-027 + FM-017. Corpus #12 (strict A/B->InvalidChoice, retake->AlreadyCompleted, division already FM-027), #19 (coverage), #25 (idempotency), #28 (uniform 404) COVERED. NEXT: pca-exam submit (corpus #18 answer-replay) OR vocational write OR remaining Phase A reads; personality prod route-flip is now possible (Federico go/no-go)."
      ]
    },
    {
      "id": "FM-DOTNET-031-pcaexam-submit-write",
      "status": "completed",
      "goal": "Phase C, the pca-exam take/submit WRITE (legacy assessmentService.ts startExamSession + submitExam). THIRD Phase-C write domain (after LIA complete/FM-029, personality lifecycle/FM-030). Closes regression corpus #18: a completed OR time-expired session 409s before any write under SELECT ... FOR UPDATE -> the report-visible answer key cannot be replayed to force 100% and no duplicate answer rows are appended, no rescore. Reuses the FM-029 write rail, the FM-013 pca-exam readers, and the FM-009 canAccessUser guard.",
      "dependsOn": [
        "FM-DOTNET-029-lia-complete-write",
        "FM-DOTNET-013-pcaexam-session",
        "FM-DOTNET-009-user-report"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/assessment.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments",
        "services/api/src/FormMaps.Infrastructure/Assessments/PcaExamWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/ExamEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "ExamScoring (pure): String(Int correctAnswer)===userAnswer, Float score/accuracy (unrounded, JS op-order), timeSpent number|HH:MM:SS|parseInt||0 coercion",
        "PcaExamWriter: StartExamAsync (exam->404, retake-block on status='Completed' ONLY, answer-key-stripped serve, create InProgress) + SubmitExamAsync (SELECT FOR UPDATE, guard isCompleted||status='Completed' before any write, in-handler score, conditional UPDATE WHERE isCompleted=false AND status<>'Completed', affected==0 fail-closed, PII-free audit actor=caller/subject=owner)",
        "PcaExamWrite.cs (PcaExamWriteStatus enum, ServedExamQuestion/ExamStartPayload/ExamSubmitResult/SubmitAnswer DTOs, 2 outcome records, IPcaExamWriter)",
        "POST /api/pcaexam/exams/{examId}/start (self-scoped) + POST /api/pcaexam/submit (ownership at route via IExamSessionReader.GetSession + canAccessUser -> privileged CAN submit) with exact legacy error map",
        "PcaExamWriteDatabaseFixture (native ExamType/ExamStatus enums, camelCase cols, non-UTC pin) + PcaExamWriterTests(11) + PcaExamWriteEndpointTests(14) + ExamScoringTests(13)",
        "web flags FORMMAPS_ROUTE_PCAEXAM_{START,SUBMIT}_TO_DOTNET (default off) + next.config rewrites"
      ],
      "guardrails": [
        "Corpus #18: submit on a completed OR time-expired session (isCompleted || status='Completed') 409s BEFORE any write under SELECT FOR UPDATE; conditional UPDATE guards WHERE isCompleted=false AND status<>'Completed' (a TimeExpired session with isCompleted=true must 409 -- Node still owns /complete timeout = real cross-stack race); affected==0 fails closed; no dup answer rows, no rescore. Retake-block on start filters status='Completed' ONLY (a time-expired session does NOT block a new start). isCorrect = String(Int correctAnswer)===userAnswer; numeric JSON answers stringify by VALUE (String(3.0)==='3'). scorePercentage/accuracyPercentage are Float (unrounded). Answer INSERT sets id/updatedAt explicitly (Prisma @default(uuid())/@updatedAt are app-level, not prod DB defaults). Answer key never served. Enum ::casts. tz-independent timestamps (Kind=Unspecified + DbType.DateTime2). Audit PII-free, actor=caller/subject=owner, only after commit."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST exams/{id}/start & submit -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: THIRD Phase-C write (pca-exam take/submit). dotnet 0/0; 313u+1c+200i. GATE: fresh review SAFE-TO-SHIP (corpus #18 confirmed strictly SAFER than legacy: row-locked/atomic/fail-closed) + Codex FIXES-NEEDED -> CONVERGED, folded: [High] numeric JSON answers must JS-stringify by value (String(3.0)==='3' matches Int key 3; was GetRawText '3.0' -> mis-scored; now JsNumberToString) + [Low] submit audit attributed session owner as actor (privileged submit -> actorUserId=caller/subjectUserId=owner). DOCUMENTED (not code): [Med] insight trigger (Bedrock) stays polyglot/out-of-write-path (standing AI-polyglot decision, FM-029 M2 caveat); [Med] es+VerbalReasoning buildQuestionRows serve rebuild DEFERRED -> flagged in-code as an explicit /start prod-cutover blocker (display-only, scoring uses DB key). Corpus #18 (answer-replay-to-100% -> 409, no dup, no rescore) COVERED + real-DB Testcontainers pins (completed->409, TimeExpired->409 cross-stack race, TOCTOU one-write-under-concurrency). Staging deploy GREEN (run 29607164704) + deploy-only canary GREEN (health 200; anon POST start/submit -> 401 missing_identity + x-formmaps-service). PROD ROUTE-FLIP DEFERRED: Node still owns /complete (timeout) = partial-lifecycle dual-write on pca_exam_sessions; also the Spanish-VR serve + insight-trigger polyglot must close first. NEXT: vocational authed write (FM-028 engine + token-gated external = Phase D risk) OR remaining Phase A reads OR FM-033 personality PROD cutover (Milestone 1 proof)."
      ]
    },
    {
      "id": "FM-DOTNET-032-vocational-score-recompute-write",
      "status": "completed",
      "goal": "Phase C, the authed vocational SCORE recompute WRITE (legacy vocational360Service.ts recomputeVocationalResult). Wires the shipped FM-028 VocationalScoring engine into a write: loads the active instrument + completed rater responses, computes, and — only when ready — UPSERTs vocational_results (idempotent on evaluatedUserId + instrumentVersion). The INTEGRATED recompute (recomputeIntegratedResult) is DEFERRED: it depends on assembleCompleteProfile (the 237-LOC DISC/PCA + MIL/LIA cross-domain assembler), an unported separate unit.",
      "dependsOn": [
        "FM-DOTNET-028-vocational-scoring",
        "FM-DOTNET-031-pcaexam-submit-write",
        "FM-DOTNET-009-user-report"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/vocational360.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocational360Service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocationalScoringService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/VocationalWrite.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/VocationalWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/VocationalEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "VocationalWriter.RecomputeScoreAsync: load active instrument (singleton) + dimensions + questions + completed vocational evaluation_groups LEFT JOIN vocational_responses -> ScoringConfig/ScoringQuestion/ScoringGroup (each evaluation_group is its own group; jsonb parsers for groupWeights/bands/scoringRule/rankingOrder/selectedValues) -> VocationalScoring.ComputeVocationalResult -> if ready UPSERT vocational_results ON CONFLICT (evaluatedUserId,instrumentVersion) (composite numeric, groupsIncluded text[], dimensionScores/rankings/weightsApplied camelCase jsonb, computedAt tz-independent) + PII-free audit; not-ready/never-computed -> no write",
        "VocationalWrite.cs (VocationalRecomputeStatus enum, VocationalRecomputeOutcome, IVocationalWriter)",
        "NEW VocationalEndpoints group: POST /api/v1/vocational360/score/{evaluatedUserId}/recompute — authenticate-only (RequireIdentity + canAccessUser on 100-char-bounded id, NO subscription gate), privileged CAN recompute; all outcomes 200 with the outcome in data",
        "VocationalWriteDatabaseFixture (hand-written DDL — no prod migration exists for the vocational tables; non-UTC pin) + VocationalWriterTests(6) + VocationalWriteEndpointTests(6)",
        "web flag FORMMAPS_ROUTE_VOCATIONAL_SCORE_RECOMPUTE_TO_DOTNET (default off) + rewrite"
      ],
      "guardrails": [
        "dimensionScores/rankings/weightsApplied jsonb MUST be camelCase (the Node reader echoes inner keys verbatim) — serialized with JsonSerializerDefaults.Web; composite is a Decimal column emitted as a JSON NUMBER (FM-028 guardrail); serialize the CONCRETE VocationalResultPayload (abstract ScoringOutcome base drops the ready fields) via object-typed data. UPSERT is idempotent on the unique key (no dup rows). LEFT JOIN so a completed group with zero active responses still counts as present (legacy include semantics). authenticate-only mount -> RequireIdentity + canAccessUser, NO subscription. Deterministic group order (createdDate,id) is a documented superset of the legacy no-orderBy (non-deterministic) order. computedAt tz-independent. Audit PII-free (IDs + composite/band), after commit."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST vocational360/score/{id}/recompute -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: authed vocational SCORE recompute write. dotnet 0/0; 313u+1c+212i. GATE: fresh review SAFE-TO-SHIP (camelCase jsonb + Decimal-as-number guardrails verified) + Codex FIXES-NEEDED -> CONVERGED, folded: empty completed groups (both reviewers — legacy Prisma include returns a group even with zero active responses; inner join -> LEFT JOIN so it still counts; pinned red-if-regressed) + test-pinning (rankings camelCase assertion + two-'teacher'-groups test). KEPT as a documented decision (Codex flagged 'blocking'): deterministic group ordering (createdDate,id) — the legacy findMany has NO orderBy so its output is itself non-deterministic (DB physical order); no byte-match exists, and the deterministic order only differs on ranking ties / duplicate group keys where TS is arbitrary -> the faithful superset. Staging deploy GREEN (run 29609809187) + deploy-only canary GREEN (health 200; anon POST recompute -> 401 missing_identity + x-formmaps-service). DEFERRED: recomputeIntegratedResult (needs the assembleCompleteProfile DISC/PCA + MIL/LIA assembler port). Prod route-flip deferred (dark, flag off). NEXT: port assembleCompleteProfile -> vocational INTEGRATED recompute, OR remaining Phase A reads (test-scores/question360/school-admin), OR FM-033 personality PROD cutover (Milestone 1 proof)."
      ]
    },
    {
      "id": "FM-DOTNET-033-vocational-result-reads",
      "status": "completed",
      "goal": "Phase A/C, the authed vocational RESULT reads (legacy vocational360Service.ts getVocationalResult + getIntegratedResult) — complements the FM-032 score-recompute write. Reads the persisted vocational_results / vocational_integrated_results rows under a read-only RLS session; Decimal->JSON number, jsonb passthrough (camelCase), never_computed when absent.",
      "dependsOn": [
        "FM-DOTNET-032-vocational-score-recompute-write",
        "FM-DOTNET-009-user-report"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/vocational360.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocational360Service.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/VocationalRead.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/VocationalReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/VocationalEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "VocationalReader.GetScoreAsync/GetIntegratedAsync: resolve active instrument version -> row on (evaluatedUserId,instrumentVersion) under OpenReadOnlyAsync; Decimal cols via ::double precision (=Number(Decimal)); jsonb passthrough as JsonElement (camelCase preserved); computedAt ISO-Z; null when no instrument or no row",
        "VocationalRead.cs (VocationalScoreRead/VocationalIntegratedRead records with computed Status='ready', IVocationalReader)",
        "Two GET routes on VocationalEndpoints (/score/{id}, /integrated/{id}) via shared AuthorizeAsync (RequireIdentity + canAccessUser on 100-char-bounded id, NO subscription; privileged CAN read); null -> 200 {status:'never_computed'}; recompute POST refactored onto AuthorizeAsync",
        "harness gains vocational_integrated_results + VocationalReaderTests(7) + VocationalReadEndpointTests(8)",
        "web flags FORMMAPS_ROUTE_VOCATIONAL_{SCORE,INTEGRATED}_READ_TO_DOTNET (default off) + rewrites"
      ],
      "guardrails": [
        "Decimal cols emitted as JSON NUMBERS (::double precision = Number(Decimal)); jsonb dimensionScores/rankings/weightsApplied pass through verbatim (stored camelCase inner keys); computedAt ISO-Z (timestamp(3) relabeled UTC); never_computed (null) when no active instrument OR no row -> read always 200s; guard = RequireIdentity + canAccessUser(100-char), NO subscription (authenticate-only mount), privileged CAN read; recompute POST behavior preserved through the shared AuthorizeAsync."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon GET vocational360/score|integrated/{id} -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: authed vocational result reads (complements FM-032). dotnet 0/0; 313u+1c+227i. GATE: fresh review SAFE-TO-SHIP + Codex SAFE-TO-SHIP (NO runtime findings) -> folded the 3 Codex LOW test-hardening gaps (fractional-composite Decimal-as-number, integrated no-active-instrument never_computed, integrated-route guard-chain anon/deny) + refreshed the stale VocationalEndpoints class doc. Staging deploy GREEN (run 29611462422) + canary GREEN (health 200; anon GET score/integrated -> 401 missing_identity + x-formmaps-service). DEFERRED (polyglot): GET /recommendations (Bedrock aiJson + PII strip + AI cache = standing AI-polyglot decision); catalog reads /instrument + /questionnaire = a later slice. Prod route-flip deferred (dark). NEXT: catalog reads (/instrument + /questionnaire), OR port assembleCompleteProfile -> vocational INTEGRATED recompute, OR remaining Phase-A reads (test-scores/question360/school-admin), OR FM-034 personality PROD cutover (Milestone 1 proof)."
      ]
    },
    {
      "id": "FM-DOTNET-034-vocational-catalog-reads",
      "status": "completed",
      "goal": "Phase A, the authed vocational CATALOG reads (legacy getInstrument + getQuestionnaire) — completes the vocational360 read surface. Authenticate-only (no canAccessUser: catalog, not per-user).",
      "dependsOn": [
        "FM-DOTNET-033-vocational-result-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/vocational360.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocational360Service.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/VocationalRead.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/VocationalReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/VocationalEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "VocationalReader.GetInstrumentAsync (active instrument + dims order asc; weight ::double precision=number; groupWeights/integrationWeights/interpretationBands + dim scaleAnchors jsonb passthrough; null when none) + GetQuestionnaireAsync (questions isActive AND group IS NULL OR =@group order asc; LEFT JOIN dimension[key+scaleAnchors] + variant[group,isActive]; scaleAnchors = q ?? dimension ?? null via FirstNonNullJson skipping SQL-null AND jsonb 'null'; options passthrough; text = variant.textEs ?? '')",
        "InstrumentDto/InstrumentDimensionDto/QuestionnaireItem records; IVocationalReader gains GetInstrument/GetQuestionnaire",
        "GET /instrument (RequireIdentity only; null->404 'No active vocational instrument') + GET /questionnaire?group (RequireIdentity; group validated {self,parent,teacher,sibling_friend} else 400 'Invalid group' BEFORE reader) on VocationalEndpoints",
        "harness += vocational_question_variants + question area/scaleAnchors/options cols; VocationalCatalogReaderTests(3) + 6 catalog endpoint tests",
        "web flag FORMMAPS_ROUTE_VOCATIONAL_CATALOG_TO_DOTNET (default off) + rewrites"
      ],
      "guardrails": [
        "authenticate-only (RequireIdentity, NO canAccessUser — catalog not per-user); weight as JSON number; jsonb passthrough (groupWeights/integrationWeights/interpretationBands/scaleAnchors/options); scaleAnchors two-level ?? fallback skips SQL-null AND jsonb 'null'; group validated before the reader (400 'Invalid group'); variant LEFT JOIN cannot fan out (unique (questionId,group)); question.group null = all groups."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon GET vocational360/instrument|questionnaire -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: vocational catalog reads — completes the vocational360 read surface. dotnet 0/0; 313u+1c+236i. GATE: fresh SAFE-TO-SHIP + Codex SAFE-TO-SHIP (no runtime findings) -> folded doc/coverage gaps (refreshed stale VocationalEndpoints class doc; pinned options jsonb passthrough). Staging deploy GREEN (run 29612690391) + canary GREEN (health 200; anon GET instrument/questionnaire -> 401). DEFERRED (polyglot): /recommendations (Bedrock). Vocational360 authed surface now .NET-capable EXCEPT the integrated recompute write (needs assembleCompleteProfile) + /recommendations (AI). NEXT: port assembleCompleteProfile -> vocational INTEGRATED recompute, OR remaining Phase-A reads (test-scores/question360/school-admin), OR personality PROD cutover (Milestone 1 proof)."
      ]
    },
    {
      "id": "FM-DOTNET-035-assemble-complete-profile",
      "status": "completed",
      "goal": "Port legacy assembleCompleteProfile (lib/assessmentProfile.ts) — the server-authoritative cross-domain read that folds LIA (parity-aware) + PCA (DISC/competences) + 360 + academics/preferences into ONE typed profile. A pure/read FOUNDATION: an internal Application/Infrastructure service (ICompleteProfileAssembler), NOT an HTTP route — no new endpoint, no web flag, no anon-401 canary. Unblocks the vocational INTEGRATED recompute write + assessment-profile insights-status.",
      "dependsOn": [
        "FM-DOTNET-034-vocational-catalog-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/lib/assessmentProfile.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/evaluation360.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/evaluationGroups.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/lia/scoring.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/AssessmentProfile.cs",
        "services/api/src/FormMaps.Application/Assessments/AssessmentProfileMath.cs",
        "services/api/src/FormMaps.Application/Assessments/PcaNormalization.cs",
        "services/api/src/FormMaps.Application/Assessments/Evaluation360Scoring.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/CompleteProfileAssembler.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/tests"
      ],
      "outputs": [
        "CompleteProfileAssembler.AssembleAsync (10 reads under ONE caller read-only RLS session; authorization is the caller's, matching the legacy bare-userId lib): pca_exam_sessions history + lia_assessment_sessions parity (supersedes per-exam when percentiles present) -> mil/perExam/composite; pca_results discResult/competences -> DISC 3-graph matrix (primary=graph2) + competences; evaluation_feedbacks JOIN evaluation_groups + questions_360 -> weighted 360 category averages (self-excluded); academics (grades GPA + latest SAT/ACT + AP/honors/IB + activities) + preferences; sha256 fingerprint",
        "Pure Application helpers: PcaNormalization (normalizeDisc/graphAt/normalizeCompetences), Evaluation360Scoring (categoryScoresFromFeedback/categoryAverages/normalizeGroupType), AssessmentProfileMath (JsNumber.ToFixed2 + ProfileFingerprint) + records (CompleteAssessmentProfile tree) + ICompleteProfileAssembler; reuses MilComposite (FM-028) verbatim for the composite",
        "harness += assessmentprofile-schema.sql (10 tables hand-authored from schema.prisma; no FK/RLS; non-UTC tz pin); 41 unit tests (JsToFixed2 gold batch + 3 Node gold fingerprint hashes + DISC + 360) + 12 CompleteProfileAssembler integration tests",
        "NO web flag / NO route (internal lib consumed by the future integrated-recompute writer)"
      ],
      "guardrails": [
        "byte-exact Node fingerprint parity: custom ordered JSON.stringify serializer (insertion-ordered keys, integral vs shortest-round-trip numbers, UTF-8 non-ASCII literal) pinned by 3 Node-computed gold hexes; JsNumber.ToFixed2 = ECMAScript toFixed(2) exact-IEEE754 ties-up (G17->decimal->AwayFromZero; NOT new decimal(double)) pinned by a 15-case gold batch incl 1.005->1 / 2.675->2.67",
        "LIA parity engine supersedes per-exam rows; composite over UNROUNDED percents; Math.round=AwayFromZero (inputs >=0); pca_exam_sessions/lia parity ordered DESC NULLS FIRST (matches Prisma's implicit desc default), student_test_scores DESC NULLS LAST (explicit in TS)",
        "360 self-excluded; GROUP_WEIGHT ladder; relation-scoped catalog fallback (no cross-relation category borrow); strict-numeric rating gate; evaluatorCount incl self; category first-seen order preserved (feeds fingerprint, deterministic superset of the orderBy-less legacy read)",
        "pca_results/user_preferences read by userId with NO isActive filter (findUnique); all queries parameterized; PascalCase/camelCase jsonb key fallback; competence level modelled as int (TIMS domain 1-4)"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "NO staging canary — internal lib (no HTTP surface); staging build gate only"
      ],
      "notes": [
        "DONE 2026-07-17: assembleCompleteProfile read foundation. dotnet 0/0; 369u + 248i (56 new unit + 12 new integration). GATE (fresh reviewer + Codex, full TS visibility) folded 6 real findings: (1) BLOCKING toFixed(2) double-rounding — G17 rounded 7.3/4=1.82499.. up to 1.825->1.83 vs V8 1.82; fixed to G30 + 27-case Node gold batch incl exact-tie/round-up cases; (2) profile map fields -> IReadOnlyDictionary (legacy object/Record shape); (3) competences drop empty-name + level as number (double); (4) fingerprint integer-like category keys ordered numerically (JS object enumeration) + Node gold pin; (5) NULLS LAST -> NULLS FIRST on the 2 implicit-desc reads (matches Prisma) + red-if-regressed NULL-endTime test; softened fingerprint docstring. Internal lib -> NO route/flag/canary (deviation from prior read slices). UNBLOCKS: vocational INTEGRATED recompute write (recomputeIntegratedResult) + assessment-profile insights-status (getInsightsStatus). NEXT: port recomputeIntegratedResult (consumes this assembler + FM-028 VocationalScoring) OR remaining Phase-A reads OR personality PROD cutover."
      ]
    },
    {
      "id": "FM-DOTNET-036-integrated-recompute",
      "status": "completed",
      "goal": "Port legacy recomputeIntegratedResult (vocational360Service.ts) — the authed vocational INTEGRATED recompute WRITE. Fuses the persisted 360 composite (FM-033) + the FM-035 profile's PCA competences + MIL cognitive channels via the FM-028 VocationalIntegration engine, and upserts vocational_integrated_results. Completes the vocational360 authed WRITE surface (the slice FM-035 was built to unblock).",
      "dependsOn": [
        "FM-DOTNET-035-assemble-complete-profile",
        "FM-DOTNET-033-vocational-result-reads",
        "FM-DOTNET-032-vocational-score-recompute",
        "FM-DOTNET-028-vocational-scoring-integration"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/vocational360Service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/vocational360.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocationalIntegrationService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/assessmentProfile.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/VocationalWrite.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/VocationalWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/VocationalEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "VocationalWriter.RecomputeIntegratedAsync (IVocationalWriter gains it): read-only session loads active instrument (integrationWeights + interpretationBands; none -> null=never_computed) -> GetScoreAsync composite (threeSixty channel) + AssembleAsync (pcaScore=CompetencesToScore(profile.pca.competences ?? []); milScore=completeness.lia ? MilToScore(mil) : null) -> VocationalIntegration.ComputeIntegratedResult -> if ready, writable session UPSERT vocational_integrated_results ON CONFLICT (evaluatedUserId,instrumentVersion) {integratedComposite/threeSixtyScore/pcaScore/milScore as numeric Decimal, band, weightsApplied normalized {threeSixty,pca,mil} camelCase jsonb, computedAt=now (@updatedAt)} + PII-free audit; not_ready/never_computed -> no write. Reuses FM-032 write rail + FM-035 assembler + FM-033 reader + FM-028 engine",
        "POST /api/v1/vocational360/integrated/{evaluatedUserId}/recompute on VocationalEndpoints (authenticate-only + canAccessUser 100-char-bound -> 404 deny; all outcomes 200: ready payload status=ready / not_ready+missing[] / never_computed). Serialize CONCRETE outcome type (abstract IntegrationOutcome drops fields)",
        "harness += integrated-recompute-schema.sql (3 vocational tables) loaded ALONGSIDE assessmentprofile-schema.sql by IntegratedRecomputeDatabaseFixture; IntegratedRecomputeWriterTests(8) + VocationalIntegratedWriteEndpointTests(6)",
        "web flag FORMMAPS_ROUTE_VOCATIONAL_INTEGRATED_RECOMPUTE_TO_DOTNET (default off) + rewrite"
      ],
      "guardrails": [
        "channel mapping faithful: threeSixty=score?.Composite (never_computed->null); pcaScore=CompetencesToScore(competences ?? []); milScore gated on completeness.lia; ordered missing [360,pca,mil]; 0-score channel counts as PRESENT (not missing)",
        "asIntegrationWeights default 0 / asBands default 80·60·40; weights renormalized to sum 1 before persist+composite (pinned with un-normalized {50,30,20}); Decimal-as-number ((decimal)double); camelCase weightsApplied jsonb; computedAt bumped on every upsert (@updatedAt, pinned)",
        "never_computed + not_ready write NOTHING (write session opened only after the ready check); plain idempotent upsert on the unique key (no TOCTOU pre-check needed, matches FM-032); reads in separate sessions before the write (faithful to TS separate awaits)",
        "authenticate-only + canAccessUser (privileged may recompute a foreign id); audit after commit, PII-free; all SQL parameterized"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST vocational360/integrated/{id}/recompute -> 401"
      ],
      "notes": [
        "DONE 2026-07-17: vocational INTEGRATED recompute write. dotnet 0/0; 369u + 262i (14 new integration). GATE: fresh reviewer NO behavioral divergence/bug (ship-quality) + Codex SAFE-TO-SHIP -> folded the fresh reviewer's 4 coverage gaps (weight-normalization identity, partial not_ready, 0-vs-null channel boundary, computedAt-advances). Caught in-flight: an EmbeddedResource EndsWith collision (integrated-vocational-schema.sql matched the vocational-schema.sql fixture loader -> renamed integrated-recompute-schema.sql) — full-suite run surfaced it. COMPLETES the vocational360 authed WRITE surface. Prod-flip deferred (dark). NEXT: remaining Phase-A reads (test-scores/question360/school-admin), OR personality PROD cutover (Milestone-1 proof), OR Phase B."
      ]
    },
    {
      "id": "FM-DOTNET-037-test-scores-reads",
      "status": "completed",
      "goal": "Port the 3 method-unambiguous test-scores GET reads (routes/test-scores.ts): /superscore + /college-fit (pure Superscore + ClassifyFit) + /students/{id}/test-scores (bespoke counselor-404/parent-403/other-403 role auth, full-row passthrough). GET / (list) + POST/PUT/DELETE writes DEFERRED to a write slice — they share the bare /api/v1/test-scores path (Next rewrites match by path not method).",
      "dependsOn": [
        "FM-DOTNET-036-integrated-recompute"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/test-scores.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/normalizeEmail.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/TestScoreComputations.cs",
        "services/api/src/FormMaps.Application/Assessments/TestScoreReads.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/TestScoreReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/TestScoreEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "TestScoreComputations (pure): Superscore (SAT best-of + satTotal-both-required; ACT best-of + composite=round(mean/4) only-if-all-4, AwayFromZero=JS Math.round) + ClassifyFit (ordered: rate<0.15->reach, sat75->safety, sat25->match, else reach; >= boundaries; null rate not reach)",
        "TestScoreReader: GetSuperscoreAsync/GetCollegeFitAsync (self, req.userId) + HasActiveCounselorAssignmentAsync/HasActiveParentLinkAsync + ListActiveScoresAsync(userId,testType). college-fit: universities WHERE 4 SAT bands NOT NULL ORDER BY acceptanceRate ASC NULLS LAST,id LIMIT 12; sat25/sat75 sums; acceptanceRate ::double precision. Full-row TestScoreRow (23 cols, ISO-Z, subScores jsonb passthrough). testDate DESC NULLS FIRST",
        "3 GET endpoints on TestScoreEndpoints (RequireIdentity only, no tenant/subscription; read-only RLS; 100-char id bound on student-view). student-view auth asymmetry: counselor+no-assignment->404 'Not found'; parent+no-link->403 'Forbidden: no active parent link'; other role->403 'Forbidden'; RAW role compare (case-sensitive); parent email trim+lowercase",
        "harness += testscores-schema.sql (student_test_scores full + universities + counselor_student_assignments + student_parent_links) + TestScoreDatabaseFixture; TestScoreComputationsTests(12) + TestScoreReaderTests(8) + TestScoreEndpointsTests(12)",
        "web flags FORMMAPS_ROUTE_TEST_SCORES_{SUPERSCORE,COLLEGE_FIT,STUDENT_VIEW}_TO_DOTNET (default off) + 3 rewrites (distinct GET paths; NO bare-path rewrite -> POST not hijacked)"
      ],
      "guardrails": [
        "GET / list DEFERRED (shares bare path with POST -> cut over together in the write slice)",
        "ACT composite AwayFromZero (=JS Math.round for positive means; pinned by a mean-30.5 case distinguishing it from banker's ToEven); Decimal->number ::double precision; jsonb null passthrough; ISO-Z; NULLS FIRST(desc)/LAST(asc) matching Prisma implicit + deterministic id tie-break (documented superset)",
        "student-view raw-role case-sensitive (Counselor != counselor -> 403); parent email normalized; counselor UserId / parent normalized email; 100-char id bound (api-standards superset, harmless vs TS full-id since a >100-char id never matches a UUID)"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon GET test-scores/{superscore,college-fit,students/x/test-scores} -> 401"
      ],
      "notes": [
        "DONE 2026-07-18: test-scores reads (built by a fork inheriting full rail context; I ran the adversarial gate + ship). dotnet 0/0; 381u + 281i. GATE: fresh reviewer NO correctness bug (faithful) + Codex SAFE-TO-SHIP -> folded 6 coverage pins (the fresh MEDIUM: rounding test used mean 31.5 which passes under BOTH AwayFromZero and banker's -> added mean-30.5 case; + NULL acceptanceRate ordering, null subScores passthrough, null testDate NULLS-FIRST, case-sensitive role->403). NEXT: test-scores WRITES (GET / list + POST/PUT/DELETE, cut over together) OR question360 reads OR school-admin reads OR personality PROD cutover."
      ]
    },
    {
      "id": "FM-DOTNET-038-question360-reads",
      "status": "completed",
      "goal": "Port the question360 READ surface (routes/question360.ts, 5 GET catalog endpoints). ⚠️ NO answer-key strip on this surface: questions_360 has no correct-answer column (that belongs to the separate pca_questions router) — verified vs schema.prisma; full-row return is correct. Writes (POST/PUT/DELETE/activate/deactivate/bulk-create) deferred — Node still owns the question360 write surface.",
      "dependsOn": [
        "FM-DOTNET-037-test-scores-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/question360.ts",
        "/Users/federicotafur/formmaps-platform/api/src/middleware/authenticate.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/auth.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/Question360Reads.cs",
        "services/api/src/FormMaps.Application/Assessments/IQuestion360Reader.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/Question360Reader.cs",
        "services/api/src/FormMaps.Api/Endpoints/Question360Endpoints.cs",
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "Question360Reader (read-only session): ListAsync (isActive=true + optional relationType single-value bind, orderBy questionNumber ASC + id tie-break) / ListByCategoryAsync / ListByParentAsync / GetByIdAsync (findUnique, NO isActive filter — faithful). Full 13-col Question360Row camelCase + ISO-Z timestamps; questions_360 is a global RLS-unpolicied catalog (no schoolId, no tenant scope)",
        "5 GET endpoints on Question360Endpoints, mount /api/question360 (NOT /api/v1). RequireIdentity ONLY (no tenant/subscription). Permission asymmetry: /GetQuestions,/all,/category/{c} auth-only; /sub-questions/{parent},/{id} require evaluations:manage->403. Non-uniform envelopes copied exactly: /GetQuestions rich {success,message:'Questions retrieved',count,relationType:raw||'all',data}; others {success,data}; /{id} 404 {success,message:'Not found'}",
        "harness += question360-schema.sql (non-UTC tz pin) + Question360DatabaseFixture; Question360ReaderTests(7) + Question360EndpointsTests(19, +4 folded envelope-shape pins)",
        "web flag FORMMAPS_ROUTE_QUESTION360_READS_TO_DOTNET (default off) + 5 rewrites (literal-before-/:id ordering)"
      ],
      "guardrails": [
        "NO answer-key strip — questions_360 has no correct-answer column (verified schema.prisma); return full row (the strip concern belongs to pca_questions/pca-exam router, NOT this one)",
        "permission asymmetry (2 of 5 reads require evaluations:manage->403; reader NOT invoked on denial); /{id} findUnique NO isActive filter (other 4 filter isActive=true); orderBy questionNumber ASC + deterministic id tie-break (documented superset); camelCase jsonb-free full row; ISO-Z",
        "403/401 body carries a `code` field (missing_permission/missing_identity) = shared guard house-style across all prior slices (additive superset; message+status match legacy exactly); relationType single-value bind (legacy array->Prisma IN->500 is a bug the port avoids)"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon GET /api/question360/{all,GetQuestions,:id} -> 401 missing_identity + x-formmaps-service"
      ],
      "notes": [
        "DONE 2026-07-20: question360 reads (built by a fork inheriting full rail context; I ran the adversarial gate + shipped). dotnet 0/0; 381u + 303i. GATE: fresh reviewer SAFE-TO-SHIP (answer-key non-leak confirmed — no such column) + Codex FIXES-NEEDED -> converged; all findings LOW/NIT resolving to established .NET port conventions (403/401 code field = shared guard house-style; relationType single-value = more correct than legacy Prisma array->500; id tie-break = documented determinism superset). Folded the one real coverage pin: bare {success,data} envelope-shape assertions on /all,/category,/sub-questions,/{id}. Staging deploy GREEN (run 29759262713) + canary GREEN (health 200; anon /all,/GetQuestions,/:id -> 401 missing_identity; x-formmaps-service present). NEXT: test-scores WRITES + school-admin reads (Batch A remaining) OR personality PROD cutover."
      ]
    },
    {
      "id": "FM-DOTNET-039-school-admin-reads",
      "status": "completed",
      "goal": "Port the school-admin READ surface sub-slice 1 (school-assessments.ts): the getSchoolUser school-scoping rail + 6 straightforward school-scoped reads. DEFERRED to sub-slice 2: /results/{studentId} rich report, /results/export CSV, /assessments/pipeline. EXCLUDED (AI-polyglot): /assessments/insights (Bedrock).",
      "dependsOn": [
        "FM-DOTNET-038-question360-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-assessments.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolAssessmentsService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolAdmin",
        "services/api/src/FormMaps.Infrastructure/SchoolAdmin",
        "services/api/src/FormMaps.Api/Endpoints/SchoolAdminEndpoints.cs",
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolAdmin",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "SchoolAdminScopeResolver (the getSchoolUser rail): resolves caller schoolId from a FRESH users.schoolId read on the caller's OWN id (NOT the JWT/tenant claim); null->400 'No school'. Reusable by sub-slice 2 + writes",
        "SchoolAdminReader (read-only RLS, raw Npgsql): 6 reads — overview (students w/ >=1 group; selfCompleted via normalizeGroupType), results (paginated name ASC, nested data.data, averageScore 1dp AwayFromZero, hasPca/pcaStatus), pca-status (school-scoped IN THE QUERY: WHERE id AND schoolId AND isActive -> uniform 404), config (missing->CONFIG_DEFAULTS, double-wrapped, aiWeights JSON.parse catch->default, String window dates verbatim), status (single-wrapped, completionRate 2dp, inProgress hardcoded 0), schedule (full rows isActive=true)",
        "6 GET routes on SchoolAdminEndpoints + shared AuthorizeAsync (RequireIdentity -> school:manage 403 -> scope 400). isActive filter divergence honored per endpoint; role set ['Student','student']; scorePercentage Float->number; coKey never selected; ISO-Z; search ILIKE unescaped (faithful to Prisma contains)",
        "harness += schooladmin-schema.sql (non-UTC pin) + SchoolAdminDatabaseFixture; SchoolAdminReaderTests(19) + SchoolAdminEndpointsTests(15)",
        "web flag FORMMAPS_ROUTE_SCHOOL_ADMIN_READS_TO_DOTNET (default off) + 4 GET-only rewrites (overview/results/pca-status/status); /config + /schedule GET rewrites left DARK (those paths carry Node-only PUTs -> FM-037 trap)"
      ],
      "guardrails": [
        "TENANT-ISOLATION HARDENING (gate HIGH, compliance-by-design): pca-status student lookup school-scoped IN THE QUERY so a super-admin RLS-bypassed session never reads a foreign-school row before the 404 (observably identical to legacy findUnique-then-check)",
        "wrapping asymmetry EXACT: config double data.data, status single data (deliberate legacy fix), results nested data.data+pagination; rounding MidpointRounding.AwayFromZero (JS Math.round); isActive divergence per endpoint (overview filters, results+status do NOT); both role spellings",
        "documented supersets: 100-char studentId bound; deterministic id/evaluatedUserId tie-break ordering; 403 code:missing_permission field (shared guard house-style)"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon GET /api/v1/school-admin/{evaluations/overview,results,assessments/status} -> 401"
      ],
      "notes": [
        "DONE 2026-07-20: school-admin reads sub-slice 1 (built by a fork; I ran the adversarial gate + shipped). dotnet 0/0; 381u + 315i + 1c. GATE: fresh reviewer SAFE-TO-SHIP + Codex FIXES-NEEDED -> converged. Folded the HIGH (query-level school scope on pca-status closes a super-admin cross-tenant read) + a super-admin-scoped-to-own-DB-school test (proves the rail uses the DB value not the token claim) + a lowercase-role coverage test. Kept documented supersets (100-char bound, tie-break ordering, 403 code field). Reconciled the 5 append-only wiring files against FM-038 by merging main into the branch (next.config.ts helper+rewrite regions needed hand-fixing where conflict boundaries fell inside shared trailing brackets). NEXT: test-scores WRITES (FM-040, in fold) OR school-admin sub-slice 2 (rich report/CSV/pipeline) OR personality PROD cutover."
      ]
    },
    {
      "id": "FM-DOTNET-040-test-scores-writes",
      "status": "completed",
      "goal": "Port the deferred test-scores WRITE + LIST surface (routes/test-scores.ts): GET / (list) + POST / + PUT /{id} + DELETE /{id} on the bare /api/v1/test-scores path. Cuts over WITH the FM-037 reads (Next rewrites match by path not method). Self-scoped writes (RequireIdentity only).",
      "dependsOn": [
        "FM-DOTNET-037-test-scores-reads",
        "FM-DOTNET-039-school-admin-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/test-scores.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/TestScoreWrites.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/TestScoreWriter.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/TestScoreRowMapper.cs",
        "services/api/src/FormMaps.Api/Endpoints/TestScoreEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "TestScoreWriter (OpenWritableAsync rail): CreateAsync (userId=caller, explicit fields, isOfficial??true/isSuperScore??false, subScores jsonb, testDate empty->null via IsNullOrEmpty matching JS truthiness) + UpdateAsync (ownership 404 BEFORE validation; PATCH-style present-fields-only; no create-defaults) + DeleteAsync (soft isActive=false, idempotent 2nd->404) + OwnsActiveRowAsync (null||!owner||!isActive -> uniform 404); PII-free audit post-commit",
        "TestScoreValidation (pure Zod port): testType enum required; inclusive int bounds; .int() float-reject; apSubject<=120; subScores object-only; .partial() update; first-error schema order; non-object top-level body -> 400 'Expected object, received <type>'",
        "TestScoreRowMapper: shared read+write full-row projection (TestScoreReader refactored onto it, FM-037 reads unchanged); all score cols Int? (no Decimal); ISO-Z; testDate DESC NULLS FIRST + id tie-break",
        "4 endpoints on TestScoreEndpoints (POST 201{success,data}; GET 200{success,data:[]}; PUT 200; DELETE 200{success,message:'Test score deleted successfully'}; 400 first-error; ownership 404 'Test score not found')",
        "TestScoreValidationTests + TestScoreWriterTests(real-DB) + TestScoreWriteEndpointsTests; web flag FORMMAPS_ROUTE_TEST_SCORES_WRITE_TO_DOTNET (default off) + 2 rewrites (bare + /:id)"
      ],
      "guardrails": [
        "corpus #8 IDOR (non-owner PUT/DELETE->404 uniform), #25 soft-delete idempotency (2nd DELETE->404, drops from list+superscore); inclusive range bounds + float-reject; isOfficial default only on create; no @@unique (dup POSTs allowed)",
        "FIDELITY (Codex-caught, folded): non-object JSON body->400 (was 200+spurious updatedAt); empty testDate ''->null via IsNullOrEmpty (whitespace-only stays 500, faithful to d.testDate? truthiness)",
        "documented accepted: enum non-string message wording; no-offset datetime TZ (UI date-only); /:id write rewrite also matches GET /superscore,/college-fit (same .NET backend)"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST/PUT/DELETE/GET bare /api/v1/test-scores -> 401"
      ],
      "notes": [
        "DONE 2026-07-20: test-scores writes+list (built by a fork; I ran the adversarial gate + shipped). dotnet 0/0; unit 416 + integration 306 + contract 1. GATE: fresh SAFE-TO-SHIP + Codex FIXES-NEEDED -> converged; folded 2 MEDIUM behavioral divergences (non-object-body 400; empty-testDate null) + corrected whitespace-testDate to IsNullOrEmpty (byte-faithful). Reconciled DI.cs + next.config.ts against FM-038+FM-039 by merging main into the branch (next.config helper+rewrite regions hand-fixed where conflict boundaries fell inside shared trailing brackets). COMPLETES the test-scores bare-path surface. NEXT: school-admin sub-slice 2 OR question360 writes OR token-gated external write rail OR personality PROD cutover."
      ]
    },
    {
      "id": "FM-DOTNET-041-school-admin-reads-2",
      "status": "completed",
      "goal": "Port the 3 heavy school-admin reads deferred from FM-039: GET /results/{studentId} (rich report), GET /results/export (CSV), GET /assessments/pipeline. Reuses the FM-039 getSchoolUser rail. Excludes /assessments/insights (AI-polyglot); defers config/schedule/reminders/setup-360 writes.",
      "dependsOn": [
        "FM-DOTNET-039-school-admin-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolAssessmentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/sanitize.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolAdmin",
        "services/api/src/FormMaps.Infrastructure/SchoolAdmin/SchoolAdminReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolAdminEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolAdmin",
        "services/api/tests/FormMaps.UnitTests/SchoolAdmin",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "StudentCompletion (pure): computeStudentCompletion port — PARITY_ALL_FIVE 5-set when a completed active LIA session exists else distinct completed examTypes; allLiaDone>=5; evalRequired=min(evalTotal,3); pcaCompleted=any isCompleted row; overall",
        "GetStudentReportAsync: student lookup school-scoped IN query (WHERE id AND schoolId; FULL id no truncation after Codex fold; NO isActive; uniform 404; coKey never selected); generatedAt via injected TimeProvider ISO-Z; averageScore round(mean*10)/10 AwayFromZero; lastPca=max updatedAt completed; mil.sessions startTime DESC; completion/pca/mil/evaluation360 payload",
        "ExportResultsCsvAsync: text/csv (no charset)+attachment disposition; raw body (auth errors still JSON); csvSafe ' -prefix on =,+,-,@,\\t,\\r (corpus #30); NO isActive/no pagination; avg invariant",
        "GetAssessmentPipelineAsync: per-exam precedence Completed>InProgress; LIA overlay completed->5 EXAM_TYPES; pca grid keys VERBATIM PascalCase (Dictionary, DictionaryKeyPolicy null); mil all-5-done; eval360 states; grade if(grade) truthiness (0 dropped); incomplete filter; students isActive=true; order gradeLevel ASC NULLS LAST,name",
        "3 GET routes on SchoolAdminEndpoints under the shared AuthorizeAsync; +3 next.config rewrites under existing FORMMAPS_ROUTE_SCHOOL_ADMIN_READS_TO_DOTNET flag (ordered /results/export + pca-status before /:studentId)"
      ],
      "guardrails": [
        "TENANT-ISOLATION: report student lookup school-scoped in query (super-admin cross-read closed; pinned by super-admin scope test)",
        "Codex MEDIUM folded: removed 100-char truncation on the report lookup key (parameterized; too-long id 404s naturally = faithful; kills prefix-false-match)",
        "csvSafe corpus #30 all 6 trigger chars pinned; completion boundaries (LIA 4-vs-5, evalTotal 1/2/3) pinned; documented superset: deterministic id tie-break ordering"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon GET /api/v1/school-admin/{results/x,results/export,assessments/pipeline} -> 401"
      ],
      "notes": [
        "DONE 2026-07-20 (Batch B): school-admin reads sub-slice 2 (fork-built; fresh SAFE + Codex FIXES-NEEDED converged). dotnet 0/0; unit 428 + integration 389 + contract 1. Folded Codex MEDIUM (untruncate report lookup) + 4 coverage pins (super-admin report scope, csvSafe all-6, LIA 4/5 boundary, evalTotal edges). NEXT: question360 writes (FM-042, folded, ready) OR token-gated external write rail (capstone, scoped)."
      ]
    },
    {
      "id": "FM-DOTNET-042-question360-writes",
      "status": "completed",
      "goal": "Port the question360 WRITE surface (routes/question360.ts): POST / + PUT /{id} + PUT /{id}/activate + PUT /{id}/deactivate + DELETE /{id} (soft + child-guard) + POST /bulk-create. Mount /api/question360, requirePermission(evaluations:manage). Collapses the FM-038 reads flag + writes into ONE surface flag.",
      "dependsOn": [
        "FM-DOTNET-038-question360-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/question360.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/Question360Writes.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/Question360Writer.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/Question360RowMapper.cs",
        "services/api/src/FormMaps.Api/Endpoints/Question360Endpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "Question360Writer (Npgsql OpenWritableAsync): create (defaults materialized, createdBy/updatedBy NULL, questionNumber as long); update (PATCH, isActive EXCLUDED over-post guard, no create-defaults); activate/deactivate; delete (child-guard count parentQuestionId+isActive>0 -> 400; soft isActive=false; success {success:true} NO data); bulk-create (per-item independent session no-transaction, 23505->'Duplicate question' else 'Failed to create question' never leaks, always 200 {createdCount,totalRequested,errors}, null element graceful per-item error 200)",
        "Question360Writes (validation): zod port create required+bounds+defaults + update all-optional isActive-excluded; first-error order; questionNumber long (>Int32 -> DB 22003 error path)",
        "shared Question360RowMapper (reader refactored onto it, FM-038 reads byte-unchanged)",
        "6 write endpoints on Question360Endpoints; SINGLE flag FORMMAPS_ROUTE_QUESTION360_TO_DOTNET gating ALL 11 endpoints (5 reads + 6 writes) in one next.config block (renamed FM-038 reads flag)"
      ],
      "guardrails": [
        "SINGLE-FLAG (Codex HIGH): Next rewrites match path not method -> GET vs PUT/DELETE on /:id inseparable -> one flag is the only design with no split-brain (was two coupled flags)",
        "int32 overflow (Codex HIGH): questionNumber long, >Int32 hits DB error path not silent wrap",
        "isActive over-post guard; createdBy/updatedBy NULL (faithful); child-guard 400 exact; DELETE {success:true} no-data; bulk no-transaction + 23505 mapping (no real unique added, dups succeed); missing-id->500 (legacy P2025, no IDOR); bulk null-element graceful 200 (documented divergence from legacy throw->500, robustness)"
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx",
        "dotnet test services/api/FormMaps.slnx",
        "staging deploy + health 200 + anon POST/PUT/DELETE/bulk-create /api/question360/* -> 401/403"
      ],
      "notes": [
        "DONE 2026-07-20 (Batch B): question360 writes (fork-built; fresh SAFE + Codex FIXES-NEEDED converged). dotnet 0/0; unit 433 + integration 398 + contract 1. Folded Codex HIGH x2 (single-flag collapse + int32 overflow) + LOW (bulk null-element kept graceful, documented) + PUT-success test. Reconciled next.config+DI against FM-041 (clean auto-merge; single-flag rename intact). COMPLETES the question360 surface (reads FM-038 + writes FM-042 under one flag). NEXT: token-gated external write rail (capstone, scoped) OR school-admin/test-scores config-schedule writes."
      ]
    },
    {
      "id": "FM-DOTNET-043-token-gated-external-write-rail",
      "status": "completed",
      "goal": "Port the token-gated external WRITE rail (routes/evaluation.ts external subset + routes/vocationalTake.ts): validate-token / submit-feedback / 360evolutor + vocational GET/submit/violations. NEW fail-closed non-tenant RLS-bypass rail (RequestContext.System -> Bypass; anonymous -> Deny). Token+email+expiry are the only boundary. Highest-risk assessments slice.",
      "dependsOn": [
        "FM-DOTNET-032-vocational-write",
        "FM-DOTNET-038-question360-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/evaluation.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/vocationalTake.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/evaluationService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/vocationalTakeService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/proctoring-service.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/{proctoring,emailNormalize,evaluationGroups,evaluation360}.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Assessments/ExternalEmailNormalization.cs",
        "services/api/src/FormMaps.Application/Assessments/ProctoringViolations.cs",
        "services/api/src/FormMaps.Application/Assessments/ExternalEvaluationContracts.cs",
        "services/api/src/FormMaps.Application/Assessments/VocationalTakeContracts.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/EvaluationExternalService.cs",
        "services/api/src/FormMaps.Infrastructure/Assessments/VocationalTakeService.cs",
        "services/api/src/FormMaps.Api/Endpoints/EvaluationExternalEndpoints.cs",
        "services/api/src/FormMaps.Api/Endpoints/VocationalTakeEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "Fail-closed non-tenant rail: all 6 external routes hardcode RequestContext.System() -> TenantGucPlanResolver Bypass (set_config app.bypass_rls=on); anonymous/null -> Deny. Token resolve: parameterized @token, case-sensitive, non-unique index -> LIMIT 1 findFirst, never lowercased.",
        "Vocational tree (flag FORMMAPS_ROUTE_VOCATIONAL_TAKE_TO_DOTNET): GET /:token (expired->410 vs submit 404 verb-asymmetry; invalid-group->400; not-found->404; completed shape) / POST /submit (one atomic tx: N upserts + completion flip; jsonb cols rankingOrder/selectedValues OMITTED-when-NA preserve-prior, scalar ratingValue/textValue NULLED; require-all incomplete->400; best-effort recompute post-tx; generic messages) / POST /:token/violations (bound cap200 + merge cap500 + flag>=3; expiry-gated 404).",
        "360 external tree (flag FORMMAPS_ROUTE_EVAL_EXTERNAL_TO_DOTNET): validate-token (resolvable-but-invalid->200 {valid:false,reason} never 4xx) / submit-feedback (non-atomic create-then-flip; 23505 UniqueViolation->409; category DROP-not-reject; camelCase feedbackItems jsonb; Decimal averageRating; raw legacy error strings) / 360evolutor (echo caller's own token, relationType question fetch take 20).",
        "New Testcontainers harness (token-rail-schema.sql, 3 tables, non-UTC TZ pin). Web: 2 dark flags + 6 rewrites under /evaluation/* before catch-all, no /api/* collision.",
        "RATIFIED DIVERGENCE (Federico 2026-07-20): submit-feedback CLOSES the legacy token-expiry gap (EnforceFeedbackTokenExpiry=true) -- legacy never checked expiry/used on submit; port rejects expired/used (compliance tightening, consistent with vocational). Legacy behavior kept as skipped DOCUMENTATION test.",
        "OUT OF SCOPE (documented): submit-feedback post-response Bedrock insight trigger stays Node (polyglot)."
      ],
      "guardrails": [
        "Fail-closed (both reviewers CONFIRMED): unresolved/absent context -> Deny, never silently open a tenant or bypass unintentionally; all 6 routes System().",
        "sensitiveLimiter parity (Codex BLOCKING, folded): RequireRateLimiting(Sensitive) restored on ONLY submit-feedback + vocational/submit (the two anonymous writes legacy gated at 10/hr); others unthrottled.",
        "zod .email() parity (Codex CONFIRMED, folded): exact zod v3 email regex ported (GeneratedRegex) applied to RAW email pre-normalization -> mailto:<x>/a@@b/a@b -> 400 (were 200-after-normalize-match); +non-string comment -> 400.",
        "last-wins questionnaire map (Codex, folded): ToDictionary(q=>q.Number) throw-on-dup -> latent 500 on schema-permitted duplicate number; replaced with last-wins loop matching TS new Map.",
        "jsonb skip-vs-null upsert asymmetry; 23505 real SQLSTATE not P2002 string; verb-dependent expiry 410(GET)/404(submit); Decimal averageRating; UTC strict-< expiry; camelCase feedbackItems (Evaluation360Scoring reads camelCase)."
      ],
      "validation": [
        "dotnet build services/api/FormMaps.slnx (0/0)",
        "dotnet test services/api/FormMaps.slnx (unit 460 + integration 481 + 1 skip + contract 1)",
        "staging deploy + health 200 + anon POST /evaluation/submit-feedback + /evaluation/vocational/submit -> 401/403 (routes dark, flags off)"
      ],
      "notes": [
        "DONE 2026-07-20 (capstone): token-gated external write rail. Fork-built off source-verified brief (main caught 5 spec divergences pre-build: string evaluationGroupId, category-drop-not-reject, camelCase feedbackItems, jsonb/scalar upsert asymmetry, closed-expiry-gap). Adversarial gate: fresh reviewer SAFE + Codex BLOCKING(rate-limiter)+CONFIRMED(email)+PLAUSIBLE(ToDictionary) -> all 3 folded + red-if-regressed tests. Federico RATIFIED closing the expiry gap. dotnet 0/0; unit 460 + integration 481(+1 skip) + contract 1 (main re-ran suite as ship gate). COMPLETES the assessments external write surface. NEXT: school-admin/test-scores config-schedule writes OR Phase B (schools/rosters/orgs)."
      ]
    },
    {
      "id": "FM-DOTNET-044-school-admin-config-schedule-writes",
      "status": "completed",
      "goal": "Port the two DB-only school-admin assessment WRITES (routes/school-assessments.ts): PUT /assessments/config + PUT /assessments/schedule. Reuse the FM-039 getSchoolUser school-scope rail. (send-reminders + setup-360 deferred to FM-045 — they need a .NET outbound-email surface.)",
      "dependsOn": [
        "FM-DOTNET-039-school-admin-reads-1"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-assessments.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolAssessmentsService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolAdmin/ISchoolAdminWriter.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolAdmin/SchoolAdminWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolAdminEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "PUT config: upsert school_assessment_settings on schoolId (PATCH — only provided fields; create sets createdBy+updatedBy, conflict updates provided + updatedBy; aiWeights truthy→aiWeightsJson=JSON.stringify). DOUBLE-wrap {data:{data}} response. READ-BACK RETURNS RAW DB VALUES (gate fix — legacy WRITE does NOT coalesce; null windows/empty retakePolicy pass through; only the GET path defaults).",
        "PUT schedule: per-item upsert assessment_schedules on composite (schoolId,gradeLevel,assessmentType); skip-incomplete (falsy gradeLevel 0/type/dates); create createdBy / conflict updatedBy; SINGLE-wrap {data:[rows]}. schedules-not-array→400.",
        "New flag FORMMAPS_ROUTE_SCHOOL_ADMIN_CONFIG_SCHEDULE_TO_DOTNET (default off) + 2 rewrites (config+schedule paths, GET+PUT together — finally lights the paths FM-039 left dark for the Node-only PUTs). Fixture (schooladmin-schema.sql) gained the write columns + composite UNIQUE (fork caught a read-shaped fixture)."
      ],
      "guardrails": [
        "GATE BLOCKING (both reviewers CONFIRMED): config PUT read-back must return RAW (updateAssessmentConfig, no || DEFAULT) — the FalsyOr coalescing is GET-only. Folded: raw values, AssessmentConfig window/retakePolicy nullable, dead FalsyOr removed, 2 tests fixed.",
        "GATE LOW (folded): malformed JSON body -> 400 (ReadBodyAsync reads raw; empty->{} legacy parity, malformed->400 no phantom write) +test.",
        "PATCH only-provided (no null-out of unpassed cols); double vs single wrap depth; aiWeights JS truthiness + compact JSON; skip-incomplete falsy; composite upsert preserves id+createdBy; 400-over-500 on bad input (documented divergence); atomic one-session schedule upserts (documented).",
        "repo hygiene: gitignored .claude/ (git add -A was capturing harness worktrees)."
      ],
      "validation": [
        "dotnet build 0/0",
        "dotnet test (unit 460 + integration 498 + 1 skip + contract 1)",
        "staging deploy run 29779252461 + canary (health 200; anon PUT config/schedule -> 401; regressions intact)"
      ],
      "notes": [
        "DONE 2026-07-20 (parallel with FM-046). Fork-built + fresh+Codex gate folded the BLOCKING coalescing bug (config PUT would fabricate default window dates/retakePolicy where legacy returns null/\"\") + the malformed-JSON phantom-write. dotnet 0/0; unit 460 + integration 498(+1 skip). NEXT: FM-045 (email writes: send-reminders + setup-360)."
      ]
    },
    {
      "id": "FM-DOTNET-046-gradebook-transcript-read",
      "status": "completed",
      "goal": "First Phase-B slice: port the student gradebook transcript READ (routes/school-gradebook.ts GET /gradebook/students/:studentId) — grades-by-year + GPA summary. Reuse the school:manage school-scope rail; own GradebookEndpoints + GradebookReader (no edit to SchoolAdminEndpoints).",
      "dependsOn": [
        "FM-DOTNET-039-school-admin-reads-1"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-gradebook.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/gradebookService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/transcriptService.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Gradebook/GpaComputation.cs",
        "services/api/src/FormMaps.Application/Gradebook/GradebookModels.cs",
        "services/api/src/FormMaps.Application/Gradebook/IGradebookReader.cs",
        "services/api/src/FormMaps.Infrastructure/Gradebook/GradebookReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/GradebookEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "GpaComputation (pure): DOUBLE arithmetic, Math.Round(x*10000, AwayFromZero)/10000 (= JS Math.round for non-negative GPA); skip null/empty grade + letter-not-in-map + credits<=0; totalCredits==0 -> {null,null,0} asymmetry; DEFAULT_UNWEIGHTED_MAP + DEFAULT_WEIGHT_BONUSES.",
        "GradebookReader: verifyStudentInSchool (schoolId==caller AND roleName in {student,Student} -> else 404 'Student not found', IDOR-safe) + getTranscriptData (student_grades where studentId+schoolId+isActive, ORDER BY academicYear DESC/semester ASC = Postgres default NULLS FIRST/LAST; full 17-col row passthrough camelCase, credits::double precision->JSON number, ISO-Z ts; byYear grouped, null/empty year->'Unknown', insertion order) + resolveGpaConfig (gpa_configurations jsonb; present-non-null object incl {} used, SQL-null/'null'/non-object->DEFAULT; weightBonuses keys lowercased, unweightedMap verbatim). Read-only RLS session.",
        "GradebookEndpoints: GET on the school:manage guard chain (RequireIdentity->permission->getSchoolUser 400 'No school'); SINGLE-wrap {success,data:{byYear,gpaUnweighted,gpaWeighted,totalCredits}}. Flag FORMMAPS_ROUTE_GRADEBOOK_READ_TO_DOTNET (default off), rewrite disjoint from /results, before the /api catch-all."
      ],
      "guardrails": [
        "GATE (fresh + Codex): NO CONFIRMED blocker on the supported path. Both flagged only unreachable corrupt-jsonb divergences: string-valued unweightedMap entry (TS concatenates->NaN->null weighted GPA; .NET coerces), non-object/array-shaped map (TS truthiness uses it->null GPA; .NET->DEFAULT), negative custom map rounding. All require malformed gpa_configurations data the UI/API can't write; .NET's non-object/non-numeric->DEFAULT is the more defensive behavior. ACCEPTED DIVERGENCES (documented, not folded) per architecturally-correct-over-bug-faithful.",
        "double (not decimal) GPA math; AwayFromZero rounding; Decimal credits->JSON number; jsonb truthiness; Postgres NULLS ordering; full-row camelCase passthrough; SINGLE wrap; exact 404/400 messages; parameterized SQL."
      ],
      "validation": [
        "dotnet build 0/0",
        "dotnet test (unit 467 + integration ~499 + contract 1) — on the main-merged tree",
        "staging deploy + canary (health 200; anon GET gradebook -> 401)"
      ],
      "notes": [
        "DONE 2026-07-20 (parallel with FM-044; built in own worktree, gated in parallel, merged after FM-044). Fork-built (repo-verify guard added after a wrong-repo dispatch). Gate found no reachable bug. Merged main (FM-044) to reconcile the next.config.ts + DI.cs append conflicts (the known trailing-bracket trap — hand-closed the gradebook rewrite block). NEXT: FM-045 (email writes) then more Phase-B (school.ts/school-courses/school-students/school-grades reads)."
      ]
    },
    {
      "id": "FM-DOTNET-045-school-admin-email-writes",
      "status": "completed",
      "goal": "Port the two school-admin EMAIL writes (routes/school-assessments.ts): POST /assessments/send-reminders + /assessments/setup-360. Introduces the FIRST outbound integration in the .NET service (SES email) + a reusable IEmailSender/templates/token-generator surface.",
      "dependsOn": [
        "FM-DOTNET-044-school-admin-config-schedule-writes"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/lib/email.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-assessments.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolAssessmentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/auth.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Email/*",
        "services/api/src/FormMaps.Infrastructure/Email/SesEmailSender.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolAdmin/SchoolAdminEmailWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolAdminEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "Email infra (FIRST AWS SDK in the service — AWSSDK.SimpleEmailV2 4.0.101): IEmailSender + SesEmailSender (Content.Simple, never throws->false, lazy factory-lambda client so no boot-time cred resolution), EmailTemplates (exact 5-char escapeHtml NOT HtmlEncoder; invite + reminder builders byte-matched incl. the subject asymmetry: invite subject RAW studentName, reminder subject ESCAPED schoolName), InvitationTokenGenerator (RNG 32 bytes -> unpadded base64url = crypto.randomBytes(32).base64url).",
        "sendReminders: validation (studentIds/assessmentTypes non-empty, >100->400 'Maximum 100 students per batch'); school null->404 'School not found'; active in-school students; per-student reminder email; {sent,failed,total}.",
        "setup360: createMany evaluation_groups (self-as-Parent no-email, parent links, counselor-as-Teacher; dedup key evaluatedUserId|lowercased-email|groupType; 48h token expiry; INSERT sets updatedAt explicitly [gate fix]) + best-effort invites post-commit; empty->400 'No students to setup'; >100->400; {created,skipped,emailsSent,studentsProcessed}. FRONTEND fallbacks: invite base https://app.formmaps.ai (.ai, raw) vs reminder base .com (strip-slash).",
        "New flag FORMMAPS_ROUTE_SCHOOL_ADMIN_EMAIL_WRITES_TO_DOTNET (default off) + 2 POST rewrites."
      ],
      "guardrails": [
        "GATE BLOCKING (fresh): setup-360 INSERT omitted updatedAt (prod NOT NULL no-default) -> 23502/500 at cutover; test schema had wrongly given it a DEFAULT, masking it. Folded: INSERT sets updatedAt; test schema default dropped to match prod (exposed + fixed 2 test seed inserts).",
        "GATE CONFIRMED (Codex): SES failure log emitted subject = raw student name (PII). Folded: generic log message (matches legacy).",
        "GATE MINOR (fresh): eager SES client at DI could brick the whole service on unresolvable creds. Folded: factory lambda (lazy).",
        "GATE ACCEPTED DIVERGENCE (both): setup360 in-loop dedup vs TS DB-only set — diverges only on intra-batch email collision where .NET is SAFER (TS 500s on the @@unique); documented, not folded.",
        "REGRESSION CORPUS: the fork found + fixed a latent PROD bug — JsonBodySanitizer threw 'Collection was modified' (500) on ANY POST/PUT with a top-level string-array body (snapshot via .ToArray()); first exercised by studentIds:[]. Add to the C#-port regression corpus."
      ],
      "validation": [
        "dotnet build 0/0",
        "dotnet test (unit 470 + integration 532 + 1 skip + contract 1)",
        "staging deploy + canary (health 200; anon POST send-reminders/setup-360 -> 401)"
      ],
      "notes": [
        "DONE 2026-07-20 (parallel forks with FM-046/047). Token generator PORTED not reused (FM-043 only ported token validate). 🔴 FEDERICO cutover prereq: ses:SendEmail on the formmaps-api-prod App Runner role + a verified SES sender identity before these can deliver in prod (ships dark). Completes the assessments email/write surface. NEXT: FM-047 calendar reads (ship dark, rewrite deferred to FM-048) + FM-048 calendar creates."
      ]
    },
    {
      "id": "FM-DOTNET-047-school-calendar-reads",
      "status": "completed",
      "goal": "Second Phase-B slice: port the 3 school academic-calendar READS (routes/school-grades.ts): GET /calendar/academic-years (nested terms), /assessment-periods (yearId gate-not-filter), /holidays. New calendar:manage permission. Ships DARK (no rewrite — the paths carry Node-only POST creates; rewrite deferred to FM-048).",
      "dependsOn": [
        "FM-DOTNET-046-gradebook-transcript-read"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-grades.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolGradesService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/auth.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/src/FormMaps.Application/Calendar/*",
        "services/api/src/FormMaps.Infrastructure/Calendar/CalendarReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/CalendarEndpoints.cs",
        "services/api/tests"
      ],
      "outputs": [
        "New permission constant CalendarManage='calendar:manage' (SuperAdmin+SchoolAdmin only, NOT counselor — enforced, not substituted for school:manage; test proves a school:manage-only principal gets 403).",
        "CalendarReader (read-only session, explicit WHERE schoolId — no RLS on these tables): getAcademicYears (academic_years isActive ORDER BY startDate DESC + nested academic_terms isActive ORDER BY sortOrder ASC via ANY(@ids) batch); getAssessmentPeriods (GATE QUIRK: yearId=param??current-year-id??'' -> empty -> [] no query; else findMany WHERE schoolId+isActive ORDER BY startDate ASC, NOT year-filtered; text[] assessmentTypes -> JSON array); getHolidays (WHERE schoolId+isActive ORDER BY date ASC, nullable endDate -> null).",
        "CalendarEndpoints: 3 GETs, own guard chain (RequireIdentity -> calendar:manage 403 -> getSchoolUser 400 'No school'); DOUBLE-wrap {success,data:{data:[]}} (distinct from gradebook single-wrap); ISO-Z; full-row camelCase passthrough.",
        "⚠️ SHIPPED DARK WITHOUT the next.config rewrite (gate fix) — endpoints exist+tested but unrouted until FM-048 co-ports the POST creates + adds the co-flip rewrite."
      ],
      "guardrails": [
        "GATE BLOCKING (both fresh + Codex CONFIRMED): a reads-only next.config rewrite for the 3 /calendar/* bare paths would ALSO capture the co-located Node POST creates (path-not-method) -> route them to the GET-only .NET service -> 404/405 -> breaks calendar creation at cutover. Folded: DROPPED the rewrite + flag helper; endpoints ship dark; FM-048 adds POST creates + the co-flip rewrite (FM-039->FM-044 precedent).",
        "gate quirk (assessment-periods yearId = gate not filter) reproduced exactly; DOUBLE-wrap; calendar:manage enforced; text[]->array; nested-terms batch; nullable endDate->null; explicit WHERE schoolId. Everything else verified clean by both reviewers."
      ],
      "validation": [
        "dotnet build 0/0",
        "dotnet test (unit 470 + integration ~544 + 1 skip + contract 1) on the main-merged tree",
        "staging deploy + canary (health 200; anon GET /calendar/* on the .NET service directly -> 401; NO web rewrite in prod)"
      ],
      "notes": [
        "DONE 2026-07-20 (parallel fork with FM-045; re-dispatched once after a cwd-misroute caught by the repo-verify guard). Gate caught my scoping miss (I wrongly briefed 'GET-only, no coupling'). Ships DARK/unrouted. NEXT: FM-048 = calendar POST creates (createAcademicYear/AssessmentPeriod/Holidays) + the co-flip rewrite to route both."
      ]
    },
    {
      "id": "FM-DOTNET-048-school-calendar-writes",
      "status": "completed",
      "goal": "Third Phase-B slice: port the 9 school academic-calendar WRITE endpoints (routes/school-grades.ts): academic-years POST/PUT/:id/set-current/DELETE, assessment-periods POST/PUT/DELETE, holidays POST/DELETE. Adds the co-flip next.config rewrite that routes BOTH the FM-047 dark reads and these writes under ONE flag FORMMAPS_ROUTE_SCHOOL_ADMIN_CALENDAR_TO_DOTNET (path-not-method: bare paths serve GET+POST).",
      "dependsOn": [
        "FM-DOTNET-047-school-calendar-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-grades.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolGradesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Calendar/ICalendarWriter.cs",
        "services/api/src/FormMaps.Infrastructure/Calendar/CalendarWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/CalendarEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "ICalendarWriter/CalendarWriter on the FM-029 writable RLS rail; hard deletes; set-current ownership-before-clear; createdBy/updatedBy stay NULL; holiday normalization (trim/slice100/50, endDate-strictly-after, batch-500); assessment-period termId fallback (deterministic superset of unordered legacy include-take-1).",
        "Co-flip next.config rewrite for the whole /calendar/* surface under FORMMAPS_ROUTE_SCHOOL_ADMIN_CALENDAR_TO_DOTNET (default OFF); supersedes the FM-047 deferred-rewrite note.",
        "Testcontainers write harness (calendar-write-schema.sql with ON DELETE CASCADE FKs, non-UTC tz pin): 22 writer + 32 endpoint tests.",
        "GATE (fresh reviewer + Codex, full TS visibility) folded 6 findings each with a red-if-regressed test: 2 Codex HIGH (non-string termId/type produced wrong SUCCESSFUL writes -> 400) + 4 consistency/faithfulness (reject wrong-typed assessmentTypes/holidays/terms; honor falsy-coalescing -> [] and empty-string name -> 201). Documented+accepted: invalid-date rollover (stricter), epoch-ms date (exotic), cross-school termId (inherited from legacy, not a divergence)."
      ],
      "verification": "dotnet build -warnaserror 0/0; CI Test API green (Testcontainers writer suite ran in CI, Docker-less locally); staging deploy + health + authed canary green (run 29796937908). Merged --no-ff to main 7d73720.",
      "commit": "7d73720"
    },
    {
      "id": "FM-DOTNET-049-school-analytics-reads",
      "status": "completed",
      "goal": "Phase-B slice: port the 4 school-analytics READ endpoints (routes/school.ts): GET /analytics/{overview,trends,performance-trends,top-performers}, mounted under /api/v1/school-admin, permission analytics:school. All method-unambiguous GETs -> real read cut-over (not dark) behind FORMMAPS_ROUTE_SCHOOL_ANALYTICS_TO_DOTNET.",
      "dependsOn": [
        "FM-DOTNET-046-gradebook-transcript-read"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolAnalytics/*",
        "services/api/src/FormMaps.Infrastructure/SchoolAnalytics/SchoolAnalyticsReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolAnalyticsEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "Pure SchoolAnalyticsMath (GRADE_MAP GPA, JsRound half-up, date bucketing) + ISchoolAnalyticsReader/SchoolAnalyticsReader (read-only RLS session); no-school -> 200 per-endpoint empty default; grades trend metric scoped by schoolId ONLY (not studentId); distinct-user PCA/counselor counting; UTC bucketing (both prod runtimes UTC).",
        "next.config rewrite (4 GET paths) under FORMMAPS_ROUTE_SCHOOL_ANALYTICS_TO_DOTNET (default OFF).",
        "46 pure-math + 17 endpoint + 14 DB reader tests.",
        "GATE (fresh reviewer SHIP + Codex): faithful. DOCUMENTED deterministic superset (FM-032 precedent): top-performers adds ORDER BY id ASC for GPA ties where legacy findMany is unordered/nondeterministic (no byte-match exists). Codex date-bucketing tz findings are non-issues under UTC prod."
      ],
      "verification": "dotnet build 0/0; CI Test API green (Testcontainers reader suite). Deploy step first failed on a concurrent-changeset race with FM-048 on the shared staging stack (NOT code); merged into main and re-verified via the main staging deploy. Merged --no-ff to main a6293de.",
      "commit": "a6293de"
    },
    {
      "id": "FM-DOTNET-050-school-manage-reads",
      "status": "completed",
      "goal": "Phase-B slice: port the 4 school.ts school:manage method-unambiguous GET reads (routes/school.ts): /dashboard/stats, /counselor-assignments/all, /notes, /counselor-workload. Real read cut-over (no write shares their path) under FORMMAPS_ROUTE_SCHOOL_READS_TO_DOTNET.",
      "dependsOn": [
        "FM-DOTNET-049-school-analytics-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolReads/*",
        "services/api/src/FormMaps.Infrastructure/SchoolReads/SchoolReadsReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolReadsEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "New SchoolReads namespace; read-only RLS reader (batched, no N+1); reuses SchoolAnalyticsMath.JsRound. dashboard KPIs (distinct-PCA, avg scorePercentage, 1-dp JsRound percentages), no-school 6-field zeros vs 10-field with-school; counselor-assignments pairs; notes (ILIKE content+student.name, 50-cap, dual empty shapes, full scalar passthrough + nested student/author); counselor-workload (studentCount-desc stable over name-asc). roleName exact 'counselor' vs student both-case. Deterministic id/createdDate tie-breaks = FM-032-precedent supersets.",
        "next.config rewrite (4 GET paths) under FORMMAPS_ROUTE_SCHOOL_READS_TO_DOTNET (default OFF).",
        "GATE fresh SHIP + Codex SHIP; folded Codex MEDIUM (SQL AVG NaN → TS `avg || 0` coerces NaN→0; .NET kept double.NaN → System.Text.Json 500 on the dashboard) with a red-if-regressed NaN test."
      ],
      "verification": "dotnet build -warnaserror 0/0; SchoolReads 36 tests + full integration 659 + unit 516 green (Docker up). Staging deploy + health + authed canary green (run 29836536506). Merged --no-ff to main f945890.",
      "commit": "f945890"
    },
    {
      "id": "FM-DOTNET-051-school-profile-settings",
      "status": "completed",
      "goal": "Phase-B slice: port GET+PUT /school/profile and GET+PUT /settings (routes/school.ts, school:manage). Co-flip (path-not-method): one flag routes all four. .NET write-owner for the schools profile/settings columns.",
      "dependsOn": [
        "FM-DOTNET-050-school-manage-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/emailNormalize.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolProfile/*",
        "services/api/src/FormMaps.Infrastructure/SchoolProfile/*",
        "services/api/src/FormMaps.Api/Endpoints/SchoolProfileEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "SchoolProfile reader (full schools-row passthrough + email=contactEmail??'') + writer (FM-029 writable rail). Mass-assignment allow-list builder (only name/details/phone/website/timezone/logoUrl + contactEmail via email + address jsonb — NO body key becomes a column); email clear/set/ignore (emailNormalize regex, not zod); address full jsonb replace; scalar bounds; empty-PUT no-op still bumps updatedAt. getSettings || vs ?? defaults + plan hardcoded 'Standard' (no column) + studentCount both-case.",
        "Two distinct no-school 404s (/school/profile 'No school linked'; /settings + null 'Not found').",
        "Co-flip next.config rewrite under FORMMAPS_ROUTE_SCHOOL_PROFILE_SETTINGS_TO_DOTNET (default OFF, both methods both paths).",
        "GATE fresh + Codex: mass-assignment guard CLEAN (no leak). Folded the one HIGH/MED both flagged: notify flags are Boolean? — JSON null must write NULL + 200 (legacy `x !== undefined`), not 400; other non-bool types keep 400 (house rule). +4 red-if-regressed tests. Accepted/documented: invalid-type→400-vs-legacy-500 (house rule); no-school+malformed-body ordering (double-error, negligible)."
      ],
      "verification": "dotnet build -warnaserror 0/0; SchoolProfile 28u+42i; full integration 702 + unit 544 green (Docker up). Staging deploy + health + authed canary green (run 29840495027). Merged --no-ff to main a8d3bcd.",
      "commit": "a8d3bcd"
    },
    {
      "id": "FM-DOTNET-052-school-users",
      "status": "completed",
      "goal": "Phase-B slice: port the routes/school.ts school:users cluster (GET /users, PUT /users/:userId/grade-level, POST+DELETE /counselors/:counselorId/assign-students, GET /counselors/:counselorId/students). One flag routes all four paths (reads+writes together, path-not-method). .NET write-owner for users.gradeLevel and the counselor_student_assignments table via these routes.",
      "dependsOn": [
        "FM-DOTNET-051-school-profile-settings"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/prismaRls.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolUsers/*",
        "services/api/src/FormMaps.Infrastructure/SchoolUsers/*",
        "services/api/src/FormMaps.Api/Endpoints/SchoolUsersEndpoints.cs",
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "SchoolUsersReader (listSchoolUsers paginated roster: schoolId+isActive, role ILIKE contains no-escape, search name|email, createdDate DESC + id-ASC tie-break, status/joinedAt derived; getCounselorStudents include-student, id-ASC tie-break) + SchoolUsersWriter (grade-level UPDATE, assign single-writable-tx, unassign HARD delete) on the FM-029 writable rail.",
        "New permission constant school:users. Per-endpoint no-school: reads 200-empty {data:{data:[],total:0}} w/o page/limit; assign/unassign 400 'No school'. grade parseInt||null ('0'->NULL) with RAW-token echo + absent-key omit. assign = deactivate-others + ON CONFLICT reactivate (assignedBy on insert only, createdBy/updatedBy NULL) — documented single-tx superset of legacy's non-atomic updateMany+basePrisma tx. unassign HARD deleteMany, gated by active-student-in-school validation. validateSchoolStudentIds roleName IN ('student','Student').",
        "One flag FORMMAPS_ROUTE_SCHOOL_USERS_TO_DOTNET next.config rewrite (default OFF) over all 4 paths, disjoint from FM-049/050 blocks.",
        "GATE fresh reviewer SHIP + Codex FIXES-NEEDED (cross-model earned its keep — fresh missed the two HIGHs). Folded 4, each red-if-regressed: [HIGH] grade-level UPDATE bumps users.updatedAt (Prisma @updatedAt) + fixture gained the column; [HIGH] reassignment deactivation bumps counselor_student_assignments.updatedAt; [MED] empty-string schoolId treated falsy (JS !schoolId) -> 403 cross-school; [MED+LOW] GradeLevelParser rewritten — no silent int32 saturation (int4 overflow throws -> 500, faithful to Postgres), number 1e3 -> String(1000)='1000', accurate doc. Documented out-of-surface residual: JSON number >=1e21 exponential."
      ],
      "verification": "dotnet build 0/0; unit 577, integration 767 (+1 skip), contract 1 — full suite green (Docker). Staging deploy 29878482225 + anon canary GREEN (health 200; all 5 routes anon->401 + x-formmaps-service). Merged --no-ff to main ed68eec.",
      "commit": "ed68eec"
    },
    {
      "id": "FM-DOTNET-053-isams-reads",
      "status": "completed",
      "goal": "Phase-B slice: port the two READ-ONLY iSAMS integration endpoints (GET /integrations/isams/status, GET /integrations/isams/jobs) from routes/school.ts (school:manage). DELIBERATELY reads-only (Federico decision) — configure/sync/test (external iSAMS vendor HTTP + credential encryption + cross-domain course/user/grade upserts) STAY IN NODE as an explicit vendor boundary, to be ported as one unit after school-courses.ts + school-students.ts land.",
      "dependsOn": [
        "FM-DOTNET-052-school-users"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "/Users/federicotafur/formmaps-platform/api/prisma/migrations/20260505140750_init/migration.sql"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/IsamsReads/*",
        "services/api/src/FormMaps.Infrastructure/IsamsReads/*",
        "services/api/src/FormMaps.Api/Endpoints/IsamsReadsEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "IsamsReadsReader (read-only RLS): status reads endpoint+lastSyncAt from isams_configs (unique on schoolId); jobs = full 12-col isams_sync_jobs passthrough, native SyncJobStatus enum via ::text, ORDER BY createdDate DESC + id-ASC (FM-032 tie-break), LIMIT 20.",
        "IsamsReadsEndpoints: 2 GETs on /api/v1/school-admin (school:manage). status has THREE distinct 200 shapes — no-school {configured:false}; no-config-row {configured:false,enabled:false} WITHOUT lastSyncAt key; config-row {configured:true,enabled,lastSyncAt} with key present even when null. enabled = !string.IsNullOrEmpty(endpoint) (legacy !!endpoint). jobs no-school -> [].",
        "One flag FORMMAPS_ROUTE_ISAMS_READS_TO_DOTNET (default OFF) over ONLY the 2 GET paths (status, jobs). The 3 POST write paths (/integrations/isams, /sync, /test) NOT rewritten; no vendor HTTP client or field-encryption code added (vendor boundary stays Node).",
        "GATE fresh reviewer SHIP + Codex SHIP (first Codex run wedged >30m no verdict; focused retry returned clean SHIP; honestly recorded as inconclusive-then-clean, never claimed approval we did not have). Folded 1 LOW: harness schema timestamp -> timestamp(3) matching the prod init migration (harness fidelity; ISO-Z output faithful either way)."
      ],
      "verification": "dotnet build 0/0; unit 577, integration 793 (+1 skip; +26 new IsamsReads), contract 1 green (Docker; a transient Docker-daemon drop mid-run was a flake, not a failure — re-run clean). Staging deploy 29928568944 + anon canary GREEN (health 200; both routes anon->401 + x-formmaps-service). Merged --no-ff to main ceeba72.",
      "commit": "ceeba72"
    },
    {
      "id": "FM-DOTNET-054-school-courses-crud",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 1 of a mini-phase): port GET /courses (list, with the read-only framework+enrollment merge) + POST /courses (create) under one flag, exact-literal /courses path (GET+POST co-flip). PUT/DELETE /courses/:courseId DEFERRED — that path collides with the un-ported /courses/pathways, /courses/import, /courses/ai-import siblings. AI endpoints (ai-suggest/ai-import) stay Node (Bedrock).",
      "dependsOn": [
        "FM-DOTNET-053-isams-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/transcriptService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolCourses/*",
        "services/api/src/FormMaps.Infrastructure/SchoolCourses/*",
        "services/api/src/FormMaps.Api/Endpoints/SchoolCoursesEndpoints.cs",
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "SchoolCoursesReader (read-only RLS): 500-cap pagination; search name|code ILIKE no-escape; department ILIKE; gradeLevel `has` (0 skipped); code-ASC+id-ASC tie-break; enrollmentCount via ONE student_course_plans groupBy (enrolled/planned, default 0, no N+1); framework merge quirk faithfully preserved (enabled frameworks' courses appended UN-paginated to EVERY page, total=schoolCount+fwCount, framework prerequisites always []).",
        "SchoolCoursesWriter (OpenWritableAsync): createCourse || defaults; 201 {id,code}; unique (schoolId,code) 23505→409 'Course code already exists'; createdBy/updatedBy NULL. New perms courses:read/courses:write. no-school→400.",
        "One flag FORMMAPS_ROUTE_SCHOOL_COURSES_TO_DOTNET over the EXACT literal /courses path only (GET+POST). Confirmed disjoint from /courses/:courseId, /courses/pathways, /courses/import, /courses/ai-import.",
        "GATE fresh reviewer SHIP + Codex FIXES-NEEDED (1 MEDIUM). Folds each red-if-regressed: (a) MED department silently coerced truthy-non-string->'' (201); now fails closed ->500 (Prisma text-column type rejection); same applied to nullable frameworkType/description. (b) CROSS-CUTTING: credits was ::double precision (number) but legacy raw Prisma Decimal serializes as a JSON STRING (transcriptService Number()s it explicitly to avoid the decimal-string; listCourses does NOT) -> trim_scale('credits')::text (string), both school + framework rows (FM-012 precedent). Documented LOW residuals (client-unreachable, uncertain Prisma coercion): maxEnrollment '0'-string; isHonors truthy-non-bool.",
        "⚠️ STANDING GUARDRAIL CORRECTION: 'Decimal->number via ::double precision' holds ONLY for computed/Number()-ified values; a RAW Prisma Decimal column passthrough serializes as a JSON STRING (decimal.js toString; emit via trim_scale::text). Prior slices with raw-Decimal passthroughs (candidate: universities.acceptanceRate FM-037) may warrant a focused audit."
      ],
      "verification": "dotnet build 0/0; unit 577, integration 841 (+1 skip; +48 SchoolCourses), contract 1 green. Staging deploy 29932949189 + anon canary GREEN (health 200; GET+POST /courses anon->401 + x-formmaps-service). Merged --no-ff to main b1800de.",
      "commit": "b1800de"
    },
    {
      "id": "FM-DOTNET-055-curriculum-frameworks",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 2): port the 4 /curriculum/frameworks endpoints (GET/PUT /curriculum/frameworks, GET /curriculum/frameworks/:type/courses [global catalog], PUT /curriculum/frameworks/:type/courses/:courseId). curriculum:manage. .NET write-owner for curriculum_frameworks + school_framework_course_overrides.",
      "dependsOn": [
        "FM-DOTNET-054-school-courses-crud"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/CurriculumFrameworks/*",
        "services/api/src/FormMaps.Infrastructure/CurriculumFrameworks/*",
        "services/api/src/FormMaps.Api/Endpoints/CurriculumFrameworksEndpoints.cs",
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "listFrameworks (4 fixed types, id/configuredAt omitted when no row, double-nested {data:{data}}, global groupBy courseCount); updateFrameworks upsert-tx configuredAt=now, empty->noop, HasEnabled undefined-skip on UPDATE; listFrameworkCourses GLOBAL (no school) 100-cap raw case-sensitive type; customizeFrameworkCourse 404/400(ToUpperCase) + create-vs-update undefined-asymmetry + merged view (singular gradeLevel key). credits emitted as decimal.js STRING (trim_scale::text). One flag over 3 path shapes.",
        "GATE fresh + Codex both FIXES-NEEDED. Folds (red-if-regressed): Codex HIGH customize credits accepts numeric-string (Prisma Decimal parity); fresh MED updateFrameworks enabled undefined-skip on UPDATE (HasEnabled flag); cross-cutting credits raw-Decimal->string. Dismissed: Codex HIGH#1 routing (verified false positive — endpoint ported, no un-ported sibling); Codex LOW#3 id-tie-break (ratified FM-032 no-op under UNIQUE(frameworkType,code)); fresh LOW stale comments (cleaned)."
      ],
      "verification": "dotnet build 0/0; unit 585, integration 833 (+1 skip), contract 1. Staging deploy 29935354699 + anon canary GREEN (4 routes 401 + x-formmaps-service). Merged --no-ff to main 344d02c (wiring conflicts vs FM-054 reconciled by hand — kept both; post-merge SchoolCourses+CurriculumFrameworks 88 green).",
      "commit": "344d02c"
    },
    {
      "id": "FM-DOTNET-056-data-mappings",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 3): port the data-mappings surface — GET /data-mappings (paginated list) + POST /data-mappings (create; source/status FORCED to manual/approved) + POST /data-mappings/bulk-approve. Permission school:data-mapping. .NET write-owner for data_mappings INSERT + bulk status-approve. PUT/DELETE /data-mappings/:id + ai-suggest DEFERRED — the bare /:id wildcard collides with the permanently-Node AI siblings (batched negative-lookahead cutover later).",
      "dependsOn": [
        "FM-DOTNET-055-curriculum-frameworks"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/DataMappings/*",
        "services/api/src/FormMaps.Infrastructure/DataMappings/*",
        "services/api/src/FormMaps.Api/Endpoints/DataMappingsEndpoints.cs",
        "services/api/src/FormMaps.Domain/Auth/FormMapsPermissions.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "listDataMappings (school-scoped, page/limit clamp 1..100/default 20, optional status filter, createdDate DESC + id ASC tie-break, full-row passthrough). createDataMapping RAW body no app-validation (Prisma type/NOT-NULL rejection = 500): externalCode/internalCourseId String kind else DBNull->NOT-NULL->500; externalSource || 'manual' (JS-falsy incl ''/0 -> 'manual', truthy non-string -> 500); externalName String?->value/NULL else 500; confidence Number|numeric-String->decimal else 500; source FORCED 'manual', status 'approved', approvedBy=caller, approvedAt/createdDate/updatedAt=now, createdBy/updatedBy NULL. NO 23505 catch -> duplicate (schoolId,externalCode,externalSource) surfaces as 500 (NOT 409, unlike createCourse). bulkApproveMappings school-scoped id-array flip -> approved (updatedAt bumps, updatedBy NOT set). confidence emitted as decimal.js STRING via trim_scale(col)::text (raw-Decimal-column guardrail, NOT ::double precision). One flag FORMMAPS_ROUTE_DATA_MAPPINGS_TO_DOTNET over the 3 collision-free literal paths.",
        "RATIFIED SAFE DIVERGENCE: bulk-approve with missing/non-array/empty mappingIds -> endpoint normalizes to empty text[] -> = ANY('{}') matches nothing -> 0 approved. Legacy Prisma { id: { in: undefined } } DROPS the filter and approves the WHOLE school -> we deliberately do NOT reproduce that footgun (fail-safe). Documented + tested.",
        "GATE: prior-session fresh reviewer = SHIP (faithful port; ONE LOW residual = confidence numeric-STRING coercion). This session RESOLVED the LOW by FOLDING (red-if-regressed): confidence string parse changed from NumberStyles.Number to AllowLeadingSign|AllowDecimalPoint|AllowExponent. Verified empirically against real decimal.js AND real .NET: the fold makes every realistic numeric string agree ('0.85'->0.85, '1e3'->1000) AND makes legacy's rejections agree ('1,000'/' 0.85 '->reject), ELIMINATING the two fail-OPEN divergences (default .NET parsed thousands-separator/whitespace strings decimal.js 500s on -> would have written a row legacy rejects). Residual divergences (hex 0x10, Infinity, NaN, ES underscore 1_000) are all FAIL-CLOSED (.NET 500 where legacy persists a nonsense value) — one-directional/safe; NOT reproduced by choice (would mean persisting NaN/Infinity/hex confidence). Exact decimal.js parity is unreachable via any NumberStyles mask. Codex INCONCLUSIVE (hung both attempts, session-recurring — NOT an approval; fold independently verified via ground-truth decimal.js + .NET probes + green suite)."
      ],
      "verification": "dotnet build 0/0; unit 585, integration 921 (+1 documented skip), contract 1 — full suite green. Staging deploy run 29953931884 SUCCESS + anon canary GREEN (health 200; GET+POST /api/v1/school-admin/data-mappings + POST /bulk-approve all anon->401 + x-formmaps-service). Merged --no-ff to main 3892721 (no wiring conflicts — branch based on FM-055 HEAD eead656, main had not advanced).",
      "commit": "3892721"
    },
    {
      "id": "FM-DOTNET-057-prerequisites",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 4): port the prerequisites surface — GET /courses/:courseId/prerequisite-chain (courses:read), PUT /courses/:courseId/prerequisites (courses:write), and GET /prerequisites/{check/:studentId/:courseId, eligible/:studentId, missing/:studentId/:courseId} (curriculum:manage) — under one flag FORMMAPS_ROUTE_PREREQUISITES_TO_DOTNET. .NET write-owner for the school_courses prerequisites/corequisites columns via the PUT. The two /courses/:courseId/* sub-paths are more specific than the deferred bare /courses/:courseId (PUT/DELETE) so no collision.",
      "dependsOn": [
        "FM-DOTNET-056-data-mappings"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Prerequisites/*",
        "services/api/src/FormMaps.Infrastructure/Prerequisites/*",
        "services/api/src/FormMaps.Api/Endpoints/PrerequisitesEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "getPrerequisiteChain (BFS over active catalog, depth-DESC STABLE sort [ties keep BFS order], cycle-safe via visited-UPPER; credits HETEROGENEOUS — in-catalog prereq emits decimal.js STRING via trim_scale(credits)::text, unresolved code emits JSON number 0; unresolved fallback name=code/department=''/frameworkType=null/isHonors=false; null/404 on missing/foreign course). updatePrerequisites (course lookup id+schoolId->404; prerequisiteRules[].courseIds resolved to school-scoped CODES ORDER BY id ASC [FM-032 superset]; corequisites ||[] + Prisma String[] semantics; @updatedAt bump + updatedBy=caller). resolveCourse (id-in-school then EXACT case-sensitive code). checkEligibility (grade gate JS-falsy 0/null-skip, prereq trim+UPPER match, 'Not in catalog' vs incomplete, exact error strings, eligible=errors==0&&missing==0). computeEligibilityMap TWO-SET (resolution=full catalog incl inactive/draft; enumeration=active+status='active' only). eligible endpoint: DOUBLE-NESTED {data:{data,total}}, gradeLevel(parseInt/NaN-excludes-all)/department(lowercase-includes) filters, projection {id,code,name,department,credits,gradeLevels}. missing endpoint OMITS eligible. Per-route perms; no-school 400; uniform 404 'Student not found'/'Course not found'.",
        "GATE: fresh reviewer SHIP (all 10 parity axes verified faithful) + Codex FIXES-NEEDED -> CONVERGED. Folds (each red-if-regressed): (1) corequisites type-reject must NOT pre-empt the missing-course 404 -> ParseCorequisites moved AFTER the course lookup (both reviewers, HIGH/MED); (2) prerequisiteRules flatMap null-rule-element throws (JS null.courseIds TypeError->500, post-lookup) [Codex HIGH]; (3) bare-string courseIds is ONE id + non-string courseId element -> Prisma id:{in} String reject->500 [Codex MED]. DISMISSED as unreachable (both reviewers agree; accepted FM-054 precedent): credits decimal.js exponential notation at exp<=-7/>=+21 (credit-hours never that large/small; reproducing decimal.js grammar deliberately rejected per FM-056); JsParseInt int32 saturation on gradeLevel (grades 6-12, no course gradeLevel near int.MaxValue; shared-helper property)."
      ],
      "verification": "dotnet build 0/0; unit 585, integration 972 (+1 skip), contract 1 — full suite green (+51 new). New Prerequisites Testcontainers harness (school_courses + student_grades + users, non-UTC tz pin). Staging deploy run 29968626827 SUCCESS + anon canary GREEN (health 200; all 5 routes anon->401 + x-formmaps-service). Merged --no-ff to main 5dffc55 (no wiring conflicts — branch based on FM-056 HEAD 9bbf765).",
      "commit": "5dffc55"
    },
    {
      "id": "FM-DOTNET-058-pathways",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 5): port the derived course-pathways read — GET /courses/pathways (curriculum:manage) — under one flag FORMMAPS_ROUTE_PATHWAYS_TO_DOTNET. A 'sequence' is a maximal chain in the transitively-reduced prerequisite DAG. Read-only; editing pathways = editing prereqs (FM-057). The literal 'pathways' segment is more specific than the deferred bare /courses/:courseId (PUT/DELETE, still Node) and disjoint from the FM-054 exact /courses — collision-free (no negative-lookahead needed).",
      "dependsOn": [
        "FM-DOTNET-057-prerequisites"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Pathways/*",
        "services/api/src/FormMaps.Infrastructure/Pathways/*",
        "services/api/src/FormMaps.Api/Endpoints/PathwaysEndpoints.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "computePathways (PURE PathwaysComputer.Compute + thin PathwaysReader). Reader loads the active catalog (WHERE schoolId AND isActive=true AND status='active' ORDER BY \"code\" ASC — same Postgres collation as Prisma orderBy, load-bearing for byCode last-wins / forward-edge push order / root order / DFS order), text[] prerequisites verbatim, delegates to the computer. Computer: byCode/inCatalogPrereqs UPPER-keyed last-wins; prereq trim (JsString.JsTrim ECMAScript set) + toUpperCase + drop empty/non-catalog (raw dups preserved); redundant-edge pruning via isTransitivePrereq (cycle-safe stack DFS — a direct edge u→v dropped when u is already a transitive prereq of another prereq of v, so no misleading sub-chain); roots = hasNoCatalogPrereqs && hasDependents; DFS root→leaf chains (chains<2 dropped, fresh path array per recursion, visited excludes currentCode from its own nexts filter + added to newVisited for children, MAX_CHAIN_LEN=12 & MAX_CHAINS=200 truncation boundaries exact); dedup by joined code key keep-first; group by chain[0] department verbatim (\"General\" only if unresolvable — dead fallback since department is non-nullable); dept-then-chainKey sort via JS localeCompare parity (CultureInfo.InvariantCulture.CompareInfo.Compare(...,CompareOptions.None), ICU, stable OrderBy). Wire: { success, data:{ truncated, groups:[{ department, chains:[[{courseId,code,name,isHonors}]] }] } }. RequireIdentity→curriculum:manage→resolve caller schoolId; null/empty→400 'No school'; NEVER 404s (empty catalog→{truncated:false,groups:[]}).",
        "GATE: fresh reviewer SHIP (faithful, full line-by-line — verified all 8 focus areas incl. the ICU '|'<digit sort ordering, the dead ?? 'General' fallback, and zero-tie stability irrelevance) → folded the ONE residual: a null prerequisites array element is an intentional robustness SUPERSET (raw ?? \"\" → dropped→200, vs legacy JS null.trim()→500) — UNREACHABLE via Prisma's non-nullable String[]; pinned red-if-regressed + in-code note. Codex INCONCLUSIVE (exceeded 600s foreground, moved to background, never returned a verdict within a bounded 9-min wait — its recurring-hang pattern; honestly recorded as NOT an approval). localeCompare parity EMPIRICALLY verified: .NET InvariantCulture ICU matches Node en-US on the load-bearing pairs (ALG12 vs ALG1|X = +1, i.e. '|' sorts BEFORE '2', which an ordinal compare gets backwards), pinned as Node gold in PathwaysLocaleCompareTests."
      ],
      "verification": "dotnet build 0/0; unit 602, integration 981 (+1 skip), contract 1 — full suite green (+26 new: 17 unit [10 computer + 1 null-element fold + 5 locale gold + reflexive/symmetric], 9 integration [4 reader + 5 endpoint]). New Pathways Testcontainers harness (school_courses only, distinct pathways-schema.sql basename — no EndsWith collision). Staging deploy run 29971802026 SUCCESS + anon canary GREEN (health 200; GET /courses/pathways anon→401 + x-formmaps-service). Merged --no-ff to main 9822e84 (no wiring conflicts — branch based on FM-057 HEAD 433fa2c, main had not advanced).",
      "commit": "9822e84"
    },
    {
      "id": "FM-DOTNET-059-course-import",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 6): port the course bulk-import CORE — POST /courses/import (202, importCourses) + GET /courses/import/:jobId (getImportJob) — under one flag FORMMAPS_ROUTE_COURSE_IMPORT_TO_DOTNET (courses:write). The failures-CSV GET /courses/import/:jobId/download-failures is DEFERRED to FM-060 (JSON.stringify-of-arbitrary-jsonb rawRow byte-parity deserves its own gold-pinned slice); its rewrite stays Node. Literal /courses/import + /courses/import/:jobId are collision-free (more specific than the deferred bare /courses/:courseId, disjoint from /courses, /courses/pathways, /courses/:id/prerequisite*).",
      "dependsOn": [
        "FM-DOTNET-058-pathways"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/CourseImport/*",
        "services/api/src/FormMaps.Infrastructure/CourseImport/*",
        "services/api/src/FormMaps.Api/Endpoints/CourseImportEndpoints.cs",
        "services/api/src/FormMaps.Application/Assessments/JsString.cs",
        "services/api/src/FormMaps.Infrastructure/DependencyInjection.cs",
        "services/api/src/FormMaps.Api/Program.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "importCourses (CourseImportWriter): per-row validate (JS-falsy code/name -> 'code and name are required') then upsert-by-(schoolId,code) case-sensitive exact. create-vs-update UNDEFINED-OMIT asymmetry (UPDATE always name/department[row.department||existing]/updatedAt; credits/gradeLevels/description present-only; empty gradeLevels [] IS written [JS-truthy]; CREATE description RAW ''->''/absent->NULL, credits row.credits?parseFloat:0, gradeLevels ||[]). Native ImportJobStatus enum (CAST(@x AS \"ImportJobStatus\") on write, ::text read). tz-independent ms-truncated timestamps (Unspecified, DbType.DateTime2). validationErrors emitted from the IN-MEMORY list (POST) / structured jsonb round-trip (GET). getImportJob (CourseImportReader): read-only RLS, missing/cross-school -> null -> 404 'Job not found'; completedAt ISO-Z or null; status ::text; validationErrors deserialized [{row,errors}] then re-emitted (no Postgres ::text spacing). ImportRowParser (pure): JS-truthy credits (string/truthy-number->text, 0/''/null->skip) + RowTypeInvalid (non-string department/description, non-int gradeLevels element, non-string/number credits -> Prisma type reject -> row fails). JsParseFloat (pure ECMAScript parseFloat; leading whitespace via the exact JsString StrWhiteSpace set).",
        "GATE: fresh reviewer SHIP (faithful for the parsed-CSV/all-strings contract; confirmed 9 probed suspicions) + Codex FIXES-NEEDED (3 HIGH) -> CONVERGED. Cross-model EARNED ITS KEEP: Codex caught the concurrent-duplicate-code POISONED-TRANSACTION 500 the fresh reviewer under-weighted (a per-row 23505 from a racing same-code import aborts the whole single transaction -> the per-row catch's error-insert then fails -> 500, vs legacy's per-row-autocommit completing the job with a failed row). Folds, each red-if-regressed: (1) ATOMIC IMPORT + PER-ROW SAVEPOINT RECOVERY (SAVEPOINT/ROLLBACK TO/RELEASE around each row's DB work so a per-row DB error is recovered and the job still completes; deterministic concurrent-23505 test via an uncommitted lock-holder + pg_stat_activity Lock-wait poll); (2) numeric-JSON credits parsed not dropped (was stored 0 vs legacy 3.5) [both HIGH]; (3) gradeLevels non-int element / non-string department|description -> RowTypeInvalid -> row fails matching Prisma Int[]/String rejection [Codex HIGH+MED]; (4) JsParseFloat leading whitespace uses the exact ECMAScript set (U+FEFF in, U+0085 out) [both MED]; (5) fixed the misleading (decimal)-overflow 'matches legacy' comment (legacy DECIMAL(65,30) would succeed for ~[7.9e28,1e35) that .NET decimal overflows; unreachable). DOCUMENTED (pinned, not reproduced): non-string filename keeps 202 + 'import.csv' (legacy 500s on the Prisma String type error outside the per-row try; hardening); incremental row-visibility mid-import (non-observable via the API); numeric code/name -> row fails either way (message-only divergence)."
      ],
      "verification": "dotnet build 0/0; unit 641, integration 1015 (+2 skip), contract 1 - full suite green (+21 new unit: ImportRowParser + JsParseFloat whitespace; +6 new integration: writer create/update/undefined-omit/validation/mixed/job-finalize/NaN-credits/atomic-commit + RowTypeInvalid + concurrent-23505-savepoint-recovery, reader shape/cross-school/ISO-Z/validationErrors, endpoint auth/perm/no-school/empty-rows/202/404/filename-default/non-string-filename/numeric-credits + 1 skip doc). New CourseImport Testcontainers harness (native ImportJobStatus enum, school_courses + import jobs/errors, non-UTC tz). Staging deploy run 29975305440 SUCCESS + anon canary GREEN (health 200; both routes anon->401 + x-formmaps-service). Merged --no-ff to main 5ce2eac.",
      "commit": "5ce2eac"
    },
    {
      "id": "FM-DOTNET-060-import-failures-csv",
      "status": "completed",
      "goal": "Phase-B slice (school-courses.ts sub-slice 6b, completes the course-import surface): port GET /courses/import/:jobId/download-failures + getImportFailuresCsv to .NET, under the EXISTING flag FORMMAPS_ROUTE_COURSE_IMPORT_TO_DOTNET (3rd rewrite added to the FM-059 block; courses:write). The deferred hard-parity piece: JSON.stringify of the ARBITRARY-keyed jsonb rawRow.",
      "dependsOn": [
        "FM-DOTNET-059-course-import"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/sanitize.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/CourseImport/FailuresCsvBuilder.cs",
        "services/api/src/FormMaps.Application/CourseImport/ICourseImportReader.cs",
        "services/api/src/FormMaps.Infrastructure/CourseImport/CourseImportReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/CourseImportEndpoints.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolAdminEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "getImportFailuresCsv (CourseImportReader.GetImportFailuresCsvAsync): read-only RLS; job findUnique + schoolId gate (missing OR cross-school -> null -> 404 'Job not found'); errors findMany ORDER BY \"rowNumber\" ASC (deterministic superset of legacy's unordered findMany, FM-032; rowNumber unique-per-job in practice); build header 'row_number,errors,raw_data' + one line per error joined by \\n. FailuresCsvBuilder (PURE): (a) JsStringify = faithful JS JSON.stringify walker over the parsed jsonb rawRow -- compact (no spaces), object keys in ELEMENT order (= Postgres jsonb normalized order, read via rawRow::text, NOT re-sorted; both Node-via-Prisma and .NET read the SAME jsonb), JS string escaping (\" \\ escaped; \\b \\f \\n \\r \\t short forms; other control <0x20 -> \\u00XX; < > & + and non-ASCII incl DEL/U+2028/U+2029 and well-formed surrogate pairs stay LITERAL; number branch double.ToString('R') shortest round-trip -- unreachable via CSV strings); (b) CsvSafe = lib/sanitize.ts formula-injection guard (leading = + - @ TAB CR -> prefix '); (c) DataLine = csvSafe(errorMessages.join('; '))->quote-double + csvSafe(JsStringify(rawRow))->quote-double, wrapped in CSV quotes. Endpoint GetImportFailuresAsync: text/csv + Content-Disposition attachment filename=course-import-failures-{jobId}.csv; 401/403/400/404 chain.",
        "GATE: fresh reviewer FIXES-NEEDED (1 MEDIUM, empirically confirmed vs Express 5.2.1) -> FOLDED. Content-Type MISSING charset: legacy res.send(<string>) rewrites the Content-Type via setCharset -> 'text/csv; charset=utf-8' EVEN after an explicit res.setHeader('Content-Type','text/csv'); the port emitted bare 'text/csv' (an Encoding arg alone does NOT append the charset) -> accented content (Spanish course names, routine for LatAm schools) mojibakes in Excel/Windows-1252-defaulting clients. Fixed HERE (Results.Text(csv,'text/csv; charset=utf-8')) AND retroactively in the FM-041 PRECEDENT (SchoolAdminEndpoints results-export CSV -- same bare text/csv + a WRONG 'legacy has no charset' comment; school-grades.ts also res.send). Tightened BOTH endpoint tests to assert the FULL Content-Type (.ToString() incl charset, not .MediaType which strips it). Codex INCONCLUSIVE (foreground 2-min timeout -> --resume-last background -> never returned within a bounded 7-min wait; recorded as NOT an approval). All OTHER parity axes CONFIRMED faithful: JsStringify (surrogates/U+2028-2029/DEL/escaping/key-order), csvSafe (regex-exact, \\n excluded), line format, ORDER BY rowNumber ASC soundness, job gate, response headers, routing (4-seg vs 3-seg no collision). Durable lesson -> [[formmaps-csharp-port-regression-corpus]] CSV/text-export Content-Type rule."
      ],
      "verification": "dotnet build 0/0; unit 659 (+FailuresCsvBuilder 18: JsStringify Node-gold + CsvSafe + DataLine), integration 1023 (+2 skip; +8 CourseImport: 3 reader failures-CSV [missing/cross-school->null, no-errors->header-only, real-Postgres byte-exact CSV with csvSafe+quote-doubling+jsonb-key-order] + 5 endpoint [anon/perm/no-school theories + 200 text/csv;charset=utf-8 + 404], + updated SchoolAdmin export-CSV content-type assertion), contract 1 - full suite green. Reused the FM-059 CourseImport Testcontainers harness. Staging deploy run 29978494786 SUCCESS + anon canary GREEN (health 200; download-failures anon->401 + x-formmaps-service). Merged --no-ff to main 271f353.",
      "commit": "271f353"
    },
    {
      "id": "FM-DOTNET-061-id-writes",
      "status": "completed",
      "goal": "Phase-B slice: port the DEFERRED /:id WRITES that complete the FM-054 (courses) + FM-056 (data-mappings) write surfaces — PUT/DELETE /courses/:courseId (courses:write) + PUT/DELETE /data-mappings/:id (school:data-mapping) — each under its EXISTING flag, PLUS the batched negative-lookahead next.config rewrites (the recurring routing deferral: Next rewrites match by PATH NOT METHOD, so /courses/:courseId((?!import|pathways|ai-import)…) + /data-mappings/:id((?!bulk-approve|ai-suggest)…) prevent a flag flip misrouting the Node-only AI/literal siblings to the .NET group).",
      "dependsOn": [
        "FM-DOTNET-060-import-failures-csv"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-courses.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolCourses/ISchoolCoursesWriter.cs",
        "services/api/src/FormMaps.Application/DataMappings/IDataMappingsWriter.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolCourses/SchoolCoursesWriter.cs",
        "services/api/src/FormMaps.Infrastructure/DataMappings/DataMappingsWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolCoursesEndpoints.cs",
        "services/api/src/FormMaps.Api/Endpoints/DataMappingsEndpoints.cs",
        "services/api/tests",
        "apps/web/next.config.ts"
      ],
      "outputs": [
        "updateCourse (SchoolCoursesWriter.UpdateCourseAsync): ownership gate SELECT schoolId (missing OR wrong-school → null → endpoint 403 'Course not in your school', UNIFORM incl not-found); 12-field undefined-omit mass-assignment (code/name/department/credits/gradeLevels/prerequisites/corequisites/frameworkType/description/maxEnrollment/isHonors/status) — RAW copy (NO create-side || falsy defaults) so present-null/wrong-type on a NOT-NULL column → 500, present-null on nullable → NULL; updatedAt ALWAYS in the SET (Prisma @updatedAt bumps even on update({data:{}})); per-field Prisma typing incl maxEnrollment Int? STRICT (numeric-string → 500, faithful to Prisma Int, DELIBERATELY unlike the create-side MaxEnrollmentOrNull); returns the passed-in courseId. deleteCourse: SOFT delete (isActive=false, status='archived', +updatedAt; row stays). updateDataMapping (DataMappingsWriter): gate → null → 404 'Mapping not found'; data ALWAYS updatedBy=userId + updatedAt + conditional externalCode/externalName/externalSource/internalCourseId (undefined-omit; externalName nullable / other 3 NOT NULL). deleteDataMapping: HARD delete. Endpoints add MapPut/MapDelete on the existing groups; the 403(courses)-vs-404(mappings) asymmetry is exact. next.config: /courses/:courseId + /data-mappings/:id negative-lookahead rewrites appended to the existing FM-054/056 flag blocks (documented for cutover verification in the live formmaps-platform/frontend/next.config.ts — not CI-exercised).",
        "GATE: fresh reviewer FIXES-NEEDED (1 finding, LOW-reachability but FAIL-OPEN) → FOLDED. credits numeric-string parse used the default NumberStyles.Number → the EXACT FM-056 confidence regression: '1,000'/' 0.85 '/'5-' silently PARSE (.NET writes a row legacy's decimal.js 500s on) — fail-OPEN. Applied the ratified FM-056 mask (AllowLeadingSign|AllowDecimalPoint|AllowExponent) to BOTH the new CreditsUpdateValue (FM-061 update) AND the FM-054 create twin CreditsOrZero + 4 red-if-regressed tests ('1e3'→1000 parses; '1,000'/' 0.85 '/'5-' throw→500, unchanged). Every OTHER axis CONFIRMED faithful (8/8): 403/404 asymmetry, 12-field per-column Prisma typing (status NOT-NULL text not enum; maxEnrollment Int-strict numeric-string→500 IS faithful), soft-vs-hard delete, updateDataMapping nullability (externalName vs 3 NOT-NULL), ownership gate one-session-no-write-on-miss + return-passed-in-id, auth 401/403/400, tz-independent @updatedAt. Reviewer NOTED (out-of-scope, pre-existing FM-054 fail-open): the create-side MaxEnrollmentOrNull ACCEPTS numeric strings where Prisma Int 500s — candidate follow-up. Codex INCONCLUSIVE (foreground timeout → background → never returned in a bounded 7-min wait; NOT an approval). Built by a plain (non-worktree) agent; I verified the tree + reviewed all writers/endpoints + confirmed DataMapping nullability against schema + ran the gate + folded."
      ],
      "verification": "dotnet build 0/0; unit 659, integration 1067 (+2 skip), contract 1 — full suite green (~44 new FM-061 integration: SchoolCourses 19 writer[+4 credits-mask] + endpoint; DataMappings 21 writer+endpoint; incl. the 403-vs-404 asymmetry pin). Reused the FM-054 SchoolCourses + FM-056 DataMappings Testcontainers harnesses. Staging deploy run 29980711687 SUCCESS + anon canary GREEN (health 200; all 4 write routes anon->401 + x-formmaps-service). Merged --no-ff to main 9cc9484. ⚠️ ROUTING: the two negative-lookahead rewrites are in apps/web/next.config.ts (staging/reference) — MUST be re-verified in formmaps-platform/frontend/next.config.ts at cutover (not CI-testable).",
      "commit": "9cc9484"
    },
    {
      "id": "FM-DOTNET-062-school-students-reads",
      "status": "completed",
      "goal": "Phase-B slice: FIRST sub-slice of school-students.ts (routes/school-students.ts, mounted /api/v1/school-admin) — the three school:manage student-identity GET reads: list, detail, community-service. Shipped DARK (no next.config rewrite): /students/:studentId carries an un-ported DELETE (soft delete) + /students carries un-ported POST invites, and Next rewrites match PATH-not-method (FM-047 precedent) → a reads-only rewrite would misroute the writes; the domain co-flips when the writes land.",
      "dependsOn": [
        "FM-DOTNET-061-id-writes"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-students.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolStudentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/evaluationGroups.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolStudents/ISchoolStudentsReader.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolStudents/SchoolStudentsReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolStudentsEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolStudents"
      ],
      "outputs": [
        "GET /students (listStudents): paginated roster (roleName IN ('Student','student') + isActive + schoolId; search ILIKE name/email no-escape; ORDER BY createdDate DESC, id ASC tie-break [documented superset]; PcaExamPagination.Resolve clamp page=max(1,parseInt||1)/limit=min(100,max(1,parseInt||20)); status=isActive?active:inactive). ENVELOPE ASYMMETRY preserved EXACTLY: no-school → 200 {success:true,data:{data:[],total:0}} (NO page/limit); happy path → res.json(result) = the SERVICE object VERBATIM {data,total,page,limit,totalPages} with NO success wrapper.",
        "GET /students/:studentId (getStudentDetail): cross-school/missing → null → 404 'Student not found'. GPA = JsRound(mean(points)*100)/100 over gradeMap (case-sensitive Ordinal keys, grade.Trim(), unmapped dropped; null when none map). creditsRequired default 120, overridden by the active graduation_rule_set for the CURRENT academic_year (both findFirst-no-orderBy → ORDER BY id ASC LIMIT 1). creditsEarned = per completed+active+grade-not-null grade: own credits>0 ? own : school_courses fallback ?? 0. assessmentStatus: PCA = pca_evaluations existence; MIL = completed>=5 ? completed : (completed>0||inProgress>0) ? in_progress : not_started (pca_exam_sessions status ::text); Eval360 = parent&&teacher&&sibling_friend all completed (Evaluation360Scoring.NormalizeGroupType) → completed; else any group → in_progress; else not_started. alertCount = student_alerts school-scoped isRead=false isDismissed=false. lastActive = ISO-Z(updatedAt ?? createdDate). Credits/required are COMPUTED (::double precision) → JSON NUMBERS. VERBATIM PCA/MIL/Eval360 keys via Dictionary<string,string> (the Web camelCase policy would mangle anonymous-object 'PCA'→'pCA'; DictionaryKeyPolicy is null so dict keys stay verbatim — the codebase idiom for PascalCase keys).",
        "GET /students/:studentId/community-service (getStudentCommunityService): cross-school/missing → null → 404. entries = full community_service_entries passthrough in Prisma field order (ORDER BY date DESC, id ASC; isActive filter). hours = RAW Prisma Decimal COLUMN → JSON STRING via trim_scale::text (FM-054 raw-Decimal rule, NOT ::double precision). status ::text enum label; verifiedAt/createdDate/updatedAt/date ISO-Z (verifiedAt null passthrough); nullable cols IsDBNull-guarded. totalHoursRequired = schools.serviceHoursRequired ?? 0. no-school (detail+community) → 400 'No school'.",
        "GATE: fresh general-purpose reviewer = SHIP (thorough byte-level parity across all 6 focus areas; NO blocking/high/medium divergence — port faithful). Two LOW accepted (NOT folded): [L1] duplicate ?search= query param — TS qs() takes first array element, ASP.NET binds a single string? as comma-joined ('a,b'); pathological (no real client), page/limit unaffected (parseInt stops at comma), and it is the SAME string? query-binding pattern used by every prior read slice (FM-050 notes) → codebase-wide documented divergence, not an FM-062 regression. [L2] normalizeGroupType Unicode-\\s strip vs ASCII-only — non-issue, controlled enum-like groupType values, identical to the already-ratified analytics port. Codex INCONCLUSIVE (wedged into background sub-task task-mrxbusoh-04tahl, never returned a verdict; NOT an approval). Built INLINE (subtle single-slice work); I scoped the live TS + built + gated + verified."
      ],
      "verification": "dotnet build 0/0; unit 659, integration 1105 (+2 skip, +38 new FM-062: 23 SchoolStudentsReader parity incl. a GPA half-up tie [2×C- + 6×F → mean 0.425 → JsRound(42.5)=43 → 0.43, RED if JsRound regresses to banker's ToEven→0.42] + 15 SchoolStudentsEndpoints mapping/shape), contract 1 — full suite green. New Testcontainers harness school-students-schema.sql (11 tables, non-UTC tz pin). Staging deploy run 29997155473 SUCCESS + anon canary GREEN (health 200; all 3 routes anon->401 + x-formmaps-service). Merged --no-ff to main 3cb1997. Deferred follow-ups: parents reads (/parents, /students/:id/parents), course-plan reads (/course-plan, /change-requests, /course-request-deadline GET), then the 14 writes (SES-entangled invites, bulk-onboard, delete, verify, review, parent links).",
      "commit": "3cb1997"
    },
    {
      "id": "FM-DOTNET-063-school-students-parents-reads",
      "status": "completed",
      "goal": "Phase-B slice: SECOND sub-slice of school-students.ts — the two school:manage parent-link GET reads: GET /parents (school-wide grouped-by-email parent roster + stats) + GET /students/:studentId/parents (the Guardians tab for one student). Shipped DARK (co-flips with the school-students writes; both paths carry un-ported POST/DELETE siblings, Next matches path-not-method).",
      "dependsOn": [
        "FM-DOTNET-062-school-students-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-students.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolStudentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/query.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolStudents/ISchoolStudentsParentsReader.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolStudents/SchoolStudentsParentsReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolStudentsParentsEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolStudents"
      ],
      "outputs": [
        "GET /parents (listParents): JOIN student_parent_links → users to school-scope via student.schoolId (the table has no schoolId col — legacy where:{student:{schoolId}} relation filter) + link.isActive; paginate LINKS (ORDER BY createdDate DESC, id ASC tie-break) THEN group by lower(parentEmail) keep-FIRST in memory (students appended; a parent linked to N students → N links → N students). PcaExamPagination.Resolve clamp; search = qs?.trim()||undefined (TRIMMED — unlike GET /students). ENVELOPE: no-school → 200 {success:true,data:[],total:0,totalPages:1} (NO page/stats); happy → res.json({success:true,...result}) = {success,data,total,totalPages,page,stats}. total = raw LINK count (search-filtered) so data.Count can be < total. Group keys id/parentName/parentEmail/parentUserId/isAccepted/acceptedAt/createdDate/students; student keys id/name/email/gradeLevel.",
        "⚠️ CASE-SENSITIVITY ASYMMETRY (faithful, PINNED): the DISPLAY grouping key is parentEmail.toLowerCase() (case-INSENSITIVE) but stats.totalParents = Prisma groupBy({by:['parentEmail']}).length on the RAW email = case-SENSITIVE → 'Pat@e.st'+'pat@e.st' collapse to ONE data row but count as TWO in totalParents. Ported as: ToLowerInvariant() dict key for data; COUNT(DISTINCT \"parentEmail\") for the stat. Stats (totalParents distinct / linkedStudents raw-link-count / pendingInvites isAccepted=false-count) are school-wide, search-INDEPENDENT, un-paginated (three separate aggregates).",
        "GET /students/:studentId/parents (listParentsForStudent): scoped by studentInCallerSchool — Super Admin (RAW exact context.Actor.Role=='Super Admin', matching req.userRole==='Super Admin') BYPASSES; else the caller must have a school (ISchoolAdminScopeResolver null → false) AND student.schoolId==caller.schoolId (IsStudentInCallerSchoolAsync; missing/null/mismatch → false); ANY miss → 404 message EXACTLY 'Not found' (NOT the FM-062 'Student not found'). links where studentId + isActive, ORDER BY createdDate DESC (+id ASC); status = isAccepted ? accepted : (tokenExpiresAt!=null && tokenExpiresAt<now ? expired : pending) via injected TimeProvider (UTC-vs-UTC, strict <); keys id/name(parentName)/email(parentEmail)/relationship(relation)/status/invitedAt(createdDate)/acceptedAt/parentUserId. Response {success:true,data:[...]}.",
        "GATE: fresh general-purpose reviewer = SHIP ('faithful 1:1 reproduction'; NO blocking/high/medium; all 6 scrutinize items confirmed incl. the case-sensitivity asymmetry + the DBNull-vs-500 audit [all nullable cols IsDBNull-guarded; unconditional GetString only on NOT-NULL cols]). One LOW accepted (NOT folded): duplicate ?search= query param — TS qs() first-element vs ASP.NET string? comma-join; pathological + the SAME codebase-wide string? binding as every prior read slice (page/limit unaffected). Codex INCONCLUSIVE (wedged into background task-mrxd0o4c-m5r8d8, no verdict; NOT an approval). Built INLINE — my own reader test surfaced + then PINNED the display-vs-stats case-sensitivity asymmetry (initial assertion 2 was wrong; code returned the faithful 3)."
      ],
      "verification": "dotnet build 0/0; unit 659, integration 1123 (+2 skip, +18 new FM-063: 10 SchoolStudentsParentsReader parity [grouping/case-sensitivity asymmetry + accepted/expired/pending status via a fixed TimeProvider + IsStudentInCallerSchool matrix] + 8 SchoolStudentsParentsEndpoints [Super-Admin bypass, 404 'Not found', no-school shape, search-trim, stats]), contract 1 — full suite green. Reused + extended the FM-062 school-students-schema.sql harness (added student_parent_links). Staging deploy run 29999146557 SUCCESS + anon canary GREEN (health 200; both routes anon->401 + x-formmaps-service; verified the 404→401 flip = new image live). Merged --no-ff to main 262b418. Deferred: course-plan reads (/course-plan, /change-requests, /course-request-deadline GET) → the 14 school-students writes.",
      "commit": "262b418"
    },
    {
      "id": "FM-DOTNET-064-school-students-course-plan-reads",
      "status": "completed",
      "goal": "Phase-B slice: THIRD sub-slice of school-students.ts — the three course-planning GET reads (GET /students/:studentId/course-plan + /course-plan/change-requests + /course-request-deadline). COMPLETES the school-students.ts READ surface. Shipped DARK.",
      "dependsOn": [
        "FM-DOTNET-063-school-students-parents-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-students.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolStudentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolStudents/ISchoolStudentsCoursePlanReader.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolStudents/SchoolStudentsCoursePlanReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolStudentsCoursePlanEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolStudents"
      ],
      "outputs": [
        "AUTH (distinct from the FM-062/063 school:manage rail): the two /course-plan reads are ROLE-gated — RequireIdentity → role ∈ {school_admin, Super Admin} RAW exact (context.Actor.Role) → else 403 'Forbidden' (NOT a permission check — a counselor holding school:manage still 403s), THEN studentInCallerSchool (Super-Admin bypass else student.schoolId==caller.schoolId) → else 404 'Not found'. /course-request-deadline = school:manage → 403; no-school → 400 'No school'.",
        "GET /students/:studentId/course-plan (getStudentCoursePlan): resolves the student's OWN schoolId; missing student OR null schoolId → the MINIMAL early-return {plan:{enrollments:[]},recommendations:[]} (plan holds ONLY enrollments — no studentId/gradeLevel/graduationProgress). Merges current-year plan rows (loaded ONLY if a current academic_year exists; sortOrder ASC + id tie-break) with ALL active grades: gradeEnrollments (completed grades) FIRST each WITH a grade key, then plan enrollments WITHOUT a grade key (endpoint emits the grade key conditionally on IsGraded). derivedGradeLevel = Math.max(9, currentGrade - (currentAYstart - yearStart)); currentGrade = gradeLevel||11 (0→11); currentAYstart = JsParseInt(academicYear.name.split('-')[0]) when a current year exists ELSE TimeProvider year; yearStart = g.academicYear ? JsParseInt : currentAYstart. Credits (plan: course?Number(credits):0; grade: Number(g.credits)) + totalCreditsRequired via ::double precision → JSON numbers; totalCreditsEarned = sum completed; isOnTrack = earned >= required*0.5; top-level plan.gradeLevel = the RAW nullable gradeLevel (NOT ||11).",
        "GET /students/:studentId/course-plan/change-requests (getStudentChangeRequests): full course_change_requests row passthrough in Prisma field order; credits = RAW Decimal → JSON STRING (trim_scale::text); action/status ::text native enums; ISO-Z dueDate/reviewedAt/createdDate/updatedAt; where studentId+isActive [+ status]; ORDER BY createdDate DESC, id ASC; LIMIT 50; count same where. The status INPUT filter casts @status::\"CourseChangeStatus\" so an INVALID label ERRORS → 500, faithfully reproducing Prisma's PrismaClientValidationError (legacy `where.status=<garbage>` 500s, does NOT silently return empty). GET /course-request-deadline (getCourseRequestDeadline): school_assessment_settings by schoolId; deadline = courseRequestDeadline || null (missing row OR null → null); {success,data:{deadline}}.",
        "GATE: fresh general-purpose reviewer found 2 MED behavioral divergences → BOTH FOLDED, each red-if-regressed: [MED-1] `??` (null-only) vs JS `||` (empty-string coalesces) on plan term/status + grade semester + the grade courseName middle fallback (course?.name || g.courseCode || '') — reachable via blank-cell imports → added a JsOr(value,fallback)=IsNullOrEmpty?fallback:value helper. [MED-2] derivedGradeLevel: an existing-but-unparseable academic-year string → JS parseInt NaN → Math.max(9, x-NaN)=NaN → JSON null, but the port's JsParseInt ?? fallback MASKED it to a number → made currentAYstart/yearStart/derivedGradeLevel NULLABLE (int?) so NaN propagates to a null gradeLevel. All OTHER axes CONFIRMED faithful (role gate 403-vs-permission, enum-cast 500 [Prisma DOES 500 on bad enum], minimal empty shape, grade-key asymmetry, decimal string-vs-number split, no unguarded-null 500 [every bare GetString maps to a prod NOT-NULL col], deadline null). LOW note (extra `code` key on the 403 body) = a SYSTEMIC cross-cutting decision across all 19+ ported endpoint files, NOT folded. Codex INCONCLUSIVE (wedged into background, no verdict; NOT an approval). Built INLINE; 2 of my own test-assertion bugs (IsOnTrack math, derivedGradeLevel=9-not-11) + a harness gap (student_grades.schoolId) caught by running the tests."
      ],
      "verification": "dotnet build 0/0; unit 659, integration 1150 (+2 skip, +25 new FM-064: SchoolStudentsCoursePlanReader parity [merge/grade-key asymmetry, derivedGradeLevel, credits fallback, isOnTrack, JsOr empty-string fold, NaN→null fold, change-requests passthrough+enum-filter 500, deadline] + SchoolStudentsCoursePlanEndpoints [role gate 403, Super-Admin bypass, 404, minimal empty shape, envelopes, deadline 400]), contract 1 — full suite green. Extended the shared school-students-schema.sql harness (native CourseChangeStatus/CourseChangeAction enums + student_course_plans/course_change_requests/school_assessment_settings + columns). Staging deploy run 30001899078 SUCCESS + anon canary GREEN (health 200; all 3 routes anon->401 + x-formmaps-service). Merged --no-ff to main 0051f69. **COMPLETES the school-students.ts READ surface** (FM-062 identity + FM-063 parents + FM-064 course-plan). Deferred: the 14 school-students WRITES (invites/resend/bulk-onboard [SES], delete [soft], verify community-service, review change-request + deadline PUT, parent invite/unlink/resend).",
      "commit": "0051f69"
    },
    {
      "id": "FM-DOTNET-065-school-students-writes",
      "status": "completed",
      "goal": "Phase-B slice: FIRST WRITE sub-slice of school-students.ts — the two non-SES DB writes (DELETE /students/{studentId} soft delete + PUT /course-request-deadline upsert). Establishes the school-students WRITE rail. Shipped DARK.",
      "dependsOn": [
        "FM-DOTNET-064-school-students-course-plan-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-students.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolStudentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/index.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolStudents/ISchoolStudentsWriter.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolStudents/SchoolStudentsWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolStudentsWriteEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolStudents"
      ],
      "outputs": [
        "DELETE /students/{studentId} (deleteStudent): ownership check under ONE writable session — SELECT users.schoolId; missing OR cross-school → false → 404 'Student not found' with NO write (session opened but never committed on that path); else UPDATE users SET isActive=false, updatedAt=@now (Prisma @updatedAt) → {success:true,data:{studentId}}. PUT /course-request-deadline (updateCourseRequestDeadline): idempotent upsert INSERT ... ON CONFLICT (schoolId) DO UPDATE SET courseRequestDeadline=EXCLUDED, updatedAt=@now; id=gen_random_uuid()::text + createdDate/updatedAt on insert, other scalar cols via DB defaults; RETURNING courseRequestDeadline → {deadline} ISO-Z|null. no-school (both) → 400 'No school'. New ISchoolStudentsWriter + SchoolStudentsWriter (OpenWritableAsync + CommitAsync; tz-independent ms-truncated timestamps via DbType.DateTime2 + Kind=Unspecified; fixed-literal SET/INSERT columns = mass-assignment guard).",
        "DEADLINE BODY PARSE = faithful port of `req.body.deadline || null` then `deadline ? new Date(deadline) : null`: JS-falsy (absent/null/''/0/false) → null (clears); non-empty string parsed via DateTimeOffset.TryParse(AssumeUniversal|AdjustToUniversal) (offset-less = UTC, matching new Date on a UTC prod server); number → epoch ms (DateTimeOffset.FromUnixTimeMilliseconds, TimeClip ±8.64e15 range); true → new Date(1); object/array/unparseable/out-of-range → 500 'Internal server error' (no write). EMPTY body → express.json {} → deadline null → 200 clears; MALFORMED JSON → 500 no write (distinguished via an empty-object sentinel — the FIX for the phantom-write divergence below).",
        "GATE: fresh reviewer (write-safety CONFIRMED sound — ownership/no-phantom-write, idempotent upsert, correct timestamps; no BLOCKING/HIGH) + Codex (RETURNED a verdict this run, not fully wedged). FOLDED 2 real write-safety divergences, each red-if-regressed: [fresh MED = Codex HIGH-2] non-string `deadline` JSON values — the port treated ALL non-strings as null (a wrong/NULL persisted value + 200 where TS new Date(number) STORES a date and new Date(object) → Invalid → 500) → reproduced JS truthiness + new Date(number/bool), object/array → 500. [Codex HIGH-1] MALFORMED JSON body — the port swallowed the parse error and treated it as an empty body → wrote NULL + 200 (a PHANTOM CLEARING WRITE), but legacy express.json() throws → global handler → 500 with NO write → distinguished empty ({}→clears) from malformed (500, no write). ACCEPTED/DOCUMENTED (not folded): [Codex MED-3] impossible-calendar-date strings ('2026-02-30') roll over in V8 new Date but .NET TryParse rejects → 500 — the same infeasible-to-reproduce new Date string grammar/normalization as the documented LOW (frontend only sends ISO date-picker strings); [Codex LOW-4] the 415 unsupported-content-type message text = a SYSTEMIC MutationContentTypeMiddleware string, not FM-065-specific. Built INLINE."
      ],
      "verification": "dotnet build 0/0; unit 659, integration 1173 (+2 skip, +23 new FM-065: SchoolStudentsWriter [delete ownership+no-write, soft-delete+updatedAt, upsert idempotency COUNT=1, null-clear] + SchoolStudentsWriteEndpoints [401/403/400, delete 404+happy, deadline parse theory incl. number/0/true + malformed-500-no-write + empty-200-clears + object/array-500]), contract 1 — full suite green. Reused the school-students-schema.sql harness (school_assessment_settings gained createdDate/updatedAt). Staging deploy run 30004291644 SUCCESS + anon canary GREEN (health 200; DELETE + PUT anon->401 + x-formmaps-service). Merged --no-ff to main 721102b. Deferred: FM-066 verify + review writes (both enum writes); FM-067/068 parent + student invite writes (SES — email-SENDS to stay Node-polyglot per Federico's lean, port only cleanly-separable DB halves).",
      "commit": "721102b"
    },
    {
      "id": "FM-DOTNET-066-school-students-verify-review",
      "status": "completed",
      "goal": "Phase-B slice: SECOND WRITE sub-slice of school-students.ts — the two review/verify enum writes: PUT /community-service/{entryId}/verify + PUT /students/{studentId}/course-plan/change-requests/{requestId}/review. Shipped DARK.",
      "dependsOn": [
        "FM-DOTNET-065-school-students-writes"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school-students.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolStudentsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/index.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/SchoolStudents/ISchoolStudentsReviewWriter.cs",
        "services/api/src/FormMaps.Infrastructure/SchoolStudents/SchoolStudentsReviewWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolStudentsReviewEndpoints.cs",
        "services/api/src/FormMaps.Api/Endpoints/SchoolStudentsWriteEndpoints.cs",
        "services/api/tests/FormMaps.IntegrationTests/SchoolStudents"
      ],
      "outputs": [
        "verify (verifyCommunityService): zod {status ∈ {verified,rejected}, note? ≤1000} → 400 'Invalid request body'; callerSchoolId = Super Admin ? null : own school (non-admin no-school → 400 'No school'); entry missing/cross-school → 404 'Entry not found'; UPDATE status(::CommunityServiceStatus)+verifiedBy=caller+verifiedAt=now+note(empty→null else slice1000)+updatedAt → full-row return (hours STRING). review (reviewChangeRequest): truthy-invalid status → 400 'Invalid status'; studentInCallerSchool (Super-Admin bypass) → 404 'Not found'; cr missing/wrong-student → 404 'Request not found'; UPDATE status(::CourseChangeStatus, omit-if-absent)+reviewedBy+reviewedAt+counselorNote(if-truthy)+updatedAt; on approved+add → CREATE student_course_plan (term=cr.semester||'Fall', planned, sortOrder 0) when admin has school+current AY; full-row return. New ISchoolStudentsReviewWriter+SchoolStudentsReviewWriter (writable session + native ::enum casts; cr-update + plan-create in ONE tx). Harness: community_service_entries.status → native CommunityServiceStatus enum; student_course_plans gained createdDate/updatedAt.",
        "GATE: fresh reviewer + Codex CONVERGED (Codex ranked BLOCKING). FOLDED red-if-regressed: [non-object/malformed body] JsonElement.TryGetProperty threw on a non-object root → UNHANDLED 500 crash + violated the codebase ValueKind==Object guard → unified body reader (empty/array → {} : verify 400-missing-status / review proceeds; primitive/malformed → 500 no-write, matching express strict/throw); [non-string `status` PHANTOM WRITE] the port treated a non-string status as absent → 200 + metadata write, but legacy 400s (truthy) or 500s (falsy) with NO status write → reproduced JS truthiness (truthy non-valid → 400 'Invalid status'; falsy present ''/0/false/null → passed as '' so the writer's enum cast 500s AFTER the 404 gates). ALSO fixed the SAME latent non-object-body crash in FM-065's deadline PUT (array → {} → 200 clears; primitive/malformed → 500). ACCEPTED/DOCUMENTED: non-string truthy counselorNote (legacy Prisma 500 vs .NET omit+200 — pathological/unreachable, .NET more lenient, no garbage persisted); the cr-update+plan-create atomicity (legacy two autocommits vs .NET one tx — unreachable, no FK on student_course_plans.courseId). Object-body happy paths + verify fully faithful (both reviewers). Built INLINE."
      ],
      "verification": "dotnet build 0/0; unit 659, integration 1201 (+2 skip, +22 new FM-066: SchoolStudentsReviewWriter [verify missing/cross-school/super-admin/note-slice/empty-null; review missing/wrong-student/status+note/approved-add-plan-create + NOT-for-other-action-or-status] + SchoolStudentsReviewEndpoints [verify zod 400/no-school/super-admin-null/404/happy; review invalid-status 400/student-not-in-school 404/super-admin bypass/happy; non-object body; truthy-non-string status 400; malformed 500-no-write; array-proceeds]), contract 1 — full suite green. Harness community_service_entries.status→enum + student_course_plans timestamps. Staging deploy run 30007320283 SUCCESS + anon canary GREEN (both PUT routes anon->401 + x-formmaps-service). Merged --no-ff to main b2cba19. Deferred: FM-067/068 SES invite writes — email-SENDS stay Node-polyglot per Federico; port only cleanly-separable DB halves.",
      "commit": "b2cba19"
    },
    {
      "id": "FM-DOTNET-067-counselor-dashboard-reads",
      "status": "completed",
      "goal": "Phase-C slice 1 (FIRST counselor slice): the 4 self-contained counselor:dashboard reads of routes/counselor.ts, mounted /api/v1/counselor, behind dark flag FORMMAPS_ROUTE_COUNSELOR_DASHBOARD_TO_DOTNET — GET /dashboard, GET /dashboard/change-requests, GET /me/students/{studentId}, GET /students/{studentId}. Deferred: enriched caseload GET /me/students (listEnrichedStudents); onboarding verify/complete stay Node. Gate (fresh+Codex) folded a HIGH tz-independent now binding + added Testcontainers reader tests.",
      "dependsOn": [
        "FM-DOTNET-066-school-students-verify-review"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/counselor.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma",
        "/Users/federicotafur/formmaps-platform/api/src/index.ts"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Counselor/ICounselorDashboardReader.cs",
        "services/api/src/FormMaps.Infrastructure/Counselor/CounselorDashboardReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/CounselorDashboardEndpoints.cs"
      ]
    },
    {
      "id": "FM-DOTNET-068-counselor-enriched-caseload",
      "status": "completed",
      "goal": "Phase-C slice 2: the deferred enriched caseload GET /me/students (counselor.ts → counselorAnalyticsService.listEnrichedStudents) behind dark flag FORMMAPS_ROUTE_COUNSELOR_CASELOAD_TO_DOTNET. Pure EnrichedCaseloadComputer (GPA/credit/3-badges/at-risk-status/career-jsonb/search+status-filter/name-ICU+numeric-sort/paginate) + CounselorCaseloadReader (~10 loads incl. required-credits resolution). pca_evaluations NOT isActive-filtered; credits ::double precision. Gate: fresh reviewer SHIP.",
      "dependsOn": [
        "FM-DOTNET-067-counselor-dashboard-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/services/counselorAnalyticsService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/routes/counselor.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Counselor/ICounselorCaseloadReader.cs",
        "services/api/src/FormMaps.Application/Counselor/EnrichedCaseloadComputer.cs",
        "services/api/src/FormMaps.Infrastructure/Counselor/CounselorCaseloadReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/CounselorCaseloadEndpoints.cs"
      ]
    },
    {
      "id": "FM-DOTNET-069-counselor-availability",
      "status": "completed",
      "goal": "Phase-C slice 3 (FIRST counselor WRITE slice — establishes the counselor write rail): GET+PUT /me/availability (counselor.ts) behind dark flag FORMMAPS_ROUTE_COUNSELOR_AVAILABILITY_TO_DOTNET (one flag co-flips read+write, same path). ICounselorAvailabilityRepository (read-only GET; writable+commit upsert on unique userId); weeklySchedule verbatim jsonb; JS-|| body resolution (timezone||'UTC', weeklySchedule||slots||[]); primitive/malformed body->500. Permission counselor:sessions. Fresh reviewer SHIP.",
      "dependsOn": [
        "FM-DOTNET-068-counselor-enriched-caseload"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/counselor.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Counselor/ICounselorAvailabilityRepository.cs",
        "services/api/src/FormMaps.Infrastructure/Counselor/CounselorAvailabilityRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CounselorAvailabilityEndpoints.cs"
      ]
    },
    {
      "id": "FM-DOTNET-070-counselor-alerts",
      "status": "completed",
      "goal": "Phase-C slice 4: counselor alerts GET /me/alerts + PUT /me/alerts/:id/read (counselor.ts) behind dark flag FORMMAPS_ROUTE_COUNSELOR_ALERTS_TO_DOTNET (two paths, one flag). Permission alerts:read. ICounselorAlertsRepository: GET read-only list (caseload-scoped, paged, real COUNT, createdDate DESC + id tie-break, optional ?studentId + ?unreadOnly); PUT writable+commit mark-read (AlertNotFound->404 'Alert not found'; NotAssigned->404 'Not found'; Ok). 🔒 Federico-RATIFIED IDOR fold: the legacy ?studentId override that bypassed caseload scoping is now caseload-scoped (non-caseload studentId -> empty). Fresh reviewer SHIP; IDOR fold verified fully closed.",
      "dependsOn": [
        "FM-DOTNET-069-counselor-availability"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/counselor.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Counselor/ICounselorAlertsRepository.cs",
        "services/api/src/FormMaps.Infrastructure/Counselor/CounselorAlertsRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CounselorAlertsEndpoints.cs"
      ]
    },
    {
      "id": "FM-DOTNET-071-counselor-sessions",
      "status": "completed",
      "goal": "Phase-C slice 5: counselor sessions GET /me/sessions + PUT /me/sessions/:id/complete (counselor.ts) behind dark flag FORMMAPS_ROUTE_COUNSELOR_SESSIONS_TO_DOTNET (two paths, one flag). Permission counselor:sessions. ICounselorSessionsRepository: GET read-only list (own+active, paged, real COUNT, startTime DESC + id tie-break, optional ?status!=all, LEFT JOIN users studentName, verbatim calendarEventIds jsonb, all cols + nested student{name}); complete writable+commit (ownership→NotYourSession→403 'Not your session'; notes counselorNotes??notes??'' nullish→non-string ''→slice5000; malformed body→500 before ownership). PUT /me/sessions/:id/cancel DELIBERATELY NOT ported (syncRecordSafe calendar side-effect stays Node). Fresh reviewer SHIP.",
      "dependsOn": [
        "FM-DOTNET-070-counselor-alerts"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/counselor.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Counselor/ICounselorSessionsRepository.cs",
        "services/api/src/FormMaps.Infrastructure/Counselor/CounselorSessionsRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CounselorSessionsEndpoints.cs"
      ]
    },
    {
      "id": "FM-DOTNET-072-counselor-notes",
      "status": "completed",
      "goal": "Phase-C slice 6: counselor notes CRUD (5 endpoints of counselor.ts) behind one dark flag FORMMAPS_ROUTE_COUNSELOR_NOTES_TO_DOTNET (three rewrites, path-not-method). Auth asymmetry: GET/POST /students/:studentId/notes + DELETE /notes/:noteId use an INLINE raw-role check (counselor/school_admin/Super Admin) with counselor-only ensureCounselorStudentAccess 404, DELETE author-bypass only when caller is a counselor (school_admin/Super Admin delete any); PUT /notes/:noteId + PUT /notes/:noteId/complete-followup use permission counselor:notes + author-ownership (403 'Not authorized'). ICounselorNotesRepository: GET read-only paged list (studentId+active, optional type filter, real COUNT, createdDate DESC + id tie-break, JOIN users authorName, dual author{name}+authorName emission); create/update/soft-delete/complete-followup writable+commit (ownership + write in one session, atomic). Faithful JS-|| create defaults + Prisma type-500s; PUT partial update with DEFERRED InvalidBody (non-owner + bad-type body -> 403 not 500); @updatedAt bumped on soft-delete + complete-followup; tags text[] verbatim; Kind=Unspecified+ms-trunc writes; ISO-Z reads; mass-assignment fixed columns. Fresh reviewer SHIP (no BLOCKING/HIGH/MEDIUM; LOWs = slice-wide accepted divergences / pathological / determinism superset).",
      "dependsOn": [
        "FM-DOTNET-071-counselor-sessions"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/counselor.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Counselor/ICounselorNotesRepository.cs",
        "services/api/src/FormMaps.Infrastructure/Counselor/CounselorNotesRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CounselorNotesEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 681, integration 1355 (+2 skip; +52 new: 24 endpoint guard/coercion/mapping [faked repo] + 28 Testcontainers repo incl. createdDate/id tie-break + role-asymmetry soft-delete + deferred InvalidBody after ownership), contract 1 - full suite green. New Testcontainers harness counselor-notes-schema.sql (users + counselor_student_assignments + counselor_notes). Staging deploy run 30047880099 SUCCESS + anon canary GREEN (health 200; all 5 note routes anon->401 missing_identity). Merged --no-ff to main 75eec6b. counselor.ts is now COMPLETE on the .NET side except onboarding (Node email/token), cancel (Node calendar-sync), ai-briefing (Bedrock/Node).",
      "commit": "75eec6b"
    },
    {
      "id": "FM-DOTNET-073-student-portfolio",
      "status": "completed",
      "goal": "Phase-C: student portfolio CRUD (5 endpoints of routes/student.ts + studentService.ts) behind one dark flag FORMMAPS_ROUTE_STUDENT_PORTFOLIO_TO_DOTNET (three rewrites, path-not-method; /portfolio/summary precedes /portfolio/:id). Self-scoped (req.userId) — RequireIdentity + RLS, no role/permission. GET /portfolio (paged list + ?type), GET /portfolio/summary (byType/hours/skills/categories aggregate), POST /portfolio (zod-validated create, 201), PUT /portfolio/:id (zod partial + bounded() slice, ownership 404), DELETE /portfolio/:id (soft delete). New StudentPortfolio namespace: IStudentPortfolioRepository + repo + StudentPortfolioValidation (Zod-parity first-error message in schema order, mirrors TestScoreValidation). Faithful: hoursPerWeek/totalHours Decimal(65,30) -> decimal.js-normalized string on the row (trim_scale::text) but ::double sum in the summary; create JS-|| defaults (type present-'' -> 'activity'; enum omit-when-absent -> 'other'); update bounded() per-field slice (observable type>50); @updatedAt bump; attachments verbatim jsonb; activityCategory enum cast; express.json({strict}) malformed/primitive body -> 500, array -> zod 'Expected object, received array' -> 400 (folded from fresh review). Fresh reviewer SHIP (no BLOCKING/HIGH/MEDIUM; folded primitive-body-500; LOWs = decimal.js exponential for non-physical extremes / jsonb numeric normalization / byType-skills determinism superset / dup-key last-vs-first, all pathological or documented).",
      "dependsOn": [
        "FM-DOTNET-072-counselor-notes"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/student.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/studentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentPortfolio/IStudentPortfolioRepository.cs",
        "services/api/src/FormMaps.Application/StudentPortfolio/StudentPortfolioValidation.cs",
        "services/api/src/FormMaps.Infrastructure/StudentPortfolio/StudentPortfolioRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/StudentPortfolioEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 704 (+23 StudentPortfolioValidation), integration 1383 (+2 skip; portfolio repo Testcontainers + endpoint faked-repo), contract 1 - full suite green. New Testcontainers harness student-portfolio-schema.sql (hand-written from schema.prisma; incl. the StudentActivityCategory enum type — prod db-push drift, no committed migration). Staging deploy run 30052499380 SUCCESS + anon canary GREEN (health 200; all 5 portfolio routes anon->401 missing_identity). Merged --no-ff to main ce59409. Opens student.ts; NEXT = student.ts applications/community-service/parents-link reads + writes -> parent.ts.",
      "commit": "ce59409"
    },
    {
      "id": "FM-DOTNET-074-student-applications",
      "status": "completed",
      "goal": "Phase-C: student applications CORE CRUD (6 endpoints of routes/student.ts + studentService.ts) behind one dark flag FORMMAPS_ROUTE_STUDENT_APPLICATIONS_TO_DOTNET (three rewrites; /applications/deadlines precedes /applications/:id). Self-scoped (req.userId) — RequireIdentity + RLS. GET /applications (createdDate desc), GET /applications/deadlines (deadline not null, deadline asc), GET /applications/:id (ownership+active 404), POST /applications (zod createApplicationSchema, 201), PUT /applications/:id (RAW body + bounded() partial, no zod), DELETE /applications/:id (soft delete -> {deleted:true}). New StudentApplications namespace: IStudentApplicationRepository + repo + StudentApplicationValidation (Zod-parity, first-error message in schema order, .default() on type/column). POST zod; PUT raw + Prisma type-500 DEFERRED past ownership (non-owner+bad body -> 404 not 500, atomic). matchScore is Int? validated as z.number() -> non-integer OR out-of-int32-range 500s at the column. column/appStatus enum cast (ApplicationColumn committed; CollegeAppStatus + fitClassification/applicationDeadline/deadlineType/universityId/appStatus = prod db-push drift, harness DDL hand-written). express.json({strict}) malformed/primitive -> 500, array -> zod 400 (POST) / empty update (PUT). The 3-seg sub-resource paths (essays/checklist/classify/ai-review) NOT swallowed by :id -> stay Node (essays+checklist = later slices; AI = Bedrock). Fresh reviewer FIXES-NEEDED -> folded: #2 BLOCKING (PUT matchScore int4-overflow was (int)-wrapped+silently-written+200 -> now rejected -> 500) + #1 LOW (POST non-string column -> zod invalid_type 'Expected ..., received <type>' not invalid_enum_value). #3 (anon+malformed 401-vs-500) = universal express.json-before-auth divergence, not folded. NOTE: enum invalid_type gap also in portfolio/testscore/question360 (future consistency pass).",
      "dependsOn": [
        "FM-DOTNET-073-student-portfolio"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/student.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/studentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentApplications/IStudentApplicationRepository.cs",
        "services/api/src/FormMaps.Application/StudentApplications/StudentApplicationValidation.cs",
        "services/api/src/FormMaps.Infrastructure/StudentApplications/StudentApplicationRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/StudentApplicationEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 719 (+15 StudentApplicationValidation), integration 1412 (+2 skip; applications repo Testcontainers + endpoint faked-repo incl. int32-overflow + enum-message folds), contract 1 - full suite green. New Testcontainers harness student-applications-schema.sql (hand-written; ApplicationColumn + CollegeAppStatus enums). Staging deploy run 30056599381 SUCCESS + anon canary GREEN (health 200; all 6 core routes anon->401; /applications/x/essays -> 404 confirming :id does not swallow the 3-seg essay sub-path). Merged --no-ff to main 9d4b5f9. NEXT = student.ts community-service CRUD -> parents-link reads/writes -> essays/checklist (non-AI) -> parent.ts.",
      "commit": "9d4b5f9"
    },
    {
      "id": "FM-DOTNET-075-community-service",
      "status": "completed",
      "goal": "Phase-C: student community-service CRUD (4 endpoints of routes/student.ts + studentService.ts) behind one dark flag FORMMAPS_ROUTE_STUDENT_COMMUNITY_SERVICE_TO_DOTNET (two rewrites; /community-service/:id + exact /community-service). Self-scoped (req.userId) — RequireIdentity + RLS. GET /community-service = computed envelope {data: entries, totalHours, totalHoursRequired} (schoolId-scoped when user has a school, date DESC, Sigma hours, serviceHoursRequired ?? 0), POST (zod create, 201; NoSchool -> 400 'No school'), PUT /community-service/:id (zod partial, owner+isActive+status=='pending' gate -> 404), DELETE /community-service/:id (same gate, -> {data:null}). New CommunityService namespace: ICommunityServiceRepository + repo + CommunityServiceValidation (Zod-parity, first-error message in schema order). NEW parity surfaces: z.string().email() (reuses ExternalEmailNormalization zod-v3 regex), date .refine(valid && non-future) via injected TimeProvider (Date.now()), update top-level .refine('At least one field is required') firing only when no field present (field errors win). hours Decimal -> trim_scale::text string on row + ::double sum for totalHours; CommunityServiceStatus enum cast; edit/delete atomic gate+write one session; express.json({strict}) body handling. Fresh reviewer SHIP (no HIGH/MEDIUM/security; no IDOR, mass-assignment-guarded; LOWs = whitespace-only-body 500-vs-400 [IsNullOrWhiteSpace convention across all slices] / empty-vs-NULL schoolId [FK-precluded] / >29-digit hours GetDecimal [shared behavior] / id tie-break determinism superset / tz-less datetime UTC-safe — all pathological or documented).",
      "dependsOn": [
        "FM-DOTNET-074-student-applications"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/student.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/studentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/CommunityService/ICommunityServiceRepository.cs",
        "services/api/src/FormMaps.Application/CommunityService/CommunityServiceValidation.cs",
        "services/api/src/FormMaps.Infrastructure/CommunityService/CommunityServiceRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CommunityServiceEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 734 (+15 CommunityServiceValidation), integration 1433 (+2 skip; community-service repo Testcontainers + endpoint faked-repo), contract 1 - full suite green. New Testcontainers harness community-service-schema.sql (users + schools + community_service_entries + CommunityServiceStatus enum). Staging deploy run 30062214232 SUCCESS + anon canary GREEN (health 200; all 4 routes anon->401). Merged --no-ff to main c28a8b5. NEXT = student.ts parents-link reads/writes -> essays/checklist (non-AI) -> parent.ts (child-link-scoped).",
      "commit": "c28a8b5"
    },
    {
      "id": "FM-DOTNET-076-student-parents",
      "status": "completed",
      "goal": "Phase-C: student parent-links CRUD (4 endpoints of routes/student.ts + studentService.ts) behind one dark flag FORMMAPS_ROUTE_STUDENT_PARENTS_TO_DOTNET (four rewrites; /parents/invite precedes /parents/:parentLinkId). Self-scoped (req.userId) — RequireIdentity + RLS. GET /parents (list active links, createdDate desc), POST /parents/invite (mint link+token -> {id, invitationUrl}, 201), DELETE /parents/:parentLinkId (unlink, ownership-only gate -> 404 'Link not found'), POST /parents/:parentLinkId/resend (regenerate token -> {invitationUrl}). NOT SES-coupled (invite/resend only create/refresh an invitationToken + return a frontend invitationUrl — no email; earlier SES flag was wrong). New StudentParents namespace: IStudentParentRepository + repo. Token via InvitationTokenGenerator (crypto.randomBytes(32) base64url); tokenExpiresAt = now+48h. Invite body RAW (no zod): parentEmail required (falsy->400 'parentEmail required'; truthy non-string->500 reproducing .toLowerCase() throw); parentName||'' / relation||'parent' (truthy non-string->500 at Prisma String). unique (studentId, parentEmail) -> duplicate caught as Postgres 23505 -> Duplicate -> 500 (right {success,message} shape). delete/resend gate = ownership-ONLY (no isActive check, matches legacy). invitationUrl = FRONTEND_BASE_URL || app.formmaps.ai. Fresh reviewer SHIP (no BLOCKING/HIGH/MEDIUM/security; no IDOR, mass-assignment-guarded, atomic gate+write; verified express global-500-on-body-error, base64url token, ownership-only gate, raw-passthrough invitationToken faithful; one LOW = Content-Type raw-body-read convention across all slices). Folded reviewer coverage note (delete already-inactive owned link -> still true).",
      "dependsOn": [
        "FM-DOTNET-075-community-service"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/student.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/studentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentParents/IStudentParentRepository.cs",
        "services/api/src/FormMaps.Infrastructure/StudentParents/StudentParentRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/StudentParentEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 734, integration 1458 (+2 skip; parents repo Testcontainers incl. unique-dup + token regen + endpoint faked-repo), contract 1 - full suite green. New Testcontainers harness student-parents-schema.sql (student_parent_links + unique (studentId, parentEmail)). Staging deploy run 30063920868 SUCCESS + anon canary GREEN (health 200; all 4 routes anon->401). Merged --no-ff to main 89e8feb. student.ts REMAINING = essays (5 ep, 1 AI stays Node) + checklist (4 ep, 1 AI stays Node) + classify (AI, Node). NEXT = essays/checklist non-AI OR parent.ts (child-link-scoped).",
      "commit": "89e8feb"
    },
    {
      "id": "FM-DOTNET-077-student-essays-checklist",
      "status": "completed",
      "goal": "Phase-C: student application ESSAYS + CHECKLIST CRUD (6 endpoints of routes/student.ts + studentService.ts, the non-AI application sub-resources) behind ONE dark flag FORMMAPS_ROUTE_STUDENT_ESSAYS_CHECKLIST_TO_DOTNET (4 rewrites). Gated by verifyAppOwnership (parent app.studentId == caller). GET+POST /applications/:id/essays, PUT /applications/:id/essays/:eid, GET+POST /applications/:id/checklist, PUT /applications/:id/checklist/:cid. New StudentApplicationSubResources namespace: one IApplicationSubResourceRepository + repo + one endpoints class (both sub-resources = one cohesive slice). AI siblings stay Node (Bedrock): POST .../essays/:eid/ai-review is a 5-seg path excluded by segment count; POST .../checklist/generate collides with /checklist/:cid (same single-trailing-segment shape) so :cid carries a ((?!generate)[^/]+) negative-lookahead (FM-061 pattern; cids are UUIDs). Parity: POST required-field (title/itemName) JS-truthy gate -> 400 '<field> is required' BEFORE ownership; every other type mismatch = Prisma reject -> 500 DEFERRED past ownership (create) and past ownership AND the sub-resource existence 404 (update). Two dueDate resolvers: create 'x ? new Date(x) : null' (falsy->null, string parse, number=epoch ms, true=new Date(1), object/array->500) vs update = raw value Prisma coerces as ISO string only (null->NULL, string parse, number/bool->500). Create coalescing prompt/notes ||null, category ||'other', wordLimit ||null (0->null on CREATE); update wordLimit no || (0 stored). Essay update bounded() slices {title:200,prompt:2000,currentDraft:50000,status:50,dueDate:50} + draftVersion bump on currentDraft change (raw compare, ||0); checklist update raw (NO bounded) + completedAt set/clear on isCompleted transition. Fixed-column INSERT/SET (mass-assignment guard); ownership at parent-app level under RLS; timestamps Kind=Unspecified+ms-trunc; response = raw Prisma passthrough. Fresh reviewer SHIP (no BLOCKING/HIGH/MEDIUM; both 404 gates verified before the deferred 500; no IDOR). Two LOWs ACCEPTED no folds (precedent-consistent with the shipped FM-065/072 create-date resolver): single-element-array dueDate on CREATE (port 500s but JS new Date([str]) ToPrimitive->valid date; pathological, future consistency pass) + tz-less datetime string AssumeUniversal-UTC vs JS local (documented, UTC-safe).",
      "dependsOn": [
        "FM-DOTNET-076-student-parents"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/student.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/studentService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentApplicationSubResources/IApplicationSubResourceRepository.cs",
        "services/api/src/FormMaps.Infrastructure/StudentApplicationSubResources/ApplicationSubResourceRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/StudentApplicationSubResourceEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 736, integration 1519 (+2 skip; 61 new tests = endpoint faked-repo + repo Testcontainers incl. ownership/deferred-500/bounded-slice/draftVersion-bump/completedAt-transition), contract 1 - full suite green. New Testcontainers harness application-subresources-schema.sql (student_applications parent + application_essays + application_checklists). Staging deploy run 30066222653 SUCCESS + anon canary GREEN (health 200; all 6 routes anon->401; ai-review->404, checklist/generate->405 on direct-.NET = the negative-lookahead validated). Merged --no-ff to main 2ce3de3. student.ts non-AI surface COMPLETE (portfolio + applications + community-service + parents + essays + checklist). NEXT = parent.ts (child-link-scoped, IDOR corpus #1, ~395 LOC).",
      "commit": "2ce3de3"
    },
    {
      "id": "FM-DOTNET-078-parent-portal",
      "status": "completed",
      "goal": "Phase-C: parent portal SELF-SCOPED surface (6 authenticated caller-owned handlers of routes/parent.ts) behind ONE dark flag FORMMAPS_ROUTE_PARENT_PORTAL_TO_DOTNET (6 rewrites). GET /profile ({...user, children}: absent user -> just {children}; children = accepted+active links inner-joined to child user, dropping orphans, shape {studentId, studentName, gradeLevel, relationship:l.relation}), GET /notifications (page=Math.max(1,parseInt||1)/limit=Math.min(50,Math.max(1,parseInt||20)), unreadOnly=='true' literal, where{userId,isActive}, createdDate desc, envelope {data:{data,total,page,limit}}), PUT /notifications/:id/read (findUnique no-isActive -> null|not-owned -> 403 'Access denied' uniform; else isRead+readAt; {success}), PUT /notifications/read-all (updateMany {userId,isRead:false} NO isActive -> {updatedCount}), GET /evaluations/pending (DB email not token, !email->[], evaluatorEmail==email.toLowerCase()+isActive+!completed, {evaluationId, studentName: name||'your student', deadline:tokenExpiryDate, token:invitationToken}), DELETE /:parentLinkId (findUnique no-isActive -> null|(parentUserId!=caller AND studentId!=caller) -> 403 'Access denied'; else isActive=false; dual-party ownership). New ParentPortal namespace (IParentPortalRepository + repo + endpoints). STAYS NODE: onboarding verify+complete (auth-cookie/JWT/password write), invite + resend (SES email), and the child-link child reads /children/:id/progress + course-plan (IDOR corpus #1, compute-heavy + runAsSystem = FM-079). The DELETE /:parentLinkId single-seg catch-all rewrite carries a ((?!profile|notifications|invite|onboarding|children|evaluations)[^/]+) negative-lookahead (keeps POST /invite on Node; parentLinkIds are UUIDs). Fixed-column writes, reads read-only / writes writable+commit, timestamps Kind=Unspecified+ms-trunc, id tie-break deterministic superset. Fresh reviewer (read all files) SHIP (no BLOCKING/HIGH/MEDIUM; 4 LOWs = order-superset / extra code on 401 / null-field JSON [confirmed no global WhenWritingNull] / RLS-parity established - no folds).",
      "dependsOn": [
        "FM-DOTNET-077-student-essays-checklist"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/parent.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/ParentPortal/IParentPortalRepository.cs",
        "services/api/src/FormMaps.Infrastructure/ParentPortal/ParentPortalRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/ParentPortalEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 736, integration 1551 (+2 skip; 32 new tests = endpoint faked-repo + repo Testcontainers incl. profile children-join + dual-party delete IDOR + pending email-scope + pagination clamp), contract 1 - full suite green. New Testcontainers harness parent-portal-schema.sql (users + student_parent_links + notifications + evaluation_groups). Staging deploy run 30068224591 SUCCESS + anon canary GREEN (health 200; all 6 routes anon->401; invite->405 [direct-.NET, lookahead prevents rewrite], onboarding/children/resend->404). Merged --no-ff to main f863b92. NEXT = FM-079 = child-link child reads /children/:studentId/progress (link->403 'Not linked', GPA/credits/assessment badges) + /children/:studentId/course-plan (link->404, runAsSystem plan/target/coursePlan).",
      "commit": "f863b92"
    },
    {
      "id": "FM-DOTNET-079-parent-child-reads",
      "status": "completed",
      "goal": "Phase-C: parent child-link-scoped reads (2 authenticated CROSS-USER handlers of routes/parent.ts) behind ONE dark flag FORMMAPS_ROUTE_PARENT_CHILD_READS_TO_DOTNET (2 rewrites). GET /children/:studentId/progress + GET /children/:studentId/course-plan. IDOR corpus #1: both gated by an accepted+active StudentParentLink (parentUserId==caller, isActive, isAccepted). New ParentChildReads namespace (IParentChildReader + reader + endpoints). COMPLETES parent.ts on .NET (only onboarding + invite/resend SES stay Node). Parity: progress link->403 'Not linked to this student' vs student-missing->404 'Student not found' (link check first); course-plan collapses both to 404 (existence-hiding). TWO RLS sessions: progress reads on the caller Identity session (legacy tenantContext, NO runAsSystem); course-plan link-check on Identity then plan/target/course-plan on a System (RLS-bypass) session (legacy runAsSystem). Compute: GPA Round2 (SchoolAnalyticsMath.JsRound(x*100)/100) over A..F gradeMap (unknown/empty grade -> no point); GPA NOT school-gated (school-less students get a GPA - caught+fixed a bug where GPA was nested inside if(schoolId)); creditsEarned school-gated raw sum; creditsRequired = current-AY active rule set totalCreditsRequired (two-step findFirst) else 120; percentage = Math.round(earned/required*100) NO 100 cap; mil averageScore = round(avg scorePercentage); pca = any pca_evaluations row (no isActive); eval360 active-total+completed; isOnTrack = gpa==null||gpa>=2.0. Decimal->double = Number(); ISO-Z dates. Fresh reviewer (read all files) SHIP (no BLOCKING/HIGH/MEDIUM; 3 LOWs = progress-under-Identity-RLS faithful / int-cast-safe / plan tie-break faithful - no folds). CUTOVER-VERIFY: confirm a real linked parent sees real progress data before flipping (prod RLS on a school-less parent's assessment/grade reads).",
      "dependsOn": [
        "FM-DOTNET-078-parent-portal"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/parent.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/ParentChildReads/IParentChildReader.cs",
        "services/api/src/FormMaps.Infrastructure/ParentChildReads/ParentChildReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/ParentChildReadEndpoints.cs"
      ],
      "verification": "dotnet build 0/0; unit 736, integration 1568 (+2 skip; 34 new tests = endpoint faked-reader + reader Testcontainers incl. link IDOR gate + GPA/credit/badge compute + no-100-cap + school-less-GPA + course-plan System reads), contract 1 - full suite green. New Testcontainers harness parent-child-reads-schema.sql (12 tables). Staging deploy run 30070091125 SUCCESS + anon canary GREEN (both routes anon->401; FM-078 parent siblings still 401 = no regression). Merged --no-ff to main 359b50e. parent.ts COMPLETE on .NET. NEXT = new domain: Phase D (academic-gaps.ts / college.ts / course-planning) OR deferred school.ts/school-courses.ts writes.",
      "commit": "359b50e"
    },
    {
      "id": "FM-DOTNET-080-academic-gaps-reads",
      "status": "completed",
      "goal": "Phase-D OPENER (domain 6): the 3 non-AI reads of routes/academic-gaps.ts behind ONE dark flag FORMMAPS_ROUTE_ACADEMIC_GAPS_TO_DOTNET (3 rewrites), mounted /api/v1/school-admin/academic-gaps: GET /summary, GET /students/:studentId, GET /recommendations/:studentId. The 4th route GET /ai-recommendations/:studentId (Bedrock) STAYS Node (distinct literal, not rewritten, keeps aiLimiter). New AcademicGaps namespace: pure AcademicGapsComputer (ports lib/gradMatch.courseMatchesCategory + lib/gradStatus.creditDeficitStatus + each endpoint's per-student gap math) + AcademicGapsReader (read-only RLS SQL loads) + endpoints. New permission grades:read (coarse pre-filter, 403 'Insufficient permissions') + getUserAndSchool rail: caller's own schoolId null->400 'No school linked' (BEFORE role check), roleName.ToLowerInvariant() not in {school_admin,counselor}->403 'Forbidden' (RAW lowercase compare, not a normalizer); counselor scoped to active assignments. 404 collapse: student missing/wrong-school/counselor-unassigned all->404 'Student not found'. PARITY: two DELIBERATE per-endpoint credit divergences preserved (summary earned adds Number(g.credits) even when the grade's course is MISSING; detail totalCreditsEarned SKIPS a missing-course grade via `if(!course)continue`) + elective fallback applied ONLY in detail's catCredits (summary missing-count + recommendations do NOT). Asymmetric empty shapes: /summary no-AY/no-ruleSet/no-students -> {data:[]} NO summary key; /students no-rules -> 3-field {gaps:[],creditsEarned:0,creditsRequired:0} NO studentId/name/gradeLevel (vs happy path 6-field); /recommendations no-rules -> {recommendations:[]}. progressPercent = min(100, JsRound(earned/totalRequired*100)); status buckets on_track/off_track(strict >30%)/at_risk; missing-count = categories short (no elective fallback, missing-course grade skipped); recommendations = per short category first-3 available (not completed, courseMatchesCategory) with reason `Helps fill ${JsNumber.ToJsonNumber(shortfall)} credit shortfall in ${category}`. Credits ::double precision (Number()-ified); dead-code areaStats (TS never returns it) correctly omitted. Determinism ORDER BY supersets over legacy unordered queries (categories sortOrder ASC,id ASC; courses code ASC,id ASC; students id ASC).",
      "dependsOn": [
        "FM-DOTNET-079-parent-child-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/academic-gaps.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/gradMatch.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/gradStatus.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/AcademicGaps/AcademicGapsModels.cs",
        "services/api/src/FormMaps.Application/AcademicGaps/IAcademicGapsReader.cs",
        "services/api/src/FormMaps.Application/AcademicGaps/AcademicGapsComputer.cs",
        "services/api/src/FormMaps.Infrastructure/AcademicGaps/AcademicGapsReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/AcademicGapsEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 48 new tests (18 computer unit + 12 endpoint faked-reader + 18 reader Testcontainers incl. scope/counselor-scoping/404-cases/HasRules-early-outs/status='active' course filter). Full suite green: unit 754, integration 1598 (+2 skip), contract 1. New Testcontainers harness academic-gaps-schema.sql (7 tables incl. category_requirements - first use). Fresh parity reviewer (17 tool uses, read all 8 files) SHIP - all 12 checkpoints faithful, only accepted determinism ORDER BY supersets. Staging deploy run 30081231364 SUCCESS + anon canary GREEN (health 200; 3 reads anon->401; ai-recommendations->404 = NOT .NET-served, stays Node; root->404). Merged --no-ff to main b2fbe9b. Dark (flag off). NEXT Phase-D = college.ts (~395, college-fit Decimal CRUD) / course-planning / superscore, OR the deferred school.ts/school-courses.ts writes (iSAMS write, staff-invite [needs .NET SES], logo->S3).",
      "commit": "8eab1b5"
    },
    {
      "id": "FM-DOTNET-081-college-applications",
      "status": "completed",
      "goal": "Phase-D college.ts Feature 1 (sub-slice 1 of 3): college applications CRUD behind ONE dark flag FORMMAPS_ROUTE_COLLEGE_APPLICATIONS_TO_DOTNET (2 rewrites, co-flip path-not-method), mounted /api/v1/college: GET+POST /students/:studentId/applications, PUT+DELETE /applications/:id. Mount = authenticate + tenantContext, NO subscription -> RequireIdentity only. Establishes the shared ICollegeAccessResolver (getStudentAccess rail, reused by the pending FM-082/083 college sub-slices): FRESH users read of the caller's own {schoolId,roleName} (NOT the JWT); schoolId null -> deny; role.ToLowerInvariant() sequential (NOT else-if) guards: student->self-only, counselor->active counselor_student_assignments, school_admin->target shares caller school, role not in {student,counselor,school_admin,super admin}->deny; super admin unrestricted. Every caller collapses ANY access failure to a uniform 404 'Not found' (legacy discards getStudentAccess's internal 400/403/404). PARITY: PUT/DELETE do findUnique {id,isActive:true} -> 404 'Application not found' (DISTINCT message) BEFORE the access 404 'Not found'. List = REDUCED shape (collegeName=name, deadlineDate=applicationDeadline) with checklistCount/essaysCount UNFILTERED by isActive (Prisma _count has no filter) via correlated subqueries; createdDate DESC (+id tie-break); studentId+isActive scope. Create: required collegeName||universityId (JS-truthy) -> 400 AFTER access; name = universities lookup (uni.name) else collegeName, JS-|| 'Unknown'; type='college'; statusToColumn map; appStatus stored RAW (appStatus||'researching') -> invalid enum / truthy-non-string -> 500; x?new Date(x):null create date resolver; deadlineType/fitClassification/universityId = x||null (empty-string falsy->null, truthy-non-string->500). Update: present-keys-only RAW, NOT bounded (college.ts does not slice); String? cols null->NULL, string(incl '')->value, non-string->500; appStatus raw enum-500; column-sync when appStatus is a truthy statusToColumn key; type-500 DEFERRED past the existence(404)+access(404) gates. Delete: existence->404 / access->404 / soft-delete / returns {success:true} ONLY (no data). AI-review/generate and college.ts Features 2 (search/list/favorites) + 3 (essays/comments) are NOT in this slice (later sub-slices / stay Node). Timestamps Kind=Unspecified+ms-trunc (ToStored) bind to timestamp cols; the resolver hands Kind=Utc -> relabel.",
      "dependsOn": [
        "FM-DOTNET-080-academic-gaps-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/college.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/College/CollegeApplicationsModels.cs",
        "services/api/src/FormMaps.Application/College/ICollegeAccessResolver.cs",
        "services/api/src/FormMaps.Application/College/ICollegeApplicationsRepository.cs",
        "services/api/src/FormMaps.Infrastructure/College/CollegeAccessResolver.cs",
        "services/api/src/FormMaps.Infrastructure/College/CollegeApplicationsRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CollegeApplicationEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 34 new tests (7 access-resolver Testcontainers [student-self/no-school/missing-caller/counselor-assignment/school_admin-same-school/super-admin/unknown-null-role] + 6 repository Testcontainers [reduced list + unfiltered counts + createdDate DESC/scope, name-lookup->fallback->Unknown, enum cast + resolved date, FindActiveOwner, present-only update + column-sync + NOT bounded + null-set, soft-delete] + 14 endpoint faked [anon 401, uniform access 404, distinct 'Application not found', 400 required, statusToColumn Theory, defaults + deferred-500 + invalid-enum + non-string + bad-date, falsy-deadlineDate null, malformed/primitive 500, update 404/access-404/deferred-500/200 + column-sync, delete {success:true}-only]). Full suite green: unit 754, integration 1632 (+2 skip), contract 1. New Testcontainers harness college-applications-schema.sql (student_applications + users + counselor_student_assignments + application_checklists + college_essays + universities). Fresh parity reviewer (21 tool uses, read all files + TS spec) SHIP - no BLOCKING/HIGH/MEDIUM; 2 LOWs pre-existing/inherited (shared FM-065/077 date-resolver epoch-range edge; FM-074 row shape). Mid-build caught+fixed 2: Kind=Utc->timestamp bind (ToStored normalizer) + endpoint-test factory early-dispose. Staging deploy run 30085153508 SUCCESS + anon canary GREEN (health 200; 4 endpoints anon->401; college.ts siblings /search + /students/:id/list + /essays/:id/comments -> 404 = NOT .NET-served, stay Node; x-formmaps-service header). Merged --no-ff to main b06c439. Dark (flag off). NEXT college.ts sub-slices: FM-082 Search+Favorites (GET /search + /students/:id/list + POST/PUT/DELETE /list/:id; University Decimal->number, favorite upsert-reactivate + 409) -> FM-083 Essays+Comments (collegeEssay + essayComment; wordCount JS quirk). Both reuse ICollegeAccessResolver.",
      "commit": "69fafaf"
    },
    {
      "id": "FM-DOTNET-082-college-search-favorites",
      "status": "completed",
      "goal": "Phase-D college.ts sub-slice 2/3: college SEARCH + FAVORITES behind ONE dark flag FORMMAPS_ROUTE_COLLEGE_FAVORITES_TO_DOTNET (3 rewrites, co-flip path-not-method), mounted /api/v1/college: GET /search (NO access gate - any authenticated caller), GET+POST /students/:studentId/list, PUT+DELETE /list/:id. Favorites reuse the FM-081 ICollegeAccessResolver (any access failure -> uniform 404 'Not found'). SEARCH: dynamic WHERE (isActive=true + name ILIKE '%q%' [Prisma contains-insensitive, wildcards NOT escaped] + state= + acceptanceRate >=min/<=max); minAdmRate/maxAdmRate = JS parseFloat (ported prefix-parse incl. NaN/Infinity; a present-empty query param is JS-falsy -> filter dropped); 17 select fields - acceptanceRate Decimal->number (::double precision), sat*/act*/studentCount Int?->number, tuition Json->jsonb passthrough (JsonElement), rest strings; take 20, name ASC. FAVORITES LIST: INNER JOIN universities (Prisma include, no university.isActive filter), where userId+isActive, createdDate DESC (+id tie-break); response = full 11-field favorite scalar row + nested university{10-field subset}. POST add-to-list: access -> 400 'universityId required' (JS-truthy, AFTER access) -> non-string universityId -> 500 (Prisma at findUnique) -> findUnique on unique (userId,universityId) -> existing&&isActive -> 409 'Already in list' (SHORT-CIRCUITS before the fit type-check) -> else reactivate(isActive=true,fit-if-present,updatedBy) / create(userId,universityId,fit,createdBy) -> 201. The create-vs-reactivate fitClassification asymmetry: CREATE absent-fit -> NULL (Prisma default); REACTIVATE absent-fit -> UNCHANGED (Prisma undefined skips) - modeled via a hasFit flag; a non-string fit on the WRITE path -> 500 (but 409 wins first). PUT /list/:id: findUnique {id,isActive:true} -> 404 'Not found' -> access(fav.userId) -> 404 -> update fitClassification (raw: null->NULL, string(incl '')->value, non-string->500 DEFERRED past the 404s; absent->unchanged) + updatedBy -> full row. DELETE: 404 / 404 / soft-delete / returns {success:true} ONLY (no data). Timestamps Kind=Unspecified+ms-trunc; ISO-Z. Features 1 (applications=FM-081) done; Feature 3 (essays/comments) = FM-083 next.",
      "dependsOn": [
        "FM-DOTNET-081-college-applications"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/college.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/College/CollegeFavoritesModels.cs",
        "services/api/src/FormMaps.Application/College/ICollegeFavoritesRepository.cs",
        "services/api/src/FormMaps.Infrastructure/College/CollegeFavoritesRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CollegeFavoritesEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 23 new tests (5 repository Testcontainers [search filters+Decimal->number+tuition jsonb+name ASC, favorites join+scope+DESC, add create/reactivate/409, reactivate-preserves-fit, FindOwner+UpdateFit+SoftDelete] + 14 endpoint faked [anon 401, search 200+parseFloat filter parse, empty-filter null, list access-404, add 400/non-string-500/409/InvalidBody-500/201, put 404/access-404/deferred-500/200, delete {success:true}, malformed/primitive 500]). New Testcontainers harness college-favorites-schema.sql (universities + university_favorites w/ unique (userId,universityId)). Full suite green: unit 754, integration 1655 (+2 skip), contract 1. Fresh parity reviewer (19 tool uses, read all files + TS + query.ts + Prisma models) SHIP - no BLOCKING/HIGH/MEDIUM; 3 LOWs all pathological/convention (bracket-notation query-param coercion; parseFloat-NaN acceptanceRate filter [Prisma gte:NaN behavior = a documented pre-cutover live probe]; DELETE/GET malformed-body = the universal auth-first/body convention). Mid-build caught+fixed a test-data bug (two favorites violating the unique (userId,universityId) - impossible in reality). Staging deploy run 30087856552 SUCCESS + anon canary GREEN (health 200; 5 endpoints anon->401; /essays/:id/comments -> 404 = Feature-3 NOT .NET-served, stays Node; x-formmaps-service header). Merged --no-ff to main a713e5e. Dark (flag off). NEXT college.ts = FM-083 Essays+Comments (GET+POST /students/:id/essays, PUT+DELETE /essays/:id, POST+GET /essays/:id/comments; collegeEssay + essayComment; wordCount JS content.trim().split(/\\s+/).length quirk [whitespace-only->1]; comment author join) - COMPLETES the college.ts mini-phase.",
      "commit": "380a0d9"
    },
    {
      "id": "FM-DOTNET-083-college-essays-comments",
      "status": "completed",
      "goal": "Phase-D college.ts Feature 3 (sub-slice 3 of 3, COMPLETES the college.ts mini-phase): college essays + comments behind ONE dark flag FORMMAPS_ROUTE_COLLEGE_ESSAYS_TO_DOTNET (3 rewrites, co-flip path-not-method), mounted /api/v1/college: GET+POST /students/:studentId/essays, PUT+DELETE /essays/:id, POST+GET /essays/:id/comments. All six reuse the shared ICollegeAccessResolver (getStudentAccess rail from FM-081): any access failure collapses to a uniform 404 'Not found'; the essay findUnique {id,isActive:true} miss -> distinct 404 'Essay not found' BEFORE the access check (PUT/DELETE + both comment routes). PARITY: wordCount = content ? content.trim().split(/\\s+/).length : 0 using the EXACT ECMAScript whitespace set (JsString.JsTrim + IsWhitespace: 25 code points, incl U+FEFF, excl U+0085 -- matches JS .trim() AND regex \\s) -- a whitespace-only content string -> trim->''->split(/\\s+/)->['']->length 1. CREATE coalesces prompt/content/essayType/studentApplicationId with x||null (empty-string falsy->null) and derives wordCount; UPDATE assigns content RAW ('' stored as '', NOT null -- the create-vs-update asymmetry) and the explicit wordCount override wins over the content-derived value (legacy assigns it last). Deferred type-500s past the existence(404)+access(404) gates: title (String NOT NULL) present-null/non-string->500; content truthy-non-string/false->500; status (EssayStatus enum, ::\"EssayStatus\" cast) ''/unknown/null/non-string->500; wordCount (Int) non-integer/out-of-int4/non-number->500. express.json malformed/primitive body->500 BEFORE the 404s (body read after RequireIdentity). List = FULL essay row + UNFILTERED _count.comments (Prisma _count, correlated subquery no isActive predicate), createdDate DESC (+id tie-break). Comment POST content required (JS-truthy)->400 AFTER the essay-404+access-404, authorId=caller, truthy-non-string content->500; GET include author:{id,name,roleName} (users INNER JOIN, both non-null String), createdDate ASC (+id tie-break). Delete soft, {success:true} only. AI/Bedrock sub-routes: none on Feature 3.",
      "dependsOn": [
        "FM-DOTNET-082-college-search-favorites"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/college.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/College/CollegeEssaysModels.cs",
        "services/api/src/FormMaps.Application/College/ICollegeEssaysRepository.cs",
        "services/api/src/FormMaps.Infrastructure/College/CollegeEssaysRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CollegeEssaysEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 46 new tests (8 repository Testcontainers [list active/student scope + UNFILTERED comment count incl inactive comment + createdDate DESC, create defaults+stored wordCount, FindActiveOwner ignores inactive/missing, update present-fields+enum cast+preserve-unset, content-null clears, soft-delete, add-comment authorId, list-comments author join + active/ASC] + 38 endpoint faked [anon 401 Theory x6, list access-404 + _count wrapper, create title-400/non-string-500/whitespace-wordCount-1/multiword/empty-null-0/truthy-nonstring-500/prompt-500/access-404/coalesce-null/malformed+primitive-500, update essay-404 'Essay not found'/access-404/content-null/derived-wordCount/empty-raw/explicit-override/only-wordCount/invalid-status-500/null-title-500/noninteger-wordCount-500/valid-status-200/malformed-before-404, delete essay-404/success-only, comment essay-404/access-404/content-400/non-string-500/ok-201/author-in-list]). New Testcontainers harness college-essays-schema.sql (EssayStatus enum + college_essays + essay_comments + users). Full suite green: unit 754, integration 1701 (+2 skip), contract 1. Fresh parity reviewer (11 files read, 14 tool uses: college.ts spec + all 4 port files + JsString + CollegeFavoritesEndpoints + 3 tests + schema.prisma) SHIP -- no BLOCKING/HIGH/MEDIUM; confirmed the content:false CREATE-null-vs-UPDATE-500 asymmetry + non-null author join + wordCount byte-parity; 1 LOW = id-ASC determinism-superset tie-break (accepted, no fold). Staging deploy run 30090803917 SUCCESS + anon canary GREEN (health 200; all 6 endpoints anon->401; unmapped /college/essays -> 404; x-formmaps-service). Merged --no-ff to main 35979ae. Dark (flag off). COMPLETES the college.ts mini-phase (Features 1/2/3 all ported). NEXT Phase-D: course-planning / superscore OR deferred school.ts/school-courses.ts writes (iSAMS write, staff-invite [needs .NET SES], logo->S3).",
      "commit": "c382096"
    },
    {
      "id": "FM-DOTNET-084-student-course-plan",
      "status": "completed",
      "goal": "Phase-D course-plan.ts sub-slice 1 of 3: student course-planning CRUD behind ONE dark flag FORMMAPS_ROUTE_STUDENT_COURSE_PLAN_TO_DOTNET (3 rewrites, co-flip path-not-method), mounted /api/v1/student: GET /course-plan, POST /course-plan/courses, DELETE /course-plan/courses/:courseId. Self-scoped (RequireIdentity, caller Identity/tenant RLS -- authenticate+tenantContext, no runAsSystem). Every endpoint gated by the requireSchoolMembership rail (a fresh users read of the caller's OWN schoolId): a missing user row OR a null schoolId is school-less -> GET returns the minimal {plan:{enrollments:[]},recommendations:[]} 200 (NO studentId/gradeLevel/totalCreditsEarned keys), POST/DELETE return 400 'You are not affiliated with a school'. PARITY: GET resolves the academic year via ?academicYearId -> findUnique by id ONLY (NOT school-scoped -- RLS bounds it) else the current-year findFirst(isCurrent) (id ASC superset); a null year -> no plan rows. enrollments = the RAW active studentCoursePlan rows (full-row Prisma passthrough, all 14 columns in schema order; term/status/notes/createdBy/updatedBy nullable), scoped studentId+schoolId+academicYearId+isActive, ORDER BY sortOrder ASC (+id tie-break). totalCreditsEarned = Sum over the caller's active COMPLETED grades of Number(credits) (::double precision), computed even when no year resolves. POST maps semester->term (String?: absent/null->NULL, string->value, non-string->500) and courseId (required String: missing/null/non-string->500, empty-string '' VALID), status hardcoded 'planned'; the Prisma type-500s DEFER past the NoSchool/NoCurrentYear 400 gates; sortOrder/isActive/notes default; response {success:true} only (no row). DELETE = hard deleteMany scoped studentId+courseId+status='planned'+schoolId, always {success:true} once the caller has a school (idempotent). Change-requests + recommendations + eligibility on the same router stay Node (later slices; distinct paths, bare /course-plan exact source does not swallow them). No AI/Bedrock.",
      "dependsOn": [
        "FM-DOTNET-083-college-essays-comments"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/course-plan.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/access.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/query.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentCoursePlan/IStudentCoursePlanRepository.cs",
        "services/api/src/FormMaps.Infrastructure/StudentCoursePlan/StudentCoursePlanRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/StudentCoursePlanEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 33 new tests (14 repository Testcontainers [membership missing-user/null-school -> empty view; current-year enrollments scoped active+AY+school ordered sortOrder ASC; full-row passthrough fields; explicit ?academicYearId not-school-scoped but plans are; unknown-year -> no rows but credits still summed; no-current-year -> no rows; totalCreditsEarned exclusions completed+active+school; create NoSchool/NoCurrentYear/InvalidBody(missing/null/non-string courseId + non-string semester)/Created-defaults/absent-semester-null-term/explicit-null-semester-null-term/empty-string-courseId-valid; delete NoSchool/planned-only-scoped-hard-delete] + 19 endpoint faked [anon 401 Theory x3; GET happy shape studentId/gradeLevel/enrollments/totalCreditsEarned/recommendations + no-school minimal shape (absent keys) + null-gradeLevel emitted null; POST 201-success-only/NoSchool-400-msg/NoCurrentYear-400-msg/InvalidBody-500/malformed+primitive-500; DELETE NoSchool-400/ok-200]). New Testcontainers harness student-course-plan-schema.sql (users + academic_years + student_course_plans[full cols] + student_grades). Full suite green: unit 754, integration 1734 (+2 skip), contract 1. Fresh parity reviewer (15 tool uses, read all port files + course-plan.ts + access.ts + query.ts + schema.prisma + tests + next.config) SHIP -- no BLOCKING/HIGH/MEDIUM; folded 2 test-coverage pins (empty-string courseId -> Created; explicit-null semester -> NULL term); 2 LOWs accepted no-fold (empty-string schoolId unreachable UUID-FK; bracket-notation ?academicYearId[x]=y coercion -> empty-enrollments vs current-year, pathological caller-own-data-only, same accepted class as FM-082). Staging deploy run 30106417426 SUCCESS + anon canary GREEN (health 200; all 3 endpoints anon->401 missing_identity; Node-only siblings change-requests/recommendations/eligibility -> 404 = NOT .NET-served; x-formmaps-service). Merged --no-ff to main 13302b4. Dark (flag off). NEXT course-plan.ts = FM-085 change-requests (POST/GET/DELETE /course-plan/change-requests[/:requestId]) -> FM-086 recommendations + eligibility (checkAssessmentCompletion + computeEligibilityMap ports).",
      "commit": "13302b4"
    },
    {
      "id": "FM-DOTNET-085-course-change-requests",
      "status": "completed",
      "goal": "Phase-D course-plan.ts sub-slice 2 of 3: student course change-requests CRUD behind ONE dark flag FORMMAPS_ROUTE_STUDENT_CHANGE_REQUESTS_TO_DOTNET (2 rewrites, path-not-method co-flip), mounted /api/v1/student: POST+GET /course-plan/change-requests, DELETE /course-plan/change-requests/:requestId. Self-scoped (RequireIdentity, caller Identity/tenant RLS), gated per-endpoint by the same requireSchoolMembership rail as FM-084 (fresh users read of the caller's OWN {schoolId, gradeLevel}; missing row OR null schoolId -> school-less; POST/DELETE -> 400 'You are not affiliated with a school', GET -> the empty envelope). POST creates a courseChangeRequest replaying legacy raw-body coalescing: courseId (required String, ''-valid), credits = body.credits || 0 (JS-falsy->0; number->value; numeric string->decimal.js parse via AllowLeadingSign|AllowDecimalPoint|AllowExponent; truthy true/object/array/non-numeric-string->500), gradeLevel = body.gradeLevel || user.gradeLevel || 9 (a truthy body.gradeLevel must be an INTEGER-VALUED Number in int4 range -- accepts 10.0/1e1 tokens via TryGetDouble + n%1==0 [F1 fold], fractional/out-of-range/string/true/object->500; falsy body -> user.gradeLevel[!=0] else 9), action (required CourseChangeAction enum, @action::\"CourseChangeAction\" cast -> invalid label PostgresException caught -> 500), dueDate = body.dueDate ? new Date(body.dueDate) : schoolAssessmentSettings.courseRequestDeadline || null (truthy body -> TryResolveJsDate [string parse/number epoch-ms/true=1ms/invalid->500]; falsy/absent -> the settings deadline or null), courseCode/courseName/semester/studentNote raw String? (absent/null->NULL, string->value, non-string->500). Every Prisma type rejection is a 500 DEFERRED past the membership 400. Response {success:true, data:<full 21-col row>} @201. GET paginates (page=max(1,parseInt||1), limit=min(50,max(1,parseInt||20)) via PcaExamPagination.JsParseInt+FalsyOr) the caller's rows where studentId+schoolId+isActive (+ optional ?status raw -> @status::\"CourseChangeStatus\" cast -> invalid label 500), createdDate DESC (+id tie-break), OFFSET/LIMIT; no-school -> 200 {data:[],total:0,page,limit,totalPages:0} (totalPages:0 literal); happy -> {data,total,page,limit,totalPages:ceil(total/limit)}. DELETE = findUnique by id (NO isActive filter) -> !row | studentId!=caller | schoolId!=membership | status!='pending' -> 400 'Cannot cancel'; else soft isActive=false -> {success:true}. Full-row passthrough: credits decimal.js STRING (trim_scale::text), action/status labels (::text), dates ISO-Z. recommendations + eligibility on the same router stay Node (FM-086). No AI/Bedrock.",
      "dependsOn": [
        "FM-DOTNET-084-student-course-plan"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/course-plan.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/access.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentCoursePlan/ICourseChangeRequestRepository.cs",
        "services/api/src/FormMaps.Infrastructure/StudentCoursePlan/CourseChangeRequestRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/CourseChangeRequestEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 50 tests (repository Testcontainers: create NoSchool/defaults(pending,active,credits-0,gradeLevel-fallback)/credits-coalesce[3,'3.5',0,'',absent]/gradeLevel[body-wins,user,0-falsy->user,none->9,integral-10.0/1e1]/dueDate[body-wins,falsy->settings,none->null]/nullable-strings/InvalidBody[missing-courseId,non-string-courseId,missing-action,invalid-enum-label,non-string-action,non-numeric-credits-string,truthy-credits,non-integer-gradeLevel,string-gradeLevel,invalid-date,non-string-nullable]; list no-school/scope-student-school-active-DESC/status-filter+paging/invalid-status-throws/credits-decimal-string; delete NoSchool/CannotCancel[missing,not-owner,wrong-school,not-pending]/pending-owned-soft-cancel + endpoint faked: anon-401 x3, POST 201+data/NoSchool-400/InvalidBody-500/malformed+primitive-500, GET envelope/no-school-empty/clamp-echo[page0->1,limit999->50], DELETE NoSchool-400/CannotCancel-400/ok-200). New Testcontainers harness course-change-requests-schema.sql (users + school_assessment_settings + course_change_requests + CourseChangeStatus/CourseChangeAction enums). Full suite green: unit 754, integration 1784 (+2 skip), contract 1. Fresh parity reviewer (19 tool uses, read all port files + course-plan.ts + access.ts + query.ts + schema.prisma + PcaExamPagination + GradeLevelParser + next.config + tests) SHIP -- no BLOCKING/HIGH; folded F1 (gradeLevel integral-Number-token 10.0/1e1 -> now 201, was 500) + endpoint clamp + integral-gradeLevel tests; F2 (credits JSON number > C# decimal range -> fail-closed residual, documented) + F3 (dueDate single-elem-array coercion) accepted no-fold (pathological, same class as DataMappings decimal-range residual). Staging deploy run 30110429562 SUCCESS + anon canary GREEN (health 200; 3 endpoints anon->401 missing_identity; FM-084 /course-plan sibling still 401 = no regression; Node-only /course-plan/recommendations + /course-plan/eligibility -> 404; x-formmaps-service). Merged --no-ff to main f5469f9. Dark (flag off). NEXT course-plan.ts = FM-086 recommendations + eligibility (the two compute GETs: port checkAssessmentCompletion [wraps computeStudentCompletion over 4 DB loads] + computeEligibilityMap [whole-catalog prereq map, reuses FM-057 checkEligibility logic]; userPreferences.preferredFields course keyword scoring take-100 slice-10) -> COMPLETES the course-plan.ts mini-phase + Phase-D non-Bedrock surface.",
      "commit": "f5469f9"
    },
    {
      "id": "FM-DOTNET-086-course-plan-recommendations-eligibility",
      "status": "completed",
      "goal": "Phase-D course-plan.ts sub-slice 3 of 3 (COMPLETES the mini-phase + the Phase-D non-Bedrock surface): the two COMPUTE reads GET /course-plan/recommendations + GET /course-plan/eligibility (routes/course-plan.ts L149-200, mounted /api/v1/student) behind ONE dark flag FORMMAPS_ROUTE_STUDENT_COURSE_PLAN_COMPUTE_TO_DOTNET (2 rewrites, path-not-method co-flip). Self-scoped (RequireIdentity, caller Identity/tenant RLS). LOCAL keyword scoring -- NOT Bedrock (the aiLimiter'd recommendations live in the separate course.ts, which stays Node). recommendations: gate on checkAssessmentCompletion (ported: 4 loads -- pca_exam_sessions userId+isActive+isCompleted->examType, lia_assessment_sessions findFirst user_id+status='completed'+is_active [the snake_case @map columns], evaluation_groups evaluatedUserId+isActive->isEvaluationCompleted, pca_evaluations userId NO-isActive->isCompleted; liaExamTypes = completed-parity-session ? 5 fixed subtests : examTypes; into the REUSED SchoolAdmin.StudentCompletion = computeStudentCompletion [liaCompleted=DISTINCT count>=5, evalRequired=min(evalTotal,3), allEvalDone=evalTotal>0 && evalCompleted>=evalRequired, pcaCompleted=any isCompleted, allDone]); !allDone -> {success:true,data:[],locked:true,completion:<7 fields liaCompleted/liaTotal:5/evalCompleted/evalTotal/pcaCompleted/allDone/readyForInsights=allDone>} (NOT school-gated -- only req.userId). else load enrolled courseEnrollment ids (isActive) + userPreferences.preferredFields (lowercased) + active courses (take 100, ORDER BY id superset) -> score (base 50; +15 per lowercased preferredField substring of `${title} ${shortDescription} ${category}`.toLowerCase(); +10 if rating>4 [the always-truthy Prisma-Decimal `c.rating &&` guard collapsed to rating::double precision >4]; min(100)); filter out enrolled; STABLE sort matchScore DESC (ties keep id order); take 10; response items = {...course, matchScore} the FULL 36-col Course passthrough (rating/recommendedScore Decimal->decimal.js STRING via trim_scale::text, 5 array cols, syllabus verbatim jsonb, ISO-Z dates, createdBy/updatedBy null EMITTED not omitted). eligibility: user{schoolId,gradeLevel}; !schoolId -> {data:[]}; else computeEligibilityMap (schoolCourse FULL catalog by schoolId [incl inactive/draft for prereq resolution] + completed studentGrades[schoolId+studentId+status='completed'+isActive]; coursesByCode = UPPER(code) map [last-by-id wins, determinism superset]; enumerate isActive && status=='active'; per course: gradeError only when studentGradeLevel truthy [g!=0] && gradeLevels.length>0 && !includes; each prereq skip !trim -> not-in-catalog=RAW code in missing / in-catalog-not-completed=resolved prereq.code in missing; eligible = !gradeError && missing empty) -> reduced endpoint shape {courseId,courseCode,eligible,missing:codes}. No AI/Bedrock.",
      "dependsOn": [
        "FM-DOTNET-085-course-change-requests"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/course-plan.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/assessmentService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolCoursesService.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/StudentCoursePlan/ICoursePlanComputeReader.cs",
        "services/api/src/FormMaps.Application/StudentCoursePlan/CoursePlanComputers.cs",
        "services/api/src/FormMaps.Infrastructure/StudentCoursePlan/CoursePlanComputeReader.cs",
        "services/api/src/FormMaps.Api/Endpoints/CoursePlanComputeEndpoints.cs"
      ],
      "verification": "dotnet build 0/0. 24 tests (9 computer unit [scorer base-50/exclude-enrolled/field+rating bonus/cap-100/strict->4/joined-text+tie-id-order/take-10; eligibility active-status-active-enumeration + full-catalog-resolution/grade-truthiness-0-falsy/raw-vs-resolved-missing-codes+blank-skip/completed-clears+case-insensitive] + 7 reader Testcontainers [not-done verdict-only; done-via-5-exam-types loads catalog+enrolled+lowercased-prefs; done-via-parity-session; DISTINCT-exam-type-count [5 rows 2 distinct -> not done]; pca-existence-not-completion; eligibility no-school->null; eligibility catalog+completed-grades + missing-prereq] + 8 endpoint faked [anon-401 x2; locked payload 7-field completion incl readyForInsights; scored full-row+matchScore+ISO-Z+null-createdBy/updatedBy+array+recommendedScore-string; eligibility no-school-empty; eligibility reduced-shape]). New Testcontainers harness course-plan-compute-schema.sql (users + pca_exam_sessions + lia_assessment_sessions[snake_case] + evaluation_groups + pca_evaluations + course_enrollments + user_preferences + courses[36 cols] + school_courses + student_grades). Full suite green: unit 763, integration 1798 (+2 skip), contract 1. Fresh parity reviewer (27 tool uses, read all port files + course-plan.ts + assessmentService.ts + schoolCoursesService.ts + schema.prisma + StudentCompletion + tests + next.config) SHIP -- no BLOCKING/HIGH/MEDIUM; verified every legacy quirk (no-isActive pca_evaluations, always-truthy rating collapse, min(evalTotal,3), DISTINCT-LIA count, raw-vs-resolved missing codes, 0-gradeLevel falsiness, full 36-field passthrough with null emission); folded 2 test-hardening pins (distinct-LIA count reader test + fuller passthrough endpoint assertions); 1 LOW (syllabus non-canonical jsonb number formatting) accepted no-fold (unreachable, leans more-faithful-to-DB). Staging deploy run 30114431977 SUCCESS + anon canary GREEN (health 200; recommendations + eligibility anon->401 missing_identity [previously 404 = now .NET-served]; FM-084 /course-plan + FM-085 /change-requests still 401 = no regression; whole course-plan.ts router now .NET-served; x-formmaps-service). Merged --no-ff to main a55cbdb. Dark (flag off). COMPLETES the course-plan.ts mini-phase (FM-084/085/086) + effectively closes Phase-D's non-Bedrock surface. NEXT = a NEW domain: deferred school.ts/school-courses.ts writes (iSAMS write, staff-invite [needs .NET SES], logo->S3 -- polyglot-gated, need Federico infra calls) OR open a structurally-hard phase (F docs/PDF lowest-risk of the remaining big ones; E messaging/video, G Stripe, H auth are the high-risk tail).",
      "commit": "a55cbdb"
    },
    {
      "id": "FM-DOTNET-087-isams-configure-write",
      "status": "completed",
      "goal": "Deferred school.ts write: port POST /integrations/isams (schoolService.configureIsams) to .NET behind the dark flag FORMMAPS_ROUTE_ISAMS_CONFIGURE_TO_DOTNET (1 rewrite, the exact literal /api/v1/school-admin/integrations/isams -- distinct from the /status+/jobs reads [FM-053] and the /sync+/test writes). The ONLY iSAMS write ported: a self-contained isamsConfig upsert + AES-256-GCM credential encryption. sync/test STAY IN NODE (the SSRF-hardened undici vendor client -- per-socket DNS re-validation + rebind/cloud-metadata blocking; sync creates user rows) -- the same cohesive vendor+side-effect boundary that kept invites [FM-067/068] and calendar-sync [FM-071] polyglot. NEW AesGcmFieldCipher = byte-compatible port of lib/fieldEncrypt.ts: .NET's native AesGcm rejects non-12-byte nonces but Node uses a 16-byte IV and isEncrypted() requires exactly 16 bytes, so built on BouncyCastle GCM (standard GHASH-based J0 derivation, identical to OpenSSL); Node key-derivation parity (64-hex->raw else base64->first 32B); LAZY key so a missing FIELD_ENCRYPTION_KEY never bricks startup/anon. IsamsConfigWriter = SELECT-then-branch upsert reproducing the create-vs-update asymmetry (authType:0 -> 'api_key' on CREATE but 500 on UPDATE), endpoint=body.endpoint||body.apiUrl (JS-||), Prisma undefined-omit vs null-SET-NULL, a truthy non-string apiKey -> encrypt-throw -> 500 with no write, unconditional updatedBy/updatedAt on update. Endpoint: RequireIdentity(401)->school:manage(403)->body(malformed/primitive->500)->schoolId(null->400 'No school')->200 {id,endpoint,configured:true}. INFRA GATE (cutover-only): prod .NET FIELD_ENCRYPTION_KEY must match prod Node so a .NET-written credential round-trips through the Node sync path.",
      "dependsOn": [
        "FM-DOTNET-053-isams-reads"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/school.ts",
        "/Users/federicotafur/formmaps-platform/api/src/services/schoolService.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/fieldEncrypt.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Security/IFieldCipher.cs",
        "services/api/src/FormMaps.Infrastructure/Security/AesGcmFieldCipher.cs",
        "services/api/src/FormMaps.Application/IsamsWrites/IIsamsConfigWriter.cs",
        "services/api/src/FormMaps.Infrastructure/IsamsWrites/IsamsConfigWriter.cs",
        "services/api/src/FormMaps.Api/Endpoints/IsamsWriteEndpoints.cs"
      ],
      "verification": "dotnet build 0/0 (added BouncyCastle.Cryptography 2.4.0 to Infrastructure -- already present transitively in the test graph). 43 new tests: 14 cipher unit (a GOLDEN Node-produced vector decrypts in .NET = real cross-impl parity; 16-byte IV + isEncrypted; both key-derivation branches; per-call IV randomness; lazy missing-key throw; tamper-fails) + 15 writer Testcontainers (create/update; the authType:0 create-vs-update asymmetry; endpoint||apiUrl coalescing incl falsy/null/'' cases; apiKey encrypt round-trip via the real cipher; no-apiKey-leaves-creds-unchanged; type-500s no-write) + 14 endpoint faked (anon 401; no-perm 403; no-school 400 'No school'; malformed/primitive 500; happy 200 {id,endpoint,configured:true}; null endpoint echoed; InvalidBody 500). Full suite green: unit 777, integration 1827 (+2 skip), contract 1. Fresh parity reviewer (17 files, 21 tool uses, INDEPENDENTLY ran Node createDecipheriv against the golden vector) SHIP -- no BLOCKING/HIGH/MEDIUM; 3 LOWs accepted no-fold (SELECT-then-branch concurrency window = same window Prisma's own upsert has; base64 key strictness already commented; express.json-first anon/no-perm+malformed->401/403 the accepted class). Staging deploy run 30120169646 SUCCESS + anon canary GREEN (health 200; POST /integrations/isams -> 401 missing_identity + x-formmaps-service:formmaps-api [.NET-served]; POST /sync + /test -> 404 [Node-only, correct]; GET /status -> 401 [FM-053, no regression]). Merged --no-ff to main c0c83ec. Dark (flag off). Cutover note: the rewrite must also land in the live formmaps-platform/frontend/next.config.ts before any prod flip (the apps/web copy is the staging artifact). NEXT = Phase F (routes/upload.ts real S3 rail [presigned, bucket formmaps-platform-uploads] + resume.ts + report.ts PDF; the broken POST /school/logo = memoryStorage stores /uploads/undefined, superseded by /upload/school-logo -> not ported) + staff-invite SES (reuse the existing SesEmailSender rail + a staff-onboarding template).",
      "commit": "c0c83ec"
    },
    {
      "id": "FM-DOTNET-088-uploads-s3-rail",
      "status": "completed",
      "goal": "Phase F sub-slice 1 (OPENS Phase F, domain 8): port routes/upload.ts (the SIX /api/v1/upload/* multipart endpoints -- school-logo, profile-image, resume, course-import, grade-import, portfolio-attachment) to .NET behind ONE dark flag FORMMAPS_ROUTE_UPLOAD_TO_DOTNET (6 exact-literal rewrites, no path-not-method hazard). The FIRST multipart + S3 surface in the .NET service. All multer.single('file') + self-scoped (authenticate + tenantContext -> RequireIdentity, caller RLS, no permission). NEW IObjectStorage/S3ObjectStorage (port of lib/s3.ts uploadAndGetUrl: PutObject ContentDisposition=attachment + 24h presigned GET; key {folder}/{unixMs}-{6 base36}{ext}; AWSSDK.S3 4.0.101.4; DI factory-lambda client = no boot-time cred resolution, like SES). NEW FileUploadValidation (validateMagicBytes per-type signatures incl. 7-bit ascii mask + <4-bytes->false; sanitizeFilename ECMAScript-\\w [^A-Za-z0-9_.-]; CSV parse -> lowercased keys, short-row key omission, dup-header last-wins incl. the short-row intersection via a null sentinel). NEW UploadRepository (school.logoUrl + coach.imageUrl writes, updatedAt bumped Kind=Unspecified). Per-endpoint validation ORDER + exact messages (school-logo: schoolId no-school 400 before file checks; resume extractedText for text/plain|csv; csv mimetype/ext + parse empty/>10000-rows 400s, S3 upload BEFORE parse; portfolio size+contentType echo); catch -> 500 'Upload failed'. The broken POST /school/logo (school.ts:173, multer.memoryStorage -> stores '/uploads/undefined', never S3) is NOT ported -- superseded by this /upload/school-logo.",
      "dependsOn": [
        "FM-DOTNET-087-isams-configure-write"
      ],
      "inputs": [
        "/Users/federicotafur/formmaps-platform/api/src/routes/upload.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/s3.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/fileValidation.ts",
        "/Users/federicotafur/formmaps-platform/api/src/lib/sanitize.ts",
        "/Users/federicotafur/formmaps-platform/api/src/middleware/upload.ts",
        "/Users/federicotafur/formmaps-platform/api/prisma/schema.prisma"
      ],
      "ownedPaths": [
        "services/api/src/FormMaps.Application/Storage/IObjectStorage.cs",
        "services/api/src/FormMaps.Infrastructure/Storage/S3ObjectStorage.cs",
        "services/api/src/FormMaps.Application/Uploads/FileUploadValidation.cs",
        "services/api/src/FormMaps.Application/Uploads/IUploadRepository.cs",
        "services/api/src/FormMaps.Infrastructure/Uploads/UploadRepository.cs",
        "services/api/src/FormMaps.Api/Endpoints/UploadEndpoints.cs"
      ],
      "verification": "dotnet build 0/0 (added AWSSDK.S3 4.0.101.4 to Infrastructure, aligned with the existing v4 AWS Core/SES). 49 new tests: 26 unit (FileUploadValidation -- magic-byte table per type + <4-bytes fail + 7-bit ascii mask; sanitizeFilename path-strip/non-word/255-cap incl. non-ASCII->_; CSV headers/rows lowercased, quoted-dequote-trim [inner-quote spaces survive], short-row omission, dup-last-wins, dup+short-row-drop, blank lines dropped, empty content) + 23 endpoint faked-storage/repo (anon 401 x6; school-logo no-school 400 / missing-file / wrong-mimetype / bad-magic / happy+logoUrl-write; profile-image coach.imageUrl write; resume binary-null-extractedText + text-extract + Content-Type-param-strip; csv non-csv/empty 400 + happy parse + .csv-by-ext; portfolio size/contentType; storage-throw 500 'Upload failed'; >5MB 500 'Internal server error' resume+csv). Fresh parity reviewer (16 files, independent Node global-error-handler analysis) SHIP -- no BLOCKING/HIGH; folded all 3 findings with red-if-regressed tests: (1) MEDIUM multer global 5MB limit reproduced (>5MB -> 500 'Internal server error' [multer errors before the handler -> global 500, NOT the handler's 'Upload failed']; bounds the S3 write against oversized uploads; the CSV explicit >5MB 400 kept as dead code mirroring Node), (2) LOW Content-Type params stripped (busboy parity -- 'text/plain; charset=utf-8' -> bare 'text/plain'), (3) LOW CSV dup-header+short-row intersection (null-sentinel). Accepted no-fold: empty-multipart -> 400 vs Node-malformed-500 (documented in ReadFileAsync); P2025-vs-0-rows on school.update unreachable (schoolId from caller's own FK-backed users row). Full suite green: unit 803, integration 1850 (+2 skip), contract 1. (Mid-run Docker crashed -> 709 Testcontainers mass-fails = infra-not-code; open -a Docker + re-ran clean.) Staging deploy run 30125439806 SUCCESS + anon canary GREEN (health 200; all 6 /upload/* anon POST -> 401 missing_identity + x-formmaps-service:formmaps-api [.NET-served]; unmapped /upload/does-not-exist -> 404 = no over-capture). Merged --no-ff to main 017e2e6. Dark. INFRA GATE (cutover-only): S3 bucket formmaps-platform-uploads + s3:PutObject/GetObject on the App Runner role (presigned reads, not public). NEXT Phase F: routes/resume.ts (PDF+S3, reuses this rail) -> routes/report.ts (PDF + SES attachment send -- extend the SES rail). PDF-render may stay polyglot if a heavy headless dep.",
      "commit": "017e2e6"
    }
  ]
}
