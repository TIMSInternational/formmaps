# FormMaps Web

React/Next.js frontend for FormMaps.

The existing FormMaps frontend should be moved here from the current production
platform. The first move should preserve behavior; cleanup and package
extraction should happen after the app builds and runs in this repo.

See:

- `docs/web/architecture.md`
- `docs/web/api-boundary.md`
- `docs/web/migration-scope.md`
